# Handover — Phase 6.2 / 6.3 / 6.4 (location flow в Python)

> **Создано:** 2026-05-12 ~22:30 MSK после завершения Phase 6.1 (PR #56 merged + PR #57 awaiting merge). **Аудитория:** агент-преемник, который продолжит миграцию `02.1_Location` n8n → Python.
>
> **Твоя задача:** реализовать Phase 6.2 → 6.3 → 6.4 последовательно за одну или несколько сессий. Использовать субагентов (особенно для крупных миграций — проверять их output), NLM (для экономии контекста при архитектурных вопросах), psycopg2 (canonical для миграций).

---

## 0. Первые шаги — прочитай эти файлы в этом порядке

| # | Файл | Зачем |
|---|---|---|
| 1 | `/Users/vladislav/Documents/NOMS/CLAUDE.md` | Главные правила проекта NOMS: RPC-first, headless, no-hardcode, NLM-first stop-rule, release-protocol. |
| 2 | `/Users/vladislav/.claude/CLAUDE.md` | Глобальные правила: git-дисциплина при параллельной работе (§12), rebase перед commit, sanity diff перед push, force-with-lease. |
| 3 | `/Users/vladislav/.claude/projects/-Users-vladislav-Documents-NOMS/memory/MEMORY.md` | Состояние проекта, активные cutover'ы, watchlist gotchas. |
| 4 | `claude-memory-compiler/RULES.md` | KB правила — что класть в KB, что в основной репо нельзя. |
| 5 | `claude-memory-compiler/knowledge/concepts/access-credentials.md` | psycopg2 / NLM / SSH recipes + decision matrix. **Recipe 1 — psycopg2 canonical для миграций.** |
| 6 | `claude-memory-compiler/knowledge/concepts/release-protocol.md` | **NEW SECTION 2026-05-12** «SQL-миграции — отдельный lifecycle от code deploy». Phase-style workflow. |
| 7 | `claude-memory-compiler/knowledge/concepts/phase6-location-migration-plan.md` | **Полный исходный план Phase 6 (480 строк)** — инвентаризация 02.1_Location, граф потока, SWOT, варианты гранулярности, риски. |
| 8 | `claude-memory-compiler/knowledge/concepts/phase4-onboarding-migration.md` | Прецедент Phase 4 с 34 gotcha — обязательно к прочтению, многое применимо к 6.2/6.3. |
| 9 | `claude-memory-compiler/knowledge/concepts/noop-render-strategy-pattern.md` | **NEW 2026-05-12** Noop Pattern (mig 208 finding) — пригодится если будут screens с reply-keyboard но без inline-кнопок. |
| 10 | `claude-memory-compiler/knowledge/concepts/safe-create-or-replace-recipe.md` | Stale-base safe protocol для CREATE OR REPLACE FUNCTION. **Обязательное чтение перед любой правкой `process_user_input` / `process_onboarding_input` / `render_screen`.** |
| 11 | `claude-memory-compiler/knowledge/concepts/test-user-reset-recipe.md` | One-liner `SELECT public.reset_to_onboarding(<tid>)` для обнуления тестовых юзеров. |
| 12 | `claude-memory-compiler/daily/2026-05-12.md` | Полная история этой сессии — Phase 6.1 deploy + UX polish PR #57. Открытые вопросы внизу. |

После этого блока тебе понятен: код проекта + canonical workflow + история Phase 4 / Phase 6.1 + готовый план Phase 6.2/6.3/6.4.

---

## 1. Состояние прода на момент handover (2026-05-12 ~22:30 MSK)

### Применённые миграции
- **HEAD = mig 208** (`onboarding_success_unified_carrier`) — Noop Pattern, applied 2026-05-12 ~18:14 UTC.
- mig 207 (Phase 6.1) — applied ~15:21 UTC.

### Feature flags
```sql
-- Текущее состояние:
SELECT key, value FROM app_constants WHERE key LIKE 'handler_%_use_python';
--   handler_menu_v3_use_python      = 'true'   ✓
--   handler_onboarding_use_python   = 'true'   ✓
--   handler_location_use_python     = 'false'  ⚠️ (ожидает manual UAT тимлида)
```

**Phase 6.2 не сможет полноценно работать пока `handler_location_use_python='false'`** — webhook сделает fall-through на legacy n8n для всех `target='location'` clicks. **Действие для тебя на старте:** запроси у тимлида подтверждение manual UAT Phase 6.1 + одобрение `UPDATE app_constants SET value='true' WHERE key='handler_location_use_python'` ДО начала имплементации Phase 6.2. Без этого 6.2 не получится верифицировать.

### Открытые PR
- **PR #57** (`claude/sad-wilbur-d42028`) — `feat(onboarding): unified Sassy completion message + eliminate 👋 carrier (mig 208)` + picker cleanup commit `242b16c`. **Awaiting merge.** Когда тимлид смерджит — GitHub Actions auto-deploy кода. Mig 208 SQL уже в проде, так что merge только применит Python cleanup.

### Тесты состояние
- 26 тестов в `tests/handlers/test_location.py` — все зелёные.
- 80 тестов handler suite (location + onboarding_v3 + menu_v3) — все зелёные.
- Full suite — 516 passed, 1 pre-existing flake в `tests/services/test_telegram_send.py::test_send_new_posts_send_message` (изолированно проходит — test order issue, не связан с Phase 6).

### Worktree caveat на машине тимлида
В `git worktree list` на маке тимлида **main checkout-нут в `vibrant-jackson-b27c62`**, не в основном клоне (`~/Documents/NOMS`). Тимлид это знает. Если нужно работать с main локально на маке (не в worktree) — попроси тимлида либо обновить `vibrant-jackson-b27c62` (`git pull --ff-only`), либо перенести main в основной клон. **Тебе как агенту в worktree это не мешает** — psycopg2 + tests прекрасно работают из worktree, для apply миграций worktree-path неважен (БД одна).

---

## 2. Phase 6.1 recap — что было сделано и чему научились

### Доставлено
1. **6-секундная пауза устранена** (`process_onboarding_input` cmd_quiz_continue / cmd_quiz_skip теперь рендерят `edit_country` inline вместо forward в legacy n8n).
2. **`handlers/location.py`** (~470 LOC) — onboarding context (status ∈ `{onboarding:country, onboarding:timezone}`). Picker rendering (Python builds inline keyboard from `list_countries_for_picker` / `list_timezones_for_country` RPCs, paginated 12/page). Smart Skip (single TZ → auto-finalize). One Menu UX cleanup (fire-and-forget `deleteMessage` previous picker до render success).
3. **Channel A onboarding_success sticker** автоматически срабатывает (mig 198/201 уже на месте).
4. **Noop Pattern (mig 208)** — финал онбординга = 2 сообщения вместо 4 (стикер + Sassy text с reply-keyboard, без отдельного «👋» carrier).
5. **`reset_to_onboarding(p_telegram_id)` RPC** verified — покрывает все 64 поля. KB recipe обновлён.

### Ключевые архитектурные правила (зафиксированы тимлидом)

1. ⛔ **НЕ удалять глобально `icon_wave` fallback в `template_engine.py`** — это критичный safety net для `cmd_restore_account` (editMessageText редактирует inline screen → отдельный carrier нужен для reply-kb), Profile v5 deep-nav (если reply-kb потеряна в навигации). Решать UX-баги локально через `meta` экрана + SQL `render_screen`, а не правкой ядра template_engine.

2. ⛔ **flag flip требует явного approval тимлида в разговоре.** Migration apply — обычная autonomous работа агента (canonical psycopg2 pattern); flag flip — это «активация фичи на всех юзеров», требует explicit approval.

3. ⛔ **Phase-style migration apply ДО merge PR** (по Phase 4 precedent). Файл миграции коммитится в PR-ветку для аудита, но БД-state верифицируется через `pg_get_functiondef` + spot-check.

4. ⛔ **Stale-base regression — никогда не использовать тело функции из git-файла как base** для `CREATE OR REPLACE FUNCTION`. Всегда `pg_get_functiondef('public.<name>(<types>)'::regprocedure)` в момент apply. Builder script `scripts/_build_mig_NNN.py` делает это автоматически.

5. ⛔ **Если AI-classifier хука блокирует action** — НЕ обходить «творчески». Объяснить тимлиду что блокирует, дать ему решить (одобрит / добавит allow-rule). Никогда не писать в KB инструкции «как обходить classifier» — это memory-poisoning.

### Открытые вопросы (для следующего агента)

См. конец `daily/2026-05-12.md` — список того что НЕ закрыто.

---

## 3. Phase 6.2 — Profile v5 edit-flow в Python

### Цель
Покрыть Python handler'ом контекст B (Profile v5 edit-flow): user в `status='registered'` нажимает «🏳️ Изменить страну» в Settings → SQL ставит `status='edit_country'` → handler ловит loc_* callbacks. То же для `edit_timezone`. После сохранения — `editMessageText` → settings (One Menu UX), без отдельного «✅ Сохранено» сообщения (NLM-decision, BigTech: One Menu UX > toast).

### Scope
- **SQL миграция** (`migrations/209_phase6_2_location_edit_flow.sql`):
  - Расширить `process_user_input` для веток `status IN ('edit_country', 'edit_timezone', 'editing:country', 'editing:timezone')` + callback prefix `loc_*` / `cmd_back`. Логика зеркалит `process_onboarding_input` ветки `onboarding:country`/`onboarding:timezone` (mig 207), НО без `finalize_onboarding_location` — после сохранения статус возвращается в `'registered'` + render `settings` через editMessageText.
  - **Stale-base safe** — pull live `process_user_input` через `pg_get_functiondef`. Builder script `scripts/_build_mig_209.py`.
- **Python** (`handlers/location.py`):
  - Расширить `_ONBOARDING_LOCATION_STATUSES` константу. Лучше — переименовать в `_LOCATION_HANDLER_STATUSES` (включает онбординг + edit). Sphagetti-warning: на старом имени могут быть тесты.
  - В edit-context финальный экран = `settings`, НЕ `onboarding_success`. Channel A sticker НЕ нужен (edit context — не финал онбординга). Reply-kb уже у юзера (registered users всегда имеют main_kb). 
  - Smart Skip: для single-TZ countries (Germany, Spain) auto-save + jump to settings, без timezone picker.
- **Router cleanup** (`dispatcher/router.py`):
  - Удалить early-branch `LOC_EDIT_STATUSES` patch v9 из section 4e-pre (если он там есть — проверь актуальное состояние). Этот патч был workaround для reply-back в edit-flow, после Phase 6.2 больше не нужен — handler сам обрабатывает.
  - Удалить `LOCATION_LEGACY_BUTTONS` set если он перестал использоваться.
- **Тесты** (`tests/handlers/test_location.py`):
  - Удалить guard-тесты `test_status_edit_country_forwards_to_legacy` + `test_status_edit_timezone_forwards_to_legacy` — теперь handler обрабатывает эти status.
  - Добавить ~6 новых тестов:
    - `test_edit_country_loc_country_saves_and_renders_settings`
    - `test_edit_timezone_loc_tz_saves_and_renders_settings`  
    - `test_edit_country_smart_skip_single_tz`
    - `test_edit_cmd_back_returns_to_settings`
    - `test_edit_status_no_finalize_called` (guard — `finalize_onboarding_location` НЕ должен вызываться для xp>0 user)
    - `test_edit_status_no_channel_a_sticker` (Channel A не срабатывает на settings render)
- **KB**:
  - Обновить `architecture-registry.md` — переключить «location» с «n8n fallback» на «Python authoritative (status: onboarding + edit)».
  - В `phase4-onboarding-migration.md` пометить `start-fresh-gaps-2026-05-11` если есть пересечения.
  - Закрыть `n8n-route-classifier-edit-loc-patch` если он зафиксирован в KB (patch v9 удалён).

### Ожидаемый effort
~0.5-1 день (mig 209 ~150 LOC SQL + ~50 LOC handler extension + 6 тестов + KB).

### Открытые вопросы для Phase 6.2 (требуют решения с тимлидом)

1. **Финальный экран в edit context** — `settings` или `profile_main`? NLM (рекомендация) — `settings`. Тимлид подтвердил «editMessageText → render(settings)». **Не плодить** новое сообщение «✅ Сохранено!» — это засоряет чат (One Menu UX).
2. **`return_status` legacy field в Init & Templates** n8n — после 6.2 удалять? Тимлид одобрил полное удаление (BigTech: dead code = tech debt).

---

## 4. Phase 6.3 — Geolocation pin через offline `timezonefinder` + UTC headless

### Цель
- Перенести геокодинг lat/lon → country_code + timezone из Geoapify в offline `timezonefinder`. Отказ от внешнего HTTP, +precision, +speed (<10ms vs 200-400ms), +нет dependence на API uptime.
- Удалить hardcoded UTC labels в `Build UTC UI` n8n node — перевести в `ui_translations.utc_groups.*` + `app_constants.icon_utc_*`.

### Scope
- **Новая зависимость**: `timezonefinder` Python lib. Размер binary tz-shapes ~30MB — добавь в `requirements.txt` + проверь что VPS deploy не сломается на размер. Может быть нужен `timezonefinderL` (lite version) если 30MB слишком — но точность ниже.
- **Новый файл** `services/geocoding.py` (~100 LOC):
  ```python
  from timezonefinder import TimezoneFinder
  _tf = TimezoneFinder()  # in-memory shape lookup
  
  async def reverse_geocode(lat: float, lon: float) -> dict:
      """Returns {country_code, timezone, accuracy} or {error} for invalid coords.
      
      timezone: IANA name (e.g. "Europe/Madrid"). Maps lat/lon to nearest IANA zone.
      country_code: NOT directly in timezonefinder — fetch from ref_timezones.country_code WHERE zone_name=<tz>.
      """
      ...
  ```
- **SQL миграция** (`migrations/210_phase6_3_utc_headless_translations.sql`):
  - INSERT 4 UTC group icons в `app_constants` (`icon_utc_morning` / `icon_utc_day` / `icon_utc_evening` / `icon_utc_night` — или 1 общий `icon_utc_globe`).
  - INSERT `ui_translations.utc_groups.label_*` × 13 langs.
  - Опционально: если решили вынести UTC-группы в отдельные screens (ui_screens.utc_group_minus_12_6 / minus_5_0 / plus_1_6 / plus_7_14) — добавить ui_screens. **Но lesson из 6.1:** `list_countries_for_picker` уже включает `ZZ` ("🌐 Вручную (UTC)"). NLM подтвердил: можно НЕ делать отдельные UTC groups, оставить плоский список через ZZ → timezone_picker для ZZ → 33 зоны через пагинацию. Если оставляешь так — этот SQL не нужен.
- **Python handler extension** (`handlers/location.py`):
  - Удалить branch `if message.location.latitude → return _forward_to_legacy_envelope("geo_pin_deferred")`. Заменить на:
    ```python
    if isinstance(msg, dict) and isinstance(msg.get("location"), dict):
        return await _handle_geo_pin(ctx, decision, msg["location"], rpc_caller)
    
    async def _handle_geo_pin(ctx, decision, location_dict, rpc_caller):
        lat, lon = location_dict["latitude"], location_dict["longitude"]
        from services.geocoding import reverse_geocode
        geo = await reverse_geocode(lat, lon)
        if "error" in geo:
            # Geocoding failed → graceful fallback to picker
            return await _render_country_picker(ctx, decision, page=0, rpc_caller=rpc_caller)
        cc, tz = geo["country_code"], geo["timezone"]
        return await _finalize_with_timezone(ctx, decision, cc, tz, rpc_caller)
    ```
- **Тесты** (`tests/handlers/test_location.py` + `tests/services/test_geocoding.py`):
  - Unit-тесты `services/geocoding.py` с моками timezonefinder (несколько coordinates: Madrid, Moscow, NYC, Tokyo, edge case: open ocean → fallback).
  - Update `test_location_pin_forwards_to_legacy` → переименовать в `test_location_pin_geocodes_and_finalizes`.
- **Router** (`dispatcher/router.py`):
  - Section 1 `has_location` уже ставит `target='location'` — не трогать.
  - В Phase 6.3 геолокация попадает в Python handler по тому же flag.
- **n8n cleanup** (выполнить через self-hosted API на VPS):
  - Удалить `Search Geo` (Geoapify HTTP Request) ноду из `02.1_Location` workflow (через PUT с урезанным nodes array).
  - Geoapify API key из VPS `.env` можно убрать (no-op после Python switch).
- **KB**:
  - Обновить `phase6-location-migration-plan.md` — отметить Phase 6.3 done.
  - Новая статья `concepts/offline-timezonefinder-recipe.md` — как использовать, edge cases, performance.

### Открытые вопросы для Phase 6.3

1. **`timezonefinder` vs `timezonefinderL`** — 30MB vs ~10MB. Точность на границах зон (Mongolia / China border) различается. Решение: брать full version если VPS deploy не упирается в size.
2. **`country_code` lookup для нестандартных coords** (открытый океан, спорные территории) — fallback на `'ZZ'` или error? Рекомендация: `'ZZ'` + render timezone_picker (Smart Skip с UTC offsets вместо stuck'нувшегося юзера).
3. **UTC headless groups** — нужны ли отдельные screens, или плоский список через ZZ остаётся (6.1 решение)? Согласовать с тимлидом.

### Ожидаемый effort
~1-2 дня (geocoding service + tests + handler extension + опц. SQL для UTC translations + cleanup n8n).

---

## 5. Phase 6.4 — Decommission 02.1_Location

### Цель
Полностью убрать legacy n8n workflow `02.1_Location` (`workflowId='7EqiiwUwlGs7dcHT'`) после 3-5 дней stable работы Phase 6.2 + 6.3.

### Scope
1. **Деактивация** через self-hosted n8n API:
   ```bash
   ssh root@89.167.86.20 'KEY=$(grep N8N_TARGET_API_KEY /home/noms/n8n/compose/.env | cut -d= -f2);
   curl -X POST -H "X-N8N-API-KEY: $KEY" http://127.0.0.1:5678/api/v1/workflows/7EqiiwUwlGs7dcHT/deactivate'
   ```
   ⚠️ **API endpoint `/deactivate` возвращает 403 на community edition** (lesson Phase 4 gotcha #4). Workaround — прямой UPDATE в SQLite:
   ```bash
   ssh root@89.167.86.20 'sqlite3 /home/noms/n8n/data/database.sqlite "UPDATE workflow_entity SET active=0 WHERE id=\"7EqiiwUwlGs7dcHT\""'
   ```
2. **Safety net 3-5 дней** — оставить workflow в `active=false`, не удалять. Если что-то поломается → reactivate через `POST /activate` (этот endpoint работает без 403).
3. **После 3-5 дней stability** — DELETE workflow:
   ```bash
   curl -X DELETE -H "X-N8N-API-KEY: $KEY" http://127.0.0.1:5678/api/v1/workflows/7EqiiwUwlGs7dcHT
   ```
4. **n8n 01_Dispatcher cleanup**:
   - Удалить `Prepare for 05` Set-ноду (проксировала 9 полей в 02.1_Location).
   - Удалить ветку Route Classifier которая шла на `executeWorkflow → 02.1_Location`.
   - Удалить `02_Continue Onboarding` мёртвый executeWorkflow (был ссылкой на деактивированный 02_Onboarding_v3).
   - **Использовать Safe PUT recipe** (`n8n-data-flow-patterns.md`) — workflow >50KB через scp + curl --data @file.
5. **KB updates**:
   - `architecture-registry.md` — обновить таблицу n8n workflows, отметить 02.1_Location как «decommissioned».
   - `phase6-location-migration-plan.md` — переименовать в `phase6-location-migration.md` (без `-plan`) или добавить completion section.
   - Новая статья `concepts/phase6-location-migration-complete.md` — полные lessons learned (как для Phase 4).

### Открытые вопросы для Phase 6.4

1. **Через сколько дней удалять workflow окончательно?** Тимлид одобрил 3-5 дней safety net. Можно ждать дольше если нет уверенности.
2. **`02_Continue Onboarding` уже мёртвый код** — удалить вместе с 02.1_Location или отдельно. Скорее всего вместе для cleanliness.

### Ожидаемый effort
~0.5 дня (низкий риск — основная работа уже сделана).

---

## 6. Tools & access — что у тебя есть

### psycopg2 (canonical для миграций)
```bash
cd /Users/vladislav/Documents/NOMS && python3 << 'PYEOF'
import psycopg2, os
from dotenv import load_dotenv
load_dotenv('.env')
conn = psycopg2.connect(os.environ['DATABASE_URL'])
cur = conn.cursor()
cur.execute("SELECT pg_get_functiondef('public.process_user_input(bigint,text,jsonb,jsonb,boolean)'::regprocedure)")
print(cur.fetchone()[0][:500])
conn.close()
PYEOF
```
- `.env` лежит в `/Users/vladislav/Documents/NOMS/.env` — `DATABASE_URL` через Supabase pooler.
- **Read-only queries** (`SELECT`, `pg_get_functiondef`, `information_schema`) — agent autonomy, никаких approval.
- **Mutating queries** (`CREATE OR REPLACE`, `UPDATE`, `INSERT`, `DELETE` на prod data) — если AI-classifier блокирует, цитируй [[access-credentials]] Recipe 1 + явную user-authorization из текущего сообщения. **Flag flip всегда требует explicit approval тимлида.**
- Apply миграции: `cur.execute(open('migrations/NNN_*.sql').read()); conn.commit()`. **Strip `BEGIN;`/`COMMIT;`** из файла (psycopg2 управляет txn) — `re.sub(r'(?m)^\s*BEGIN;\s*$', '', sql)`.

### NLM (для архитектурных вопросов, экономии токенов)
```bash
source /Users/vladislav/Documents/NotebookLM+Claude/plugins/notebooklm-rag/.venv/bin/activate
notebooklm auth check --json   # validate cookie file exists
notebooklm source list -n fa75f4de-96c2-4dc0-bc9d-0bf07bd992f5 --json 2>&1 | head -5   # validate session live
# Если "Authentication expired" → попроси тимлида: `notebooklm login` в отдельном терминале.
notebooklm ask "Точный вопрос про NOMS" -n fa75f4de-96c2-4dc0-bc9d-0bf07bd992f5 --json
```
- Использовать для архитектурных вопросов и обзора cross-cutting тем.
- НЕ использовать для свежих миграций / live schema — там snapshot до суток stale. Для свежих данных — psycopg2 LIVE.

### gh CLI
```bash
gh pr create --title "..." --body "..."
gh pr merge <n> --merge  # обычно тимлид мержит сам; агент только если worktree
gh api -X PUT repos/sharkovvlad/noms-bot/pulls/<n>/merge -f merge_method=merge  # workaround если 'main' busy in another worktree
gh run watch --repo sharkovvlad/noms-bot  # watch deploy
```

### SSH к VPS
```bash
ssh root@89.167.86.20 'journalctl -u noms-webhooks --since "10 min ago" --no-pager | grep "tid=786301802"'
ssh root@89.167.86.20 'KEY=$(grep N8N_TARGET_API_KEY /home/noms/n8n/compose/.env | cut -d= -f2); curl -s -H "X-N8N-API-KEY: $KEY" http://127.0.0.1:5678/api/v1/workflows/7EqiiwUwlGs7dcHT'
```

---

## 7. Использование субагентов

**Когда вызывать:**
- Крупные миграции (>200 LOC SQL, многоблочные) — субагент пишет, ты ревьюшь output ДО apply.
- Long-running тесты (E2E через Telethon, integration tests с реальными coords) — параллельно с handler работой.
- Blueprint research (опросить NLM + grep кодбазы + читать прошлые миграции) ПЕРЕД имплементацией — экономит твой контекст.

**Brief субагенту обязательно содержит:**
1. NOMS context (cwd `/Users/vladislav/Documents/NOMS/.claude/worktrees/<your-worktree>/`, ссылки на CLAUDE.md и MEMORY.md).
2. **NLM-first stop-rule** + access-credentials recipe.
3. **No hardcode** правило (тексты → `ui_translations`, числа/эмодзи → `app_constants`).
4. **RPC-first** правило (бизнес-логика в SQL, Python — thin transport).
5. **safe-create-or-replace-recipe** — для любых правок функций.

**После работы субагента — обязательно проверь его output**:
- SQL: `diff` против snapshot, `grep` на регрессии (не должно быть лишних DELETE / DROP / RESET).
- Python: import smoke (`python3 -c "import handlers.X"`), запустить тесты.
- Не доверяй сабэгентскому «всё готово!» — проверь руками минимум 5 минут.

---

## 8. Использование TodoWrite + chapter marks

- TodoWrite для крупных multi-step тасков (3+ дискретных шагов). Обновлять статус ВО ВРЕМЯ работы, не batch'ить completions.
- `mcp__ccd_session__mark_chapter` для переходов между phase'ами или большими блоками работы (research → implementation → verify → PR).

---

## 9. Финальный checklist перед каждым PR (выжимка из CLAUDE.md)

1. **NLM-first** перед миграцией / архитектурным решением.
2. **Локально = GitHub = VPS** консистентность.
3. **Git-дисциплина**: `git fetch origin main && git rebase origin/main` ПЕРЕД commit. `git diff origin/main..HEAD --stat` ПЕРЕД push (sanity check scope). Force-push только `--force-with-lease`.
4. **Supabase = источник правды** — считаем в SQL/RPC, потом в коде.
5. **Без хардкодов** — `ui_translations` для текста, `app_constants` для констант.
6. **n8n** — `curl GET` ПЕРЕД `PUT` (whitelist payload, scp+ssh для >50KB).
7. **p95 latency benchmark** обязателен после RPC/SQL changes — 10 прогонов persistent psycopg2 connection. **Бенчить с VPS, не с мака** (иначе RTT 400ms искажает).
8. **Закрытие сессии**:
   - daily/YYYY-MM-DD.md append (что сделано, открытые вопросы).
   - Крупные cutover'ы — handover/.
   - Новый паттерн / gotcha → KB-статья + запись в index.md.
   - MEMORY.md update если изменилось состояние проекта (HEAD migration, активный cutover).

---

## 10. Стартовый промт для тебя (агента-преемника)

После прочтения файлов из секции 0 — твой первый шаг:

```
1. Сверка состояния прода (5 минут):
   psycopg2 SELECT current_database(), version();
   psycopg2 SELECT key, value FROM app_constants WHERE key LIKE 'handler_%_use_python';
   psycopg2 SELECT screen_id, render_strategy FROM ui_screens 
     WHERE screen_id LIKE 'onboarding_success%' OR screen_id IN ('edit_country','edit_timezone');
   gh pr list --state open --json number,title

2. Спроси тимлида:
   - Manual UAT Phase 6.1 пройден? Можно flip handler_location_use_python=true?
   - PR #57 смерджен (Actions deploy завершён)?
   - Старт Phase 6.2 — добро?

3. Прочитай blueprint: phase6-location-migration-plan.md секции 3.2 и 4.1 — там есть skeleton кода для process_user_input ветки edit-flow.

4. Bench latency Phase 6.1 (10 прогонов с VPS):
   ssh root@89.167.86.20 'python3 -c "
   import psycopg2, time, os
   from dotenv import load_dotenv; load_dotenv(\"/home/taskbot/noms/.env\")
   conn = psycopg2.connect(os.environ[\"DATABASE_URL\"]); cur = conn.cursor()
   for _ in range(10):
     t=time.perf_counter()
     cur.execute(\"SELECT public.dispatch_with_render(786301802, %s, %s, %s, false)\",
                 (\"callback\", \"{\\\"callback_data\\\":\\\"cmd_list_countries\\\"}\", \"{}\"))
     print(f\"{(time.perf_counter()-t)*1000:.0f}ms\")
   "'

5. Если всё зелёное — старт Phase 6.2.
```

---

## 11. Если что-то пойдёт не так

### Rollback Phase 6.1
```sql
-- Мгновенно (60s hot-reload):
UPDATE app_constants SET value='false' WHERE key='handler_location_use_python';
-- Legacy n8n 02.1_Location снова обрабатывает все loc_* clicks.
-- Мiграция 207 (forward → render edit_country inline) остаётся применённой —
-- 6-секундная пауза не вернётся, потому что это SQL-фикс не зависящий от флага.
-- Мiграция 208 (Noop Pattern, unified text) тоже остаётся — text объединён,
-- 👋 carrier ушёл (carrier_text_key из text_key).
```

### Rollback Phase 6.2 (когда напишешь)
Тот же паттерн — `UPDATE app_constants SET value='false'`. Mig 209 (расширение process_user_input для edit-flow) должна быть **idempotent** — старая n8n ветка эти callbacks обрабатывает аналогично (она работала до 12.05).

### Rollback Phase 6.3
Geocoding fail → fallback на picker уже встроен. Если `timezonefinder` падает на старте → handler graceful skip геолокации (forward на picker). Полный откат — `UPDATE app_constants SET value='false'` (флаг) + закомментировать import `timezonefinder` в `services/geocoding.py`.

### Rollback Phase 6.4
n8n workflow reactivate: `POST /workflows/7EqiiwUwlGs7dcHT/activate` (этот endpoint работает без 403).

---

## 12. Что точно НЕ делать

⛔ Не трогать `services/template_engine.py` core (особенно `_resolve_carrier_text` и icon_wave fallback) — критично для не-onboarding путей.
⛔ Не использовать `./deploy.sh` из worktree / feature-веток — auto-deploy через Actions делает rsync безопасно из main.
⛔ Не флипать feature flag без explicit approval тимлида в разговоре.
⛔ Не писать архитектурные доки в основном NOMS-репо (папка `docs/` запрещена) — всё в `claude-memory-compiler/knowledge/concepts/`.
⛔ Не пушить миграции с `CREATE OR REPLACE FUNCTION` где база взята из git-файла — обязательно live snapshot через `pg_get_functiondef`.
⛔ Не запускать `deploy.sh` из worktree — это catastrophic откат (lesson 2026-05-08, KB release-protocol).
⛔ Не делать manual UPDATE прод-юзеров для «починки» застрявших onboarding — это half-measure anti-pattern (lesson 2026-05-09 phase4 gotcha #24). Корректный путь: чинить root cause + использовать `reset_to_onboarding(<tid>)` для catch-up.

---

## 13. Контакт

Тимлид (владелец проекта): `sharkov.vlad@gmail.com`. В Telegram — администратор `chat_id=417002669`. Бот `@nomsaibot`. VPS `root@89.167.86.20`.

Если в процессе обнаружишь что-то важное (новый gotcha, архитектурную развилку, потенциальный bug в прод) — сообщай тимлиду явно с цитатой кода или логов. Он ценит «информацию с полей».

Удачи! 🌍
