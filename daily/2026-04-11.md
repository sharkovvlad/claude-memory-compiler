# Daily Log: 2026-04-11

## Sessions (migrated from CLAUDE.md Changelog)

### Session: 2026-04-11 — Profile UX: Training + Speed + Phenotype кнопки
- Что:
  1. **Build Profile Text v4.1** — trainingMap/speedMap, строки Training Type и Goal Speed в "Твой план", 6-рядная keyboard (Training, Speed, 🧬 Телосложение)
  2. **Command Classifier** — `cmd_edit_phenotype` → `coming_soon`
  3. **Переводы** — `profile.body_type` для 13 языков
- Зачем: Кнопки Training/Speed/Phenotype не появлялись — replace-патчи не сматчили. Keyboard переписана целиком.
- Применено: ✅ n8n API (PUT sxzbSc61YuHFbV4i) + ✅ Supabase REST

### Session: 2026-04-11 — Shop UX Pattern A + Payment Premium Screen

#### ui_translations — 4 новых payment ключа × 13 языков
- Что: `payment.my_subscription`, `payment.active_until` ({date}), `payment.plan_label` ({name}), `payment.renew_plan` — для экрана управления подпиской
- Применено: ✅ Supabase REST PATCH × 13 (ar, de, en, es, fa, fr, hi, id, it, pl, pt, ru, uk)

#### 04_Menu — Shop Pattern A (sxzbSc61YuHFbV4i)
- Что:
  1. **Merge Data**: добавлен `mana_recharges_today: input.mana_recharges_today || 0` (был undefined)
  2. **Build Shop Text** (инлайн): полная перезапись по Pattern A
     - `isPremiumUser` объявлен первым (до текстового блока)
     - Мана-блок и кнопка Мана: `if (!isPremiumUser)` — скрыты для premium
     - Исправлен double-emoji: убран `iconFreeze + ' '` и `iconMana + ' '` перед `s.buy_freeze/buy_mana` (перевод уже содержит `{{icon_*}}`)
     - Premium-юзер: кнопка "👑 Ваша подписка" → `cmd_premium_plans` вместо Go PRO
     - Free/trial: Freeze + Mana + Go PRO (без изменений)
  3. **Edit Shop Message**: `parse_mode Markdown → HTML`
- Применено: ✅ n8n API PUT HTTP 200

#### 10_Payment — Экран подписки для Premium (xYKmBTWI4c0VoSqs)
- Что:
  1. Новая нода **Get Subscription** (HTTP GET Supabase `user_subscriptions?status=eq.active&order=created_at.desc&limit=1`)
  2. Цепочка: `Merge Data → Get Subscription → Payment Router` (было: Merge Data → Payment Router напрямую)
  3. **Build Plans Text** переписан: 2 ветки
     - `isPremiumUser = true` → показывает детали подписки из `Get Subscription`: тариф, дата истечения (DD.MM.YYYY), метод оплаты + кнопки ✨/🔥/⭐ для продления/смены тарифа
     - `isPremiumUser = false` → прежний экран выбора тарифов с ценами
  4. **Show Plans**: `reply_markup` теперь приходит из `Build Plans Text` динамически (ранее был захардкожен)
- Зачем: Premium юзер при нажатии "👑 Ваша подписка" в Магазине видел планы без кнопок. Теперь: детали подписки + кнопки продления.
- Применено: ✅ n8n API PUT HTTP 200
- Верификация: 17/17 структурных проверок ✅

### Session: 2026-04-11 — Bug Fix: cmd_premium_plans routing + cmd_select_plan_* (04_Menu)
- Что:
  1. **Build Profile Text**: `callback_data: isPro ? 'cmd_noop' : 'cmd_premium_plans'` → `callback_data: 'cmd_premium_plans'`. Фикс был затёрт предыдущим деплоем (из stale base).
  2. **Command Classifier**: добавлены `command.startsWith('cmd_select_plan_')` и `command.startsWith('cmd_pay_crypto_')` в payment route. Без этого кнопки выбора тарифа и крипто-оплаты уходили в `coming_soon`.
- Зачем: Профиль → "Премиум" и Shop → "Ваша подписка" показывали "Скоро!" вместо Plans экрана. Profile кнопка был перезатёрт stale base при деплое Shop изменений.
- Применено: ✅ n8n API PUT HTTP 200 (sxzbSc61YuHFbV4i)
- **Правило:** При деплое через PUT всегда использовать `/tmp/*_live.json` (только что скачанный с n8n) как базу, а не старый `/tmp/*.json` из начала сессии.

### Session: 2026-04-11 — "One Menu" UX: удаление старого меню при нажатии reply-кнопки

#### Migration 056: last_bot_message_id в v_user_context
- Что: `DROP VIEW + CREATE VIEW` v_user_context — добавлены `u.last_bot_message_id` и backfill missing `training_type/phenotype/goal_speed/target_*` (из migration 055, которые не были добавлены в live view)
- Зачем: `CREATE OR REPLACE VIEW` не позволяет вставлять колонки в середину списка. Migration 056 пересоздаёт view с 43 колонками.
- Применено: ✅ n8n temp workflow (Postgres pooler) → view пересоздан, 43 колонки подтверждено

#### Phase 2: Dispatcher — last_bot_message_id
- Что: "Prepare for  04" Set node — добавлено поле `last_bot_message_id: $json.last_bot_message_id || 0` (31-е поле)
- Применено: ✅ n8n API PUT (6XzdbQqpoUG0nsLe) HTTP 200

#### Phase 3: 04_Menu — 9 новых нод + Merge Data update
- Что:
  1. **3× "Delete Old Menu (Stats/Profile/Progress)"** — HTTP Request POST deleteMessage, параллельная ветка от Build Stats/Profile/Progress Text, fire-and-forget, `onError: continueRegularOutput` (игнорирует ошибки при удалении)
  2. **3× "Extract Menu ID (Stats/Profile/Progress)"** — Code node: `($json.result || $json).message_id` — универсально для HTTP Request и Telegram node responses
  3. **3× "Save Menu ID (Stats/Profile/Progress)"** — HTTP Request POST `save_bot_message` RPC, Supabase credential, fire-and-forget после отправки
  4. **Merge Data** — добавлен `last_bot_message_id: input.last_bot_message_id || 0`
  5. **04_Menu: 80 → 89 нод**
- Цепочки:
  - `Build Stats Text ─┬─→ Is Callback? (Stats) → ... → Send Stats Response → Extract Menu ID (Stats) → Save Menu ID (Stats)`
  - `                  └─→ Delete Old Menu (Stats) [dead-end]`
  - (то же для Profile и Progress)
- Зачем: При нажатии "☀️ Мой день" / "👤 Профиль" / "🚀 Прогресс" — старое меню удаляется. Food logs и AI ответы никогда не трогаются (save_bot_message вызывается ТОЛЬКО для menu-сообщений). `last_bot_message_id=0` → deleteMessage вернёт ошибку 400 → игнорируется.
- Применено: ✅ n8n API PUT (sxzbSc61YuHFbV4i) — 89 нод, all 9 new nodes confirmed

### Session: 2026-04-11 — Bug Fix: Payment Router $json → $('Merge Data') (10_Payment)
- Что: Все 7 условий Payment Router Switch: `$json.command` → `$('Merge Data').item.json.command`
- Зачем: Добавление `Get Subscription` HTTP node между Merge Data и Payment Router сломало `$json` — n8n Data Flow Rule #1: HTTP Request заменяет `$json`. Payment Router видел Supabase response вместо command → ни одно правило не матчилось → кнопки "Ваша подписка" и "Премиум" не давали отклика. Execution status=success но 0 outputs у роутера.
- Также: Dispatcher `Prepare for 04` — добавлен `callback_query_id` для `answerCallbackQuery`. 04_Menu — добавлена нода `Answer Callback Query` (parallel от Command Classifier, fire-and-forget).
- Применено: ✅ n8n API PUT HTTP 200 (xYKmBTWI4c0VoSqs, sxzbSc61YuHFbV4i, 6XzdbQqpoUG0nsLe)

