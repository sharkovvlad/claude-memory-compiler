# Daily — 2026-05-18

## Mig 246 — calculate_user_targets v7 «safety baseline guards» (P0.3 + P0.4)

**Контекст:** mono-mig завершает P0.3 (min kcal floor) + P0.4 (BMI-aware tiered guards) из [[concepts/calc-user-targets-roadmap]]. v6 (mig 234) age guards preserved 1:1; v7 — atomic baseline для всех BMI extremes + medical floor 1200ж/1500м. Storage уже была applied вчера (mig 239, PR #89).

### Что сделано

- **`migrations/246_safety_baseline_guards.sql`** написан, applied транзакционно через psycopg2 на prod.
- **4 feature flags** в `app_constants` (`safety_guard_min_kcal_enabled`, `safety_guard_bmi_14_enabled`, `safety_guard_bmi_185_enabled`, `safety_guard_bmi_60_enabled` = `true`) для granular per-guard rollback без миграции.
- **Step 1b «BMI sanity + BMI-aware guards»** (new) — BMI 11/16.5/64+fast/64+other → 4 tier'а severity (hard block / hard regulated × 2 / informational).
- **Step 8b «Min kcal floor»** — `v_target := GREATEST(actual, medical_floor, BMR)`. 3 enum'а warning: `bmr_floor_triggered` / `medical_floor_1200_triggered` / `medical_floor_1500_triggered`.
- **Step 8c «guard_audit_log INSERTs»** — `to_regclass`-guard'нутые INSERTs для FTC/legal traceability (trigger_name + event + metadata jsonb).
- **JSON return +4 полей** (`bmi_value`, `bmi_warning`, `min_kcal_warning`, `min_kcal_floor_applied`).
- **COMMENT** → `'v7 (mig 246): safety baseline (min_kcal_floor + BMI-aware guards). v6 (mig 234) age guards preserved 1:1.'`
- **Snapshot** `users_targets_backup_20260518_pre_v7` (35 rows, drop ≥ 2026-05-25).
- **Backfill DO-блок** прогнал все registered через v7 → delta=0 для всех 5 prod users.

### Sentinel verification (8 кейсов transactional BEGIN/ROLLBACK)

✅ Все ожидаемые warnings сработали, baselines (#7/#8) match v6 exactly:

| # | Trigger | Result |
|---|---|---|
| 1 | BMI 11 maintain | `extreme_cachexia_recommend_medical`, target=1378 |
| 2 | BMI 16.5 lose | `underweight_lose_override`, effective_goal=maintain, target=1613 |
| 3 | BMI 64 lose+fast | `extreme_obesity_clamp_slow`, deficit clamped from 20% → 10% |
| 4 | BMI 64 maintain | `extreme_obesity_informational` (informational, нет override) |
| 5 | BMI 15.6 lose+fast | `underweight_lose_override` (BMI guard precedes min_kcal) |
| 6 | M/40/170/55 lose+fast | NO trigger (target=1532 > 1500 floor, BMI=19 > 18.5) |
| 7 | F/30/165/70 baseline | NULL warnings, target=1728 (v6 exact match) |
| 8 | M/25/180/75 gain baseline | NULL warnings, target=2415 (v6 exact match) |

**Audit log live verification** (BMI=11 sentinel): 1 row в `guard_audit_log` с правильным `trigger_name='bmi_aware_extreme_cachexia_recommend_medical'`, metadata JSONB заполнен. INSERT работает на live storage mig 239.

### Latency

- **p95 = 44.11 ms** (25 runs persistent psycopg2 с VPS, warmup excluded).
- Дельта v6→v7 ≈ 0 ms. Все 4 feature flag lookups hit same `app_constants(key)` PK index; BMI = одна арифметика; новые IF блоки no-op для prod 5 users.
- Target <50 ms — OK.

### Pre-apply Rule 7 protocol

- `git fetch origin main` → HEAD на проде = mig 245.
- `gh pr list --state open` → нет конфликтующих PR.
- `ls migrations/ | tail` → 245 last. Свободный = **246**.
- Изначально файл был назван 239 (когда сессия началась 17.05 вечером) — параллельные агенты обогнали (mig 239 storage merged как PR #89, 240/241/242 i18n age warnings, 243/244/245 i18n Phase 4). После git rebase + rename: 239 → 246.
- Stale-base: pg_get_functiondef живого v6 → v7 body. NLM не использовал — функция в живой БД авторитет, NLM возможно stale.

### Sanity-check pre-push

`git diff origin/main..HEAD --stat`: ровно 1 новый файл (migration), плюс 2 KB-файла (roadmap + personalized-macro-split). Никаких удалений в чужих файлах.

### Lessons

1. **Storage dependency была не synchronized.** Когда сессия началась — storage agent ещё не был spawn'нут. Я подготовил mono-mig с `to_regclass`-guard'ом для audit_log INSERTs (defensive net на случай rollback storage). Параллельный agent сделал storage за вечер (mig 239 merged до моего apply). Pattern: для guard'ов, пишущих в инфраструктурные таблицы — всегда `to_regclass` check, не assumption «таблица будет existed».

2. **Sentinel BEGIN/ROLLBACK с прогревом v7 в той же tx работает.** Можно apply миграцию + сразу запустить sentinels против ТОЛЬКО-ЧТО-applied функции, всё внутри одной транзакции. ROLLBACK откатывает и функцию, и sentinel users — zero side effects. Это сократило диагностический цикл «применить → проверить → откатить» с минут до секунд. Pattern идеален для CREATE OR REPLACE FUNCTION migrations.

3. **«Expected: target=3090» в spec был неверным baseline.** Spec предполагал `phenotype='monw'` (LBM proxy = weight × 0.85), а я в INSERT не задал phenotype → default phenotype используется. Реальный v6=v7=2415 для default phenotype. Не блокер — sentinel'ы #7/#8 предназначены для проверки «zero drift v6→v7», абсолютные числа второстепенны. Lesson: при копировании sentinel-spec из roadmap всегда явно указывать `phenotype`, `pal_coefficient` и все опциональные поля — иначе baseline matches только если spec автор и agent одинаково проинтерпретировали defaults.

4. **`v_user.goal_speed := 'slow'` в plpgsql работает.** Можно мутировать field of a record variable, загруженного через `SELECT * INTO v_user FROM users`. Step 6's `COALESCE(v_user.goal_speed, 'normal')` после этого читает clamp'нутое 'slow'. Без необходимости вводить отдельную переменную `v_effective_goal_speed`.

### Артефакты

- **Migration:** `migrations/246_safety_baseline_guards.sql` (+~280 строк, including snapshot + backfill DO + flags + v7 body + 4 enum branches BMI + 3 enum branches min kcal + audit_log).
- **Snapshot:** `users_targets_backup_20260518_pre_v7` (35 rows).
- **PR:** pending (создаётся в конце сессии).
- **KB updated:**
  - `concepts/calc-user-targets-roadmap.md` — Status block 2026-05-17 → 2026-05-18, P0.3+P0.4 marked ✅ done.
  - `concepts/personalized-macro-split.md` — добавлен v6 reference + полная v7 секция (изменения, sentinels, latency, lessons, что не покрыто).
- **Open work (для следующих сессий):**
  - Python handler updates для `bmi_warning` + `min_kcal_warning` rendering (Profile screen + day_summary + first-trigger modals).
  - Copywriter spawn для 5 warning families × 5 surfaces × 13 langs = ~325 translation entries для bmi/min_kcal (analog mig 240/241/242 для age guards).
  - L1+L2 cultural review для hard block / hard regulated warning bodies (per `agent-collaboration-protocol` Rule 9).
  - Retrofit cron NOT required — BMI 14/18.5/60 и min_kcal floor триггерятся at every `calculate_user_targets` call (синхронно), не нужно one-time retrofit.

### Следующая P0 задача

**P0.6 pregnancy/lactation** — заблокирована на UX-wireframe от owner'а (где собирать `is_pregnant` флаг: onboarding step / Profile toggle / retrofit popup). Clinical spec готов в [[concepts/pregnancy-lactation-clinical-spec]]. Spawn — когда owner даст UX-wireframe.
