# 2026-06-09

## Sage VARIATION GUARD — wiring fix (PR #376)

**Контекст:** В handover `2026-06-08_sage-payload-meta.md` (PR #375) была открыта 🔴 задача task_7839624f: «дебаг, почему META `VARIATION GUARD` не появляется в `SAGE_MY_DAY_PROMPT` даже на 5-й my_day одного юзера со стампом в `ai_coach_logs`». В handover я выдвинул две гипотезы — SDK shape и фильтр `success=true`. Обе оказались мимо.

**Найденная причина (одна строка):** [services/sage.py:569](services/sage.py:569) — `regen_my_day_insight_cache` вызывал `generate_my_day_insight(ctx, day_summary)` **без** пробрасывания `rpc_caller`. У `generate_my_day_insight` default `rpc_caller=None` (был помечен `# reserved` ещё до PR #375). Внутри `_fetch_recent_sage_reactions` первой же строкой `if rpc_caller is None: return []` → пустой список реакций → `repeat_protein_stamp=False` → META не приклеивалась. food_log-путь не страдал — `handlers/food_log.py:139` всегда прокидывал `rpc_caller=_supabase.rpc`.

**Что отметено:**
- **SDK shape** — `supabase_client.SupabaseClient.rpc` возвращает `response.json()` прямо; для `RETURNS TABLE` это `list[dict]` нативно, без `APIResponse.data`. Никакого unwrap не нужно.
- **`success=true` фильтр** — на проде `psql -c "SELECT success, count(*) FROM ai_coach_logs WHERE context_type LIKE 'sage_%' AND created_at > now()-interval '2 hour' GROUP BY 1"` дал 19/19 строк с `success=t`. Фильтр в `mig 496` корректен.

**Почему unit-тесты не поймали:** `test_my_day_e2e_repeat_meta_flows_to_prompt` (PR #375, 38 тестов) инжектировал `rpc_caller` прямо в `generate_my_day_insight(..., rpc_caller=rpc)`. Тест доказывал «лист работает, если ему дать дверь», не «оркестратор пробрасывает дверь листу». Ровно паттерн `feedback_integration_tests_rpc.md` / KB [[concepts/sage-payload-meta-override-pattern]] — unit-моки скрывают wiring drift.

**Фикс:**
- [services/sage.py:569](services/sage.py:569) — `result = await generate_my_day_insight(ctx, day_summary, rpc_caller=rpc_caller)`.
- Удалён комментарий `# reserved` на параметре (он теперь активный).
- Новый тест `test_regen_my_day_cache_forwards_rpc_caller_for_repeat_meta` — мокает оба RPC (`get_day_summary` + `get_recent_sage_reactions`) и проверяет, что в финальный OpenAI-промпт попал `VARIATION GUARD`. Это тест на **wiring**, не на лист.
- Две стейл-ассерции в `test_sage.py` (count'ы RPC-вызовов в my_day cycle) обновлены: было 2→3 на success, 1→2 на LLM fail (теперь между ними всегда `get_recent_sage_reactions`).

**Применено:** PR [noms-bot#376](https://github.com/sharkovvlad/noms-bot/pull/376) открыт, не merged. GitHub Actions auto-deploy — после merge.

**Проверка на проде (после merge):**
```bash
ssh root@89.167.86.20 'journalctl -u noms-webhooks --since "5 min ago" | grep SAGE_MY_DAY_PROMPT | grep -c "VARIATION GUARD"'
```
Должно быть `>0` для юзеров с повторяющимися sage-реакциями.

**Тесты:** `pytest tests/services/` — 689 passed, 1 skipped (barcode missing module — не моё).

**Git-дисциплина (CLAUDE.md §12):** rebase before commit (clean), sanity-check `git diff origin/main..HEAD --stat` — 3 файла все sage-related, +68/-12, без collateral changes. Force-with-lease не понадобился (новая ветка).

## KB-аудит и MEMORY-чистка (`/kb-close --audit`)

- **Audit script:** 157 концептов, **0 orphans**, **13 merge candidates** (все низкоscore: `sticker-architecture-adr↔n8n-self-hosting`, `supabase-db-patterns` как canonical для трёх разных тем — false positives, общие словари / хабы; `n8n-self-hosting` уже merged stub с 06-05). **Реальных дублей не найдено.**
- **Stale:** 17 концептов > 60 дней. Единственный `high` severity — `user-profile-personalization.md` — уже имеет stale-баннер от 06-05. Остальные 16 — `warning` (n8n-tagged живые legacy-доки или Python-cutover статьи типа `food-log-python-cutover.md`, обновлять без content change = плохой сигнал). Не трогал.
- **KB concept update:** [[sage-payload-meta-override-pattern]] — секция «Wiring gotcha» добавлена + open issue про VARIATION GUARD заменён на ✅ resolution с PR #376. `updated: 2026-06-09`, source `daily/2026-06-09.md`.
- **Lint:** 0 errors, 54 warnings (бэклинки), 444 suggestions (sparse articles) — не введены моими правками, базовая KB-hygiene; чинить не сейчас.
- **MEMORY:** бэкап `MEMORY.backup-2026-06-09.md` создан. До чистки 26.8 KB / 128 строк → после 20.8 KB / 128 строк (−22 % bytes, line count не менялся — компрессия per-line). Сжаты длиннейшие entries: line 9 Sage META (1311→645 chars, детали перенесены в KB [[sage-payload-meta-override-pattern]] и handover 06-08), line 10 Luteal (969→657), line 11 Ambassador (864→520), line 26 n8n legacy (722→453), line 54 Cron tz-refactor (540→295), line 35 i18n остатки (717→290). Durable bullets сохранены, удалено только descriptive / numerical детали с pointer'ами в KB/daily/handover. Линия 9 после сжатия включает новый wiring-test durable. 20.8 KB всё ещё marginal над 20-KB strict-порогом — дальнейшее сжатие рискует durable lessons; флагую владельцу для возможного запуска `/anthropic-skills:consolidate-memory` если поднимется выше 22 KB.

**Audit closeout (per kb-close G):** слито 0 дублей (реальных не было), помечено 0 stale (high уже banner'нут, warning'и не пилили без content change), добавлено 0 orphans в index (orphans=0), MEMORY 26.8 → 20.8 KB.

---

## ~16:30 — Вывод легаси n8n: удалены 04.2_Edit_StatsDaily + 08.3_Friends (n8n 5→3)

**Что:** расследование трёх остаточных n8n-блоков → удаление двух мёртвых, сохранение одного живого.
- **08.3_Friends** (`su5JZbUXOgE614Lo`) — DELETED. Сирота: 0 executeWorkflow-ссылок, payout НЕ внутри (обрабатывается в Python `handlers/admin_stars.py`). Caveat из registry снят.
- **04.2_Edit_StatsDaily** (`wgY05rXde1PbszSk`) — DELETED. Все триггеры в Python: `cmd_show_meals`→menu_v3 (`meals_picker`), `cmd_view_meal_`/`del_`/`start_edit_`=0/30д (Edit Meal cutover 06-08). NLM подтвердил headless (`meal_action`/`get_meal_action_data`, `stats_main`/`get_daily_stats_rpc`).
- **04_Menu Safe PUT** — убраны ноды `Go to 04.2` + `Prepare for 04.2` (124→122), ветка Switch #3 «meals» опустошена `[]` (позиции 26 остальных сохранены), active=True.
- **04_Menu ОСТАВЛЕН** — живой fallback (96 событий/2нед: `cmd_wellbeing_today`, `cmd_diet_*`, `cmd_cycle_view`, `cmd_edit_waist`, главные reply-кнопки).

**Применено LIVE:** оба DELETE (HTTP 200) + Safe PUT 04_Menu — в живом n8n. Smoke: `/health` 200, 0 ошибок в n8n-контейнере и боте, 0 оставшихся ссылок на 04.2.

**Git:** PR #378 (только снапшоты `n8n_workflows/`, деплоя не требует). **Open для owner:** PR #378 можно смержить для записи (опционально).

**KB:** обновлён `architecture-registry.md` (header 5→3, строки 04.2/08.3/04_Menu/admin_payout, caveat снят, blocker-нота 160) + `stage7-global-cutover.md` (§ВЫПОЛНЕНО-06-09 с durable-уроком).

**Durable урок:** для executeWorkflow-only sub-workflow гейт необратимого DELETE = **структурная недостижимость** (убрать ВСЕ executeWorkflow-ссылки Safe PUT'ом, полная ref-проверка по всем workflow) — надёжнее «подождать N дней». Техника Safe PUT Switch: мёртвую ветку опустошать `[]`, не удалять (иначе сдвиг позиций остальных выходов).

**Следующий шаг к выключению 04_Menu** (отдельный проект, НЕ удаление): разобрать остаточный трафик — настоящие функции (wellbeing/diet/cycle/waist) мигрировать в Python, misroute-баги (cmd_diet_*/cmd_skip_meal/cmd_edit_waist падают в legacy «silent fail» по комментариям router.py) починить в router.py.
