# Daily Log: 2026-05-27

## Sessions

### Recovery gap + UX redesign sprint (agent elastic-banzai-35ff6a, session 9)

**Context:** Owner reported residual P0 bugs after mig 360 recovery — reply-kb labels render as raw `buttons.st...` / `buttons.p...` in es (and ALL langs), plus onboarding broken for F/15-50 user 786301802 (es).

**Investigation (live SELECT audit):**
- Mig 360 multi-pattern parser recovered 78 `buttons.*` keys + 6 `onboarding.*` keys.
- **Gap:** 5 keys × 13 langs still NULL:
  - `buttons.stats` — referenced ONLY by `build_main_reply_keyboard()` RPC (n8n-era hardcoded; no migration history → parser missed).
  - `buttons.profile` — same.
  - `onboarding.maternal_status_question` — exists in mig 259 (RU/EN) + 269 (11 langs) but parser regex missed `to_jsonb($v$...$v$)` form for these.
  - `onboarding.maternal_trimester_question` — same.
  - `onboarding.maternal_lactation_type_question` — same.

**Mig 361 applied LIVE** (PR #208, CI green, awaiting merge):
- Safe per-leaf `jsonb_set` (KB [[jsonb-shallow-merge-antipattern]]).
- Historical text sources: mig 259 (RU/EN strings) + mig 269 §B regex `*X*`→`<b>X</b>` for RU/EN HTML conversion + mig 269 §A for 11 langs.
- Hand-crafted `buttons.stats` (My Day) + `buttons.profile` (Profile) × 13 langs — **NO emoji prefix** because `ui_screen_buttons.icon_const_key=icon_myday/icon_profile` already prepends icon (KB [[double-emoji-button-anti-pattern]]).
- Post-verify DO block: `NOTICE: mig 361: all 5 × 13 = 65 keys verified present.` confirmed.
- Live SELECT × 13 langs × 5 keys = 65/65 OK.

**Owner's UX redesign requests (in flight, planning phase):**

1. **Track B — `payment.my_subscription_body` redesign.** Current single-line `{plan_label}\n\n{state_line}\n\n💬 Noms: ...` → restructure to «Тариф / Способ оплаты / Доступ до / Авто-продление / [Прогресс-бар если ≤14дн] / Sage». Owner approved progress-bar + auto-renewal toggle. Date format must be **localized per lang** (Headless principle: in SQL `to_char`, not Python template). RPC `get_subscription_business_data` already returns `payment_method`/`stripe_subscription_id`/`cancelled_at` — extend with `payment_method_line` / `access_until_line` / `auto_renewal_line` / `days_remaining_line` / `sage_line_contextual`. Sage line will have 3 variants (active, renewal_soon ≤14dn, cancelled) — 3 different `ui_translations` keys, RPC picks one.

2. **Track C — `delete_account.warning_body` emotional redesign.** Grateful tone (owner-confirmed), include `meals_logged` total (new RPC field), emoji selection via `/nlm` from `app_constants` icon catalog. Constraint: zero shame, zero pressure ("это твой выбор").

3. **Track D (deferred) — CI guard for `content || payload` anti-pattern.** Tier 1 from yesterday's handover.

4. **Track E (deferred to separate session) — Menu latency optimization.** Double `GET v_user_context` × 2 per callback (170ms of 330ms total). Hot-path refactor in `dispatcher/menu.py`.

**Files:**
- `migrations/361_recovery_gap_buttons_stats_profile_maternal.sql` — 371 lines, applied LIVE.
- PR #208 — https://github.com/sharkovvlad/noms-bot/pull/208

**State on 2026-05-27 ~16:00 MSK:**
- Migration HEAD on `origin/main`: 360.
- Migration HEAD on prod DB: 361 (applied via psycopg2 before PR merge — standard pattern per CLAUDE.md «Apply autonomously»).
- After PR #208 merge → no file/DB drift.

**Tracks B + C SHIPPED in same session:**

**Mig 362 (PR #209)** — subscription status UX redesign:
- RPC v2 `get_subscription_business_data` returns 5 new fields (payment_method_line, access_until_line, auto_renewal_line, days_remaining_line, sage_line_contextual).
- Headless date format in SQL via per-lang `to_char` CASE (no Python locale dependency).
- 14 new `payment.*` keys × 13 langs = 182 jsonb_set per leaf.
- 4 contextual Sage variants (active/renewal_soon/cancelled/trial).
- Premium-hide pattern (LEADING `\n`) for optional lines.
- Auto-renewal derived from `payment_method='stripe' AND cancelled_at IS NULL` — no Stripe API call (Phase 6.4 TODO unchanged).
- Subagent for 11 langs (ar/de/es/fa/fr/hi/id/it/pl/pt/uk) with per-lang glossary adaptation. L1 self-critic clean.
- Smoke-test admin (wallet/TON cancelled): correct sage_cancelled + ru locale date "8 авг 2026" + empty auto_renewal_line (prepaid).

**Mig 363 (PR #210)** — delete_account emotional redesign:
- New constant `icon_oh = '😳'` in `app_constants` (no hardcode).
- RPC patch: `get_profile_business_data` + `meals_logged_total` via live-baseline (`pg_get_functiondef` → inject → `CREATE OR REPLACE`). Idempotent (skips if already patched).
- 8 new `delete_account.*` keys × 13 langs = 104 jsonb_set per leaf.
- Tone: gentle/grateful farewell (zero pressure, anti-shame).
- Owner's literal phrase «доверял мне» was RU-masculine-marked → adjusted to gender-safe «Спасибо за доверие эти дни».
- HI Hinglish-Latin per glossary §4. AR/FA ZWNJ+LRM applied.
- Smoke-test admin: `meals_logged_total=870` returned by patched RPC; templates render correctly for ru/en/es/de.

**Open PRs:**
| PR | Track | Status |
|---|---|---|
| #208 | mig 361 recovery gap | ✅ MERGED 2026-05-27 |
| #209 | mig 362 subscription UX | ⏳ awaits owner merge (CI auto-runs) |
| #210 | mig 363 delete account UX | ⏳ awaits owner merge (CI auto-runs) |

**Bug found mid-session:** Python build-script `\n` inside SQL `--` comments interpreted as literal newline → broke `CREATE OR REPLACE` syntax. Fixed in mig 362 by manually rewriting 2 comments. Note for future RPC scaffolding: use `\\n` or avoid `\n` text inside f-string comments.

**Migration HEAD on prod DB:** 363. After PR #209 + #210 merge → file-HEAD matches.

**Tier 1 SHIPPED late in same session (after #209+#210 merged):**

**Track D — CI guard for `content || payload` anti-pattern (PR #211):**
- `.github/workflows/pr-jsonb-merge-guard.yml` — pre-merge regex scan of `migrations/*.sql` for `\bcontent\s*\|\|`.
- Posts/updates single PR comment via `<!-- pr-jsonb-merge-guard -->` tag marker (idempotent, mirrors `pr-migration-collision.yml`).
- False-positive escape: per-line `-- jsonb-merge-safe: <reason>` suppression.
- CI runs on PR's own file (no migration touched) — green.

**Track E — TD-#19 per-request context cache (PR #212):**
- New: `dispatcher.context.enter_request_scope()` + `get_user_context(..., force_refresh=False)`.
- Cache lives in `ContextVar` (per asyncio-task scope). Set at top of `_route_or_forward_locked`. Subsequent `get_user_context(tid)` calls within same update return memoized value.
- Backward-compatible: callsites OUTSIDE scope (no `enter_request_scope` called) hit DB as before. No callsite was forced to migrate.
- Cache invalidation: `handlers/onboarding_v3.py:711` language refresh passes `force_refresh=True` (set_user_language just mutated row).
- 6 new tests in `tests/dispatcher/test_context.py` cover: no-scope-no-cache, memoize-within-task, per-telegram-id, force_refresh bypass, None-caching (no retry storm), inter-scope reset.
- 468/469 pytest passing. 1 pre-existing failure unrelated (`test_route_table[cmd_cancel_subscription on registered]` expects `menu_v3` target but router returns `payment` — out of scope).
- Expected impact: callback latency p95 330 ms → ~250 ms on flows hitting ≥2 fallback branches (most callbacks today).

**Stars payment clarification:**
Owner asked «надо делать Stars» — investigated and found Stars FULLY WORKS in Python end-to-end:
- Router: `cmd_pay_stars_*` in `PROFILE_V5_CALLBACKS` → `target='payment'` → Python handler.
- `handlers/payment.py:_handle_pay_stars` does `sendInvoice` with `currency='XTR'`.
- `webhook_server.py:_handle_pre_checkout` + `_handle_successful_payment` → creates `user_subscriptions` row with `payment_method='telegram_stars'`.
- 2 real Stars users in prod (tids 1670095403 active, 417002669 replaced).
- mig 362 RPC v2 correctly renders Stars subscription: tested for tid=1670095403 → `«Способ оплаты: Stars»` + `«Доступ до: 20 авг 2026»` + empty `auto_renewal_line` (prepaid, no Stripe).
- What's deferred to Phase 6.4 is NOT one-time Stars payment (works), but Telegram **Stars recurring subscriptions API** (newer feature — auto-renew). For now Stars is correctly prepaid: pay → N months → expires → user can re-buy manually. My earlier «Phase 6.4 not integrated» wording was ambiguous; clarified in commit messages and handover.

**Final open-PR queue at session close:**
| PR | Track | Status |
|---|---|---|
| #208 mig 361 recovery gap | A | ✅ merged |
| #209 mig 362 subscription UX | B | ✅ merged |
| #210 mig 363 delete account UX | C | ✅ merged (after `git rebase --onto origin/main` cleared spurious mig 362 collision) |
| #211 jsonb merge guard | D | ⏳ awaits owner merge (CI green) |
| #212 TD-#19 context cache | E | ⏳ awaits owner merge (CI green) |

**Lessons learned this session:**
1. **`\n` in Python f-string SQL comments** breaks SQL parser. Future RPC scaffolding via Python: use `\\n` or rewrite comment to avoid `\n` substring.
2. **Subagent header format drift** — copywriter subagent C used `-- ── XX (annotation) ──` instead of `-- ── XX ──`. Permissive parser required. Worth tightening subagent brief to enforce exact header format.
3. **Worktree git operations from `cd /Users/vladislav/Documents/NOMS && git ...`** operate on main clone, not worktree (because each bash invocation cwd-resets). For mig commit/PR work, this is fine (everything in main clone) — just be aware that worktree's git state diverges from main clone's.
