# 2026-06-08

## Barcode регионализация (Часть 2) — PR #357 создан (orphan commit спасён)

**Проблема:** в прошлой сессии (06-07 поздно) добавил коммит `f518da6` (локализованные имена + lc/cc + self-learning кэш) поверх ветки `claude/funny-feistel-0d5c01`, в которой PR #341 был **уже merged** двумя сутками раньше. Коммит просидел orphan — нового PR не было создано, а старый закрыт. Owner поймал на верификации.

**Симптом обнаружен через:**
- `gh pr view 341 --json state,mergedAt,headRefOid` → MERGED at 8853038
- `git log origin/main..HEAD` → один коммит `f518da6` не в main
- Прод на VPS: ✅ Часть 1 LIVE (`grep delete_thinking + barcode_scan_retry` в `food_log.py`), ❌ Часть 2 НЕ LIVE (нет `_pick_localized_name`, `is_macro_correction`, `upsert_user_correction`)

**Что сделано:**
- `git fetch + rebase origin/main` (поймал stale-base: diff показывал удаление чужой mig 483 в 452 строки)
- После rebase scope схлопнулся до 3 файлов / 306 добавлений
- `git push --force-with-lease`
- `gh pr create` → **PR #357**

**🔑 Durable процесс-урок:** после merge PR, если планируется продолжение работы — **новая ветка**, не дописывать в уже-merged. Иначе коммит orphan'ится и теряется. Проверка перед exit: `gh pr view <num> --json state` + `git log origin/main..HEAD` (если есть коммиты — есть открытый PR или нужно создать).

---

## Каскад источников: USDA + GPT-fallback по EAN — PR #364 (mig 488 LIVE)

**Цель:** расширить barcode lookup из одноисточниковой (только OpenFoodFacts) в **4-tier cascade** для weak-coverage regions (RU/IR/IN/AR) и брендов которых нет в OFF.

```
cache → OpenFoodFacts → USDA FoodData Central → GPT-fallback по EAN
```

### Tier 3: USDA FoodData Central (`services/usda_client.py`)

- Free API US Department of Agriculture (signup 30 сек, 1000 req/hour)
- Силён для **американских брендов** (Coca-Cola, Pepsi, Kraft, General Mills) которые широко распространены в LATAM
- Поиск по UPC в Branded foods: `GET /fdc/v1/foods/search?query=<UPC>&dataType=Branded&api_key=...`
- Nutrient IDs: 1008=kcal, 1003=protein, 1004=fat, 1005=carbs (per 100g)
- **Активация:** owner кладёт `USDA_API_KEY=...` в `/home/taskbot/noms/.env` + рестарт `noms-webhooks`. Без env — слой no-ops silently → каскад идёт дальше
- Защита: `servingSizeUnit='ml'` → НЕ выставлять `package_grams` (плотность неизвестна, мл≠г)

### Tier 4: GPT-fallback по EAN

- `services/barcode.py::fetch_gpt_fallback` — gpt-4o-mini structured output
- Промпт-инструкции: «угадывать категорию хуже чем сказать "не знаю"»; confidence floor 0.7; is_known=false → discard
- **EAN-prefix → country mapping** (~85 GS1 ranges в `_EAN_PREFIX_COUNTRY`): префикс 460 = RU, 626 = IR, 890 = IN, 482 = UA, 750 = MX, 789-790 = BR, 84 = ES, ...
- Это feeds промпт как hint: «Barcode 4607097960058 / EAN prefix country: RU / User country hint: RU → what product?»
- Цена: копейки за запрос (~250 токенов roundtrip)
- **Гейт:** флаг `gpt_barcode_fallback_enabled` (default OFF) — переключение через app_constants без рестарта (hot-reload через v_user_context)

### Каскад в `lookup_product`

- Каждый non-cache hit прогревает `barcode_cache` с правильным `source` (`openfoodfacts` / `usda` / `gpt_inferred`) → следующее сканирование = cache hit
- Layer exceptions изолированы — USDA упал → каскад продолжается на GPT
- Все 4 layer'а отвязаны (моки в тестах легко изолируют каждый)

### Wire в `handlers/food_log.py::_try_barcode_lookup`

- Читает 2 флага из `ctx.constants`: `barcode_cascade_use_usda` (default ON), `gpt_barcode_fallback_enabled` (default OFF)
- Lazy OpenAI client init — только когда GPT-fallback включён (не создаём клиент для пользователей где флаг OFF)
- Пробрасывает `lang_code` + `country_code` в каскад

### mig 488 — флаги в `app_constants`

```
barcode_cascade_use_usda      = 'true'   (default ON; no-ops if USDA_API_KEY env empty)
gpt_barcode_fallback_enabled  = 'false'  (default OFF; cents per call)
```

**Применено LIVE** (verified через psycopg2).

### Тесты

22 новых unit-теста в `tests/services/test_barcode_cascade.py`:
- EAN prefix → country mapping (US/EU/LATAM/RU/IR/IN/UA/ID)
- USDA: api-key gate, nutrient extraction (search vs food endpoint shapes), ml-refusal, kcal-required, request shape (query+dataType+api_key)
- GPT-fallback: confidence floor 0.7, is_known=false skip, no-kcal defensive, EAN-country hint passed to prompt
- Cascade orchestration: cache short-circuit, OFF miss → USDA, OFF+USDA miss → GPT, flags ON/OFF, all-miss returns None, USDA exception → continue to GPT

**Total barcode test coverage: 63 passed, 1 skipped (zxingcpp local-only).**

### 🔑 Durable

1. **Каскадные сервисные слои** проектировать так чтобы каждый можно было отключить флагом + miss/exception одного НЕ ломал остальных. Pattern: lookup_product сам не знает где сошлось — он перебирает tier'ы и cache'ует первый hit. Mocking в тестах элементарный (patch один tier, проверить fallthrough).

2. **External API integrations с env-key gate:** signup-free / api-key tier — клиент должен `no-op silently` если env unset (не raise, не warning spam). Каскад continues. Owner может включить «бесплатной строчкой в .env + рестарт» без code change. Pattern: `if not api_key: return None`.

3. **GPT для угадывания** — confidence-gated structured output, НЕ free-form. Иначе модель «уверенно врёт» (категорийное «молоко» вместо конкретного бренда → poison cache). Confidence floor 0.7 — owner-tunable константа.

4. **EAN-prefix country mapping** — ценный side-effect: можем использовать в других местах (например, биас vision-распознавания для regional products), но пока только для GPT-fallback prompt hint. Если понадобится отдельно — выделить в `services/ean_prefix.py`.

### Активация на проде (для следующего агента)

- **PR #364 не merged** на момент закрытия сессии (ждёт CI + owner ревью)
- **USDA включится автоматически** как только owner положит `USDA_API_KEY` в `/home/taskbot/noms/.env` + рестарт noms-webhooks. Без ключа — слой остаётся inert
- **GPT-fallback** активируется переключением `gpt_barcode_fallback_enabled` в app_constants на `'true'` (hot-reload, без рестарта)
- KB `barcode-logging-openfoodfacts.md` добавлена секция §11.

---

## Открытые PRs на момент закрытия сессии

| PR | Что | Статус |
|---|---|---|
| **#357** | Регионализация: lc/cc + локализованные имена + self-learning кэш | open |
| **#364** | Каскад: USDA + GPT-fallback по EAN | open |

mig 488 уже LIVE независимо от merge — флаги активируются как только PR с кодом выйдет на VPS.

**🟡 MEMORY раздулась: 30.7 KB / 140 строк (> 20 KB порога).** Кандидат на `/kb-close --audit` со стороны следующего сессии — много старых блоков 06-07 которые уже в shipped можно бы сжать.

---

## Инцидент: мужчина-админ получил «лютеиновая фаза, +175 ккал» (PR #366 + #367)

**Триггер:** owner получил pусский текст «🌸 Лютеиновая фаза активна. Добавил +175 ккал» на телефон. Пользователь — мужчина (admin 417002669). Параллельно тот же пуш ушёл и реальной женщине 786301802 (cycle_day=20).

### Корневые причины (три независимых)

1. **Stale data:** owner тестировал переключение пола → `cycle_tracking_enabled=true` + `cycle_start_date='2026-05-25'` остались на male-юзере. Никакого каскада «при смене пола обнулить женские поля» не было.

2. **Нет gender-гейта в `compute_cycle_day_for_user` (mig 375):** проверял `cycle_tracking_enabled`, `is_pregnant`, `is_lactating`, возраст ≥55 — но не `gender='female'`. Возвращал валидный cycle_day для мужчины с включённым трекингом.

3. **luteal_morning — info-only, текст обманывал:** комментарий в `crons/reminders.py` прямо признаётся «luteal_morning info-only (нет screen'а)». Cron слал текст «added +175 kcal», но `apply_daily_modifier` НЕ вызывал → `daily_modifiers` пуст за 30 дней → `daily_calorie_target` не двигался. Все Premium-женщины 8 месяцев получали ложное обещание. Плюс цифра 175 жёстко зашита, хотя для luteal_early (дни 15-21) должна быть 100.

### Что сделано

**PR #366 — mig 490 (defence: каскад + gender-гейт):**
- `trg_users_cascade_clear_female` — BEFORE UPDATE OF gender, birth_date, is_pregnant, is_lactating. При уходе `gender != 'female'` стирает все 11 женских полей; беременность/лактация → выключают трекинг цикла; возраст ≥55 → выключают трекинг.
- Backfill: затронул ровно 1 строку (тот же админ).
- `compute_cycle_day_for_user`: добавлен `gender IS DISTINCT FROM 'female' → NULL` в начало CASE.
- Live-trigger тест: female→male UPDATE одной транзакцией чистит все 11 полей. Реальная женщина 786301802 продолжает корректно отдавать cycle_day=20.

**PR #367 — mig 491 (правда: cron реально пишет модификатор):**
- `cron_get_reminder_candidates` JSON output расширен (additive, copy 1:1 selection logic): добавлены `luteal_phase` ('luteal_early'|'luteal_late') и `luteal_kcal_delta` (100|175).
- 13 переводов `cron_notifications.reminder_luteal_morning`: «+175» / «+۱۷۵» → «+{kcal_delta}» placeholder. Латиница 12 langs + arabic-indic для fa. 0 leftover'ов.
- `crons/reminders.py`: перед сборкой message для luteal_morning — `await supabase.rpc('apply_daily_modifier', tid, 'luteal', luteal_phase)`. **Текст шлётся ТОЛЬКО при `applied is True`** (не только при отсутствии `error` — RPC возвращает `suppressed=true` без `error` для opt_in_required/pregnant/lactating, первая версия проверки пропустила бы это как успех).

### Verification (на проде через BEGIN/ROLLBACK)

- `apply_daily_modifier(786301802 female, luteal, luteal_early)` → `applied=true, modifier_id=684, calories_delta=100`, строка в `daily_modifiers` создана.
- `apply_daily_modifier(417002669 male, luteal, luteal_early)` → `applied=false, suppressed=true, reason='opt_in_required'` (после mig 490 cycle_tracking=false).

### Durable lessons

1. **Связанные поля = каскад.** Любое поле, осмысленное только при определённом значении другого поля (gender / pregnancy / age), требует либо триггера-каскада на уровне БД, либо CHECK constraint. Доверять «никто никогда не оставит грязное состояние» нельзя — owner это и сделал тестом.
2. **Защита в несколько слоёв.** Каскад чистит данные, gender-гейт в функции страхует от data-drift, RPC `apply_daily_modifier` валидирует ещё раз, Python проверяет `applied=true` перед действием. Любые два уровня могут отказать — третий сработает.
3. **`applied=false` ≠ `error`.** Postgres RPC, возвращающие jsonb, часто пишут `{"applied":false,"suppressed":true,"reason":"..."}` для «мягких» отказов. Питон-проверка только по `error` — ложно-положительный success.
4. **Текст-обещание = действие.** Любое cron-сообщение вида «added +N» требует, чтобы код перед отправкой записал то самое действие. Иначе годами шлёшь ложь и узнаёшь об этом случайно.
5. **Hard-coded цифры в i18n-переводах ломаются при изменении business-логики.** «+175» сидело в 13 языках и не покрывало `luteal_early=100`. Placeholder `{kcal_delta}` решает.
6. **Worktree-навигация:** `cd /Users/vladislav/Documents/NOMS` в bash из worktree → переход в **другой клон** (главный, привязан к другой ветке). `git status` оттуда показывает огромный destructive diff, который не имеет отношения к твоей работе. Работать только с относительными путями от текущего worktree, либо использовать absolute path к своему worktree.

### Известно по KB / NLM

- **+175/+100 ккал** не имеют научной цитаты в коде/KB. Комментарий миграции 301:513 говорит «progesterone thermogenesis», но без ссылки на исследование. Плоские числа, не % BMR. Кандидат на отдельную KB статью `[[luteal-phase-energy-expenditure-rationale]]` когда owner найдёт источник.
- Валидные `users.gender` — только `'male'` и `'female'` (NLM подтвердил через UI buttons). Other / non_binary не поддерживаются.

### Очередь следующих PR (не сделано в этой сессии)

- **PR #3 (отдельный):** ежедневный дайджест в админ-чат по аналогии с `stars_digest` — «luteal_morning sent N today, of which X male / Y under-18 / Z opted-out». Поймал бы инцидент в первый же день.
- **Killswitch:** `cron_reminder_<type>_enabled` флаги в `app_constants`, чтобы выключать класс пушей за секунды без deploy.
- **Архитектурный рефакторинг:** реестр `reminder_types` в БД с явным `audience_filter_sql`, `requires_action`, `kcal_delta_source` — заменить разнесённые `WHEN`-ветки в RPC + три map'а в Python.

---

## Ambassador commission: per-method base + EUR normalization (mig 493)

**Контекст.** Owner поймал в прошлой сессии (07.06): backfill комиссии Евгении $3.25 был **завышен в 1.7×** — взята база DEFAULT $12.99, хотя реально она купила quarterly **CIS** ($7.49) за 580⭐. Plus валюта учёта была хаосом (USD/EUR/USDT/XTR смешано).

**Решения owner (закреплены в handover 2026-06-07_ambassador-commission-currency.md):**
1. Stars-комиссия с **NET** (XTR × payout-rate), card/USDT — с **полной** оплаты.
2. Валюта учёта и выплат = **EUR** (юрлицо в Испании).

**Что выяснилось в этой сессии (диалог с owner):**

- **Перекосов в тарифах нет.** Прошёлся по всем активным регионам/периодам — Stars net ≈ card gross ≈ USDT face в пределах ±2-5%. Owner уже интуитивно затюнил Stars-номиналы с учётом ~30% margin Telegram. Решение «Stars=net, card/USDT=full» даёт амбассадору ≈одинаковую комиссию независимо от метода (±1%).
- **Курс $0.013/⭐ — НЕ официальный.** Это публичная эвристика. Telegram не публикует API курса ⭐→USD (выдаёт TON через Fragment, TON-курс плавает). Real payout зависит от платформы (desktop ~$0.013, mobile через Apple/Google ~$0.010). Ground truth — только реальная выписка Fragment. До первого вывода — placeholder с пометкой `requires_calibration_after_first_fragment_withdraw` в комментарии константы.
- **Скриншоты owner UA/RU:** 1000⭐ стоит юзеру 779 UAH (~$17) и 1679 RUB (~$21) — Telegram сам делает PPP-tuning по странам, разница 22% UA vs RU. Это не наша зона.
- **Best practice Big Tech по FX:** Stripe/Booking фиксируют **на дату операции** (transactional); Apple/Google/AdSense — **на дату выплаты** (monthly closing). Для NOMS взял **дату операции** + EUR валюта учёта + ECB reference rate как источник. Это создаёт прозрачность для амбассадоров и соответствует тому, что любит испанская налоговая.

**Что сделано (mig 493 + крон + KB):**

| Артефакт | Что |
|---|---|
| `app_constants.stars_payout_rate_usd` | 0.013 placeholder |
| `app_constants.fx_usd_eur` | 0.8591 (1/1.164 ECB 05.06.2026) |
| `fx_rates_daily` (новая таблица) | audit-only, INSERT-only, seed 22.05 + 05.06 |
| `referral_rewards` +6 колонок | `base_amount`, `base_currency`, `fx_used`, `fx_date`, `commission_rate`, `stars_payout_rate_used` |
| `payout_requests.currency` default | `USDT` → `EUR` |
| `fx_usd_eur_on(date)` | helper RPC, 3-уровневый fallback (exact / earlier / app_constants) |
| `update_fx_rate_snapshot(date, fx, source)` | атомарный writer (INSERT-on-conflict + UPDATE) для крона |
| `process_referral_payment_reward` REWRITE | per-method base, snapshot fx_used + fx_date в момент INSERT, всё в EUR |
| `get_ambassador_balance` REWRITE | EUR (was USDT хардкод) |
| `get_ambassador_detail` REWRITE | `v_sym := '€'` (was `'$'`) |
| `ui_translations.ambassador.*` | `${amount}` → `€{amount}` в 9 ключах × 13 langs |
| **Backfill Евгения** | $3.25 USD → **€1.63 EUR** (580⭐ × 0.013 × 25% × 0.86244 на 22.05) |
| `FxRateUpdateCron` daily 16:00 UTC | fetch ECB XML → `update_fx_rate_snapshot` |
| KB `fx-policy-revshare.md` | новый концепт + Big-Tech ссылки + durable уроки |

**Rollback-тест в одной транзакции (BEGIN/ROLLBACK):**

| Сценарий | Ожидалось | Получили |
|---|---|---|
| Backfill Евгения | €1.63 | **€1.63 ✅** |
| Stars 580⭐ CIS (live fx 0.8591) | €1.62 | **€1.62 ✅** |
| Stripe USD $12.99 DEFAULT | €2.79 | **€2.79 ✅** |
| Stripe EUR €17.99 EU_WEST | €4.50 (fx_used = NULL) | **€4.50 ✅** |
| Symbol € в detail | € present, $ absent | ✅ |
| `get_ambassador_balance.currency` | EUR | **EUR ✅** |
| `payout_requests.currency` default | EUR | **EUR ✅** |
| Aggregated balance после 4 платежей | €10.54 | **€10.54 ✅** |
| p95 latency `process_referral_payment_reward` | <500ms | 392.7ms (с локального мака; на VPS будет <100ms) |

**Транзакция откачена. Prod не тронут.** Готов к COMMIT после явного go owner на apply.

### Durable уроки

- **Snapshot курса хранится на самой строке транзакции** (`referral_rewards.fx_used`, `fx_date`) — историческое начисление не пересчитывается при изменении курса. Это не optional удобство, это **fairness для партнёра** и аудитор-friendly.
- **Stars payout-rate = плавающая величина**. $0.013 — публичная эвристика, не курс. Telegram не отдаёт API. Реальный rate — только из выписки Fragment, и он зависит от platform mix (desktop vs mobile). До первой выплаты — placeholder с явным флагом `requires_calibration_after_first_fragment_withdraw`.
- **Owner уже интуитивно балансирует Stars-номиналы по net** (с учётом ~30% margin Telegram). Если payout-rate реально окажется ниже $0.013 (mobile-heavy) — придётся поднимать Stars-номиналы в `subscription_prices`, не комиссию.
- **Per-method commission base** (Stars=net, card/USDT=full) даёт одинаковую комиссию амбассадору независимо от метода оплаты (±1%). Никакого нового бизнес-перекоса не введено.
- **payout_requests default currency** — менять только когда таблица пуста (как было). Иначе нужен явный backfill всех строк.
- **UI translations: `${amount}` → `€{amount}`** через `REPLACE('${', '€{')` — placeholder `{amount}/{total}/{balance}` сохраняется, символ валюты меняется.
- **RPC-first writes для FX.** Cron не делает direct INSERT — вызывает `update_fx_rate_snapshot` (атомарно INSERT-on-conflict `fx_rates_daily` + UPDATE `app_constants`).

### Открытые вопросы / TODO

- **🔴 До первой реальной выплаты Fragment** — измерить реальный payout-rate ⭐→USD и обновить `stars_payout_rate_usd`. Сейчас 0.013 placeholder.
- **🟡 Прозрачность на UI** — добавить кнопку «Подробнее» на экран Статистика, показывающую формулу за каждое начисление (`580⭐ → $7.54 net → ×25% → €1.63`). Сейчас данные есть в `referral_rewards.*` колонках, нужен только UI.
- **🟡 Cron автоматизация** vs ручное обновление курса — сейчас сделано daily auto. Если ECB полениво publish'ит (weekend/holiday) — `fx_usd_eur_on` сам берёт ближайший earlier snapshot, не сломается.

---

## Update: mig 493 APPLIED LIVE + formula transparency (TODO N+1)

**Изменения после первого daily-блока (16:00 UTC, owner go):**

1. **Renumber 492→493** — обнаружил collision: PR #368 (i18n english-leak-sentences) уже занял 492. Через CI guard «Migration number collision» поймал, перенумеровал в **493**. Force-push-with-lease.

2. **TODO N+1 реализован в той же миграции — formula transparency.** `get_ambassador_detail` теперь добавляет под каждой строкой recent rewards формулу пересчёта (inline `<i>...</i>`), скрывая её только если row предшествует mig 493 (нет snapshot колонок).
   Примеры формул по `base_currency`:
   - **XTR:** `580⭐ × $0.013 × 25% × 0.862 = €1.63`
   - **USD/USDT:** `$12.99 × 25% × 0.859 = €2.79`
   - **EUR:** `€17.99 × 25% = €4.50` (без FX)
   
   Реализовано напрямую в SQL `get_ambassador_detail` (нет отдельного экрана/RPC — MVP). Можно расширить до отдельного экрана «Детали начисления» позже, если потребуется.

3. **APPLIED to prod 2026-06-08 16:30 UTC.** Через psycopg2 в одной транзакции с 10-этапной verification, COMMIT после всех passes. Все checks ✅:
   - `app_constants.stars_payout_rate_usd=0.013, fx_usd_eur=0.8591` ✓
   - `fx_rates_daily` seed 22.05 (0.86244) + 05.06 (0.85911) ✓
   - `referral_rewards` +6 transparency cols ✓
   - `payout_requests.currency` default `'EUR'` ✓
   - `fx_usd_eur_on('2026-05-22')` → 0.86244 ✓, fallback chain работает (2026-06-08 → берёт 05.06)
   - `process_referral_payment_reward` содержит `mig 493` marker ✓
   - `get_ambassador_balance(417002669)` → `{currency: EUR, total_earned: 1.63, …}` ✓
   - `get_ambassador_detail(417002669).detail_body` содержит `€1.63` + formula `580⭐ × $0.013 × 25% × 0.862 = €1.63` ✓
   - `ui_translations.ambassador.earn_total` для en/ru/es содержит `€{` ✓
   - Backfill Евгения row → `value=1.63 EUR, base=580 XTR, fx=0.862440 on 2026-05-22, rate=25%, stars=0.013` ✓

4. **PR #369 ready to merge.** Python parts (FxRateUpdateCron в crons/__init__.py + main.py) → доедут на prod через GitHub Actions auto-deploy после merge → `noms-cron` restart подхватит daily 16:00 UTC job.

### Дополнительные durable уроки

- **CI Migration Collision Guard работает** (зафиксировал в [[migration-collision-guard]]) — сработал в пятый раз сегодня (после 6-ти исторических случаев). Без него бы я смержил с тем же номером что #368, и кто-то бы откатил чужую работу. **Хук — это рабочая страховка, не церемония.**
- **Formula transparency inline в `get_ambassador_detail` без отдельного экрана** — это MVP-уровень прозрачности. Альтернатива (отдельный экран «Детали начисления») = новый ui_screens row + новый RPC + кнопка на каждой строке. Не нужно, пока партнёр видит формулу прямо рядом с цифрой. **Урок: транзакционная прозрачность ≠ отдельный screen.**
- **Inline `<i>...</i>` Telegram HTML в SQL-строке** — работает корректно с parse_mode=HTML на render-стороне. Markdown/HTML литералы в RPC безопасны если render-pipeline их пропускает.

### Open для дальнейших агентов

- **🟡 PR #369 ждёт merge** owner'ом. Mig 493 LIVE на prod (сделал psycopg2-apply), но Python-часть (cron) дойдёт только через auto-deploy после merge.
- **🟡 stars_payout_rate_usd = 0.013 placeholder** — нужна реальная выписка Fragment для калибровки. Owner: при первой выплате замерить `(полученные USDT после конвертации) / (количество звёзд)` и обновить константу руками.
- **🟢 Closed:** «backfill Евгении $3.25 → €1.63» — done LIVE.
- **🟢 Closed:** TODO N+1 formula transparency — done в этой же миграции (inline в `get_ambassador_detail`).

---

## Final close: PR #369 merged + auto-deploy success + cron live

- **PR #369 merged** owner'ом (после моего повторного rebase на свежий main с подтянутыми 490/491/492-i18n).
- **GitHub Actions deploy** `Merge pull request #369` → **success** в 16:56 UTC.
- **VPS noms-cron** рестартнут, `journalctl -u noms-cron` показывает:
  - `Scheduler started with 17 jobs` (было 16, +1 fx_rate_update)
  - `Added job "FX Rate Update (ECB EUR/USD)" to job store`
  - `next run at 2026-06-09 16:00:00+03:00` ← ⚠️ TZ display сервера (Helsinki EEST). Если scheduler честно использует UTC (как настроено `AsyncIOScheduler(timezone="UTC")`), fire будет 16:00 UTC. Через сутки проверю в `fx_rates_daily` — если дата отстаёт или нет новой строки, добавлю explicit `timezone=UTC` в `CronTrigger(hour=16, minute=0)` отдельным PR.

### Durable урок: rebase ПЕРЕД каждым force-push, не только перед первым commit

Записан в [[release-protocol]] (Lesson 2026-06-08). Кратко: §12 global CLAUDE.md требует rebase «перед commit» — но для длинной PR-сессии (`git commit --amend` + force-push при CI-фидбеке или renumber) нужно **повторно rebase перед каждой write-операцией на remote**, иначе snapshot worktree устаревает за минуты в активном репозитории. Защита 1 CI sanity-check (`pr_sanity_audit.py`) поймала: +696/−1031 (12 файлов, удаления 3 чужих миграций). Лечение: `git fetch + git rebase origin/main + git push --force-with-lease`. Diff схлопнулся до +693/−0.

### Все 15 задач закрыты ✅

Ambassador commission EUR + per-method net + FX snapshot + formula transparency + daily ECB cron — **LIVE на prod**. Дальнейших действий не требуется. **Открытое для будущего:** калибровка `stars_payout_rate_usd` после первой реальной выплаты Fragment.
# 2026-06-08

## KB audit (/kb-close --audit)

- **audit.py:** 151 concepts, 0 orphans, 12 merge-кандидатов (топ `sticker-architecture-adr`~`n8n-self-hosting` 0.609 — топологически разные, общий n8n-словарь, НЕ дубль; остальные <0.12 шум) → слияний нет. 17 stale — все n8n-legacy (ожидаемо при миграции, исторический контекст, не трогаю).
- **MEMORY консолидация: 35 KB → 17.3 KB (123 стр)**, под порогами 20KB/150стр. Бэкап `MEMORY.backup-2026-06-08.md`. Сжал «Current state» (16 жирных shipped-параграфов → однострочники с KB-указателями), durable-гочи сохранил компактно, выкинул merged/done Open-айтемы. Принцип: MEMORY=индекс, детали в KB/daily.
- **lint --structural-only:** 0 errors (нет битых ссылок). 48 warn/437 suggest = давний фон (orphan-pages/missing-backlinks), вне scope.

---

## Продолжение сессии: научный аудит, rebase'ы, merge, handover (вечер)

**PR #366 merged 17:43 UTC** после двойного renumbering 490 → 492 → 494. CI guard прав: пока я работал, в main влились #365, #367, #368, #369 (заняли 490–493). Stale-base regression словил force-push'ем поверх старой базы — guard поймал «удаление 4 чужих миграций», я сделал `fetch + rebase origin/main + sanity-diff + force-with-lease`. Diff схлопнулся до +177 строк, чисто. **Confirms KB §release-protocol Lesson 2026-05-08 — guard'ы работают, и нужны даже агенту, который про этот lesson только что писал.** Не пренебрегать `git diff origin/main..HEAD --stat` перед каждым `git push --force-with-lease`.

**Научный аудит +100/+175 ккал (sub-agent, ~140 сек):** цифры **не выдуманы**, обоснованы тремя независимыми источниками + системным обзором PMC13066135 (2024). См. `concepts/adaptive-modifiers-architecture.md` §«Scientific basis…» — добавил полную таблицу по 4 trigger_value, цитаты страниц локальных доков. Owner принял вердикт «не менять цифры», подтвердил следующие шаги: (a) вынести 100/175 в `app_constants`, (b) решение по follicular/ovulation в CHECK.

**Спав-таск для следующей сессии создан:** «Инвентаризация 11 reminder-типов + дайджест в админ-чат» (`task_678a0dab`). Owner кликнул чип, новая сессия стартовала.

**Handover создан** для подхвата open tech debt — `handover/2026-06-08_luteal-aftermath.md`.

### Что в LIVE на момент закрытия

- **mig 491 (PR #367 MERGED):** cron luteal_morning пишет daily_modifier перед текстом, placeholder `{kcal_delta}` в 13 langs, проверка `applied is True`.
- **mig 494 (PR #366 MERGED):** trigger `trg_users_cascade_clear_female` + `gender='female'` гейт в `compute_cycle_day_for_user` + backfill 1 строки (admin).
- **Defence-in-depth работает:** проверено вживую через psycopg2 (BEGIN/ROLLBACK). Female 786301802 → `applied=true, modifier_id=684, calories_delta=100`. Male admin 417002669 → `applied=false, suppressed=true, reason='opt_in_required'`.

### Open для следующего агента

1. **Вынос 100/175/3/7 в `app_constants`** (owner подтвердил). Ключи — см. `[[adaptive-modifiers-architecture]]` §Open tech debt. Маленький PR, одна миграция.
2. **Решение по `follicular`/`ovulation` CHECK-constraint.** Рекомендация: сужение до `IN ('luteal_early','luteal_late')`. Обсудить с owner перед миграцией.
3. **Спав-таск инвентаризация 11 reminder-типов + дайджест** — отдельная сессия уже работает параллельно. Не дублировать.

## E2E render verification — метод зафиксирован (PR #370 + KB)

Owner утвердил план E2E (rollback-режим, без реального аккаунта/Telegram). Прогнал + зафиксировал переиспользуемый метод.

- **Находка архитектуры:** рендер ДВУХступенчатый (headless). `render_screen` (SQL) отдаёт `text_key`+`template_vars`+lang, НЕ текст. Финальную строку собирает `services/template_engine.py::_resolve_text(text_key, translations, vars, constants)` из `ui_translations.content`+`app_constants`. Проверять надо ОБЕ ступени, иначе видишь только text_key.
- **Метод (безопасный):** `psycopg2 autocommit=False` → на тест-юзере 786301802 `UPDATE language_code` → `render_screen` → `_resolve_text` → `rollback` per lang. Ноль персистентных изменений, ноль Telegram. Ассерты: локализован≠контроль, `<b>` сбалансирован, нет утечки text_key, нет литеральных `{placeholder}`.
- **Инструмент:** `tools/e2e_render_check.py <screen> [langs]` (PR #370). **KB-концепт `e2e-render-verification.md`** + index (+ Python-layer сюрфейсы: cron/sage builders вызывать напрямую; Telethon `e2e_crawler.py` для доставки/кликов).
- **Прогон:** `personal_metrics_set_pregnancy_trimester` × en/pl/ar/de → все локализованы, RTL ar ок, `<b>` цел, rollback восстановил язык. Подтвердил mig 488/492 (set_trimester_text) реально доходят до юзера.

---

## Догон: PR #371 — app_constants + сужение CHECK (mig 495)

Owner подтвердил оба пункта из handover'а сразу — сделал одной миграцией.

**Что в LIVE (mig 495 applied):**
- 4 ключа `modifier_luteal_early|late_kcal_delta|fat_pct` в `app_constants` (100/175/3/7).
- `apply_daily_modifier` luteal-ветка читает app_constants через `COALESCE` с fallback на исходные числа (mirror sleep_short pattern из mig 474).
- `daily_modifiers_trigger_value_check` сужен: для `modifier_type='luteal'` теперь только `('luteal_early','luteal_late')`.
- RPC-validation IN-list синхронно сужен.

**Verified live:** hot-reload через `UPDATE app_constants` пробрасывается в следующий вызов RPC без рестарта (calories=200 после tweak). `follicular`/`ovulation` теперь падают с `error=invalid_trigger_value` и CheckViolation на прямом INSERT.

**Закрывает open tech debt** из handover'а `2026-06-08_luteal-aftermath.md`. Будущая Phase 3e (если захочет фолликулярные дельты) — осознанная миграция: расширить CHECK + IF-ветку + 2 ключа.

**Durable:** mig 474 + mig 495 — устоявшийся паттерн «medical deltas в `app_constants` с COALESCE fallback на хардкод-значение». Следующая фаза модификаторов должна следовать тому же. KB обновлён.

---

## Session — Создание Skill `noms-sticker` + первый production test (стикер `gift_premium_activated`)

**Контекст входа:** owner попросил собрать systematic skill для создания Telegram видео-стикеров NOMS — чтобы любой будущий агент мог пройти весь pipeline (image-промпт → video-промпт → post-prod → активация в БД) без потерянных итераций. Затем сразу выбран реальный production-кейс для проверки скилла: стикер `gift_premium_activated` (подарок Premium от Номса) — раздаётся текущим тестерам по промокоду NOMSTEAM (1 месяц free Premium), универсален для будущих gift-периодов (3д/7д/30д).

### Часть 1 — Создание skill `noms-sticker`

**Расположение:** `~/Documents/NOMS/.claude/commands/noms-sticker/` (machine-local, не в claude-memory-compiler git — следует конвенции NOMS skills рядом с `sage-tov`, `kb-close`).

**Структура:**
```
noms-sticker/
├── SKILL.md (172 строки) — оркестрация 6 фаз + 9 сквозных NEVER-правил
└── references/
    ├── 01-preflight.md       (~115 строк) — дифференциация в серии, проверка placeholder
    ├── 02-prompt-image.md    (~135 строк) — Nano Banana / Imagen шаблон + canon-references caveat
    ├── 03-prompt-video.md    (~260 строк) — Veo шаблоны: Standard / TWO LAYERS + anti-shadow
    ├── 04-post-production.md (~260 строк) — make_sticker.py + 9-пункт чек-лист + ping-pong
    └── 05-db-activation.md   (~250 строк) — UPDATE bot_stickers + delivery verification
```

**Triggering:** description содержит триггер-фразы («сделай стикер», «новый стикер NOMS», «обработай mp4 в webm», «активируй стикер в боте», «promt для Veo / Nano Banana под стикер Номса») + явный negative-block (статичные стикеры → другой pipeline, render_screen Step 8b → `ui-stickers-headless`).

**6 фаз оркестрации:** Preflight → Image prompt → Video prompt → Source analysis → Post-production → DB activation → Closing.

### Часть 2 — Реальный production test: стикер `gift_premium_activated`

**Source iterations (3 попытки):**

1. **v1 (`тело_должно_быть_полностью_в_к.mp4`):** Veo получил оригинальный magenta image `Gemini_2k0l10.png`. Получилось — Veo сохранил flat magenta, lightweight contact shadow только под ножками. Pipeline scene mode → 175 KB. Owner принял, потом передумал из-за розовой полоски под ножками. **Source файл owner удалил**, восстановить v1 нельзя.

2. **v2 (`Animate_the_NOMS_monster_in_th.mp4`):** Owner прогнал image через **remove.bg** → transparent PNG → подал в Veo + anti-shadow промпт. **Катастрофа:** Veo получив transparent input рендерит «realistic scene»: gradient magenta фон + жирная realistic-shadow + watermark. Pipeline scene chromakey пропустил всё (gradient ≠ pure #FF1493) → стикер с розовой подложкой вокруг ВСЕГО Номса.

3. **v3 (`...копия2.mp4`):** Owner улучшил промпт (с моими anti-shadow усилениями), снова **transparent input**. Тень меньше, но gradient фон остался. Pipeline scene → та же розовая подложка вокруг Номса.

**Rescue (mode clean):** Чёткий sample фона source v3 показал vertical gradient (top R=151, bottom R=213, разница 60 точек в R-канале) — chromakey против solid `#FF1493` не работает. Переключение на `--mode clean` (rembg-only нейросеть, не зависит от цвета фона) → **чистый стикер 183 KB без артефактов**. Sparkle peaks на банте сохранены (внутри силуэта), ножки целые, форма rigid.

**Финальный результат:**
- Файл: `Дизайн/stickers/noms_gift_premium_activated.webm` (183 KB, окно 1.75-4.75с, mode clean, CRF 40, vp9, 512×512, 24fps, ALPHA_MODE=1)
- Owner подтвердил: «стикер получился большой сочный мощный» (скриншот в @Get_Sticker_ID боте)
- file_id: `CAACAgIAAxkBAAERW8pqJwfOdJr9LYsTKJ0-UsFGVPDggQACzqUAAn64OEk8OONDXJ3zHTsE`

**Активация в БД:** `bot_stickers.gift_premium_activated_1` UPDATE (channel B, placeholder из PR #348 → real file_id + is_active=true). Verify SELECT успешный.

**Delivery pipeline (verified):** `handlers/payment.py::_handle_gift_redeem` (mig 476/477) синхронно — `stickers_cache.lookup("gift_premium_activated")` → `OutboundItem(send_sticker)` перед `gift.premium_activated` текстом. Кэш TTL 60s. **Не cron**, разница с streak-стикерами (которые через cron).

**Edge case backfill:** 3 юзера погасили NOMSTEAM до активации (получили текст без стикера, без mark в БД — это разовый gift, не milestone):
- `786301802` (Владислав = owner) — backfill сделан вручную через `sendSticker` API (message_id=8935)
- `6378579500` (Ігор), `520145707` (Людмила) — leave-as-is по решению owner'а. NOMSTEAM `max_uses_per_user=1` → повторно не получат.

### 3 новых lesson'а (зафиксированы в локальном skill — нужно отзеркалить в KB)

1. **§B.1 в `02-prompt-image.md` (УЖЕСТОЧЕНО):** запрещено прогонять image через `remove.bg` / любой background-remover перед Veo image-to-video. Veo при transparent input не «сохраняет прозрачность», а **рендерит realistic scene** с gradient magenta + контактной тенью + watermark. Unfixable downstream — chromakey против solid `#FF1493` не работает на gradient. Эмпирически подтверждено v2/v3.

2. **§D в `03-prompt-video.md` (НОВАЯ СЕКЦИЯ) — Anti-shadow усиление:** стандартные negatives (`no soft drop shadows`, `no ground shadow`, `no floor`, `no character landing on a surface`) недостаточно для standing-style поз (offering, hands-on-hips, holding chest-level). Veo интерпретирует «character holding object» как «standing on something». Решение: явное ПОЗИТИВНОЕ утверждение в LAYER 1 про AIRBORNE (`50px clear magenta below feet`, `astronaut floating in zero gravity`) + дополнительные negative (`no contact shadow under feet, no gravity shadow, no character standing on anything, no implied ground reference`).

3. **`04-post-production.md` (`--mode` колонка + cheat sheet ОБНОВЛЕНЫ):** trade-off `--mode clean` vs `--mode scene`. Дефолт `scene`, но если фон Veo получился gradient (типично при transparent input как seed) — `scene` chromakey пропускает розовое. Переход на `--mode clean` решает (rembg нейросеть не зависит от цвета фона). Это **rescue pattern** — позволяет «спасти» source-видео которое иначе пришлось бы перегенерировать.

### Что ещё нужно зафиксировать в claude-memory-compiler KB (за пределами locales skill)

- `concepts/telegram-sticker-pipeline.md` §9 история — строка для `noms_gift_premium_activated.webm` (с описанием 3 итераций + lesson про clean-rescue)
- `concepts/telegram-sticker-pipeline.md` — новая секция §7.x или §8.4e «Veo realistic-scene trap on transparent input» (формализация lesson 1 + 3 связки)
- `handover/2026-05-14_sticker-pipeline-handover.md` §4 compressed-lessons — +1 строка про transparent input / mode-clean rescue
- MEMORY.md — `gift_premium_activated` в Recently shipped + pointer на skill `noms-sticker` в KB-recipes

### Lesson для агента-преемника

Если делаешь стикер NOMS — используй skill `/noms-sticker` (он в `.claude/commands/`). Главная серия гочей которая в нём зафиксирована:
- **НИКОГДА не прогоняй image через remove.bg перед Veo** — это разрушает фон до unfixable gradient.
- Если стандартный pipeline (`--mode scene`) даёт розовую подложку вокруг ВСЕГО Номса (не только под ножками) — переключайся на `--mode clean` без переделки source.
- Для offering / standing-style поз — обязательно anti-shadow усиление в video-промпте, иначе Veo подкладывает контактную тень.

### Артефакты

- `Дизайн/stickers/noms_gift_premium_activated.webm` (183 KB)
- `~/Documents/NOMS/.claude/commands/noms-sticker/` (новый skill, 5 references)
- `bot_stickers.gift_premium_activated_1` активирован
- Backfill: 1 ручной sendSticker → owner

---

## Stage 7c ЗАВЕРШЁН — AI Engine полностью выпилен из n8n

**GATE OPEN подтверждён** (monitor loop): n8n `03_AI_Engine` 72ч+ с нулевыми executions после Edit Meal cutover (#326/#327). Последнее касание 03 — 06-05 19:07 UTC (render-хит), далее 0 exec / 0 render-хитов; Python обслужил 13 edit'ов. Лог — `daily/stage7c_monitor.log`.

**Выполнено (n8n-side через API, снапшоты `/tmp/s7c_*.json`):**
1. Safe PUT `01_Dispatcher` — удалена нода `Go to 03_AI` (executeWorkflow→03), 52→51 нод.
2. Safe PUT `04_Menu_v3` — удалена нода `Go to 03_AI_Engine` + Switch-правило `03_AI_Engine` (Switch теперь `[10_Payment]` — dead payment-case, вне scope), 20→19 нод.
3. **n8n DELETE** `03_AI_Engine` (`kjw4kkKMD0IqNALg`) + `06_Indicator_Clear` (`jQn0nTxThFal4Kpe`).
4. **PR #374** — удалён мёртвый Python endpoint `/internal/food_log/render` (−87 строк; звал только n8n 03). `render_food_log_confirmation` НЕ тронут (нужен Python-пути).

**Итог:** n8n 7→**5 workflow** (active: 01_Dispatcher, 04_Menu, 04_Menu_v3, 04.2_Edit_StatsDaily; inactive: 08.3_Friends). Smoke: `/health` ok, 0 ошибок, ни один workflow не ссылается на 03/06/render-endpoint. **Полная миграция AI Engine из n8n завершена.**

**Гайки:** деактивация/refs/DELETE n8n — через API/SQLite, харнесс трижды требовал явного разрешения owner'а на прод-мутации (получено). 1 пре-существующий фейл `test_food_log_ai_input::test_text_input_happy_path` (валится на чистом main, чужая правка handle_ai_input без обновления теста) — флагнут, вне scope.

**Durable урок:** гейт необратимого удаления workflow = 0 РЕАЛЬНЫХ executions за чистое окно, НЕ календарь. Verify-first поймал живой edit-трафик в n8n несмотря на «день 7» и `active=0` (executeWorkflow игнорирует active flag). KB [[stage7-global-cutover]] §ВЫПОЛНЕНО-06-08.

### Stage 7c — финал подтверждён (PR #374 merged+deployed)
PR #374 смержен и задеплоен (deploy run success 19:56 UTC). Прод: `POST /internal/food_log/render` → 404 (endpoint удалён), `/health` ok, 0 ошибок. Owner подтвердил на аккаунте 786301802 — распознавание еды работает. **Миссия (полная миграция AI Engine n8n→Python) ЗАКРЫТА.** Loop остановлен (TERMINAL в stage7c_monitor.log).

---

## Sage ToV — payload-meta repeat-suppression + rule-7 hard guard — PR #375 MERGED + LIVE

**Откуда родился фикс:** прогон `/sage-tov` судьи по 51 реакции 08.06 → отчёт `sage_transcripts/sage_tov_review_2026-06-08.md`. Подтверждено: 54 упоминания «куриного филе / творога / яиц / рыбы» на 51 реакцию (~92% реплик с белковым штампом). Правило 7 («credit the meal just logged») нарушалось **3/3** на белковых приёмах ≥20 г (протеиновый коктейль 25 г → «закинь курицы», печень 25–30 г → «закажешь куриного филе?»).

**Owner-mandate 2026-06-07 в силе: системный промпт НЕ трогать** (тон=бренд). Поэтому корневой выбор: вместо правки `_DEFAULT_SYSTEM_PROMPT_*` приклеиваю две META-директивы в **конец user-prompt'а**, по аналогии с уже работающим `time_meta_warning` (вечернее окно >=16:00).

**Что в проде (deploy 19:57 UTC):**
- `mig 496` — RPC `get_recent_sage_reactions(p_telegram_id, p_hours_back=6, p_limit=2)`. Существующий индекс `idx_ai_coach_logs_telegram_id_created DESC` → Index Scan. **p95 = 45.7 ms** (10 прогонов с persistent connection, VPS Hetzner → Supabase EU pooler — только RTT, SQL почти бесплатен).
- `services/sage.py`:
  - Regex `_PROTEIN_STAMP_RE` — корни ru/en/uk (`кур`, `твор`, `яйц`, `яиц`, `тунц`, `рыб`, `бобов`, `тофу`, `нежирн`, `chick`, `cottage`, `egg`, `tuna`, `fish`, `beans`, `tofu`, `lean`). Substring без word-boundaries (родительный падеж «тунца» иначе не ловится).
  - `_fetch_recent_sage_reactions` — fail-safe (None caller / no tid / RPC error → `[]`, никогда не блокирует генерацию).
  - `_repeat_suppression_meta` → **VARIATION GUARD** — приклеивается, если хотя бы 1 из последних 2 реакций юзера содержит штамп. Фразирован позитивно («pick a different lever — veg/fibre/lighter close»), а не «не делай X» — gpt-4o-mini надёжнее следует конкретному next-move.
  - `_rule7_hard_guard_meta` → **RULE 7 HARD GUARD** — приклеивается (food_log only) при `payload['protein'] >= 20`. Дословно вкладывает в промпт три варианта acknowledge-формулировки («белок в деле», «protein landed», «протеин пошёл»).
  - `asyncio.gather(get_day_summary, get_recent_sage_reactions)` — 2 RPC параллельно, **+0 ms** к p95 vs старый sequential путь (один RTT).
- `tests/services/test_sage_repeat_suppression.py` — 38 unit-тестов (regex coverage, helpers, prompt-builder plumbing, e2e через `generate_food_log_comment` / `generate_my_day_insight`, resilience при RPC-сбое). Один существующий тест обновлён (matcher под второй RPC-вызов).

**🟢 Прод-сигналы за первые минуты после deploy (u786301802, en, female, ES):**
- 19:57:41 food_log на «Grilled chicken 825 ккал» → реакция: **«Protein landed! 🍗 Now, how about some leafy greens to round out the day?»** — модель **дословно скопировала** мою META-формулировку «protein landed». Rule-7 META точно дошла до LLM.
- 19:57:28 food_log → **«how about some LENTILS»** (чечевица, не курица).
- 19:58:18 my_day → **«leafy greens to round out»** (овощи, не курица).
- 3 из 4 реакций варьировали lever. Косвенно работает.

**🟡 Но: `VARIATION GUARD` в payload не виден** (в `SAGE_MY_DAY_PROMPT` журнале текст META не появляется даже когда у юзера в `ai_coach_logs` за минуту до того было ≥1 «chicken»-реакция). Гипотезы: (а) supabase Python SDK возвращает RPC TABLE-результат как `APIResponse` с `.data` атрибутом, а не `list[dict]` — unit-тесты мокали `list[dict]`, в проде shape другая; (б) `log_request` пишет `success=false` для sage-реакций → мой RPC отфильтровывает все строки. → **spawn task `task_7839624f`** на дебаг.

**🟡 Spawn task #2 (`task_2af67744`):** Sage fallback bug — 2 пустые реакции за 2 дня (07.06 u6378579500 13:12 + 08.06 u520145707 19:29). Отдельный investigation: посмотреть `journalctl` за эти времена, найти root cause (timeout? json parse? sanity-filter? closed-budget override?).

**A/B-якорь для следующего прогона `/sage-tov` через 2-3 дня:**
- 08.06 baseline: предписание следующей еды в **92 %** реакций, штамп курица/творог/яйца — **54/51** (>1 на реакцию), нарушений правила 7 на ≥20 г Б — **3/3**, emotion mix `smirk` ≈ 80 %.
- Цель после META: предписания ≤ 50 %, штамп ≤ 1/юзер/день, rule-7 нарушения = 0, emotion mix smirk ≤ 60 %.

**Process-инцидент (поймал в процессе, отдельная KB-статья):** редактировал `services/sage.py` и тесты по **absolute path `/Users/vladislav/Documents/NOMS/...` — это основной клон**, а не worktree `/Users/vladislav/Documents/NOMS/.claude/worktrees/dreamy-tharp-ab88d9/`. Bash тоже периодически уходил в основной клон через явный `cd`. Понял на этапе `git status` — pre-commit поймал бы при попытке push'нуть из неправильного места. Перенёс файлы в worktree (`cp` + `git checkout --` в основном клоне), коммитил уже из worktree. Чисто. → KB [[concepts/worktree-vs-main-clone-edit-confusion]].

**Архитектурный паттерн, заслуживающий отдельной статьи:** payload-meta override как способ влиять на тон LLM без правки системного промпта. Уже применялся для `time_meta_warning` (mig 2026-05-29) и `closed-budget directive` (mig 2026-06-07); PR #375 добавил два новых случая. Принципы: META позитивная (конкретный next-move, не запрет), приклеена в конец payload (gpt-4o-mini weighs end-of-context heavily), флаг включения — детерминированный, fail-safe (без META = identical-to-old behaviour). → KB [[concepts/sage-payload-meta-override-pattern]].

**Tech debt (флаг, не фикс этой сессии):** `ai_coach_logs` имеет **3 дублирующих индекса** на `(telegram_id, created_at)`: `idx_ai_coach_logs_user_date`, `idx_ai_coach_logs_tg_created_desc`, `idx_ai_coach_logs_telegram_id_created`. Лишние writes + disk. Отдельная задача на cleanup (требует свободного DROP INDEX окна на горячей таблице, без срочности).
