# Daily / Handover — 2026-05-20

## Mig 285 — Streak food-log i18n + log_meal_transaction RPC fix

**Bug, replayed:** owner-observed на live tid=417002669 и 786301802 — после food log бот говорит «🏆 Стрик: 18 дн.» / «🏆 Стрик: 3 дн.» на RU юзерах. Англоязычный получит ту же RU-строку.

**Root cause (NLM-assisted):**
- `log_meal_transaction` RPC хардкодит RU PL/pgSQL прямо в SQL-функции:
  ```sql
  v_streak_msg := v_icon_streak || ' Стрик: ' || v_new_streak || ' дн.';
  v_streak_msg := '📅 Старт нового стрика!';
  ```
- Это **double violation:** no-hardcode + i18n broken (один lang для 13).
- Phase 4 UX-копирайтинг (mig 231 `ru_terminology_streak_to_seria`) audit'ил только `ui_translations` JSONB — не `pg_proc`. **Разные источники истины.** На surface food-log streak ключа в словаре вообще не существовало.
- Surface не имел ключа в `ui_translations` потому что строка собиралась inline в SQL через конкатенацию. Phase 4 не мог его найти.

**Lesson (new general):** ВСЕ surface-генерирующие RPC должны быть audit'нуты на hardcoded localized strings отдельно. JSONB-словарь — лишь часть i18n-surface, RPC bodies — вторая часть. Без сoupled audit'а Phase 4 пропускает RPC composition surface'ы. См. KB pointer ниже.

**Решение (Variant A locked by owner):**
- 2 новых ключа: `gamification.streak_kept` + `gamification.streak_new_start` × 13 langs = 26 entries
- RPC rewrite: SELECT translation by `v_user.language_code` → REPLACE `{streak}` → return as `streak_message`
- Backward-compat: field `streak_message` сохранён (n8n `03_AI_Engine` не трогаем)
- Forward-prep: новый field `streak_event` enum (`kept | new_start | same_day | none`) — готовится к будущей миграции composition n8n→Python

**Pipeline (writer→critic с 6-й осью):**
- Critic axes (по запросу owner'а): naturalness, sassy_sage_tone, anti_shame (mandatory 5/5), brevity, **cross_language_comprehension** (NEW), cultural_fit
- Writer rating: 23/26 approved, 3 fix_needed (FA ZWNJ + IT Serie→Striscia ×2)
- Critic verifyед UK `стрік` legit (glossary line 1281: canonical UK fitness term, NOT RU loanword)
- Critic verifyед IT `striscia` canonical для celebration (glossary line 437: `striscia` для празднования, `serie` для нейтрального отчёта)

**RU terminology decision:** «Серия» — owner-approved для consistency с profile/progress/push. `report.streak_label` («Дней в ударе») оставлен нетронутым — другой surface, daily review, idiomatic vibe там работает.

**Per-language native (no RU calque):**
- es Racha (LatAm momentum) | de Streak (fitness-DE Anglicism) | fr Série | it Striscia
- pt Sequência | pl Passa | uk стрік (canonical UK fitness) | id Streak (loanword natural)
- hi Streak (Hinglish-Latin per glossary) | ar سلسلة (silsila, pan-Arab positive) | fa پشت هم

**Live test (psycopg2 sentinel via BEGIN..ROLLBACK):**
- 13 langs × 2 branches (kept + new_start) — все produce correct localized strings
- User state untouched post-rollback (verified)
- Length budget ≤35 chars везде (max 31 FR new_start)
- HI Hinglish-Latin renders ok mid-Devanagari context
- AR/FA RTL bidi с Latin digit `{streak}` substitution clean

**Snapshots для rollback:**
- `public.backup_ui_translations_streak_i18n_20260520` (13 rows)
- `public.backup_log_meal_transaction_v_pre_streak_i18n` (1 row, function body)
- Drop через 7 дней

**PR:** [#127](https://github.com/sharkovvlad/noms-bot/pull/127), HEAD migration 285.

**KB updates needed (next session):**
- [[concepts/i18n-rpc-audit-pattern]] — NEW concept: при i18n-rollout всегда сканировать `pg_proc` отдельно от `ui_translations` JSONB. Owner-observable bug (это разные источники истины). См. lesson выше.
- [[concepts/sassy-sage-multilingual-glossary]] — добавить рекомендацию: для FA ZWNJ never between separate words separated by space (LLM-частый error pattern).
- [[concepts/copywriter-playbook]] — добавить step: «scan pg_proc for Cyrillic literals before claiming i18n done».

**Open RU-hardcoded RPCs (watchlist, не в scope mig 285):**
- `apply_discount_code` — 4 error/success messages
- `get_my_plan_business_data` — goal_type/goal_speed RU/UK switch (только 2 langs mapped)
- `get_personal_metrics_business_data` — пол: «Мужской/Женский/Не указан» без lang switch
- `get_women_health_business_data` — «❓ Не указано»/«❌ Нет»/«✅ Да·T{trimester}» частично hardcoded
- `log_meal_transaction` — оставшийся «Еда» fallback на line 94 для пустого food_name
