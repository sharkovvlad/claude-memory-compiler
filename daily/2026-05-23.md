# Daily Log: 2026-05-23

## Sessions

## Memory Maintenance

### Memory Flush (01:00)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (09:07)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details


---

## Mig 310 — Phase 3b Sleep Modifier UX + PM debts cleanup

**Цель:** end-to-end sleep modifier workflow + 4 PM product debts из roadmap §P2.4 (Premium gating, maternal supportive banner, daily_metrics qualitative col, waist_retrofit cron debt fix).

### Architectural pivot vs Phase 3a (mig 301)

Premium check переехал из reader (`compute_daily_modifier_stack`) в writer (`apply_daily_modifier`):
- Writer пишет honest journal с `suppression_reason='premium_required'` для free users
- `metadata.locked_deltas` хранит точные computed deltas (для аудита + future premium activation)
- Reader (`compute_daily_modifier_stack`) группирует на 2 buckets: `active_modifiers` (applied=TRUE) + `premium_locked_modifiers` (suppression_reason='premium_required')
- Teaser возвращает **qualitative_descriptor** (e.g. `protein_boost_for_recovery`), НЕ precise numeric deltas (variant B per PM Q1 — curiosity gap без leak формулы через API proxy)

### Free-tier lifestyle logging (per PM polishing #1)

UPSERT в `daily_metrics.sleep_quality_qualitative` — **UNCONDITIONAL**, выполняется ДО Premium gate. Free user может вести календарь сна бесплатно (лестница привычки); компенсаторные macro-deltas только Premium.

### Maternal supportive banner (per PM polishing #1)

Изменён gate 3c (mig 301 silent skip → mig 310 supportive metadata):
- `metadata.show_supportive_banner = TRUE`
- `metadata.banner_i18n_key = 'modifier.maternal_protective.<modifier_type>'`
- Return добавляет `show_supportive_banner=TRUE` для UI hook
- show_modal остаётся FALSE (не модалка, а passive banner)

### One-Menu UI Contract (per PM polishing #2)

`sleep_checkin` screen: `render_strategy='replace_existing'` + `next_on_submit='my_plan'` + `meta.one_menu_terminal=TRUE`. После клика на short/okay/great старая клавиатура гаснет, мгновенный re-render `my_plan` (юзер сразу видит новую плашку или Premium teaser).

### Реализация

**Schema:**
- `daily_metrics.sleep_quality_qualitative TEXT CHECK (NULL OR IN short/okay/great)` — parallel к dormant `sleep_hours` numeric (reserved для future Apple Health/WHOOP).
- `daily_modifiers_suppression_reason_check` extended: добавил `'premium_required'`.

**RPC edits** (surgical через pg_get_functiondef baselines):
- `apply_daily_modifier` — добавлены: (1) unconditional daily_metrics UPSERT для sleep, (2) Premium gate перед superseded INSERT path, (3) maternal supportive metadata изменены.
- `compute_daily_modifier_stack` — добавлена `locked_modifiers` CTE + новый `premium_locked_modifiers` key в return (qualitative_descriptor только, без deltas).
- `get_day_summary` — surface `premium_locked_modifiers` (additive, legacy keys preserved).
- NEW: `set_user_sleep_quality(p_tid, p_value)` — thin wrapper для UI button save_rpc (UI pattern требует 2-arg signature).
- `cron_get_reminder_candidates` — добавлен `'sleep_checkin'` branch (09 local, skip если уже ответил today).

**UI rows:**
- `ui_screens.sleep_checkin` (replace_existing + inline_kb + one_menu_terminal meta).
- 4 buttons (short/okay/great + back) с `meta.save_rpc='set_user_sleep_quality'` + `meta.save_value` + `target_screen='my_plan'`.

**Python:**
- `main.py:REMINDER_TYPES` — добавлены `sleep_checkin` + `waist_retrofit` (последний — fix pre-existing tech debt mig 295).
- `crons/reminders.py` — 4 entries в TRANSLATION_KEY_MAP / STICKER_CATEGORY_MAP / FALLBACK_TEMPLATES.

**i18n × 13 langs:** 15 keys через copywriter subagent (anti-shame, gender-neutral, Telegram SRE constraints, ≥5/5 critic для maternal protective banner). Apply через per-key jsonb_set с явным созданием parents (deep_merge bulk-overwrite заблокирован auto-classifier — fair call, прошёл surgical pattern).

### Тесты

- `tests/integration/test_phase3b_sleep_ux.py` — 12/12 PASS:
  - T1 Premium sleep applied + daily_metrics upsert
  - T2 **Free user — premium teaser + UNCONDITIONAL lifestyle log** (KEY test)
  - T3 Premium full deltas
  - T4 Maternal supportive banner metadata
  - T5 daily_metrics ON CONFLICT (UPSERT)
  - T6 РПП регрессия (sleep НЕ триггерит cachexia gate)
  - T7 set_user_sleep_quality wrapper
  - T8/T9 cron sleep_checkin shape + skip after answer
  - T10 get_day_summary keys (premium_locked_modifiers added)
  - T11 Teen vegan + sleep multi-mig (DIAAS × Schofield × teen sleep +25%)
  - T12 waist_retrofit cron extension shape
- Regression: 57/57 PASS (8 RFM + 13 v11 + 8 v12 + 16 mig 301 + 12 mig 310).
- p95 `get_day_summary` post-mig 310: 183ms (mig 301 baseline 116ms, +67ms на compute_stack split + EXISTS check для locked_modifiers). В пределах <200ms.

### Test fixture update (BREAKING для mig 301 tests)

После apply mig 310 fixture в `test_adaptive_modifiers_foundation.py` + `test_v12_personas.py` обновлены — set Premium=True по умолчанию (rolled back на teardown). Без этого engine tests fail на Premium gate.

### Digital Twin v9 regen

`tools/digital_twin_export.py` обновлён — set Premium=True (rolled back). 8 CSV files регенерированы, **0/74 ✗ diffs** в sheet6_diff_vs_expected.csv (engine math unchanged).

### Gotchas (для следующих агентов)

1. **`ui_screens` schema** — нет `screen_type`/`layout`/`sort_order` полей. Use `render_strategy` + `input_type`. Buttons: `row_index`+`col_index`+`text_key`+`callback_data`+`meta` (NO `sort_order`/`button_label_key`).
2. **`ui_translations` — JSONB per language**, не key-value rows. Schema: `(lang_code TEXT, content JSONB)`. Для nested keys нужен deep merge или per-key jsonb_set с явным созданием parents (PG jsonb_set НЕ создаёт intermediate objects автоматически).
3. **`user_subscriptions` нельзя DELETE** — FK на `payment_events`. Toggle Premium через UPDATE existing row (status='active'/'cancelled').
4. **subscription_status TEXT cache** — drift-prone. Canonical check: `user_has_renewable_sub(tid)` RPC (mig 296 helper).
5. **`waist_retrofit` reminder type** теперь wire'нут в Python (был pre-existing bug — RPC в mig 295 supports, но `main.py:REMINDER_TYPES` пропустил).

### Sub-phases pending (handover)

- **Phase 3c** (mig 311+): Stress UX + actual РПП modal escalation вызов (Python handler читает `show_modal=TRUE` → renders modal) + teen escalate modal + `modifier.maternal_protective.stress` i18n.
- **Phase 3d** (mig 312+): Luteal opt-in flow + privacy disclaimer + cycle auto-detect cron + maternal supportive metadata reuse.
- Sticker pipeline для `sleep_checkin_intro` — отдельная design session (отсутствует в этом PR, screen meta.show_sticker_once=TRUE но sticker категория ещё не загружена в bot_stickers).
### Memory Flush (15:00)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details


---

## Sage Noms v2 — JSON mode, tg-emoji, fallback × 13 langs (2026-05-23, session 2)

### PRs opened this session

- **PR #160** — `claude/sage-time-of-day-fix`: Noms character v2 (JSON mode, emotion→tg-emoji, UX polish + mig 314)
  - `services/sage.py`: `generate_food_log_comment` now returns `tuple[str, str] | None` (text, emotion)
  - JSON mode via `response_format={"type": "json_object"}`, LLM outputs `{"text": "...", "emotion": "smirk|proud|side-eye|sage"}`
  - `EMOTION_TO_EMOJI` dict maps emotion → `(telegram_custom_emoji_id, fallback_char)`; side-eye + sage IDs pending from owner
  - `_compute_macros_focus()`: High Protein/Carbs/Fat/Balanced/Low Calories from kcal percentages
  - `_build_user_prompt` rewritten: User Local Time, Meal Phase, Food Summary, Macros Focus, Daily Remaining, Streak, Diet, Notifications Mode, Silent Context
  - tamagotchi_stage REMOVED from prompt (Noms prohibits Pet refs in LLM output)
  - `_MAX_OUTPUT_CHARS` raised 200→280 (30-char headroom over 250 target)
  - `handlers/food_log.py`: tuple unpacking, tg-emoji HTML, rounded macros in КБЖУ display
  - `dispatcher/context.py`: `notifications_mode` field added
  - `migrations/314_food_log_ux_header_prefix.sql`: removes header line, updates noms_comment_prefix "💬 Noms"→"Noms 💬" × 13 langs
  - 60 tests pass
  - Migration collision: originally 313 (collides with mig 313 delete-account-blocker merged in main) → renamed to 314

- **PR #161** — `claude/sage-fallback-13langs`: mig 315 — Sage fallback phrases × 13 languages
  - Replaces old mig 312 "Pet-style" fallback (ru+en only) with Noms v2 "neurons overloaded" brand voice
  - 5 variations per language for all 13: ar, de, en, es, fa, fr, hi, id, it, pl, pt, ru, uk
  - Applied and verified locally (DO block confirms 13×5)

### Hot DB changes (no migration, applied to prod earlier in previous session)
- `sage_food_log_temperature`: 0.9→0.7
- `sage_food_log_max_tokens`: 80→300 (Cyrillic tokenization requires ~1.5 tokens/char)
- `sage_food_log_timeout_ms`: 2500 (VPS→OpenAI RTT ~968ms, 1500ms was too tight)
- `sage.system_prompt_food_log` RU+EN: Noms v2 persona (JSON format, no Pet refs, dynamic tone modes)

### Pending owner action
- side-eye + sage custom emoji IDs: will be provided by owner → update `EMOTION_TO_EMOJI` in sage.py + DB constant
- notifications_mode field: verify it comes through `v_user_context` SELECT * (may need view refresh in DB)
