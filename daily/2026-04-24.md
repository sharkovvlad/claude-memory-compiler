# Daily Log: 2026-04-24

## Migration 125 — Stats "Исправить" routes to meal list (spec §6.6 aligned)

**Проблема:** После migration 124 на headless `stats_main` кнопка `[✏️ Исправить]` имела `callback_data='cmd_edit_last'`, который в `04_Menu_v3` (route `edit_last`) всегда открывает edit для ПОСЛЕДНЕГО блюда. На Stats экране это UX-баг: юзер смотрит итог дня → хочет исправить конкретное блюдо (не обязательно последнее) → получает редактор последнего. Spec §Макет 6.6 предписывает: ≥2 записи → показать список для выбора.

### Migration

| # | Filename | Scope |
|---|----------|-------|
| 125 | `125_stats_main_edit_routes_to_meals_list.sql` | Split single button → two mutually-exclusive buttons at (row=0, col=0 / col=1), same text/icon, разный callback по count блюд за день |

**Логика:**
- `meals_today = 1` → `cmd_edit_last` (skip list — один meal, нет неоднозначности)
- `meals_today >= 2` → `cmd_show_meals` → 04_Menu_v3 route `meals` → Execute Workflow `04.2_Edit_StatsDaily` → список с `cmd_view_meal_{id}` → detail → `cmd_start_edit_{id}`
- `meals_today = 0` → обе кнопки скрыты (legacy-поведение сохранено)

### Flow

1. **NLM discovery (conv d45e9a31):** 4 вопроса. Подтверждено: legacy 04.2 уже имеет полный flow `get_meals_today` → `Build List Message` → inline list с `cmd_view_meal_{id}`. `food_log_result` кнопки живут в legacy `bot_menus` (не `ui_screen_buttons`) → migration 125 его НЕ затрагивает.
2. **Live GET 04.2** (n8n API, updatedAt 2026-04-08) — 16 нод, route `cmd_show_meals` полностью готов, PUT не требуется.
3. **Discovery: UNIQUE constraint** на `ui_screen_buttons (screen_id, row_index, col_index)` — первая попытка apply с обоими кнопками на (0,0) упала с duplicate key. Фикс: col_index 0 / 1 (визуально всё равно одна кнопка, т.к. условия взаимоисключающие).
4. **Применена** через psycopg2 + DATABASE_URL.

### Verification

- **SQL-уровень:** DO `$verify$` блок в миграции: `COUNT=2`, `cmd_edit_last × 1 + cmd_show_meals × 1`, visible_condition содержит `= 1` / `>= 2`.
- **render_screen smoke:**
  - admin 417002669 (1 meal today) → `keyboard=[[{cb: "cmd_edit_last"}]]` ✅
  - simulated 2 meals (via rollback transaction) → `keyboard=[[{cb: "cmd_show_meals"}]]` ✅
  - user 8643503390 (0 meals) → `keyboard=[]` ✅
- **Идемпотентность:** SQL начинается с `DELETE FROM ui_screen_buttons WHERE screen_id='stats_main'`, повторный run безопасен.

### Architectural decisions

- **SQL-only fix (no n8n PUT)** — logic в headless layer (visible_condition в БД), 04.2 не трогали. Альтернатива была: в 04.2 `Build List Message` добавить skip-single-meal логику (if `meals.length===1` → auto-redirect на `cmd_start_edit_{id}`). Отвергли т.к. (а) n8n PUT создаёт риск webhook re-register, (б) логика размазывается между БД и JS.
- **row_index=0 с col_index 0/1** — interim (UNIQUE constraint enforced). Render_screen показывает только одну visible кнопку в row → Telegram рисует 1 кнопку без пустых слотов.

### Files touched

**Created:**
- `migrations/125_stats_main_edit_routes_to_meals_list.sql` — 95 lines, applied
- `claude-memory-compiler/daily/2026-04-24.md` — this file

**Not touched:**
- `food_log_result` (legacy `bot_menus`) — `cmd_edit_last` там корректен (только что залогированное блюдо = последнее)
- `04.2_Edit_StatsDaily` n8n workflow — route `cmd_show_meals` работал до патча
- `04_Menu_v3` classifier — правила `cmd_edit_last` / `cmd_show_meals` / `cmd_view_meal_*` / `cmd_start_edit_*` уже на месте

### Known limitations

- **Skip-single-meal = не true skip:** при 1 блюде кнопка ведёт напрямую в edit (`cmd_edit_last`), но при ≥2 путь 3-шаговый: list → detail → edit. Spec §6.6 описывает именно такое поведение.
- **Меню из 2 кнопок в `ui_screen_buttons`** — визуально одна (взаимоисключающие). Если будущая миграция добавит реальную вторую кнопку на (row=0, col=2+) — не сломается.
- **Phase 3A iteration 3 (full headless meal_list / meal_detail)** остаётся deferred — этот фикс не конкурирует с тем roadmap'ом, но делает UX корректным сейчас.

---

## Session 13 — Phase 3A Progress Hub kickoff (migrations 129-132)

Первый headless экран Progress Hub: `progress_main`. Unblocked после Session 12 Python Middleware (PUT Dispatcher безопасен — Telegram Trigger disabled).

### Applied (4 migrations)

| # | Filename | Scope |
|---|----------|-------|
| 129 | `129_progress_main_headless.sql` | New wrapper RPC `get_progress_rpc` (reuses `get_progress_business_data` + `get_progress_insight`, FLAT top-level, eager-resolves `league_position_text`/`progress_insight` per lang). Seed `ui_screens.progress_main` + `progress.main_text` × 13 langs + 4 inline buttons (Квесты/Лига/Банда/Магазин). |
| 130 | `130_progress_fastpath_patch.sql` | `process_user_input` fastpath: `cmd_progress → progress_main` (idiom: `pg_get_functiondef + REPLACE + EXECUTE`, same pattern as Session 11 migration 123). |
| 131 | `131_progress_mana_display.sql` | Patch `get_progress_rpc`: добавлен eager-resolved `{mana_display}`. Premium → `{tr:progress.mana_unlimited}` ("Безлимит" / "Unlimited" / ...). Free → `"{mana_current}/{mana_max}"` literal. Template updated для всех 13 langs. |
| 132 | `132_nav_stack_init_progress.sql` | Fix Back button bug: `process_user_input` fastpath теперь seed `nav_stack='["progress"]'` для `cmd_progress` (вместо `'[]'`). Child legacy screens push поверх → Back pops → parent='progress' → Back Target Router 'progress' output → legacy Progress re-render. (Workaround пока Quests/League/Friends/Shop не headless — см. deferred task.) |

### n8n changes

- **01_Dispatcher PUT #1** (2026-04-24 ~09:21 UTC) — Route Classifier v1.12:
  - `cmd_progress` добавлен в `PROFILE_V5_CALLBACKS` set (→ routes to v3)
  - Reply-text detection: `text.includes(constants.icon_progress)` → command='cmd_progress' → v3
  - **Webhook stable после PUT (T+10s, T+25s)** — Session 12 migration работает. Trigger disabled = no auto-setWebhook. First production PUT без webhook drift.

### Live test результаты (user Vlad, 417002669)

| Scenario | Result |
|---|---|
| Reply "🚀 Progress/Прогресс/Progreso" → progress_main | ✅ Render: streak=19, mana=Безлимит, XP=6345, 💎548, 🏆 🥒 Pickle, 👑 1-е место, Noms insight |
| 4 inline buttons [Квесты/Лига/Банда/Магазин] → legacy screens | ✅ Все открываются (legacy renders — headless migration для них отложена) |
| Back из Quests | ✅ После migration 132 возвращает на legacy Progress (вместо 👋 wave как было до fix) |
| mana_display Premium | ✅ "Безлимит" (не "434/500") |
| Back из League/Банда/Магазин | ✅ Возвращает на legacy Progress |

### p95 latency

- `get_progress_rpc` 10 runs: p50=89.6ms, p95=158.7ms
- `render_screen(progress_main)` 10 runs: p50=93.3ms, p95=182.3ms
- Watchlist <500ms threshold — OK. Выше `stats_main` (82ms) потому что wrapper вызывает 2 RPC + 4 translations lookups.

### Bugs reported & disposition

| # | Bug | Fixed? | Notes |
|---|---|---|---|
| 1 | Reply kb ES + content RU language mismatch | Deferred (spawned task) | Rare-occurrence UX, не блокирует MVP |
| 2 | Premium: показывает "434/500" вместо "Безлимит" | ✅ migration 131 |
| 3 | Back button из Quests/League/Friends/Shop → 👋 | ✅ migration 132 (goes to legacy Progress, not 👋) |
| 4 | Банда shortcut → сразу "Как работает" | Deferred (spawned task) | Legacy routing bug |
| 5 | Shop "Продлить" → Back → wrong route | Deferred (spawned task) | Legacy 10_Payment nav_stack issue |

### Architectural decisions

- **Wrapper RPC (`get_progress_rpc`)** over rewriting `get_progress_business_data` — preserves existing callers (legacy 04_Menu Progress screen), adds FLAT shape + eager translations для Dumb Renderer compatibility.
- **Migration 132 workaround** вместо full headless Quests/League — Session 13 scope был progress_main; полная миграция 4 children — это Phase 3A iteration 2, отдельная сессия.
- **Numbering collision** — другой агент применил 125 (stats "Исправить" fix) утром, я начал с 125 не зная. При обнаружении collision в git tree мои файлы renamed 125/126/127/128 → 129/130/131/132. **DB изменения прошли OK (разные таблицы)**, только file numbering clean-up.

### Deferred work (spawned tasks)

1. Reply keyboard language refresh при смене lang или при каждом top-level render
2. Migrate Quests/League/Friends/Shop to headless (Phase 3A iteration 2) — после этого migration 132 workaround можно откатить
3. Bug #4 (Banda shortcut) + Bug #5 (Shop Продлить Back) — legacy routing fixes

### Files touched

**Created:**
- `migrations/129_progress_main_headless.sql` — 220 LOC
- `migrations/130_progress_fastpath_patch.sql` — 60 LOC
- `migrations/131_progress_mana_display.sql` — 140 LOC
- `migrations/132_nav_stack_init_progress.sql` — 70 LOC

**Modified:**
- 01_Dispatcher Route Classifier jsCode (via PUT) — cmd_progress + icon_progress reply-text

**Not modified:**
- 04_Menu_v3 (process_user_input fastpath patch достаточен)
- 04_Menu legacy (оставлен для children Quests/League/Friends/Shop render + Back Target Router 'progress' output)

### Validation of Session 12 Python Middleware

First production Dispatcher PUT после Session 12 migration **прошёл без webhook drift** ✅. Подтверждено что disable Telegram Trigger action достигла цели (no more auto-setWebhook on PUT).

---

## Session 14 — Progress Hub iteration 2: 4 children headless (2026-04-24)

**Контекст:** Session 13 мигрировала `progress_main` headless, но 4 inline-кнопки (Квесты/Лига/Банда/Магазин) всё ещё роутились в legacy 04_Menu — создавало UI flip при Back и требовало nav_stack workaround (migration 132). Session 14 завершает Phase 3A iteration 2 — полная headless миграция всех 4 children + откат workaround'а.

### Applied migrations

| # | File | Scope |
|---|------|-------|
| 133 | `133_quests_headless.sql` | `get_quests_rpc` + `ui_screens.quests` + template + fastpath `cmd_quests → quests` |
| 134 | `134_league_headless.sql` | `get_league_rpc` + `ui_screens.league` + template (top-12 standings, time_left, insight) + fastpath |
| 135 | `135_friends_info_headless.sql` | `get_friends_info_rpc` + `ui_screens.friends_info` + template (reward+stats+link+CTA) + fastpath |
| 136 | `136_shop_headless.sql` | `get_shop_rpc` + `ui_screens.shop` + template (balance/mana/freezes) + fastpath |
| 138 | `138_rollback_nav_stack_init_for_progress_children.sql` | Откат migration 132: `nav_stack = '[]'` для всех fastpath callbacks *(переименован с 137 из-за коллизии с параллельным агентом, см. Session 14 follow-up)* |

Все pattern идентичен migration 129: wrapper RPC (FLAT shape, eager-resolved composites per user lang с EN fallback) → ui_screens row (`back_screen_id_default='progress_main'`, `parent_screen_id='progress_main'`) → единственная Back-кнопка (`buttons.back` / callback `cmd_progress` / `icon_back`) → `ui_translations.{screen}.main_text × 13 langs` (identical template с `{{icon}}`, `{tr:}`, `{var}`).

### n8n Dispatcher (Route Classifier)

`PROFILE_V5_CALLBACKS` расширен: `+cmd_quests, +cmd_league, +cmd_friends_info, +cmd_shop` — PUT через n8n API. Теперь все 4 callback → v3 fastpath → headless render, минуя legacy 04_Menu.

### Bug fixed — `jsonb_set` intermediate key gotcha

Migration 135 изначально упала в preview: `friends_info.main_text` возвращал NULL для всех 13 langs. Причина: `jsonb_set(content, '{friends_info,main_text}', val, true)` **не создаёт** intermediate `friends_info` key если того не было. Section `friends_info` была новой (до этого существовали только `referral.*` keys).

Fix — top-level merge `content || jsonb_build_object('friends_info', ... || jsonb_build_object('main_text', tmpl))`. Патч применён через psycopg2 + обновлён migration 135 файл для идемпотентности на свежей БД.

### UX — естественный Back flow

Before Session 14:
- User: Progress → Квесты (legacy) → [🔙 Назад]
- Legacy 04_Menu Back Target Router `progress` rule → RE-render legacy Progress screen
- Headless progress_main и legacy Progress разный layout → UI flip

After Session 14:
- User: Progress (headless) → Квесты (headless) → [🔙 Назад: `cmd_progress`]
- PROFILE_V5_CALLBACKS → v3 → process_user_input fastpath → render progress_main headless
- **Один и тот же UI** — seamless. `render_strategy='replace_existing'` editMessageText на том же message_id.

Migration 132 workaround стал лишним — откачен migration 137 (`nav_stack = '[]'::jsonb`).

### Validation

**Render (все 4 screens, user 417002669 lang=es):**
- `quests` — daily/weekly sections, ✅/⬜ checkboxes, Noms insight "Quedan 3 — ¡tú puedes!"
- `league` — top-12 standings с [TÚ] marker + zone emoji (🟢/⚪/🔴) + "2d 13h" timer + insight_leader
- `friends_info` — reward_block (+50💎 и +100🌟), stats, invite link `https://t.me/nomsaibot?start=ref_...`, cta_newbie_1
- `shop` — 💎 Saldo + 🧪 Maná 434/500 + ❄️ Congelaciones: 0, premium_teaser

**Fastpath:** `cmd_quests/league/friends_info/shop` → `screen_id=<screen>`, `status=render` ✅

**p95 latency (10 runs, render_screen):** quests 93.9ms, league 80.7ms, friends_info 137.5ms (один outlier, p50=70ms ok), shop 86.2ms, progress_main 95.0ms. Все < 150ms, значительно под 1s watch threshold.

### Architectural notes

- **Zone emoji hardcoded** (🟢/⚪/🔴) в `get_league_rpc` — `app_constants` не имеет `icon_zone_*`; добавление deferred до полноценной миграции League Info sub-screen.
- **Action buttons** (`cmd_buy_mana/freeze/recharge/premium_plans`) в shop — оставлены в legacy 08.4_Shop workflow; миграция 136 переводит только hub-render. Следующая итерация: shop actions headless (требует `save_rpc` + confirmation screens).
- **League insight_close** — gap вычисляется локально в RPC из standings array (my_rank-1 opponent). Если `get_league_standings` не вернул соседа → fallback `collect_xp`.

### Deferred (spawned tasks — не блокируют релиз)

1. **League header duplication** — template `{{icon_league}} <b>{tr:league.title}</b>` + translation сама = "🏆 Clasificación de liga" → "🏆 🏆 …" в preview. Minor — либо убрать `{{icon_league}}` из template, либо title из переводов.
2. **Quests blank line spacing** — двойной \n между daily и weekly секциями (rtrim + array_to_string сепаратор). Читаемо, но можно ужать.
3. **Shop action buttons** headless — отдельная миграция (save_rpc pattern).
4. **Profile v5 spec §4 Noms tips** — `quests.noms_insight` / `shop.noms_insight` как отдельные ключи (сейчас используем `insight_remaining` / `premium_teaser` как proxy).
5. **Банда shortcut bug** и **Магазин→Продлить→Назад** — pre-existing legacy bugs, теперь частично снимаются через headless routing.

### Files touched

**Created:**
- `migrations/133_quests_headless.sql` — 279 LOC
- `migrations/134_league_headless.sql` — 347 LOC
- `migrations/135_friends_info_headless.sql` — 299 LOC
- `migrations/136_shop_headless.sql` — 239 LOC
- `migrations/138_rollback_nav_stack_init_for_progress_children.sql` — 85 LOC (was 137, renamed после коллизии)

**Modified (n8n via PUT):**
- `01_Dispatcher` → Route Classifier jsCode → `PROFILE_V5_CALLBACKS` +4 entries

**Not modified:**
- Legacy 04_Menu — live (но больше не вызывается для этих 4 callback)
- 04_Menu_v3 — fastpath patch через SQL достаточен
- 08.4_Shop — action buttons остались legacy

### Closed loop: iteration 2 checklist

- [x] `quests` headless + fastpath + Back → progress_main
- [x] `league` headless + fastpath + Back → progress_main
- [x] `friends_info` headless + fastpath + Back → progress_main
- [x] `shop` headless + fastpath + Back → progress_main (hub-render only, actions deferred)
- [x] PROFILE_V5_CALLBACKS updated (+4)
- [x] Migration 132 rollback (workaround obsolete)
- [x] p95 latency within threshold (all < 150ms)

Session 14 закрывает Progress Hub iteration 2. Next: Profile v5 children (my_plan/personal_metrics/help/settings) headless migration — Phase 3B.

---

## Session 14 follow-up — Session 13 UX bugs diagnosis (Bug A Banda shortcut + Bug B Shop "Продлить" → Back)

**Контекст:** В Session 13 live test пользователь сообщил про два UX-бага:
- **Bug A:** В headless `progress_main` клик `[👥 Банда]` (callback `cmd_friends_info`) открывает вторичный info-экран «Как работает Банда?» вместо основного Friends с реферальной ссылкой.
- **Bug B:** В Shop клик `[Продлить текущий]` → открывается payment screen → клик `Назад` не возвращает в Shop.

### Диагностика

**NLM snapshot устарел:** NLM ответы (sync ~раз в сутки) описывали поведение ДО сегодняшних deploy. Ground truth — через DB + свежий fetch Dispatcher/04_Menu/10_Payment.

**Session 13 timeline:**
- Session 13 executions в n8n: `10:14–10:30 UTC`
- Migration 135 (`friends_info` headless) applied: `10:46 UTC` (`ui_screens.shop created_at`)
- Dispatcher Route Classifier updatedAt: `10:48 UTC`

→ Обе правки deployed **ПОСЛЕ** Session 13 tests.

### Bug A — скорее всего уже исправлен

Подтверждено в DB:
- `ui_screens.friends_info` существует, `business_data_rpc=get_friends_info_rpc`, `text_key=friends_info.main_text`
- `process_user_input(telegram_id=417002669, cmd_friends_info)` → `screen_id=friends_info`
- `render_screen('friends_info')` возвращает template_vars с `referral_link=https://t.me/nomsaibot?start=ref_417002669` ✅
- `referral.title` = «Твоя Банда» / «Your Squad» (не «Как работает Банда»)
- Dispatcher Route Classifier L173 содержит `cmd_friends_info` в `PROFILE_V5_CALLBACKS` → routes в `menu_v3` headless

Заголовок «ℹ️ Как работает Банда?» исходит из legacy `04_Menu → Build Info Text (Friends)` (node js: `iconInfo + '<b>' + r.info_title + '</b>'`). Это legacy путь. Callback `cmd_friends_info` больше туда не попадает после Dispatcher deploy.

**Action:** попросить user ре-тест. Если баг повторяется — копать n8n execution logs.

### Bug B — защитная миграция применена (137)

**Симуляция flow через back_nav:**

| # | Entry path (nav_stack before Back) | back_nav result | Back Target Router |
|---|------------------------------------|-----------------|---------------------|
| A | `[progress_main, shop, premium_plans]` | `parent=shop, source=stack` | rule[4] shop → Build Shop Text ✅ |
| B | `[profile_main, my_plan, premium_plans]` | `parent=my_plan, source=stack` | rule[1] sub_screen ✅ |
| C | `[premium_plans]` (CTA/expiry bypass) | `parent=NULL, source=empty_no_anchor` | **fallback → Send Main Menu ❌** |

**Root cause:** `premium_plans` отсутствовал в `ui_screens`. Когда стек содержит только `premium_plans` (например, user попал через subscription expiry notification → cmd_premium_plans без предварительного push_nav(shop/my_plan)), anchor fallback в `back_nav` не мог резолвить `back_screen_id_default` → возвращал NULL → Back Target Router default → main menu.

### Migration 137 — premium_plans nav-stack anchor

```sql
INSERT INTO public.ui_screens (
    screen_id, render_strategy, text_key, business_data_rpc, input_type,
    validation_rules, meta, back_screen_id_default, parent_screen_id
) VALUES (
    'premium_plans', 'replace_existing', 'payment.plans_title', NULL, 'inline_kb',
    '{}'::jsonb,
    jsonb_build_object('canonical', false, 'anchor_only', true, 'rendered_by', '10_Payment'),
    'shop', 'shop'
);
```

- `render_strategy='replace_existing'` — чтобы CHECK constraint прошёл. Реальный рендер всё равно делает legacy 10_Payment (не `render_screen`).
- `back_screen_id_default='shop'` — safe default anchor для edge case C.
- `meta.anchor_only=true` — маркер для будущих агентов: запись-anchor, не полноценный headless screen.

**Verification (внутри миграции):**

```
nav_stack=['premium_plans'] → back_nav(uid, 'premium_plans')
  → {parent: 'shop', source: 'anchor_after_pop'} ✅
```

Applied via psycopg2 + `DATABASE_URL`. NOTICE: `Migration 137 verified: premium_plans anchor → parent=shop, source=anchor_after_pop`.

### Не тронуто (по дизайну)

- 10_Payment workflow — рендер Plans/Method Selection остаётся legacy
- Route Classifier — `cmd_premium_plans` НЕ в PROFILE_V5_CALLBACKS, идёт в legacy как раньше
- Back Target Router — rule `shop` уже существовал, дополнений не требуется

### Action items

- [x] Migration 137 applied (premium_plans anchor)
- [ ] User ре-тест Bug A (`[Банда]` из headless progress_main → main Friends screen)
- [ ] User ре-тест Bug B (Shop → Продлить → Back → Shop)
- [ ] Если Bug B edge case C встретится реально — миграция уже защищает
- [ ] Future: если `premium_plans` мигрируется на headless — заменить `render_strategy` с заглушки на честное значение, убрать `meta.anchor_only`

### Почему не стали править n8n сейчас

Bug A: deploy уже был сделан, реальных evidence что баг активен в post-deploy state нет. Не хочется триггерить n8n PUT webhook drift (CLAUDE.md §"PUT Dispatcher безопасен ... PUT в 04_Menu / 10_Payment не триггерит webhook drift" — но всё равно: без evidence баг не воспроизводится).

Bug B: edge case C защищён миграцией, flow A/B и так работают. Если user увидит другой симптом — будем диагностировать отдельно.

---

## Session 14 iteration 3 — Progress Hub UX alignment (user-approved layouts)

**Контекст:** После live-test migration 133-136, user заявил несоответствие макетам §22/§23/§24. Провели сравнительный анализ (Legacy / Spec / Current Headless) для всех 4 children. User dictated layout decisions.

### Decisions

| Screen | Решение |
|---|---|
| **Quests** | Spec + Legacy section headers (📅 Weekly, 🏅 Achievements) |
| **League** | Spec BUT 4-neighbor window (rank±2) вместо full zone split (ближе к legacy) |
| **Friends** | Full Spec §11.1 (remove reward_block, add stats_active/pending, Share + Info buttons) |
| **Shop** | Spec + premium-branch mana line ("Безлимит"), 4 buttons inline prices, free→[Mana+GoPRO] / premium→[ManageSub] |
| **Back button everywhere** | **Universal `cmd_back`** (не `cmd_progress`, не `cmd_back_to_progress`). Relies on `ui_screens.back_screen_id_default=progress_main` + hierarchical back (migration 117). |

### Migration 139 (applied 2026-04-24 15:47)

`139_progress_children_spec_alignment.sql` — 550 LOC, consolidated update:

1. **`get_quests_rpc` v2** — 3 sections:
   - daily — no header
   - weekly — `📅 <b>{section_weekly}</b>` header (was "Недельные:" / "Semanales:" / ...)
   - one-time — `🏅 <b>{section_achievements}</b>` header (was "Достижения:" / "Logros:" / ...)
   - frequency values verified via DB: `'daily' / 'weekly' / 'one-time'` (with hyphen, not underscore).
2. **`get_league_rpc` v3** — 4-neighbor window (rank±2), edge-shifted to maintain 5 rows when possible. Bold HTML для user row. `zones_formatted` removed.
3. **`get_friends_info_rpc` v2** — spec §11.1 stats block (squad_count + stats_active + stats_pending + earned_label + pro_goal), empty-state uses `referral.no_invites`. `reward_block` removed (это §11.2 how_it_works content).
4. **`get_shop_rpc` v2** — premium-branch `mana_line`: если `subscription_status <> 'free'` → `shop.mana_unlimited` (новый ключ × 13 langs); иначе → `shop.mana_status` + current/max.
5. **Templates** — сняли double-emoji prefix в quests/league (translation title уже содержит 📋/🏆); league restructured (week_label + time_label сверху, потом window, потом your_rank).
6. **Новые translation keys × 13 langs** (human-authored для 13 главных langs):
   - `shop.mana_unlimited` = "Мана: Безлимит" / "Mana: Unlimited" / "Maná: Ilimitado" / ...
   - `shop.btn_buy_freeze` = "Заморозка — 500 💎" / "Freeze — 500 💎" / ...
   - `shop.btn_buy_mana` = "Мана — 300 💎" / "Mana — 300 💎" / ...
   (prices inlined в button labels per legacy feel)
7. **ui_screen_buttons rewired:**
   - quests: `[🔙 Назад → cmd_back]` (1 button)
   - league: `[ℹ️ {info_button} → cmd_league_info]` + `[🔙 Назад → cmd_back]` (2)
   - friends_info: `[📤 Share → share_invite_link]` + `[ℹ️ Info → cmd_friends_how_it_works]` + `[🔙 Назад → cmd_back]` (3)
   - shop: `[❄️ Freeze 500💎]` + `[🧪 Mana 300💎 free only]` row 0; `[👑 Go PRO free]` + `[👑 Manage Sub premium]` row 1 (mutually exclusive via `visible_condition`, разнесены по col 0/1); `[🔙 Назад → cmd_back]` row 2.
   - **Unique constraint** `(screen_id, row_index, col_index)` не позволяет 2 кнопки на одной позиции — mutually exclusive pairs используют разные `col_index` (0 / 1) даже если только одна будет видна.

### Verification (2026-04-24 user=417002669 lang=es premium)

**Render previews (es, премиум-ветка):**

```
QUESTS:
<b>📋 Tus misiones</b>
⬜ Madrugador ... (+20 XP)
⬜ Pausa Consciente ... (+20 XP)
⬜ Hat-trick ... (+20 XP)
📅 <b>Semanales:</b>
✅ ... (+75 XP)
✅ ... (+50 XP)
✅ ... (+100 XP)
💬 Noms: "Quedan 3 — ¡tú puedes!"

LEAGUE:
<b>🏆 Clasificación de liga</b>
Tu liga: 🥒 Pickle
Semana: 20.04 — 26.04
⏰ Termina en: <b>2d 8h</b>
<b>1. Vladislav (TÚ) — 930 🌟</b>
2. Sofia — 890 🌟
... (5 rows window)
Tu puesto: #1 de 20
💬 Noms: "¡Eres el #1! ..."

FRIENDS:
👥 <b>Tu Pandilla</b>
¡Invita amigos y gana recompensas!   ← no_invites path (count=0)
PRO gratis: 0/4
<b>Tu enlace de invitación:</b>
<code>https://t.me/nomsaibot?start=ref_417002669</code>
💬 Noms: "Adelgazar solo apesta..."

SHOP (premium):
🛒 <b>Tienda</b>
💎 Saldo: 548 NomsCoins
🧪 Maná: Ilimitado   ← premium branch
❄️ Tienes: 0
💬 Noms: "Maná ilimitado · Sin límites diarios"
Buttons: [❄️ Freeze — 500 💎] [👑 Gestionar suscripción] [🔙 Atrás]
         (Mana button hidden — premium; Go PRO hidden — премиум уже)
```

**cmd_back flow — все 4 children → progress_main через `back_screen_id_default`:**
```
from quests        → screen_id=progress_main ✅
from league        → screen_id=progress_main ✅
from friends_info  → screen_id=progress_main ✅
from shop          → screen_id=progress_main ✅
```

**p95 latency (render_screen only):** quests 152ms, league 109ms, friends_info 206ms, shop 194ms. Все < 250ms.

### Latency observation — n8n webhook overhead (не в scope migration 139)

Last 15 n8n executions (postmortem):
| Workflow | p50 | p95 | max |
|---|---|---|---|
| Dispatcher `6XzdbQqp…` webhook | **~1.3s** | **2.5s** | 2.6s |
| menu_v3 `ju0h4WSt…` integrated | 320ms | 500ms | 645ms |

Suммарный user-perceived click → render = **1.1–3.1s**. `render_screen` = 90-200ms (10%). Остальные **90% — n8n + Telegram API overhead** (dispatcher flow + Telegram `editMessageText` roundtrip). Это задачи отдельной сессии на профилинг Python proxy + n8n workflow optimization. **Кнопки не срабатывают у тестовых аккаунтов** — предположительно Telegram Trigger cold-start или webhook race в Python proxy. Нужны Python proxy logs для подтверждения.

### Action items

- [x] Migration 139 v2 applied (550 LOC, идемпотентна)
- [x] Все 4 screens → spec alignment + user decisions
- [x] Back = `cmd_back` везде (canonical, no dup callbacks)
- [x] Shop: free/premium branch split (mana line + button visibility)
- [ ] **Deferred**: `cmd_league_info` / `cmd_friends_how_it_works` / `cmd_buy_mana` / `cmd_buy_freeze` / `cmd_premium_plans` / `cmd_profile_subscription` / `share_invite_link` — пока роутятся в **legacy** (04_Menu / 08.x / 10_Payment). User увидит UI flip при клике. Следующая итерация headless.
- [ ] **Deferred**: n8n+Telegram overhead profiling (click → render 1-3s, не в БД)
- [ ] **Deferred**: first-click misses diagnosis (Telegram Trigger cold-start? proxy race?)

### Files touched

- `migrations/139_progress_children_spec_alignment.sql` — 550 LOC (переписан из v1, применён v2)

### No n8n PUT в этой session

Dispatcher `PROFILE_V5_CALLBACKS` уже содержит 4 callbacks (+cmd_quests/league/friends_info/shop, added Session 14). Action callbacks (`cmd_league_info`, `cmd_friends_how_it_works`, `cmd_buy_*`, `cmd_premium_plans`, `cmd_profile_subscription`, `share_invite_link`) **НЕ добавлены** в PROFILE_V5_CALLBACKS — остаются legacy-routed до будущей миграции.

---

## Session 14 iteration 4 — Migration 140 (БД-only): Stateful Banda + League empty + info subscreens

**Scope:** SQL-only migration (никакого n8n / Python кода — параллельный агент переписывает 01_Dispatcher на Python). После apply — handover message ниже.

**Constraints session (user direct):**
- БД-only (n8n PUT и Python deploy → handover другому агенту)
- `cmd_back` универсальная кнопка везде (no `cmd_back_to_X` дубликаты)
- Без хардкодов — все thresholds/icons из `app_constants`
- Spec файл `profile-v5-screens-specs.md` обновить чтобы синхронизировать с реальной БД

### Migration 140 applied 2026-04-24

`140_progress_hub_phase_2_alignment.sql` (~750 LOC, idempotent) — single migration:

**Part 1 — `app_constants` (2 new icons):**
- `icon_partner = 💼` (Ambassador title)
- `icon_pro_goal = 🏆` (PRO goal visual prefix)

**Part 2 — `ui_translations` (4 new keys × 13 langs):**
- `league.subtitle` = "Топ-5 переходят в следующую лигу." (UX polish per spec §22)
- `league.until_monday` = "До понедельника" (empty state §10.3)
- `league.empty_tip` = "А пока — копи XP и готовься!" (empty state §10.3)
- `referral.how_it_works_text` = template для §11.2 screen

**Part 3 — `get_friends_info_rpc` v3 (Stateful Novice/Ambassador):**
- Computed `v_is_ambassador = (ambassador_tier='active' OR paid_referral_count >= ambassador_threshold OR is_trainer)`. **COALESCE на ambassador_tier обязателен** — без него `NULL='active'` даёт `NULL`, и `IF NOT NULL` ошибочно роутит дефолтных Novice users в Ambassador path (баг найден на первом apply, исправлен).
- Novice path: structured stats (squad/active/pending/earned всегда даже при count=0 — user decision) + `🏆 PRO goal {paid}/{premium_threshold}` (target из `app_constants.referral_premium_threshold` = 4) + `🔗 link block` + `cta_newbie_1`/`cta_active_1`.
- Ambassador path: вызывает `get_ambassador_stats` + `get_ambassador_balance`. Builds body: subtitle + 4 stat lines (👥 Привлечено / 📈 Конверсия / 💰 Баланс / 💸 Всего выплачено) + promo block (если codes есть, иначе fallback на invite link) + random `noms_ambassador_1/2/3`.
- Returns FLAT shape: `{title_resolved, icon_resolved, body_formatted, link_block, cta_line, is_ambassador_active, ...}`.

**Part 4 — `get_league_rpc` v4 (empty state + ► marker + content_block):**
- Empty state branch: `my_rank IS NULL OR total = 0` → countdown days_until_monday (UTC, ISODOW) + `empty_block`.
- Active state: ► marker для self row (без `(TÚ)` суффикса per spec §10.1), 4-neighbor window (rank±2, edge-shifted to 5 rows).
- **`content_block`** — единый placeholder для template. RPC сам компонует header (your_league + week_label + timer line через `{{icon_clock}} {tr:league.timer_label}: <b>{time_left}</b>`) + subtitle + window + your_rank. Это убирает residual blank lines от пустых placeholder'ов в empty state.
- **rtrim bug fixed**: PostgreSQL `rtrim(text)` без 2-го аргумента трогает только пробелы — `\n` не убирает. Исправлено на `rtrim(text, E' \t\n\r')`.

**Part 5 — NEW `get_friends_how_it_works_rpc`:**
- Eager-resolves `referral.info_step3` placeholders `{coins}/{xp}/{icon_coin}/{icon_xp}` (single-shot interpolate — Dumb Renderer не повторяет проход по `{var}`).
- `{target}` в `info_step4` resolved из `app_constants.referral_premium_threshold`.
- Reward amounts из `app_constants.referral_escrow_coins=50` / `_xp=100`.
- Returns: `{steps_block, reward_block}`.

**Part 6 — Templates rewrite:**
- `friends_info.main_text` — universal Novice/Ambassador shell (`{icon_resolved} <b>{title_resolved}</b>` + `{body_formatted}` + `{link_block}` + Noms).
- `league.main_text` — упрощён до title + `{content_block}` + Noms (RPC композит).
- `league.info_text_full` — composite из existing `{tr:league.info_text} + {tr:league.groups_form}.` (НЕ создаём новый translation — реюз).

**Part 7 — `ui_screens` (2 new):**
- `league_info` (text-only, `back_screen_id_default='league'`)
- `friends_how_it_works` (с `business_data_rpc=get_friends_how_it_works_rpc`, `back_screen_id_default='friends_info'`)

**Part 8 — `ui_screen_buttons` rewire:**
- `friends_info` — DELETE existing 3, INSERT 6 rows: 2 Novice (visible if `tier <> 'active' AND paid<5 AND NOT is_trainer`) + 3 Ambassador (visible if `tier='active' OR paid>=5 OR is_trainer`) + 1 Common Back. Threshold `5` хардкод в visible_condition с inline комментарием `mirrors app_constants.ambassador_referral_threshold` (ui_screen_buttons.visible_condition не делает sub-query lookup без потери performance).
- `league_info` + `friends_how_it_works` — single Back каждый.

**Part 9 — `process_user_input` fastpath:**
- Extend IF list: + `'cmd_league_info'`, `'cmd_friends_how_it_works'`.
- Extend CASE: `cmd_league_info → league_info`, `cmd_friends_how_it_works → friends_how_it_works`.

**Part 10 — Verification + Hardcode Audit:**
- Проверка FLAT shape всех 3 RPCs.
- `render_screen` no errors для всех 4 screens.
- Fastpath screen_id correct.
- Button counts (6 / 1 / 1).
- Hardcode audit (regex grep на `'4'`/`'50'`/`'100'` в RPC bodies — должны быть из app_constants).

### Bugs found & fixed during apply

| # | Bug | Fix |
|---|---|---|
| 1 | `v_is_ambassador` NULL propagation (NULL OR FALSE = NULL → IF NOT branches к Ambassador path для default Novice users) | Wrap в `COALESCE(ambassador_tier, '') = 'active'` |
| 2 | `rtrim(text)` без 2-го аргумента не убирает trailing `\n` | Explicit charset: `rtrim(text, E' \t\n\r')` |
| 3 | Template extra blank lines в active league (`{window_formatted}\n{empty_block}\n` рендерило 2 пустых строки) | Refactored — RPC компонует весь body в `content_block`, template имеет только title + content_block + Noms |

### Verification (psycopg2 после apply)

```
Novice (default user):
   👥 Твоя Банда + structured stats + 🏆 PRO goal + 🔗 link + Noms cta_newbie_1
   Buttons: [share_invite_link] + [cmd_friends_how_it_works] + [cmd_back]
   is_ambassador_active = false

Ambassador (UPDATE is_trainer=true; ROLLBACK):
   💼 Панель Партнёра + <i>subtitle</i> + 4 stats + 🔗 link (no promo code yet) + Noms ambassador
   Buttons: [share_ambassador_code] + [cmd_start_payout] + [cmd_ambassador_stats] + [cmd_back]

League ACTIVE: 4-neighbor window с ► marker, subtitle, your_rank, без extra blank lines.
League EMPTY (DELETE league_memberships; ROLLBACK): countdown 7д до понедельника + empty_tip.
league_info: text from league.info_text + groups_form.
friends_how_it_works: 4 шага с резолвленными 50 💎 / 100 🌟 / 4 целью + reward block.

Fastpath: cmd_league_info → league_info, cmd_friends_how_it_works → friends_how_it_works ✅

p95 latency: friends_info 81ms, league 115ms, league_info 90ms, friends_how_it_works 83ms.
Idempotency: re-apply succeeded — все NOTICE printed, no errors.
```

### Spec file alignment

`claude-memory-compiler/knowledge/concepts/profile-v5-screens-specs.md` обновлён:
- §10 status → ✅ live (migration 140), 4-neighbor window вместо 3-zone split, ► marker, empty state countdown.
- §11 status → ✅ live, Stateful UI implemented, app_constants reuse highlighted.
- §21 quests макет → реальный 3-section layout (📅 Недельные / 🏅 Достижения), `cmd_back`.
- §22 league макет → 4-neighbor window + active/empty branches.
- §23 friends_info → Stateful UI с button context-матрицей.

### Out of scope (deferred)

- **Migration 141 (Shop transactions §9.2-§9.7)** — требует расширения headless framework (action_rpc pattern + idempotency guard against double-charge). Отдельная сессия дизайна framework + 5 screens (~1000 LOC).
- **Migration 142+ (Ambassador Payout §12)** — 8 экранов, FSM, phone collection, admin chat. Целая отдельная сессия.
- **n8n / Python Dispatcher** — параллельный агент.

### NLM verification — TODO

NLM сессия истекла во время написания (cookie expired). User дал зелёный свет на psycopg2 verification как primary (CLAUDE.md правило: live > NLM при расхождении). После re-login user'ом — рекомендуется кросс-чек NLM по 3 вопросам:
1. "Какой механизм Stateful UI используется?" — expect `visible_condition` + RPC branching (precedent profile_main).
2. "Какие RPCs существуют для Ambassador Dashboard?" — expect get_ambassador_stats / get_ambassador_balance / create_ambassador_code.
3. "Где хранится referral_premium_threshold?" — expect `app_constants` (= 4).

---

### 📨 HANDOVER → Dispatcher Python rewrite agent

Migration 140 added 2 new headless screens that need PROFILE_V5_CALLBACKS routing in the new Python Dispatcher (replacing 01_Dispatcher n8n):

**Add to PROFILE_V5_CALLBACKS set:**
- `cmd_league_info` → `process_user_input` resolves `screen_id='league_info'` через fastpath
- `cmd_friends_how_it_works` → resolves `screen_id='friends_how_it_works'`

**Existing PROFILE_V5_CALLBACKS** (уже routed в `process_user_input` v3, не менять):
```
cmd_get_profile, cmd_my_plan, cmd_settings, cmd_personal_metrics, cmd_help,
cmd_update_weight, cmd_edit_weight/age/height, cmd_delete_account, cmd_confirm_delete,
cmd_edit_goal/activity/training/speed/gender/lang/country/timezone/notifications_mode,
cmd_get_stats, cmd_progress, cmd_quests, cmd_league, cmd_friends_info, cmd_shop
```

**Goal:** complete Headless architecture migration. После Python Dispatcher deploy + добавление этих 2 callbacks — все Progress Hub screens (quests, league, league_info, friends_info, friends_how_it_works, shop) будут полностью headless. Action callbacks (`cmd_buy_freeze/mana/premium_plans`, `share_invite_link`, `share_ambassador_code`, `cmd_start_payout`, `cmd_ambassador_stats`) остаются legacy-routed до migration 141+ (требует action_rpc framework).

**Files modified by Session 14 iteration 4 (БД-only):**
- `migrations/140_progress_hub_phase_2_alignment.sql` — applied 2026-04-24, idempotent.
- `claude-memory-compiler/knowledge/concepts/profile-v5-screens-specs.md` — секции §10/§11/§21/§22/§23 sync с реализацией.

**Что новый Dispatcher должен помнить:**
1. PROFILE_V5_CALLBACKS — Set callback'ов которые роутятся в headless v3 (process_user_input fastpath).
2. Reply-button text detection: `text.includes('🚀 Прогресс')` / etc → `command='cmd_progress'` → v3.
3. Action callbacks (вне PROFILE_V5_CALLBACKS) — fallback в legacy 04_Menu / 08.x / 10_Payment.
4. После каждого PUT — проверка webhook stability (если deploy на свой сервер — этой проблемы быть не должно).

### Hotfix: icon_pro_goal duplicate removed (per user dup-check rule)

User audit указал: `icon_pro_goal=🏆` дублирует existing `icon_league=🏆`. Согласно правилу "не дублируем сущности" — `icon_pro_goal` удалён из `app_constants`, RPC `get_friends_info_rpc` теперь pull `v_icon_pro_goal := SELECT value FROM app_constants WHERE key='icon_league'` (semantic reuse, тот же 🏆 emoji). `icon_partner=💼` оставлен — уникален.

**Final app_constants additions от migration 140:** только +1 ключ (`icon_partner`).

### NLM verification (post-apply, 2026-04-24)

Conversation `d45e9a31-6a18-4e85-b5c3-3f928db1ddb9`. Все 3 архитектурных вопроса подтвердили реализацию:

**Q1: Stateful UI patterns** — NLM confirmed `visible_condition` + business_data_rpc branching как established pattern. Existing screens: `profile_main` (free/premium), `stats_main` (count-based), `shop` (subscription split), `edit_*` (registration/registered context), `friends_info` (tier-based — наша новая реализация). Variant 1 в migration 140 точно совпадает с precedent.

**Q2: Ambassador RPCs** — все 4 RPC существуют (`get_ambassador_stats/balance`, `get_referral_info`, `create_ambassador_code`). users поля: `ambassador_tier text`, `paid_referral_count int`, `is_trainer bool`, `ambassador_commission_rate numeric`, `referral_count`, `referrer_id`, `trainer_commission_rate`. **НЕТ полей `ambassador_balance_usdt`/`ambassador_promo_code`** в users — баланс computed через RPC, **promo codes хранятся в отдельной таблице `discount_codes`** (с `referrer_id` + `is_ambassador_code` flag). Мой RPC корректно обрабатывает empty codes case (fallback на referral link).

**Q3: app_constants thresholds + dup detection** — все 4 thresholds confirmed (4/5/50/100). После cleanup: `icon_pro_goal` **больше не существует** в БД, 🏆 закреплён **только за `icon_league`** ✅. NLM подтвердил архитектурное наблюдение: дубликаты по emoji value — норма (semantic separation позволяет менять иконку одной фичи без impact на другую — например `icon_coin/icon_crystal/icon_crypto` все = 💎 для разных контекстов). Пользовательское правило "не дублируем" применимо когда semantic пересекается; в нашем случае `icon_pro_goal` semantic фактически = `icon_league` (визуальный prefix для PRO goal не имеет отдельной идентичности) — reuse уместен.

### Final state

- ✅ Migration 140 applied + idempotent
- ✅ icon_pro_goal duplicate removed, icon_league reused
- ✅ NLM verification passed (3/3 architectural questions confirmed)
- ✅ Spec file synced (§10/§11/§21/§22/§23)
- ✅ Changelog + handover written
- ⏳ Live test pending — Python Dispatcher rewrite (parallel agent), затем добавление `cmd_league_info`/`cmd_friends_how_it_works` в PROFILE_V5_CALLBACKS

**Files touched (БД-only session):**
- `migrations/140_progress_hub_phase_2_alignment.sql` — ~750 LOC, applied (+ dup cleanup)
- `claude-memory-compiler/knowledge/concepts/profile-v5-screens-specs.md` — sync sections
- `claude-memory-compiler/daily/2026-04-24.md` — this changelog

---

## Session 14 iteration 5 — Cleanup + ТЗ для следующих миграций (2026-04-28)

### File rename: 140 → 142 (collision resolution)

Параллельный агент применил `140_debounce_window_500ms.sql` (25.04 20:32) и `141_debounce_dedicated_column.sql` (25.04 20:33). Мой `140_progress_hub_phase_2_alignment.sql` (применён 27.04 23:53) численно столкнулся. **БД changes уже применены — переименование чисто файловое.**

```
git mv migrations/140_progress_hub_phase_2_alignment.sql \
       migrations/142_progress_hub_phase_2_alignment.sql
```

Header file обновлён `migration 140` → `migration 142 (renamed from 140 — collision with parallel agent debounce migrations)`.

### Migration 143 — cleanup process_user_input duplicate WHEN clauses

**Bug:** мой Part 9 (fastpath patch) в migration 142 при 3 hotfix re-apply'ах оставлял дубли `WHEN 'cmd_league_info' THEN 'league_info'` и `WHEN 'cmd_friends_how_it_works' THEN 'friends_how_it_works'` — REPLACE pattern не truly idempotent.

**Pre-cleanup state:**
- `process_user_input` body length: 19,028 chars
- 26 WHEN-веток в CASE (вместо 14 canonical)
- 6 копий каждой моей WHEN-ветки (от migration 142 v1+v2+v3+...)

**Migration 143 idiom:** `pg_get_functiondef + regexp_replace + EXECUTE`. Pattern:
```sql
regexp_replace(
    v_src,
    '(\s*WHEN ''cmd_league_info''\s+THEN ''league_info''\s+WHEN ''cmd_friends_how_it_works''\s+THEN ''friends_how_it_works''){2,}',
    canonical_single_pair, 'g'
)
```

`{2,}` quantifier — заменяет 2+ повторений на 1 канонический pair. POSIX ARE.

**Post-cleanup (verified):**
- Body length: 19,323 → 18,206 chars (saved ~1,100 chars)
- 14 canonical WHEN-веток (правильные, по 1 на callback)
- Verification DO-block: 9 fastpath callbacks (cmd_progress, cmd_quests, cmd_league, cmd_friends_info, cmd_shop, cmd_league_info, cmd_friends_how_it_works, cmd_get_profile, cmd_get_stats) → все возвращают correct screen_id ✅
- Idempotent: re-apply notice "process_user_input has cmd_league_info × 1 — no cleanup needed" ✅

**Functional impact pre-fix:** NONE — CASE WHEN matches first, дубли были dead branches. Cosmetic / parsing overhead.

### NLM/diagnostic feedback for parallel agent

Параллельный агент провёл диагностику self-hosted n8n executions errors — root cause **`self-signed certificate in certificate chain`** в Postgres credential (`Postgres_pooler_6543`, `allowUnauthorizedCerts: false` на self-host vs `true` на cloud). Migration 142 **здесь не при чём** — подтверждено агентом.

Type C error (`PostgREST filter parse error в 03_AI_Engine`) — также не связан с моими RPC changes (трогает таблицу `bot_stickers` через PostgREST, не RPC).

**Бот сейчас работает быстрее, sticker исчезает корректно** — параллельный агент починил credentials.

### ТЗ для migration 144 (Shop transactions §9.2-§9.7)

**Файл:** `.claude/specs/migration_144_shop_transactions_spec.md`

Покрывает §9.2-§9.7 — buy_freeze_confirm / buy_mana_confirm / success / failure (insufficient coins / mana full / recharge limit) / premium teaser. ~800-1000 LOC миграция.

**Архитектурная точка:** требует расширения headless framework — `save_rpc` whitelist расширяется с `set_user_%` на `set_user_% OR shop_action_%` + новые wrapper RPCs `shop_action_buy_freeze` / `shop_action_buy_mana` с idempotency guard (5-second window через `coin_transactions` audit). Detailed в спецификации.

**Constraints:** БД-only, без хардкодов, `cmd_back` универсальная, idempotent re-apply (учитывая урок migration 143). NLM cross-check рекомендуется ДО implementation.

### Files modified Session 14 iteration 5 (БД-only)

**Renamed:**
- `migrations/140_progress_hub_phase_2_alignment.sql` → `142_progress_hub_phase_2_alignment.sql`

**Created:**
- `migrations/143_cleanup_process_user_input_duplicates.sql` — applied, idempotent
- `.claude/specs/migration_144_shop_transactions_spec.md` — ТЗ для следующего агента

**Not touched:**
- n8n workflows (параллельный агент)
- Python proxy / Dispatcher
- Other RPCs / tables

### State of Headless migration roadmap

| Migration | Status | Scope |
|---|---|---|
| 122-128 | ✅ live | Stats Headless + Profile v5 Edit + Top-level nav |
| 129-131 | ✅ live | Progress Hub kickoff (progress_main) |
| 132 | ✅ rolled back (138) | nav_stack workaround obsolete after 133-136 |
| 133-136 | ✅ live | Quests / League / Friends / Shop hub-render |
| 137 | ✅ live | premium_plans nav anchor (parallel agent) |
| 138 | ✅ live | nav_stack init rollback |
| 139 | ✅ live | Progress children spec alignment v1 |
| 140 (parallel) | ✅ live | Debounce window 500ms |
| 141 (parallel) | ✅ live | Debounce dedicated column |
| 142 | ✅ live | Stateful Banda + League empty + info subscreens (Session 14 iter 4) |
| 143 | ✅ live | process_user_input duplicates cleanup (Session 14 iter 5) |
| **144** | 📝 ТЗ ready | Shop transactions §9.2-§9.7 — следующая сессия |
| **145+** | 🟡 deferred | Ambassador Payout §12 (FSM, phone, admin chat) |

---

## Session 14 iteration 6 (2026-04-28) — Migration 144 confirmed + spec 145 ready

**144 verified в БД:** 8/8 screens applied (`buy_freeze_confirm/success`, `buy_mana_confirm/success`, `shop_error_no_coins/mana_full/recharge_limit/already_premium`), 2 wrapper RPCs (`shop_action_buy_freeze/buy_mana`) с idempotency. Параллельный агент завершил.

**Handover для n8n/code агента (для 144):**
```python
PROFILE_V5_CALLBACKS.update({
    'cmd_buy_freeze', 'cmd_buy_mana',
    'cmd_confirm_buy_freeze', 'cmd_confirm_buy_mana',
})
```
+ из 142: `'cmd_league_info', 'cmd_friends_how_it_works'`.

### Migration 145 ТЗ — Ambassador Payout §12

**Файл:** `.claude/specs/migration_145_ambassador_payout_spec.md`

**Покрытие:** §12.1–§12.8 — Method Picker / Phone collection (с anti-spoofing) / Wallet input (text_input FSM) / Confirm / Success / Admin Approval Card (chat 417002669) / Approved/Rejected notifications.

**Архитектурные решения проработаны:**
- Reuse existing `request_payout` / `admin_review_payout` / `get_ambassador_balance` / `get_payout_business_data` RPCs.
- Reuse existing `workflow_states` FSM (`payout_method` → `payout_wallet` → `payout_confirm`) — заполнить NULL `screen_id` поля.
- Reuse existing 17 `payout.*` translation keys; добавить 11+ новых для phone collection.
- NEW `users.payout_draft jsonb` column — хранит partial state между screens.
- NEW save RPCs: `set_user_phone` / `set_payout_method` / `set_payout_wallet`.
- NEW action RPC: `payout_action_request` — wrapper над `request_payout` с 30-сек idempotency window.
- `process_user_input` whitelist расширение: `set_user_% OR shop_action_% OR payout_action_%`.

**Split с handover для n8n/code агента (post-145):**
- БД-сторона: 8 ui_screens + RPCs + translations + FSM links.
- n8n/Python-сторона: phone contact API (Telegram special handling), admin chat listener (NOTIFY/cron), pattern match для `admin_payout_(approve|reject)_(uuid)` callbacks.

**Размер:** ~1000-1200 LOC SQL.

### Roadmap update

| Phase | Migration | Status |
|---|---|---|
| Progress Hub Phase 2 | 142 | ✅ live |
| Cleanup duplicates | 143 | ✅ live |
| Shop Transactions §9 | 144 | ✅ live |
| **Ambassador Payout §12** | **145** | **📝 ТЗ ready** (`.claude/specs/migration_145_ambassador_payout_spec.md`) |
| 146+ | — | 🟡 deferred (payout history, promo code creation, B2B invoice detail) |

После 145 завершится **headless миграция всего Profile v5 / Progress Hub flow.** Останутся только: Onboarding (legacy 02_Onboarding_v3 — отдельная архитектура), Food logging (03_AI_Engine — AI-related, не headless), Cron pushes (Python).
