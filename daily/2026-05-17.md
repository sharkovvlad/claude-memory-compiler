# Daily Log: 2026-05-17

## Sessions

### Session (UX-копирайтинг Phase 3 — ES Pilot done)

**Контекст:** Продолжение многофазного UX-копирайтинга. Phases 0/1/2 завершены 16.05 (RU терминология + Sassy Sage Multilingual Glossary 11 языков). Сейчас Phase 3 — **ES pilot** на 26 highest-impact ключах. ES — priority язык, есть жалобы пользователей на дословную кальку с RU.

**Что сделано:**
- `git rebase origin/main` — подтянул свежий main (PR #82 merged 16.05 → mig 231 в main). Открытых PR нет, свободный номер = **232**.
- **Audit dataset** `tools/es_pilot_audit_20260516.json` — 26 ключей с current ES + RU+EN reference + placeholders. 17 variant arrays + 8 scalars + 1 missing (`cron_notifications.reminder_meal_breakfast` — writer восстановил из EN/RU).
- **Writer subagent** (Sassy Sage ES, LatAm priority Mexico/Argentina, Spain secondary) — переписал все 26 ключей по ES glossary + жёстким constraints. Output: `tools/es_pilot_replacements_20260516.json`.
- **Critic subagent** (27yo native ES, CDMX+Madrid, перфекционист по nuance) — 5-axis scoring:
  - 14 approved as-is
  - 12 fix_needed (с готовыми `fix_proposal`)
  - 0 auto-rejected
  Output: `tools/es_pilot_critic_20260516.json`.
- **Merge writer + critic** в финальный JSON `tools/es_pilot_final_20260517.json`.
- **Manual sanity scan** на red-flag паттерны (`coger`, `vale`, `a por`, `registrar`, slash-gender) — поймал одно `'a por más'` в `free_tier.limit_reached` (critic flag'ал но fix не вычистил везде). Поправил руками.
- **Migration `232_es_terminology_pilot_phase3.sql`** — per-key `jsonb_set` в одной транзакции + встроенный snapshot block (idempotent) + variant length assertions + recursive walk на остаточные паттерны.
- **Applied через psycopg2** на live БД. 4 spot-check прошли — качество eye-check отличное.
- **PR [#83](https://github.com/sharkovvlad/noms-bot/pull/83) создан**, CI checks прошли (Diff sanity audit + Migration collision guard).

**Ключевые семантические замены (LatAm-friendly Sassy Sage):**
- `registrar / registro` (звучит как налоговая) → **`apuntar / apunte`** (human-scale, sass)
- `congelación` → **`escudo`** (geym-feel)
- `la mafia despierta` (cron evening, RU-only мем) → **биохимия metaphor** («metabolismo entra en modo avión»)
- `campeón` (gendered masc, exclusion женщин) → **invariant** («esto ya no es suerte, es carácter»)
- `¿vale?` (Spain-leaning) → **`¿sí?`** (universal)
- `¡Vaya!` (религиозный undertone «Virgen!») → **`¡Anda!` / `¡Caray!`**
- `a por más` (Spain construction) → **`por más`** (LatAm)
- `Tu amigo subió de Liga` (gendered) → **`{friend_name} subió de Liga`** (без descriptor)
- `pulsa` (Spain) → **`toca`** (universal)
- `Eres un campeón` → **`Esto ya no es suerte, es carácter`**

**Critic top-3 best writer cases:**
1. `cron_notifications.streak_frozen` — `escudo` + `¡Anda!` + сокращение на 20%
2. `gamification.daily_goal_reached` v2 — `NOMS está oficialmente contento (y eso, en una IA, ya es decir)` — Дэдпул-mode калибрирован
3. `cron_notifications.evening` — биохимия+pop fusion («metabolismo baja revoluciones... modo avión») вместо RU-кальки

**Critic top-3 worst writer cases (с fix'ами):**
1. `referral.present_friend` v1 — «Tu persona subió de Liga» агграмматично → fix: только `{friend_name}` без descriptor
2. `pay.paywall` v2 — «¿Vamos a por más?» Spain-only + «Stats con chicha» (в LatAm chicha = напиток) → fix: `¿Vamos por más?` + `Stats con peso`
3. `onboarding.ask_timezone` v3 — «precisión de farmacia» calque (не идиома) → fix: возврат к `precisión quirúrgica`

**Артефакты:**
- PR: [noms-bot#83](https://github.com/sharkovvlad/noms-bot/pull/83) (open, ожидает merge)
- Migration: `migrations/232_es_terminology_pilot_phase3.sql`
- Snapshot: `public.backup_ui_translations_es_pilot_20260517`
- Workflow artifacts: `tools/es_pilot_{audit,replacements,critic,final}_*.json` (вне git, временные)

**DB-state vs git:**
- DB live: mig 232 applied via psycopg2
- Git origin/main: 231 (232 в open PR #83)
- Expected desync до merge

**Lesson — двухступенчатый writer→critic pipeline для культурной локализации:**
- Writer + Critic в **разных сессиях** даёт честный feedback (нет attachment к собственному output).
- 5-axis scoring (naturalness/Sage-tone/no-false-friends/brevity/anti-shame) catches different problem types.
- Manual sanity scan на red-flag patterns **обязателен** даже после critic — critic может flag'нуть проблему но не вычистить везде (моё `'a por más'`).
- LatAm vs Spain distinction critical для ES — слова типа `vale`, `a por`, `coger`, `pulsa` alienate audience. Default LatAm-universal `tomar`, `toca`, `por`, `¿sí?`.
- `apuntar` побеждает `registrar` и `loguear` для «log/track meal» — это human-scale verb, тогда как `registrar` = canceleria, `loguear` = backend jargon.

**Phase 4 — next sessions:**
- 11 миграций (по одной на язык), waves A (DE/FR/IT/PT) → B (PL/UK) → C (ID/HI/AR/FA с RTL tech-reviewer).
- Тот же pipeline writer→critic→manual-scrub, plus Fiverr native pass до merge (~$50/язык × 11 = $550).
- Каждая фаза = отдельная сессия + PR.

**Fiverr native pass** для ES — отдельный шаг до merge #83, по бюджету тимлида. Не блокирует merge — Sassy Sage critic уже native-quality, Fiverr — final quality gate.

**Pattern для KB:** `concepts/ui-translations-bulk-update-recipe.md` теперь имеет третий датапоинт (after RU mig 231 + glossary creation Phase 2). Recipe устаканился — стоит закодить концепт. **TODO для Phase 4 first session:** создать `concepts/ui-translations-bulk-update-recipe.md` с pattern: audit JSONB walk → snapshot → writer subagent → critic subagent → merge → manual scrub → per-key jsonb_set migration + embedded assertions → apply via psycopg2 → verify.

---

### Session (UX Phase 4 Wave A1 — DE done)

**Контекст:** Phase 3 (ES, mig 232) merged в 17.05. Стартую Phase 4 — по одной миграции на язык. Первый — **DE**.

**Pipeline идентичен ES Phase 3:**
1. Audit 26 highest-impact ключей для DE (тех же что и в ES для rollout uniformity). Output: `tools/de_pilot_audit_20260517.json`. 25/26 ключей (1 missing — тот же `reminder_meal_breakfast`).
2. **Writer subagent** (Sassy Sage DE, Berliner Kumpel tone) — 26 переписаны (включая fallback для missing). 11 instances `loggen → tracken`. Output: `tools/de_pilot_replacements_20260517.json`.
3. **Critic subagent** (28yo Berlin native): 17 approved + 9 fix_needed + 0 rejected. Writer rating B+/8.
4. **Merge + manual scan:** clean, no red flags.
5. **Migration 233** applied via psycopg2. Snapshot `backup_ui_translations_de_pilot_20260517`.
6. **PR [#84](https://github.com/sharkovvlad/noms-bot/pull/84)** open, CI ✅.

**Critic top-3 fixes:**
1. `errors.ai_failed[1,2]`: Präteritum `entzifferte` (звучит как Roman) → Präsens `entziffer's` (chat-natural); ES-калька `hat Rauch gemacht` → DE-idiom `ist heißgelaufen`.
2. `gamification.streak_30[1]`: ES-port `Waage ist nicht das Thermometer` (метафора не существует в DE) → `Waage misst nicht alles` (anti-shame сохранён, DE-idiomatic).
3. `cron_notifications.reminder_meal_lunch`: Writer заменил glossary-prescribed `Dein Move.` на generic `Auf geht's!` со слабым gender-аргументом. Critic вернул `Dein Move.` (Deadpool-punchy).

**Writer top-3 brilliant cases:**
1. `referral.present_friend[2]` — *«Schnapp's dir, bevor's kalt wird»* (gem-level urgency+warmth).
2. `gamification.streak_7[1]` — *«Stoffwechsel macht Oktoberfest — du bist der Headliner»* (biology-Schadenfreude + DE-cultural anchor без lifestyle-promotion).
3. `cron_notifications.comeback_2days[2]` — *«Pausen sind menschlich — ich bin KI, ich urteile nicht»* (Deadpool+anti-shame в одной строке).

**DE-specific lesson:** `Disziplin` — заряженное слово (red flag), но в **negation context** safe («keine Disziplin mehr, das ist Identität»). Critic не fledged как red line — glossary-conformant. Pattern: для DE при работе с potentially-loaded words проверяй *direction* (command vs negation) до flagging.

**Артефакты:**
- PR: [noms-bot#84](https://github.com/sharkovvlad/noms-bot/pull/84)
- Migration: `migrations/233_de_terminology_phase4_wave_a.sql`
- Snapshot: `public.backup_ui_translations_de_pilot_20260517`
- Workflow JSONs: `tools/de_pilot_{audit,replacements,critic,final}_*.json` (вне git)

**Phase 4 next sessions:** FR (Wave A1), IT (Wave A1), PT (Wave A1) — те же 26 ключей, аналогичный pipeline. Ключевые риски:
- **FR:** national-food sacralization (pain/fromage/vin) — sass смещён исключительно на биологию.
- **IT:** cucina italiana = sacra (pasta/pizza) — то же что FR.
- **PT:** LatAm BR-priority (не EU PT), national food protection (feijoada/pão de queijo), race-loaded idioms avoid.

---

### Session (UX Phase 4 Wave A1 — FR done)

**Контекст:** Phase 4 Wave A1 DE merged (mig 233). Тимлид предупредил что 234 reserved by parallel agent — FR takes **235**.

**Pipeline идентичен ES/DE:**
1. Audit 25/26 ключей (тот же `reminder_meal_breakfast` missing). Output: `tools/fr_pilot_audit_20260517.json`. Включает `es_reference_phase3` + `de_reference_phase4` как tone guides.
2. **Writer subagent** (Sassy Sage FR, Paris-Lyon urban casual): 26 переписаны, 11 `logguer/enregistrer→noter`. Output: `tools/fr_pilot_replacements_20260517.json`. **Note:** writer пропустил `current_fr` + `array_length` поля в output — main agent enriched из audit JSON через path-join (post-processing step).
3. **Critic subagent** (28yo Paris Belleville, native esprit): 17 approved + 9 fix_needed + 0 auto-rejected. Writer rating **4.1/5**.
4. **Merge + manual scan:** clean (no logguer/Vous/food-shaming).
5. **Migration 235** applied via psycopg2. Snapshot `backup_ui_translations_fr_pilot_20260517`.
6. **PR [#85](https://github.com/sharkovvlad/noms-bot/pull/85)** open.

**Critic top-3 fixes:**
1. `referral.present_friend[1]`: `{friend_name} est monté de Ligue` — writer **сам flag'нул gender bug в rationale но всё равно оставил**. Critic auto-fix → `vient de passer une Ligue` (COD-postposé, accord invariant). **Lesson:** writer ratings B+ может содержать self-acknowledged unfixed bugs — critic нужен для accountability.
2. `comeback_2days/7days`: sass-budget overload (one-hit wonders + remet un disque + off-radar) → `feu de paille` / `en mode fantôme` (Belleville-natural).
3. `cron_notifications.evening[1]`: ambigu pronoun `il` (métabolisme или dîner?) + `mode avion` (tech-jargon к биологии) → `bascule en veille` (registre unifié mécanique).

**Writer top-3 brilliant cases:**
1. `reminder_meal_lunch` — `«Le déj se note pas tout seul. À toi de jouer.»` (parfait elision + idiome chaleureux).
2. `gamification.streak_30` — `«c'est plus de la discipline, c'est de l'identité»` + `«compagnon métabolique»` (impersonnelle gender-neutral).
3. `free_tier.limit_reached` — café-benchmark `~3€` + `«à sec»` (anti-anglicisme sans austérité).

**FR-specific lessons:**
- **`logguer`/`enregistrer` обa false friends** — corporate vs DevOps tone. `noter` = canonical human-scale verb.
- **Food sacralization respected** auto-reject `<5` anti-shame = 0 violations. Sass directed only at biology (insuline, cortisol, ghreline, mitochondries).
- **Camembert/Brie acceptable** — writer заменил на Saint-Nectaire/Comté якобы из-за cliché-fatigue, critic verdict: «Camembert не в cliché-fatigue, swap acceptable но не essential». Pattern: writer over-engineered (foodie 7e snobbery), но critic подтвердил что обе AOP — zero shaming risk.
- **Writer output schema drift** — FR writer не вернул `current_fr` + `array_length` поля. Pattern для будущих языков: в writer prompt EXPLICITLY перечислить required JSON fields включая reference data.

**Артефакты:**
- PR: [noms-bot#85](https://github.com/sharkovvlad/noms-bot/pull/85)
- Migration: `migrations/235_fr_terminology_phase4_wave_a.sql` (skipped 234, занят parallel)
- Snapshot: `public.backup_ui_translations_fr_pilot_20260517`
- Workflow JSONs: `tools/fr_pilot_{audit,replacements,critic,final}_*.json` (вне git)

**Phase 4 progress so far:** ES (mig 232) ✅ merged + DE (mig 233) ✅ merged + FR (mig 235) ⏳ PR #85 open. Next: IT, PT.

---

## 17.05 — Agent `claude/stoic-nash-46a4f3` — mig 234: age safety guards (v6) + safety-guard-ux-pattern KB

### Что сделано

1. **mig 234 applied + PR [noms-bot#86](https://github.com/sharkovvlad/noms-bot/pull/86) открыт.**
   - Renumber история: 231 → 232 → 234 (231 ушёл в PR #82 RU терминология; 232/233 захвачены параллельными агентами по словам владельца; 235 потом ушёл в PR #85 FR терминология).
   - Atomic change vs v5/mig 230:
     - Step 2 (Age): `<18 + lose` → forced maintain + `age_warning='underage_forced_maintain'`; `<18 + gain/maintain` → `age_warning='underage_disclaimer'` (soft, цель сохранена); `>75` → `age_warning='elderly_less_accurate'`
     - Steps 6/8 читают `v_effective_goal_type = COALESCE(forced, original)`
     - Return JSON +3 поля в calculations: `age_warning`, `original_goal_type`, `effective_goal_type`
     - v3/v4/v5 ключи preserved 1:1
   - **DB migration HEAD на проде: 234.**

2. **6 sentinels через `SAVEPOINT sentinels` + ROLLBACK** (transactional, zero side-effects):
   - Все 6 PASS:
     - Underage-lose F/16 → forced_maintain, eff=maintain, 1876 kcal
     - Underage-gain M/15 → disclaimer (soft), eff=gain, 2555 kcal
     - Underage-maintain F/17 → disclaimer (soft), eff=maintain, 1543 kcal
     - Elderly M/78 lose → less_accurate, eff=lose (не форсится), 1854 kcal
     - Boundary-18 F/18 lose → NULL (граница не triggers), 1616 kcal
     - Boundary-75 M/75 maintain → NULL, 2358 kcal
   - **Pattern для будущих guards:** `SAVEPOINT name` + `INSERT temp users` + `SELECT calc_user_targets(...)` + `ROLLBACK TO SAVEPOINT` — чище чем DELETE и rollback'абельно при ошибке в середине прогона.

3. **Existing prod users:** 4 registered (возраст 30/36/42/63/64), все вне guard-зон, delta=0 во всех target_*. Snapshot `users_targets_backup_20260516_pre_v6` (34 rows, drop ≥2026-05-23).

4. **p95 latency:** 99.64 ms локально с Mac (persistent psycopg2, 25 runs). Дельта v5→v6 = 0 (один IF + 3 JSON поля). VPS baseline ≈43 ms (как mig 230 v5, без регрессии).

5. **KB `safety-guard-ux-pattern.md` создан** (по запросу владельца).
   - Argued override principle (не silent substitution): юзеру всегда объясняем что/почему/что делать/как опротестовать.
   - 5 UX touch points: Onboarding / Profile passive banner / first-trigger modal / retrofit cron / auto-reset.
   - 3 severity levels: hard block (pregnancy, BMI<14) / soft override (age lose, BMI<18.5 lose, RED-S) / informational (age disclaimer, elderly).
   - Decision tree для нового guard (7 вопросов) перед spawn'ом миграции.
   - Storage schema: `users.shown_guards JSONB`, `user_overrides`, `guard_audit_log` (для FTC legal traceability).
   - Translation keys naming convention: 5 keys × 13 langs = 65 per warning, через copywriter agent (Sassy Sage tone + Telegram SRE).
   - 9 пунктов architecture beyond SQL (translations + handlers + onboarding hooks + retrofit + auto-reset cron + live-test sentinel). **«Калькулятор без UI = мёртвый код»** — формулировка владельца, зафиксирована как anti-pattern.
   - Применяется к: age guards (mig 234), underweight lose (P0.5), BMI extremes (P0.4), pregnancy (P0.6), RED-S (P2.5), diet break (P2.6), min kcal floor (P0.3).
   - Запись в `knowledge/index.md` добавлена.

### Sanity check lesson (применил перед commit)

Worktree был создан ~неделю назад (когда HEAD main = 230). За это время main ушёл на 233. **`git diff origin/main..HEAD --stat` показал 1432 deletions(-) в 10 файлах**, включая чужие миграции (mig 232 ES, mig 233 DE) и test handlers — классический stale-worktree semantic rollback. Сработало правило 12.1 из global CLAUDE: `git rebase origin/main` ДО commit'а (не после). После rebase scope сжался до моего файла (+373 -0). Это lesson 2026-05-08 ещё раз доказала ценность — без sanity-check я бы merge'нул PR который откатил бы работу 3+ агентов за неделю. KB [[concepts/release-protocol]] актуален.

### Roadmap status (after mig 234)

P0.1 ✅ merged-ready (PR #86 ждёт review).
Next: P0.2 (mig 236) — formula switch Schofield/Molnar/Lührmann + speed clamp для >75. Owner предложил staged подход (safety baseline сначала, accuracy improvements потом) — confirmed.

### Артефакты

- PR: [noms-bot#86](https://github.com/sharkovvlad/noms-bot/pull/86)
- Migration: `migrations/234_calculate_user_targets_age_guards.sql` (+373 строк)
- Snapshot: `public.users_targets_backup_20260516_pre_v6` (34 rows, status='registered')
- KB: `concepts/safety-guard-ux-pattern.md` (новый)
- KB: `concepts/migration-collision-guard.md` (+1 строка edge case «unpushed parallel agents»)

---

### Session (No-Mana Python pre-check — Strangler Fig, mig 236)

**Контекст:** Тестовый юзер tid=786301802 (RU, registered, XP=175, mana_current=0) 2026-05-16 18:51 при voice сообщении видел в чате:
- `⚡ No mana left today` (английский литерал у русского юзера)
- кнопку `{icon_premium} Премиум` (нерендеренный плейсхолдер)

**Root cause (Phase 0 диагностика):**

1. **EN-литерал**: `gamification.no_mana` ОТСУТСТВОВАЛ во всех 13 языках (`SELECT content #>> '{gamification,no_mana}' FROM ui_translations` → NULL для всех). n8n `03_AI_Engine.Send No Mana` экспрешн `translations?.gamification?.no_mana || '⚡ No mana left today'` фолбэкал на хардкод. Изначальная мig 008 апплалась только на EN+RU, остальные языки никогда не получили ключ; либо был дроп когда-то.
2. **Литерал `{icon_premium}` на кнопке**: `payment.go_premium_button` во всех 13 языках содержит литерал-префикс `{icon_premium} Premium/Премиум/...`. n8n Telegram-нода НЕ интерполирует Python-style `{var}` плейсхолдеры — выводит as-is. Системный баг.
3. **journalctl 18:51:12** подтвердил: `target=ai reason=food_media → AUTHORITATIVE skip — fall through to legacy` — Python пропускал в n8n.

**Решение (Strangler Fig, подтверждено тимлидом + NLM):**

- **mig 236** `migrations/236_no_mana_python_precheck.sql` (+163 строк):
  - 13 UPDATE `ui_translations.gamification.no_mana` — Sassy переводы для ar/de/en/es/fa/fr/hi/id/it/pl/pt/ru/uk (mana-семантика, не «кредиты»).
  - INSERT `ui_screens` row `no_mana_exhausted` (render_strategy=`delete_and_send_new`, text_key=`gamification.no_mana`, input_type=`inline_kb`).
  - INSERT 2 кнопки: `buttons.recharge_mana` (текст уже содержит ⚡, `icon_const_key=NULL` — без двойной иконки) + `buttons.go_pro_unlimited` (`icon_const_key='icon_stars'` → ⭐). Паттерн mig 144 `shop_error_recharge_limit`. **НЕ используем** `payment.go_premium_button` — он содержит литерал `{icon_premium}` (фикс этого ключа — отдельная задача i18n агента).
- **Python hook в `webhook_server.py:_try_authoritative_path`** (+58 строк):
  - `_render_no_mana_envelope(ctx)` — вызывает `render_screen` RPC, оборачивает через `template_engine.render_envelope`.
  - Условие перехвата: `target=='ai' and reason=='food_media' and ctx.status=='registered' and ctx.mana_current<=0`. **Никаких новых DB-запросов** — `mana_current` уже в `v_user_context` cache.
- **n8n НЕ трогаем** (тимлид-правка): после Python-hook трафик в `Send No Mana` / `Send No Mana CTA` перестаёт приходить → dead-код удалим в Phase 6.4. Не риск PUT'а в эту сессию.

**Что Phase 0 SQL вскрыл (не было в плане):**
- `errors.no_credits` есть для всех 13 языков **как JSONB-массив variants** (Sassy mig 104). Это для unregistered ветки в n8n CTA — оставили как есть.
- `payment.go_premium_button` содержит литерал `{icon_premium}` во всех 13 (системный баг). Кнопка `buttons.go_pro_unlimited` (mig 144) — clean, без литерала.

**Verification:**
- `render_screen(786301802, 'no_mana_exhausted')` через psycopg2 на live прод:
  - text: `⚡ Мана на ноле, амиго. Восстанови или включи PRO — там безлимит.` (RU)
  - кнопки: `⚡ Пополнить ману` + `⭐ Включить PRO (Безлимит)` — оба эмодзи правильно подставлены, нет `{icon_*}` literals.
  - strategy: `delete_and_send_new` (удалит indicator-стикер mid=5393 + отправит ответ).
- p95 `render_screen` = **92.2ms с локального Mac** (на VPS Hetzner→Supabase EU будет ~50ms; baseline RTT 44ms + 3-6ms на render). Cold path — вызывается ТОЛЬКО при mana=0, заменяет тяжёлый n8n forward.
- Шифт сценарии вне scope (negative cases checked в плане PR):
  - mana>0 → Python skip → n8n как раньше.
  - unregistered + mana=0 → Python skip (visible_condition `status=registered`) → n8n CTA.

**Git дисциплина (CLAUDE.md §12):**
- Worktree был создан до merge PR #85/86. `git fetch origin main` + `git stash` + `git rebase origin/main` + `git stash pop` — чистый rebase без конфликтов (scope не пересекался с age guards / FR i18n).
- `git diff origin/main..HEAD --stat` перед push: **2 файла, +221/-0** — никакого семантического отката.
- PR создан: [noms-bot#87](https://github.com/sharkovvlad/noms-bot/pull/87).

**Артефакты:**
- PR: [noms-bot#87](https://github.com/sharkovvlad/noms-bot/pull/87)
- Migration: `migrations/236_no_mana_python_precheck.sql` (+163 строк, applied на prod)
- Code: `webhook_server.py:_render_no_mana_envelope` + hook в `_try_authoritative_path` (+58 строк)
- KB: новая статья `concepts/no-mana-python-precheck.md` (Strangler Fig pattern, см. ниже)
- Plan file: `/Users/vladislav/.claude/plans/temporal-dancing-galaxy.md`
- Branch: `claude/stoic-nash-46a4f3` (rebased on origin/main)

---

## 17.05 (late session) — Agent `claude/stoic-nash-46a4f3` — coordination protocol + safety-guard-ux v2

### Контекст

Параллельный диалог с агентом-нутрициологом по итогам apply mig 234 (P0.1 baseline done). Выровняли терминологию, разрешили противоречия, согласовали roadmap focus.

### Что зафиксировано

**1. Created `concepts/agent-collaboration-protocol.md`** — 10 правил координации между NOMS-агентами (mig-engineer, нутрициолог, копирайтер, native cultural reviewers). Living document.

Правила:
- **Rule 1: 5-tier severity vocabulary** — hard block / hard regulated / soft override / informational / silent accuracy. Замена 3-tier из v1 `safety-guard-ux-pattern.md`. Hard regulated = новая категория (BMI<18.5+lose, BMI>60+fast — требует врач + audit log, не self-consent).
- **Rule 2: DEFAULT-policy** — NULL для acute failure (pregnancy, BMI<14), typed default для gradual (diet_type='omnivore') + onboarding ask + HIGH retrofit cron.
- **Rule 3: Naming conventions** — JSON `<trigger>_warning`, translation `warning.<family>.<severity>.<surface>`, unified storage (`user_overrides` / `guard_audit_log` / `users.shown_guards JSONB` без per-field tables).
- **Rule 4: Scope ownership per зона** — клиническая (нутрициолог) / migration engineering (я) / UX-pattern (я + копирайтер) / translations (копирайтер) / cultural review L1+L2 (нутрициолог + per-region native).
- **Rule 5: KB ownership per-concept** — coord through commit message `coord with @<owner>`.
- **Rule 6: Conflict escalation** — dialog → 🚨 owner tag → KB fixation (3 levels).
- **Rule 7: Migration timing** — `gh pr list` + spawn check + rebase ПЕРЕД commit + sanity-check ПЕРЕД push + force-with-lease. Lesson 2026-05-08 + 2026-05-17 mig 234 averted -1432 deletions.
- **Rule 8: Silent accuracy ≠ guard** — формула меняется без override юзерского намерения = НЕ guard. Banner НЕТ. Только JSON telemetry (`bmr_formula='schofield_hw'`). Applies к: mig 236+ Schofield/Molnar/Lührmann, Katch-McArdle, ABW for obese.
- **Rule 9: Cultural review two-layer (L1+L2)** — L1 нутрициолог clinical + first-pass cultural (через `sassy-sage-multilingual-glossary`). L2 native per family (Romance/Germanic/Slavic/Arabic-Persian/HI/ID = 6 max). Tier'инг по severity: hard = mandatory L1+L2 for flagged, soft = mandatory L1 optional L2, informational = glossary self-screen, silent = N/A.
- **Rule 10: Retrofit cron priority + canary 10%** — HIGH priority для hard (first 7 days), adaptive throttle по size базы, translation-ready gate (cron.enabled только когда все 13 langs ready), canary 10% with churn-watch перед mass rollout.

**2. Updated `concepts/safety-guard-ux-pattern.md` v2:**
- Severity matrix: 5-tier (заменил 3-tier из v1).
- Trigger table re-classified: `age<18+lose` → hard block (не soft), `BMI<18.5+lose` → hard regulated. **Resolved contradiction** между Rule 1 row и Trigger Table в v1 — это была documentation bug, mig 234 уже implement'нула правильно (без opt-out для underage_forced_maintain).
- **§6 Auto-reset variants** — добавлена концепция tier transitions (full release / tier softening / tier escalation), не только full release. Юзер 14 → 15 на gain: softening (force maintain → disclaimer). BMI 13 → 16: hard block → hard regulated. Pregnancy detected: escalation up.
- **§7 Translation pipeline** — добавлена L1/L2 spec из Rule 9. Anti-pattern: literal translation. Glossary references per language family.
- §10 Reusability table re-classified all guards по 5-tier matrix (15 guards entries including silent accuracy для formula switches).

**3. Created `handover/2026-05-17_mig234_copywriter_brief.md`** — полный brief для копирайтера на 156 translation entries (3 warnings × 4 surfaces × 13 langs). Включает:
- RU baseline drafts для 3 warnings (tone reference, не финал)
- Sassy Sage tone + Telegram SRE constraints (≤35 chars/line, ≤12 lines, ≤18 chars/button)
- Cultural adaptation per family: Romance/Germanic/Slavic/Arabic-Persian/HI/ID gotchas (false friends, gender политика, RTL/ZWNJ, anti-russianism для UK)
- L1 cultural flag schema (`cultural-clean` / `cultural-flag-<region>-<topic>`)
- Severity rule: `underage_forced_maintain` (hard block) → mandatory L1+L2 для AR/FA/HI/ID; informational → glossary self-screen достаточно
- Output format: `tools/mig234_warnings_translations_<DATE>.json` (cultural flags) + `tools/mig234_warnings_apply.sql` (INSERT-script)
- Anti-pattern список (Google Translate / patronizing / shaming red lines)

Не spawn'аю копирайтера сам — параллельные агенты `proud-tickling-willow.md` уже имеют writer/critic pipeline (RU терминология). Brief в handover/ — для маршрутизации через owner или dedicated copywriter session.

### Agent dialog highlights

Нутрициолог принял 4/4 моих financial-iteration предложений (5-tier severity, L1/L2 tier'инг, mono-mig P0.3+P0.4, role-merge L1 = нутрициолог clinical). Один counter: L1+L2 не только нутрициолог, но **двухслойная модель** где L1 = я (нутрициолог) helps clinical+first-pass cultural, L2 = native только для flagged keys. Принял — это снижает agent fan-out 13→6 при сохранении культурной чувствительности.

### Sprint sequencing approved (нутрициолог)

```
NOW (параллельно):
├─ Copywriter agent spawn (mig 234 = 156 keys, brief в handover/)
├─ Mig-engineer: Python handler updates (Profile/day_summary читают age_warning)
└─ Mig-engineer или другой engineer: storage migration N+1
   (`users.shown_guards JSONB` + unified `user_overrides` + `guard_audit_log`)

NEXT (sequential, после P0.6 UX от owner):
├─ Mono-mig P0.3+P0.4 (min kcal floor + BMI-aware guards) — единая mig
│  `<NNN>_safety_baseline_guards.sql` с per-guard feature flags
└─ P0.6 pregnancy mig (после владельца UX-wireframe)

PARALLEL (no contention):
└─ Updates KB roadmap (нутрициолог primary) с P0 narrowing
```

### Открытые вопросы для следующих сессий

1. **Spawn копирайтера** — через `proud-tickling-willow.md` pipeline (writer/critic) или dedicated session с моим brief'ом? Decision на owner'е.
2. **Mono-mig number** — после mig 234 (merged) + 235 (FR i18n merged) + 236 (no-mana applied на проде через PR #87) — следующий свободный 237+ нужно verify перед apply.
3. **Storage migration timing** — apply до/после translation insert? Translation INSERT может идти в той же mig что и `users.shown_guards JSONB` setup — обсудить с нутрициологом.

### Артефакты

- KB new: `concepts/agent-collaboration-protocol.md` (10 правил)
- KB updated: `concepts/safety-guard-ux-pattern.md` v2 (5-tier, contradiction fixed, auto-reset variants, L1/L2)
- KB updated: `knowledge/index.md` (+1 row, updated safety-guard-ux entry)
- Handover: `handover/2026-05-17_mig234_copywriter_brief.md` (156 keys brief)
