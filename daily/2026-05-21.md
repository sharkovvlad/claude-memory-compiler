# 2026-05-21

## n8n cleanup: DELETE deactivated workflows (02_Onboarding_v3 / 02.1_Location / 10_Payment)

**Цель:** убрать 3 деактивированных workflow'а из n8n SQLite + зачистить dangling executeWorkflow refs в active workflows. Долг из CLAUDE.md «Технический долг» секции.

### Что сделал

**Phase 0 (pre-flight):**
- Snapshot всех 9 workflows на VPS → `/tmp/n8n_snapshots/`. Архивные snapshots целевых трёх скопированы в `n8n_workflows/02_Onboarding_v3.json`, `n8n_workflows/02.1_Location.json`, `n8n_workflows/10_Payment_final_pre_delete.json` (последний — отдельным именем потому что `10_Payment.json` уже был от более ранней версии).
- Подтвердил `active=false` для всех трёх через API GET.
- Grep всех 6 active workflows на target IDs выявил 7 executeWorkflow nodes требующих cleanup:
  - `04_Menu_v3` (0xJXA5M4wQUSiGXT): `Go to 02_Onboarding_v3`, `Go to 02.1_Location`, `Go to 10_Payment`
  - `01_Dispatcher` (7jVRdAvVlzOqIMEi): `02_Onboarding`, `Go to 05_Location`
  - `04_Menu` (JQsipPWxijse3F0b): `Go to 05`, `Go to 10_Payment`
- `app_constants` чистая — никаких rows со ссылками на target IDs (только флаги `handler_*_use_python` уже всё `true`).
- Python code refs только в `tools/parity_check.py` и `tests/tools/test_parity_check.py` — это «historical» mapping для diagnostic парити-чекера, не runtime. Не трогаю.

**Phase 1 (cleanup refs):**
- Python script `/tmp/clean_n8n_refs.py` — generic remover executeWorkflow nodes по списку target IDs. Корректно чистит outgoing connections от removed nodes + incoming edges других нод к ним. Whitelist payload `{name, nodes, connections, settings}` + settings whitelist.
- Validated: node counts `04_Menu_v3` 23→20, `01_Dispatcher` 54→52, `04_Menu` 130→128. Нет dangling edges. JSON валидный.
- scp + `curl --data @file` PUT для каждого (size 21K / 82K / 178K). Все три HTTP 200, `active: true` сохранён.
- GET-verify: target IDs в active workflows = NONE.

**Phase 2 (DELETE):**
- `T9753zO3ZyiYsgkp` (`10_Payment`) — HTTP 200 сразу.
- `wzjYmMOurCbp4czk` и `7EqiiwUwlGs7dcHT` — HTTP 500. Docker logs показали `SQLITE_CONSTRAINT: FOREIGN KEY constraint failed`.
- Виновник найден: `workflow_published_version` в n8n 2.17.7 имеет dual FK — один CASCADE, один **`ON DELETE RESTRICT`**. У onboarding/location были live rows там; у 10_Payment не было (видимо разная история publish).
- Backup БД (`/home/noms/n8n/data/database.sqlite.bak.20260521_162350`), затем single-TX DELETE из `workflow_published_version` + `workflow_publish_history` для обоих ID (host-side sqlite3, WAL mode, без stop'а контейнера). После — повторный API DELETE → HTTP 200 для обоих.

**Phase 3 (verify):**
- GET всех 3 deleted IDs → HTTP 404 ✅.
- Список workflows на VPS: 7 штук (6 active + 1 inactive `08.3_Friends` — не моё, оставил).
- `https://nomsbot.com/health` → HTTP 200 `{"status":"ok"}`.
- `journalctl -u noms-webhooks --since "5 min ago" | grep -iE "ERROR|Traceback"` → пусто.
- `docker logs --since 5m noms-n8n | grep -iE "error|constraint|fail"` → пусто.

**Phase 4 (KB):**
- `knowledge/concepts/architecture-registry.md` обновил: header date 2026-05-21, три DELETED строки в таблицах workflows, граф вызовов очищен, секция «inactive sub-workflow всё равно работает» переписана (safety net больше нет для этих трёх).
- `knowledge/concepts/n8n-data-flow-patterns.md` дописал секцию «Lesson 2026-05-21: DELETE workflow blocked by `workflow_published_version` FK (RESTRICT)» — symptom, root cause, cleanup recipe.

### Что НЕ удалил (out of scope)

- `tools/parity_check.py` references — это historical mapping, оставлено как есть.
- `dispatcher_python_authoritative_admin_only` row из `app_constants` — отдельная миграция, не блокирует cleanup workflows.
- `08.3_Friends` (inactive, su5JZbUXOgE614Lo) — не было в задаче.

### Файлы изменены

- `n8n_workflows/02_Onboarding_v3.json` (new, snapshot pre-delete)
- `n8n_workflows/02.1_Location.json` (new, snapshot pre-delete)
- `n8n_workflows/10_Payment_final_pre_delete.json` (new, snapshot pre-delete)
- KB: `architecture-registry.md`, `n8n-data-flow-patterns.md`

### State n8n после

| Active (6) | Inactive (1) |
|---|---|
| 01_Dispatcher, 03_AI_Engine, 04_Menu, 04_Menu_v3, 04.2_Edit_StatsDaily, 06_Indicator_Clear | 08.3_Friends |

Backup: `/home/noms/n8n/data/database.sqlite.bak.20260521_162350` (132 MB, держать ~7 дней).
