# Daily Log: 2026-05-29

## Sessions

## Memory Maintenance

### Session (11:19)

**Контекст:** Закрытие сессии по PR #205 (4 бага subscription UX) + аудит Track A Payment P1

**Ключевые обсуждения:**
- Пользователь спросил про реальный статус Track A — Payment P1 items (Dunning, Trial, Stars i18n, localised dates, Stars Subscriptions)
- Обсуждение стратегии закрытия сессии: handover сейчас vs ждать тестов пользователя — решили handover сразу (контекст перегружен)

**Принятые решения:**
- **Handover сейчас, не ждать тестов** — контекст тяжёлый (90+ tasks), риск reset'а. Пользователь тестит асинхронно, баги передаст следующему агенту
- **Track A приоритеты:** n8n cleanup (15 мин) → crypto/promo dates (30 мин) → Dunning UX (1-2 дня) → TON UX → Stars Subscriptions → Trial auto-convert (требует product decision)

**Выводы:**
- **A.1 Dunning UX — НЕ реализовано.** `cron_check_subscription_expiry` обрабатывает только уже истёкшие. Нет напоминаний за 7/3/1 день до expiry, нет translation keys
- **A.2 `/start payment_success`** — закрыто через HTML заглушку `nomsbot.com/payment-success` (mig 313), deep-link больше не приходит
- **A.3 Trial 7d** — план есть в БД, но auto-convert cron НЕ реализован, Stripe path блокирует trial
- **A.4 Stars success i18n** — ✅ полностью (mig 290/306/307)
- **A.5 Localised dates** — частично: subscription ✓, crypto path — статичный текст без даты, promo — пустой
- **A.6 Stars Subscriptions** — external (BotFather config), не код

**Задачи:**
- [ ] Пользователь проводит тесты A/B/C из PR #205 (RU текст, повторная отмена, reply-kb при удалении)
- [ ] P1.3: n8n cleanup — удалить nodes 121-124 в `04_Menu` (Safe PUT)
- [ ] P1.2: Аудит crypto/promo dates + 1 mig если нужно
- [ ] P1.1: Dunning UX — mig + cron + 13 langs (7/3/1 день до expiry)
- [ ] P2.1: Trial auto-convert — нужно product решение от владельца (charge? fall to free? upsell?)
- [ ] Handover записан: `claude-memory-compiler/handover/2026-05-26_payment_p1_and_uvelovka22_close.md`


## Session — Nutritionist Agent 10 (вечер, 19:30-21:00 МСК)

**Контекст:** Подхват после Nutritionist 9 handover (2026-05-28). Track A — payment отдан отдельному агенту. Track B (Stage 7c cleanup) + Track C (Phase-aware Sage / BMI warnings / cycle UAT) — мои. План сессии: 1→2→3→4 (n8n pre-check → deactivate → Python cleanup → MEMORY update).

**Главная находка (P0):** **Stage 7 (Python AI Engine) — НЕ global, admin-only canary** с 21.05. MEMORY и handover Nutritionist 9 утверждали «GLOBAL CUTOVER 25.05 / all 1000+ users». Реальность: `ai_engine_beta_testers=[417002669]` (только owner). За 24ч 75% recognition еды идёт через n8n `03_AI_Engine`. Stage 7c **нельзя** делать сейчас — сломаются 97 активных юзеров.

**Доказательства собраны через 4 независимых источника:**
1. `app_constants.ai_engine_beta_testers = [417002669]` — admin only
2. webhook_server.py:1626 gate явно проверяет `_user_in_ai_beta_allowlist`
3. SQLite n8n execution_entity: `03_AI_Engine`=13 executions/2 дня (last 16:26 МСК сегодня), `06_Indicator_Clear`=4 (last 16:24)
4. food_logs split 24h: others=9, owner=3 (75% non-owner через n8n)
5. Git commit 203b798 «force dynamic Sage for n8n users» — author знал что non-admin идут через n8n

**Дополнительные находки (handover-неточности):**
- BMI/min_kcal warnings — **уже сделаны** через mig 270 (351 строка × 13 langs) + `safety_guards.py` + RPC `get_unshown_guards`/`mark_guards_shown`. Не дублировать.
- Maternal sub-menu labels — **уже сделаны** через mig 269+286+361.
- Migration HEAD = 371 (не 360, не 365 — за 2 дня +6 миграций ушло в main параллельными агентами).
- Active users last 7d = 97 (92 new + 5 registered), не 1000+ как MEMORY говорила.
- Phase-aware Sage — НЕ «easy win» как handover утверждал. В `services/sage.py:685` явный safety review block: «cycle_phase NOT included (РПП risk)». Реализация без owner override нарушит safety policy.

**Что сделано в сессии:**
- ✅ Цель 1 (pre-check) — выявила блокер, отчёт собран
- ⛔ Цели 2 + 3 (deactivate + Python cleanup) — BLOCKED корректно, ничего не тронуто в проде
- ✅ Цель 4 (MEMORY + daily + handover) — обновил `MEMORY.md` (HEAD 360→371, Stage 7 reality, BMI/maternal closed, новые pointers); создал handover-стаб `2026-05-29_stage7_status_reality_check.md` с 3 опциями для owner-а (promote canary to global / keep hybrid / defer).

**Принятые решения:**
- Adaptive TDEE и Phase-aware Sage отложены до отдельной сессии с design doc (owner approval)
- Stage 7c остаётся открытым tech debt с явным условием «после global cutover decision»

**Что нужно от owner-а:**
- Решение по Stage 7 (см. handover 2026-05-29_stage7_status_reality_check.md — 3 опции)
- Решение по большим фичам: design doc сессия для Adaptive TDEE отдельно

**Метаданные:**
- Ни одной миграции этой сессией не создано (BLOCKED + только документация)
- Ни одного PR не открыто
- Никаких изменений в `app_constants`, `users`, n8n workflows, или прод-коде
- Только обновления документации: MEMORY.md (project memory), 2 daily entries, 1 handover


---

### Session (17:45) — cron-fix cycle close + P0 + P1

**Контекст:** Сессия 28-29.05 закрытие 3 owner-topics (⚠️ БЖУ / backups / cron audit) + 3 UAT bugs (Issue 1/2/3) + P0 regression от собственного mig 366 + P1 architect-approved reply-kb reattach.

**Migrations applied LIVE:**
- **mig 366** (28.05): F1 sleep/stress inline-keyboard + F2 zen-filter + F3 activity mutex 30min. PR #219.
- **mig 367** (29.05): DROP 3 food_log backup tables + CI guard `pr-backup-table-guard.yml`. PR #220.
- **mig 368** (28.05): `get_daily_stats_rpc` exposes `*_status_token`+`local_hour`; Python `_maybe_suppress_macro_warn_under_cutoff` в `handlers/menu_v3.py` (presentation-time gate в Python, не SQL — защита от cache-idempotency). app_constants `macro_warn_hour_cutoff=15`. PR #221.
- **mig 369** (29.05): `INTERVAL '30 minutes'` → `app_constants.reminder_activity_mute_minutes` tunable. PR #222.
- **mig 370** (29.05): `reminder_sleep_checkin`/`reminder_stress_checkin` scalar → 3-variant JSONB array × 13 langs. Python `crons/reminders.py:_load_translations` теперь `dict[str, list[str]]` с scalar wrap для backward-compat. PR #223.
- **mig 371** (29.05): INSERT missing `buttons.share_location` × 13 langs (mig 210 забыла, был English fallback). PR #224.
- **mig 372** (29.05) — **🚨 P0 от собственного mig 366**: после ответа на cron-pushed sleep/stress reminder — `process_user_input` искал button по `screen_id = users.nav_stack[top]`, button defined только на `sleep_checkin`/`stress_checkin` screen → lookup fail → save skip → toast skip → `daily_modifiers` пустой → F3 mutex от mig 366 НИКОГДА не срабатывал → reminder daily 27-29.05 без записи ответов. Fix: INSERT 6 button rows в `_global_floating_actions` virtual screen (mig 211 pattern, same meta). PR #226.
- **mig 374** (29.05) — **architect-approved P1 reply-kb reattach**: после save_via_callback с meta-flag `reattach_main_kb_after_save:true` — `process_user_input` SET `users.pending_kb_reattach=TRUE`, `render_screen` consume + flush в single render. 4-part architecture (ALTER column + 12 meta updates + PUI patch + render_screen patch). Trade-off (owner-accepted): «мерцание» reply-kb принимается за гарантию возврата в главное меню. PR #228.

**Python-only fixes (no mig):**
- **PR #225 (28.05)**: `services/sage.py:_build_my_day_prompt` добавлена META warning «It is EVENING in user's local time (>=16:00)» если `local_hour >= 16`. Lesson: LLM weighs end-of-context tokens сильнее середины, even data fields like `User Local Time: 18:00` могут быть проигнорированы если rule в средине prompt'а слабее.

**KB updates (claude-memory-compiler/knowledge/):**
- **NEW** `concepts/cron-reminder-suppression-tunables.md` — pattern: timing/threshold values (mute-windows, hour cutoffs) живут в `app_constants` (category=cron/ux), не хардкодом в RPC body. Discoverability + hot-reload tuning + audit trail через NLM.
- **NEW** `concepts/cron-pushed-callback-fallback-pattern.md` — P0 lesson: cron-pushed inline button MUST быть covered одним из 3 safety mechanisms (dispatcher whitelist > PUI fast-path > `_global_floating_actions`). Decision flow checklist для future agents. Audit subagent (29.05) подтвердил: остальные cron-pushed callbacks (cmd_quests, cmd_progress, cmd_premium_plans, cmd_edit_last, cmd_delete_last) уже safe — никаких новых gaps.
- **Extended** `concepts/sassy-sage-dialog-variants.md` — секция «Update 2026-05-29»: cron_notifications.reminder_* variants теперь применяются и в Python pipeline (`crons/reminders.py`). Pattern: PostgREST `->` (не `->>`) для native JSONB type, scalar wrapping в 1-elem list для uniform handling, per-user `random.choice(variants)` — каждый юзер видит свой вариант в один tick.
- `knowledge/index.md` updated: 3 entries в Cron-секцию + Start-here lookup table.

**E2E verifications LIVE:**
- mig 366 zen-filter: `cron_get_reminder_candidates('sleep_checkin')` smoke OK
- mig 367 backup drop: post-drop 0 backup-pattern tables в `public`
- mig 368: payload contains `p_status_token=under, local_hour=17` для admin
- mig 369: 0 hardcoded INTERVAL, 4 dynamic, hot-reload tested (UPDATE→45→smoke→revert→30)
- mig 370: 13/13 langs × 2 keys → array(3); rotation 10 picks dist 6/1/3
- mig 371: 13/13 langs с 📍 prefix
- mig 372: `dispatch_with_render(admin, 'cmd_stress_none')` → toast `✅ Учтено` + `daily_modifiers` written + nav `stats_main`
- mig 374: TEST 1 cmd_stress_moderate → `reply_keyboard PRESENT` + carrier `messages.saved` + flag flushed; TEST 2 immediate cmd_show_meals → `reply_keyboard NULL` (single-shot confirmed, не infinite re-render)

**Open для следующего агента:**
- **Issue 2 (Pattern A approved, owner-deferred):** Onboarding city resolver — 3-tier (location share + AI text parse via GPT-4o-mini + manual list) + Profile menu sync. Infrastructure ready (`services/sage.py:_get_openai_client`, `services/geolocation.py:reverse_geocode`, router intercept pattern `_matches_manual_list_button`, `handlers/location.py` Canonical Hybrid). KB изучить [[canonical-hybrid-location-picker]] + [[onboarding-v3-map]] + [[nav-stack-architecture]] перед implementation. Effort: ~1-2 часа.

**Принятые решения:**
- 3-variant rotation для sleep/stress (а не scalar) — owner-approved через approval question
- Owner-deferred Issue 2 для thoughtful design session (manual list vs AI vs geolocation интеграция)
- mig 372 instead of touching `crons/reminders.py` save_bot_message TODO — minimal scope, focused на root cause
- Python META warning vs DB prompt edit для Sage time-awareness — smaller blast radius, easier revert

**Метаданные:**
- 9 PR'ов (#219, #220, #221, #222, #223, #224, #225, #226, #228), все merged
- 8 миграций (366-372 + 374; 373 от другого агента — Stage 7 cleanup)
- 2 новых KB concept + 1 extension + index update
- Python touched: `crons/reminders.py` (mig 366 helper + mig 370 array support), `handlers/menu_v3.py` (mig 368 macro-warn-suppress), `services/sage.py` (PR #225 META warning)
