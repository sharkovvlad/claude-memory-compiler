# Daily Log: 2026-04-10

## Sessions (migrated from CLAUDE.md Changelog)

### Session: 2026-04-10 — Migration 055: Personalized Macro Split
- Что:
  1. **7 новых колонок в `users`**: `training_type`, `phenotype`, `goal_speed`, `target_weight_kg`, `target_protein_g`, `target_fat_g`, `target_carbs_g`, `phenotype_answers`
  2. **25 новых app_constants**: protein_g_per_kg_* (4), fat_pct_* (4), goal_speed_* (6), phenotype_* (3), icons (6), fail-safes (2)
  3. **`calculate_user_targets` v3**: макро по training_type (г/кг × target_weight), LBM Proxy по phenotype, goal_speed (slow/normal/fast)
  4. **`set_user_training_type`** (новый RPC): для онбординга и профиля
  5. **`set_user_goal_speed`** (новый RPC): для настройки скорости из профиля
  6. **`set_user_activity` v2**: sedentary → auto-set training_type + skip training step
  7. **`v_user_context`** — добавлены training_type, phenotype, goal_speed, target_*_g, target_weight_kg
  8. **`get_day_summary` v3** — возвращает target_protein_g/fat_g/carbs_g
  9. **Build Stats Text v4** (n8n) — макро показываются как X/Yг с персональными целями из БД
  10. **Backfill**: training_type из activity_level для existing users
- Зачем: Все пользователи видели одинаковые макро-статусы (30/25/45). Теперь: макро зависят от training_type (strength: 2.0 г/кг белка, cardio: 1.4, mixed: 1.6, sedentary: 1.2) и phenotype (LBM proxy). Goal speed настраиваемая (slow/normal/fast).
- Применено: ✅ n8n temp Postgres workflow + ✅ n8n API (PUT 04_Menu)
- Верификация: calculate_user_targets(417002669) → protein=120g (1.2g/kg × 100), fat=80g (30%), carbs=300g ✅; get_day_summary → targets present ✅
- Specs: `.claude/specs/phenotype_quiz_spec.md` (Phase 2), `.claude/specs/adaptive_modifiers_spec.md` (Phase 3)

### Session: 2026-04-10 — Payment UX v2: Tier-first + Method Selection + Fixed Regional Pricing
- Что:
  1. **Migration 051** — SQL RPCs: `get_pricing_region()` переписан с JSON-based на `ref_countries.pricing_tier` (раньше возвращал DEFAULT для всех); `get_user_price()` переписан на `subscription_prices` таблицу; НОВЫЙ `get_all_plan_prices(telegram_id)` — возвращает 3 плана с ценами в валюте юзера + Stars + USDT за 1 вызов
  2. **ui_translations** — `{icon_card}` → `{{icon_card}}`, `{icon_crypto}` → `{{icon_crypto}}`, `{icon_stars}` → `{{icon_stars}}` в payment строках (Template Engine обрабатывает только `{{}}`); добавлены `payment.select_method`, `payment.back_to_plans`, `payment.plan_name_monthly/quarterly/yearly` для 13 языков; `shop.buy_freeze/buy_mana` теперь с `{{icon_*}}` и описанием ("❄️ Заморозка — {cost}" вместо "Купить — {cost}")
  3. **01_Dispatcher** — Route Classifier: `cmd_select_plan_*` и `cmd_pay_crypto_*` → menu route
  4. **04_Menu** — Command Classifier: `cmd_select_plan_*` и `cmd_pay_crypto_*` → payment route
  5. **10_Payment** — полная переработка UX:
     - Экран 1 "Планы": 3 кнопки выбора плана с ценой в валюте юзера (`cmd_select_plan_monthly/quarterly/yearly`) + Промокод + Назад
     - Экран 2 "Способ оплаты": 💳 Карта (URL → Stripe с правильным plan_id), ⭐ Stars, 💎 USDT + Назад к планам
     - Убраны 3 параллельных Get Price HTTP ноды (race condition), заменены на 1 RPC `get_all_plan_prices`
     - Payment Router: +2 выхода (`select_plan`, `crypto_plan`)
     - Новые ноды: Get All Prices, Get Plan Price, Build Method Selection, Show Methods
     - Build Crypto Instructions: теперь plan-specific (`cmd_pay_crypto_quarterly` → $8 USDT)
  6. **08.4_Shop** — `icon_mana` fallback `⚡` → `''` (DB имеет 🧪); убран двойной icon prefix в кнопках (перевод уже содержит `{{icon_*}}`)
- Зачем: Планы показывали 7 кнопок вперемешку, RPC `get_user_price` возвращал NULL для всех юзеров, кнопки Card и Crypto показывали `{icon_card}` литерал, Card был захардкожен на `plan=monthly`. Теперь: tier-first UX, карта в приоритете, цены в валюте юзера (EUR для ES, RUB для RU), Stars/USDT как альтернативы.
- Применено: ✅ n8n temp Postgres workflow (3 функции) + ✅ Supabase REST API (переводы) + ✅ n8n API (PUT 4 workflows)
- Верификация: 24/24 OK

### Session: 2026-04-10 — Migration 052: food_logs performance indexes
- Что: Добавлены 3 индекса на `food_logs`: `idx_food_logs_user_consumed` (telegram_id, consumed_at DESC), `idx_food_logs_user_created` (telegram_id, created_at DESC), `idx_food_logs_meal_id` (meal_id). Итого 7 индексов (4 уже существовали).
- Зачем: `get_day_summary` делал seq scan по всей таблице (500+ строк, растёт). `WHERE telegram_id=X AND consumed_at >= today` без индекса = O(n) вместо O(log n).
- Применено: ✅ n8n temp Postgres workflow (pooler 6543) + верификация через pg_indexes (7 rows)
- Файл: `migrations/052_food_logs_indexes.sql`

### Session: 2026-04-10 — «Мой день» v3: UX-полировка + правила индикаторов
- Что:
  1. **app_constants** — добавлены 4 ключа: `macro_threshold_ok_pct=30`, `macro_threshold_warn_pct=85`, `macro_threshold_over_pct=110`, `stats_bar_blocks=10`. Теперь все пороги макро-статусов в БД, не в коде.
  2. **ui_translations** — `report.streak_label` обновлён для 13 языков: ru="Дней в ударе", en="Day streak", de="Tage am Stück" и т.д. Термин "Стрик" заменён на понятный для всех возрастов.
  3. **n8n 04_Menu — Build Stats Text v3.1**:
     - `macroStatus()` — вариант D: < 30% = нет иконки, 30–85% = ✅, 85–110% = ⚠️, > 110% = 🔴. Убрана `📉` (непонятная семантика утром). Пороги из `app_constants`.
     - Fallback `streak_label`: 'Стрик' → 'Streak' (английский fallback)
  4. **n8n 04_Menu — Typing Action (Stats)** — новый HTTP Request нод между Menu Router[stats] и RPC Get Stats. Fire-and-forget `sendChatAction('typing')`. Пользователь видит "печатает..." сразу при нажатии "☀️ Мой день".
  5. **CLAUDE.md** — новые правила UX: §8 (Индикатор при каждом нажатии), §9 (Стратегии снижения задержек).
  6. **Bugfix: is_registered** — статус `edit_activity` не входил в список registered → бот показывал CTA вместо кнопок [Исправить/Удалить]. Исправлено: whitelist ONBOARDING_STATUSES вместо whitelist registered. DB: сброс `status='edit_activity'` → `'registered'` для telegram_id=417002669.
- Зачем: «Мой день» показывал 📉 утром (0% = ложная тревога), стрик непонятен новым пользователям, задержка до ответа без feedback = ощущение "завис". Пороги макро в коде = нарушение правила БД-источника правды.
- Применено: ✅ Supabase REST (app_constants + ui_translations × 13) + ✅ n8n API (PUT sxzbSc61YuHFbV4i + 03_AI_Engine)

### Session: 2026-04-10 — Migration 052: food_logs performance indexes
- Что: 3 новых индекса на `food_logs`: `idx_food_logs_user_consumed (telegram_id, consumed_at DESC)`, `idx_food_logs_user_created (telegram_id, created_at DESC)`, `idx_food_logs_meal_id (meal_id)`. Итого 7 индексов на таблице.
- Зачем: get_day_summary и edit workflows делали full scan. Индексы подготовлены под sargable запросы.
- Применено: ✅ n8n temp Postgres workflow (pooler 6543) → удалён после применения

### Session: 2026-04-10 — Migration 053: Fix get_day_summary index sargability
- Что: `get_day_summary` переписан — `DATE(created_at AT TIME ZONE 'UTC') = CURRENT_DATE` (non-sargable) заменён на `consumed_at >= v_day_start AND consumed_at < v_day_end` (sargable range). Границы дня вычисляются один раз в DECLARE. Используется `consumed_at` для food_logs (→ `idx_food_logs_user_consumed`) и `created_at` для xp_events (→ `idx_xp_events_user_date`, уже существовал).
- Зачем: Несмотря на наличие индексов, PostgreSQL не мог их использовать из-за функции DATE() над колонкой → full scan при каждом нажатии "☀️ Мой день". Теперь все 4 запроса внутри функции используют индексы.
- Применено: ✅ n8n temp Postgres workflow (credential Postgres_pooler_6543, ID e9Rza51wAsB6UmU3) → workflow yHrYcJnktHBWlrg7 удалён после применения
- Верификация: `get_day_summary(417002669)` → success=true, meals_count=1, xp_today=25, streak_current=5 ✅

### Session: 2026-04-10 — UX: Typing indicators + answerCallbackQuery + Voice transcription fix
- Что:
  1. **n8n 04_Menu** — добавлены `Typing Action (Profile)` и `Typing Action (Progress)` как параллельные ветки от Menu Router[2] и Menu Router[12]. Fire-and-forget `sendChatAction(typing)`.
  2. **n8n 04_Menu** — добавлена `Answer Callback Query` как параллельная ветка от Command Classifier[0]. Fire-and-forget `answerCallbackQuery` для всех inline-callback нажатий — убирает спиннер с кнопки мгновенно.
  3. **n8n 03_AI_Engine** — исправлен `Parse AI`: для голосовых сообщений `raw_user_input` теперь берётся из `$('Transcribe').item.json.text` (Whisper), а не из пустого `message.text`. Исправлен `RPC Log Meal`: параметр `p_raw_user_input` тоже читает из Transcribe для voice.
- Зачем: Profile/Progress зависали без фидбека. Inline-кнопки "крутились" 3 сек без answerCallbackQuery. Голосовые логи навсегда теряли транскрипцию (515+ старых записей без raw_user_input).
- Применено: ✅ n8n API (PUT sxzbSc61YuHFbV4i, 24ZOwWEmdGOYS2EH)

### Session: 2026-04-10 — Персонализация cron-уведомлений: {name} + новые ключи переводов
- Что:
  1. **ui_translations** — добавлены 6 новых ключей в секцию `cron_notifications` для 13 языков: `league_promoted`, `league_demoted`, `league_result` (плейсхолдеры: `{name}`, `{league}`, `{coins}`, `{rank}`), `streak_milestone`, `streak_frozen`, `streak_broken` (`{name}`, `{streak}`, `{coins}`, `{remaining}`). До этого cron-скрипты использовали хардкоженные английские строки.
  2. **`crons/league_cycle.py`** — добавлен хелпер `_load_user_names()`; `.replace("{name}", ...)` для promote/demote/result уведомлений.
  3. **`crons/streak_checker.py`** — добавлен хелпер `_load_user_names()`; `.replace("{name}", ...)` для milestone/frozen/broken уведомлений; milestone-запрос расширен полями `first_name`, `username`.
- Зачем: cron-уведомления ("ты повышен в лиге", "стрик сломан") были безличными. Теперь: "Влад, твой стрик 50 дней — легенда!".
- Пропущено: `onboarding.welcome` (имени ещё нет), `msg_welcome_back` (через n8n Template Engine, не Python).
- Применено: ✅ Supabase REST API (ui_translations) + ✅ Python crons (league_cycle.py, streak_checker.py)

### Session: 2026-04-10 — «Мой день» v3.2: UX-полировка + правила индикаторов
- Что:
  1. **app_constants** — добавлены 4 ключа: `macro_threshold_ok_pct=30`, `macro_threshold_warn_pct=85`, `macro_threshold_over_pct=110`, `stats_bar_blocks=10`. Теперь все пороги макро-статусов в БД, не в коде.
  2. **ui_translations** — `report.streak_label` обновлён для 13 языков: ru="Дней в ударе", en="Day streak", de="Tage am Stück" и т.д. Термин "Стрик" заменён на понятный для всех возрастов.
  3. **ui_translations** — добавлены `report.xp_today_label` (ru="за сегодня", en="today" и т.д.) и `report.mana_label` (ru="Мана", en="Mana" и т.д.) для 13 языков.
  4. **n8n 04_Menu — Build Stats Text v3.2**:
     - Заголовок на 1 строку: `📊 Мой день  ▫️ 2026-04-10 🕐 15:50`
     - `macroStatus()` — вариант D: < 30% = нет иконки, 30–85% = ✅, 85–110% = ⚠️, > 110% = 🔴. Убрана `📉`. Пороги из `app_constants`.
     - XP footer: `🌟 +25 XP за сегодня` (локализованный `xp_today_label`)
     - Мана для free-пользователей: `🧪 Мана: 1/2`. Для premium — скрыта.
     - Fallback `streak_label`: 'Стрик' → 'Streak' (английский fallback)
  5. **n8n 04_Menu — Typing Action (Stats)** — новый HTTP Request нод между Menu Router[stats] и RPC Get Stats. Fire-and-forget `sendChatAction('typing')`.
  6. **CLAUDE.md** — новые правила UX: §8 (Индикатор при каждом нажатии), §9 (Стратегии снижения задержек).
- Зачем: Заголовок занимал 2 строки зря; «Мой день» показывал 📉 утром (0% = ложная тревога); стрик непонятен новым пользователям; XP без контекста "за сегодня" непонятен; free-юзеры не видели сколько логов осталось.
- Применено: ✅ Supabase REST (app_constants + ui_translations × 13) + ✅ n8n API (PUT sxzbSc61YuHFbV4i)

### Session: 2026-04-10 — Shop UX: мана скрыта для premium + fix пустых inputs
- Что:
  1. **04_Menu — Go to 08.4_Shop**: заполнены `workflowInputs` (были пустыми `{}`). Теперь передаются: `telegram_id`, `language_code`, `callback_message_id`, `command`, `translations`, `constants`, `subscription_status`, `nomscoins`, `mana_current`, `mana_max`, `streak_freezes`. До этого Shop всегда получал дефолты (subscription_status='free', nomscoins=0).
  2. **08.4_Shop — Build Shop Text**: мана-блок (текст + кнопка `cmd_buy_mana`) скрыт для premium-пользователей. Premium имеет mana_max=500 — recharge не нужен.
  3. **Bug fix (прошлая сессия)**: `Extract Plan → Get Plan Price` dangling connection в 10_Payment. Починено в прошлой сессии, но changelog не был записан.
- Зачем: Premium юзеры видели бесполезный блок "Пополнить ману" со статусом "Mana: 500/500". UX-мусор. Пустые inputs = Shop никогда не получал реальный subscription_status → всегда казался free.
- Применено: ✅ n8n API (PUT /workflows sxzbSc61YuHFbV4i, tQz8iUsEc7YRBLh0) HTTP 200
- Верификация: `Go to 08.4_Shop` inputs → 11 полей ✅; `isPremiumUser` проверки × 5 в Build Shop Text ✅

### Session: 2026-04-10 — Smoke Test: Payment Card bug fixes
- Что:
  1. **10_Payment — Build Method Selection**: Stripe URL исправлен с `https://89.167.86.20:8443` на `http://89.167.86.20:8443` — сервер запущен без SSL (HTTP), HTTPS давал TLS-ошибку
  2. **webhook_server.py — create-checkout**: endpoint теперь возвращает `RedirectResponse(302)` вместо `{"url": "..."}` JSON — браузер/WebView автоматически редиректится на Stripe checkout
- Зачем: Кнопка "Card" не открывала Stripe — браузер видел TLS ошибку (https → HTTP сервер), а если бы не было ошибки — показывал бы сырой JSON
- Применено: ✅ n8n API (PUT xYKmBTWI4c0VoSqs) + ✅ rsync + systemctl restart noms-webhooks
- Статус Card: Редирект работает, но Stripe не настроен (STRIPE_SECRET_KEY пуст) → до добавления ключей будет 500. Stars и USDT работают без Stripe.
- Для активации Card: добавить STRIPE_SECRET_KEY и STRIPE_WEBHOOK_SECRET в /home/taskbot/noms/.env на VPS

### Session: 2026-04-10 — Performance: Ускорение reply-кнопок (Phase 1-3)
- Что:
  1. **Migration 054** — `app_constants_cache`: кэш-таблица (1 строка JSONB) + триггер `trg_refresh_constants_cache` (AFTER INSERT/UPDATE/DELETE на app_constants). `v_user_context` пересоздан с `CROSS JOIN app_constants_cache` вместо `(SELECT jsonb_object_agg(...))` subquery. Экономия ~30-50ms на каждый запрос.
  2. **Template Engine v2.0** (01_Dispatcher) — добавлен Phase 3: `cleanText()` — очистка `\\n → \n` в переводах выполняется один раз в Dispatcher, а не 5 раз в каждом sub-workflow.
  3. **cleanText() удалён** из Merge Data в 5 workflows: 04_Menu, 08_Progress, 08.1_Quests, 08.3_Friends, 08.4_Shop.
  4. **08_Progress инлайнирован в 04_Menu** — 5 нод (RPC Progress Insight, Build Progress Text, Has Callback, Edit/Send Progress) перенесены прямо в 04_Menu. Нода "Go to 08_Progress" (Execute Workflow) удалена. Убран overhead Execute Workflow hop (~30-50ms).
- Зачем: Reply-кнопки (Profile, Stats, Progress) имели задержку ~400-800ms. Основные bottleneck: `jsonb_object_agg` full scan на 60+ строк при каждом запросе; дублирование cleanText в 5 workflows; лишний Execute Workflow hop для Progress.
- Применено: ✅ Migration 054 (n8n temp workflow → Postgres pooler) + ✅ n8n API (PUT 6 workflows: Dispatcher, 04_Menu, 08_Progress, 08.1_Quests, 08.3_Friends, 08.4_Shop)
- Верификация: v_user_context → 185 constants keys ✅; Simulated webhook Profile/Stats/Progress → all success ✅; 08_Progress больше не вызывается как отдельный workflow ✅
- Оценочная экономия: ~65-110ms на каждое нажатие reply-кнопки

### Session: 2026-04-10 — Performance: Inline 08.1_Quests, 08.2_League, 08.3_Friends, 08.4_Shop + Typing Actions
- Что:
  1. **08.1_Quests, 08.2_League, 08.3_Friends, 08.4_Shop инлайнированы в 04_Menu** — 21 нода из 4 sub-workflows перенесена напрямую в 04_Menu. Execute Workflow ноды (Go to 07_Quests, Go to 09_League, Go to 08.4_Shop, Go to 08.3_Friends) удалены.
  2. **7 Typing Action нод добавлены** как параллельные ветки от Menu Router: Stats, Profile, Progress, Quests, League, Shop, Friends. Fire-and-forget `sendChatAction(typing)` — пользователь видит "печатает..." при каждом нажатии кнопки.
  3. **Итоговый размер 04_Menu**: 56 → 80 нод. Все 4 sub-workflow остаются активными (для backward compatibility).
- Зачем: Каждый Execute Workflow hop = ~30-50ms overhead. 4 ноды × 40ms = до 160ms экономии. Typing Actions устраняют ощущение "зависания" при нажатии кнопок.
- Применено: ✅ n8n API (PUT sxzbSc61YuHFbV4i) — 80 нод, active=true
- Верификация: Все 4 цепочки (Quests/League/Friends/Shop) проверены через структурный анализ; Typing Action ноды для 7 маршрутов подтверждены в live workflow ✅
- Оценочная дополнительная экономия: ~120-200ms суммарно (за все маршруты)

### Session: 2026-04-10 — Bug Fix: Shop mana block + Profile Premium button + Payment HTML

#### Shop mana block (08.4_Shop, tQz8iUsEc7YRBLh0)
- Что изменено: Добавлена нода "Get User Status" (HTTP Request GET Supabase users) в начало 08.4_Shop. Merge Data теперь предпочитает значения из БД над workflowInputs. Build Shop Text скрывает мана-блок (текст + кнопка cmd_buy_mana) для premium-пользователей.
- Зачем: n8n PUT API не сохраняет workflowInputs.value для executeWorkflow нод — subscription_status всегда был 'free'. Самодостаточный GET решает проблему навсегда.
- Применено: ✅ n8n API HTTP 200

#### Profile Premium button (04_Menu, sxzbSc61YuHFbV4i)
- Что изменено: Build Profile Text — `callback_data: isPro ? 'cmd_noop' : 'cmd_premium_plans'` → `callback_data: 'cmd_premium_plans'` для обоих состояний.
- Зачем: Premium-пользователи при нажатии "Premium ✅" видели "Скоро!" (cmd_noop → coming_soon). Теперь оба — free и premium — попадают на экран Plans.
- Применено: ✅ n8n API HTTP 200

#### Payment parse_mode + Plans UX (10_Payment, xYKmBTWI4c0VoSqs)
- Что изменено:
  1. Show Methods: parse_mode Markdown → HTML; fullResponse: false → true (диагностика ошибок Telegram API)
  2. Show Plans: parse_mode Markdown → HTML
  3. Build Method Selection: `*title*` → `<b>title</b>`
  4. Build Plans Text: `*title*` → `<b>title</b>`; иконки тарифов ✨(monthly) 🔥(quarterly) ⭐(yearly) в кнопках
- Зачем: Markdown V1 + emoji в тексте → silent fail Telegram API. Кнопки выбора метода оплаты не появлялись. Иконки тарифов улучшают UX.
- Применено: ✅ n8n API HTTP 200

