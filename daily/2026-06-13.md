# 2026-06-13 — Sage ToV PR-1: voice cards + late_night_close META

## TL;DR

[PR #392](https://github.com/sharkovvlad/noms-bot/pull/392) **OPEN** (создан вечером 06-12, числится по дате создания как 06-13 в этом дневнике из-за полуночи):
- 8 voice cards в оба system prompt'a Sage (food_log A/B/C + my_day D/E/F/G/H)
- Новая `_late_night_close_meta` функция — owner-mandated h≥22 cutoff
- Migration 503 — флаг `sage_late_night_close_enabled` в `app_constants`
- 17 новых unit-тестов на late_night branching (HARD/SOFT) + structure snapshot
- 211 sage tests green; pre-existing non-sage failures (например carbs int/float в `test_food_log_ai_input`) — без изменений vs main, flagged отдельным таском

## Контекст — почему был нужен этот PR

После 06-12 утреннего ToV review:
- `emotion=smirk` 90.5% (181/200 за 7 дней) — monoculture полная
- «как насчёт» в 31% реакций, «куриного филе» — 31%, «шпинат/брокколи» — 22%, food emoji — 72%
- 6 META (rule7, variation_guard, ban_list, cold_start, budget_directive, time_meta_warning) не сдвинули главную метрику «≤25% реакций с предписанием еды» с 92 до 91% за месяц cycle'ов

**Диагноз:** системные промпты оба перегружены **negative space** (FORBIDDEN-секции, «не делай X», «никогда Y»). Positive space (как голос Номса звучит) — одна строка про 70/30 bro/scientist + 5 примеров только в RULE 7b. Models cargo-cult'ят дефолтный шаблон gpt-4o-mini, а не character spec.

## Что сделано

### 1. Voice cards в оба системных промпта

Между `YOUR CHARACTER` и `GENERATION RULES` — секция `VOICE EXAMPLES (CRITICAL — this is the tone-of-voice anchor; ADAPT to context, do NOT copy verbatim)`. Cards написаны на русском (single-source per mig 412), output language = параметр `Response language`.

**food_log промпт (Cards A–C):**
- Card A — closed budget close после плотного дня (emotion=smirk):
  > Свиная рулька, пирог, грибы — день получился плотным и со своим почерком. Завтра стрелка выровняется сама, без догрузок.
- Card B — Rule 7b balanced meal (emotion=smirk):
  > Яйца и руккола. Завтрак инженера: всё на месте, ничего лишнего. Серия идёт, день стартует ровно.
- Card C — open budget, протеин залогирован, varied push (emotion=smirk):
  > Творог в деле. К вечеру день просит хруста — огурец или болгарский, что-то с водой и текстурой. 🥒

**my_day промпт (Cards D–H):**
- Card D — quiet_steady, нет push (emotion=sage):
  > Цифры держатся, серия дней в ударе идёт своим темпом. День закрывается ровно — без советов и без лишних движений.
- Card E — cold_start (emotion=sage):
  > День только разворачивается. Куда поведёт — посмотрим на обеде, торопиться с подсказками рано.
- Card F — target_met_fat_heavy (emotion=smirk):
  > Оладьи, сырники, паста — вечер с характером. По жирам ушли в плюс, но это сегодня — завтра возвращаемся к лёгкости.
- Card G — severe_deficit / under_target_evening (emotion=sage):
  > Логов сегодня немного. День у всех разный — если что-то осталось, отметишь в спокойной обстановке. Без догрузок и команд.
- Card H — fasting_logged (emotion=sage):
  > Голодание идёт своим темпом. Когда выйдешь — лёгкая еда без рывков, тело само скажет когда и сколько.

### 2. Late-night close META

Owner-mandated cutoff `local_hour >= 22`. Нутрициологическое обоснование — **Sutton 2018, Chow 2020, NHANES Sun 2021**: energy intake ≥22:00 ассоциируется с худшим HbA1c/BMI; meals <2h до сна → fragmented sleep + GERD risk. Для среднего bed-time 23:30-00:30 — h≥22 правильный default cutoff. Мы **не** говорим «не ешь» (РПП-safety), мы перестаём **пушить**.

```python
def _late_night_close_meta(local_hour, *, day_status, protein_pct, kcal_pct):
    if local_hour < 22:
        return None
    soft_eligible = (
        day_status in ("severe_deficit", "under_target_evening")
        and protein_pct_f < 60.0 and kcal_pct_f < 80.0
    )
    if soft_eligible:
        return (SOFT_TEXT, "late_night_soft")   # «пара ложек творога» допустимо
    return (HARD_TEXT, "late_night_close")       # next_meal_suggested=false принудительно
```

Гейт за `app_constants.sage_late_night_close_enabled` (default `true`). food_log surface не имеет `day_status` → всегда HARD CLOSE. my_day имеет → SOFT branch доступен.

### 3. Migration 503

```sql
INSERT INTO public.app_constants (key, value, category, description) VALUES
    ('sage_late_night_close_enabled', 'true', 'sage', '...')
ON CONFLICT (key) DO NOTHING;
```

Idempotent. Applied к live DB перед merge — флаг exists at 'true', код пока не читает (до deploy). После merge → GitHub Actions deploy → код активирует. Rollback — `UPDATE app_constants SET value='false' WHERE key='sage_late_night_close_enabled'`.

## Owner-фичи цикла

### «У тебя информация с полей» — поднял голос на эталоны Номс-агента

Owner делегировал калибровку через отдельного Номс-агента (LLM character). Тот предложил 4 эталона; я флагнул 4 проблемы:

1. **D-revised: «метаболическому навигатору тут ловить нечего»** — самореференс character'a, breakage 4-й стены. Убрал.
2. **C-revised: «Творог зафиксирован» + «без лишних калорий»** — DB-операционная формулировка + anti-shame trigger («лишних» = подразумевает overdone). Заменил на «Творог в деле» + убрал «без лишних калорий».
3. **I3-revised: «метаболическая система уходит на ночной покой»** — псевдонаучный термин (такой системы в физиологии не выделяют). Убрал биохим, оставил «ни белка, ни клетчатки сегодня уже не понадобится».
4. **I-SOFT-revised: «поддержит мышцы и ЦНС»** — functional medical claim на пищевой продукт (FORBIDDEN-секция явно запрещает). Заменил на «снимет вопрос».

Также — эмоции: предложение Номса было D=proud / I-SOFT=smirk; я предложил D=sage / I-SOFT=sage (proud в quiet_steady дне без achievement = «МОЛОДЕЦ!»-синдром; ирония в late+severe_deficit = неуместна). Owner подтвердил мой выбор.

### Архитектурное недоразумение — i18n миграции

Owner упомянул «миграции для 13 языков». Пояснил: voice cards живут в **Python system prompt**, не в `ui_translations`. Это **single-source per mig 412** — output language задаётся параметром `Response language`. Никаких per-language миграций для voice cards не потребовалось и не должно. Только одна `app_constants` строка для флага.

### Auto-mode на сессию

Owner: «делай всё последовательно, не спрашивай лишнего». Применил после трёх стратегических решений (cards, эмоции, late-night cutoff). Калибровка остальных 5 cards под D-utверждённый регистр прошла без дополнительных вопросов.

## Out of scope (PR-1.5 после замера)

- **Компакт DAY STATUS HINTS / FORBIDDEN секций** — ~600 токенов в my_day, ~300 в food_log. Замеряется отдельно после PR-1, чтобы понять чистый вклад voice cards в metrics.
- **Explicit deficit/overshoot markers в payload** — предложение Номс-агента. Отложено по той же причине.

## Watch-items после deploy

| Метрика | Текущая | Цель | SQL |
|---|---|---|---|
| `emotion=smirk` доля | 90.5% (06-12) | ≤50% | `SELECT emotion, count(*) FROM ai_coach_logs WHERE context_type LIKE 'sage_%' AND created_at > now()-interval '48h' GROUP BY 1` |
| «как насчёт» rate | 31% | ≤10% | `SELECT count(*) FILTER (WHERE ai_message ~* 'как насч[её]т') * 100.0 / count(*) FROM ai_coach_logs WHERE context_type LIKE 'sage_%' AND created_at > now()-interval '48h'` |
| `late_night_close` daily fires | unknown | 1-3/day на active user после 22 | `SELECT day_context->'metas_fired', count(*) FROM ai_coach_logs WHERE ... AND day_context->'metas_fired' ? 'late_night_close'` |
| `late_night_soft` rate | unknown | редко (<10% от late_night) | то же с `? 'late_night_soft'` |
| Штамп protein-mentions/реакция | 0.64 | ≤0.5 | `SELECT avg(...regex match per row)` |

Замер через 24-48ч (META activation) + 3-4 дня (тон-эффекты, `/sage-tov` rerun).

## Pre-existing test failures (не моё)

- `tests/handlers/test_food_log_ai_input.py::test_text_input_happy_path` — carbs `20` (int) vs `20.0` (float) assertion. Подтверждено git stash + rerun на main. Spawned task_id=task_1623c580 ([chip created](#)).
- 38 integration / mig тестов в `tests/integration/test_phase3*`, `test_v12_personas`, `test_mig313`, `test_mig358` — все pre-existing failures на main. Не sage-related.

## KB extensions

- Extend [[concepts/sage-payload-meta-override-pattern]] — новые секции «Voice cards as primary tone anchor (PR #392)» + «late_night_close META — owner-mandated h≥22 cutoff» (см. ниже).
- Запись в MEMORY.md «Recently shipped» (если PR merged).

## Lessons (для KB compiler'a)

1. **Few-shot voice cards beats N+1-я META** для tone control. После 6 МETA-итераций emotion=smirk вырос (80→100%) — кризис «curse of instructions». Эталоны в начале промпта снимают это быстрее чем правила в конце.
2. **functional-claim filter** — новое правило для Sage tone review: любое «X поддержит Y» / «X улучшает Z» — псевдомедицинский claim, нарушает FORBIDDEN secrtion. Я флагнул на эталоне Номс-агента «творог поддержит мышцы и ЦНС».
3. **«лишних калорий» = anti-shame trigger** — слово «лишний» в FORBIDDEN-секции уже фактически было («перебор», «too much fat»), но не явно перечислено. Эталон Номс-агента включал это — поправил.
4. **psевдонаучные «системы»** — «метаболическая система» не существует как термин. Sage scientist part обязан использовать настоящие термины (метаболизм, обмен веществ) или не использовать никакие.
5. **i18n misconception** — voice cards в system prompt НЕ требуют per-language миграций. Single-source pattern (mig 412) — output language как payload параметр. Каждый раз при tone-of-voice работе нужно явно пояснять owner'у.

---

## Вечер 06-13 — maternal-exclusion soft-landing для cmd_stress_* (PR [#393](https://github.com/sharkovvlad/noms-bot/pull/393))

**Контекст.** Owner поднял задачу из той же reminder-инвентаризации, что породила сёстры [#381](https://github.com/sharkovvlad/noms-bot/pull/381) (waist_retrofit) и [#382](https://github.com/sharkovvlad/noms-bot/pull/382) (day_close): беременная/кормящая юзер тапает кнопку из 18:00 stress_checkin push — `apply_daily_modifier` корректно отвечает `{suppressed:true, reason:'maternal_exclusion', show_supportive_banner:true, banner_i18n_key:'modifier.maternal_protective.stress'}`, но бот молчит (cmd_stress_high) или показывает лживый «✅ Учтено!» toast (cmd_stress_none/moderate).

### Scope corrected (важная находка)

Brief упоминал sleep + stress + luteal. Live проверка `apply_daily_modifier(tid, 'sleep', 'short')` для maternal + не-Premium → `reason='premium_required'`. Для maternal + Premium → `applied=true`. Sleep **никогда** не возвращает `maternal_exclusion` — gated в [mig 474 line 900](https://github.com/sharkovvlad/noms-bot/blob/main/migrations/474_protein_calibration_gain_floor_sleep.sql): `IF p_modifier_type IN ('luteal','stress') AND ...` с явным комментарием `Isolation: sleep NOT in this condition`. Luteal cron filters maternal users на push-уровне (`cron_get_reminder_candidates`). Net scope = **только 3 stress callback'а**.

### Half-wired feature (с mig 317)

KB [[concepts/adaptive-modifiers-architecture]] §«Maternal supportive banner» (2026-05-24) описывала: «вместо `бот молчит` — supportive banner» с `banner_i18n_key='modifier.maternal_protective.<type>'`. SQL контракт работал с этого момента; Python НЕ читал banner_i18n_key — половинно-внедрённая фича пролежала 20 дней. Аналогичный кейс mig 374 reply-kb re-attach — нужно проверять и SQL-сигналы, и Python-consumer'ы как одну поверхность при code-review (см. §Lessons).

### Mig 504 (LIVE — applied via psycopg2 BEGIN/COMMIT)

1. **i18n refresh** `modifier.maternal_protective.stress` в 13 langs. RU/EN дословно от owner; 11 langs inline по той же 5-элементной структуре (данные записаны · фаза названа · safety rule · sign-off · 🤍). Gender-neutral через passive/imperative; AR/FA — 2nd-person feminine (аудитория = pregnancy/lactation). Старый generic текст («Stress is extra tough, your body is amazing») за 20 дней **0 раз показан юзеру** — Python не читал ключ. Нулевой регрессионный риск.
2. **process_user_input patch** — surgical 8 строк после `callback_alert_text` emit. Когда save_via_callback вернул suppressed+maternal_exclusion → `v_telegram_ui || {'maternal_soft_landing_key': banner_i18n_key}`. Stale-base guard: брал `pg_get_functiondef('public.process_user_input')` с live (sha1=bf4bd2934c97, 29947 chars), не из git.

### Python ([handlers/menu_v3.py](handlers/menu_v3.py))

- `_maybe_build_maternal_soft_landing_item(ctx, telegram_ui)` — helper. i18n lookup через `_lookup_translation` + EN fallback (defence — частичный 13-lang update не должен ронять envelope).
- `_envelope_from_rpc_result` appends item в КОНЕЦ envelope (после main render + reply_kb_refresh). Покрывает headless cmd_stress_none/moderate.
- `_handle_stress_high` — `elif save_result.get("suppressed") and save_result.get("reason")=="maternal_exclusion"` после существующего `if save_result.get("show_modal")`. show_modal=FALSE для maternal gate ([mig 474 line 925-928](https://github.com/sharkovvlad/noms-bot/blob/main/migrations/474_protein_calibration_gain_floor_sleep.sql)) — старая ветка не fires. Clinical RPP/teen modal по-прежнему приоритет (не double-fire на teen pregnant юзере).

### Тесты (5 новых)

- `test_maternal_soft_landing_appended_for_headless_save` — telegram_ui carries key → envelope contains 🤍 item.
- `test_no_soft_landing_when_signal_absent` — без signal noop.
- `test_soft_landing_falls_back_to_en_when_translation_missing` — Hindi user без `modifier` ns → EN fallback.
- `test_stress_high_maternal_soft_landing` — Python intercept path.
- `test_stress_high_clinical_modal_takes_priority` — show_modal=TRUE не triggers maternal elif.

56/56 menu_v3 unit tests pass. Pre-existing flake `test_waist_save_toast_shows_recalc` (Supabase pooler DNS с Mac) — подтверждён через git stash на baseline.

### End-to-end live verify

Через psycopg2 BEGIN/ROLLBACK: установил `is_pregnant=TRUE` для tid=1631091395, pushed nav stack→stress_checkin, вызвал `process_user_input` с callback `cmd_stress_none` → response.telegram_ui.maternal_soft_landing_key = `modifier.maternal_protective.stress`, response.screen_id = `stats_main` (navigation сохранён), `callback_alert_text=✅ Logged` остался (owner может решить позже — наш scope ADD не REMOVE). i18n resolve → owner-approved EN text с 🤍.

### Durable уроки

1. **Half-wired features живут долго.** SQL emit + Python consume — две поверхности одного контракта. Code review должен проверять обе при добавлении нового signal. Mig 317 эмитил `banner_i18n_key` — никто не проверил что Python его читает; пролежало 20 дней. Чёткий чек-лист в KB [[concepts/adaptive-modifiers-architecture]]: при добавлении новых signal-полей в RPC return — gгepнуть Python codebase на это поле и при отсутствии consumer'а оставить либо TODO либо инлайн-комментарий с эпохой.

2. **Brief vs implementation — verify по primary source, не по brief'у.** User'ский brief упоминал sleep + stress + luteal. Я мог бы ринуться писать sleep handler без проверки. `pg_get_functiondef` показал isolation comment и я скорректировал scope. **Live RPC — авторитет, brief — гипотеза.**

3. **`pg_get_functiondef`-base для CREATE OR REPLACE** — stale-base guard (KB [[concepts/pre-migration-discovery-recipe]]) подтвержден ещё раз. mig 497 в git был current; mig 504 build pull'нул live и patched in-memory — никакого drift'а с прошлыми deploys.

4. **Migration number race в multi-agent окне.** За 1 день параллельные агенты перебрали 500/501/502/503 (PR #388-#392). Я делал sleep+stress задачу в worktree от 06-12, finished 06-13 — между моими `git fetch` ВНУТРИ сессии номер раздвинулся на 4 единицы. Pre-commit hook поймал бы коллизию, но проверка перед началом работы дешевле. **При выходе из паузы → git fetch + ls migrations + gh pr list ДО первого CREATE OR REPLACE.**

### KB updates

- KB [[concepts/adaptive-modifiers-architecture]] §«Maternal supportive banner» — снимаю «UI hook» статус (теперь живой end-to-end). Добавляю §«Wiring history» с эпохой mig 317 emit → 20 дней stale → mig 504 plumb.

### Open для следующего

1. После merge — мониторинг 24-48ч на maternal stress reactions. Спав-таск может посмотреть `journalctl | grep maternal_soft_landing` для частоты.
2. Owner может решить дополнительно — suppress лживый `callback_alert_text=✅ Logged` для maternal-suppressed случая. Сейчас остался (scope этого PR — ADD, не REMOVE). Доп. SQL в той же `process_user_input` секции: `IF maternal_exclusion → v_callback_alert_text := NULL` (1 строка).
3. Possibly apply ту же pattern для других RPC что эмитят суetти-сигналы — grep по `show_supportive_banner` / `show_modal` в migrations/ показал что они purely-stress; `luteal` для maternal не эмитит (gated на cron-level).
