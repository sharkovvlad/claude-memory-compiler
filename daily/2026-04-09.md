# Daily Log: 2026-04-09

## Sessions (migrated from CLAUDE.md Changelog)

### Session: 2026-04-09 — Migration 043 + n8n: sync_user_profile + get_display_name
- Что: RPC `sync_user_profile(telegram_id, first_name, username)` — обновляет имя при изменении + `last_active_at` при стейлинге >5мин; helper `get_display_name(telegram_id)` — first_name → username → 'User'
- Зачем: username никогда не сохранялся; first_name не обновлялся после создания аккаунта; last_active_at обновлялся только при лог еды (inactivity reminder был сломан)
- Применено: ✅ Supabase SQL (psycopg2) + ✅ n8n Dispatcher: Auto Create User +username field, новая нода Sync Profile (parallel)

### Session: 2026-04-09 — Migration 044: display_name + {name} + language normalization
- Что:
  1. `v_user_context` — добавлена колонка `display_name` (first_name → username → 'User')
  2. `cron_get_reminder_candidates` — добавлен `display_name` в JSONB каждого кандидата + фильтр `is_bot=false`
  3. `users.language_code` — одноразовый UPDATE нормализации (`pt-BR` → `pt`, `zh-CN` → `en`)
  4. `ui_translations` — `{name}` добавлен в `reminder_streak_warning` и `reminder_inactivity` для всех 13 языков
- Зачем: персонализация напоминаний ("Vlad, your streak is at risk!"); фикс бага с региональными кодами языка (pt-BR не находил переводы → бот молчал)
- Применено: ✅ Supabase SQL (psycopg2)
- n8n: ✅ Template Engine v1.2 — добавлена Phase 2 замены `{name}`; Auto Create User — нормализация language_code при создании
- Python: ✅ `crons/reminders.py` — `text.replace("{name}", ...)` + deploy + restart noms-cron

### Session: 2026-04-09 — Migration 047: DB Cleanup — dead columns + broken RLS
- Что:
  1. Удалены мёртвые столбцы из `users`: `credits` (deprecated mig 025), `auth_user_id` (NULL у всех), `edit_attempts` (NULL у всех)
  2. Удалены мёртвые столбцы из `food_logs`: `tokens_used`, `health_score` (оба NULL во всех записях)
  3. Удалены 3 нерабочие RLS-политики "Owner Access" (users, food_logs, daily_metrics) — зависели от `auth_user_id` который всегда NULL, бот использует service_role
- Зачем: Чистка схемы после аудита NLM. Убраны артефакты нереализованных фич и сломанные RLS
- Применено: ✅ Supabase SQL (psycopg2)
- НЕ удалено (проверено pre-flight): `ai_response_payload` уже удалён в mig 039; индексы food_logs на месте (4 штуки); is_bot фильтр в cron_regenerate_mana уже есть

### Session: 2026-04-09 — Migration 046: League XP Reset + Mana Guard + Icons + Bot Aggression
- Что:
  1. **league_xp_weekly пересчитан** с ALL-TIME XP (3765) на CURRENT WEEK only (625). `cron_process_league_week` никогда не запускался → XP накопился с февраля. Одноразовый UPDATE из `xp_events`
  2. **`recharge_mana_with_coins`** — добавлен guard `MANA_ALREADY_FULL`: если `mana_current >= mana_max` → возврат ошибки без списания coins. Перевод `error_mana_full` уже был в ui_translations
  3. **`{icon_premium}` → `{{icon_premium}}`** — single-brace плейсхолдер не обрабатывался Template Engine Phase 1. UPDATE в ui_translations для 13 языков (payment.plans_title + payment.go_premium_button)
  4. **NPC боты усилены** — элитные: Kenji Y 75→92, Aisha N 68→85, Sofia D 58→68, Mia R 52→62, Marco V 50→58. Добавлены 2 ультра-бота: Leo M (95 XP/день), Nina S (88 XP/день)
  5. **Markdown escape** — n8n 08.2_League Build Leaderboard: добавлена функция `escMd()` для экранирования `_*[]` в именах. Имена ботов: `_` заменено на пробел (`Aisha_N` → `Aisha N`)
- Зачем: XP в лиге нереалистичный (15x отрыв); мана обнулялась при ошибочном recharge (300 coins за 2 маны); кнопка "Премиум" показывала `{icon_premium}` литерал; боты неконкурентоспособные; Markdown в именах ломал Telegram
- Применено: ✅ Supabase SQL (psycopg2) + ✅ n8n API (08.2_League Markdown escape)

### Session: 2026-04-09 — Bug Fix: League Button + XP Standings
- Что:
  1. **n8n 04_Menu** — нода `Go to 09_League` (584935ff): добавлены workflowInputs (`telegram_id`, `language_code`, `callback_message_id`, `translations`). До этого inputs были пустыми → 08.2_League не получала данные → кнопка молчала
  2. **Migration 045** — `get_league_standings` v3: для реальных пользователей `lm.xp_earned` → `u.league_xp_weekly`. Одноразовый UPDATE синхронизировал `xp_earned = league_xp_weekly` для текущей группы
- Зачем: Лига не открывалась (пустые inputs = undefined telegram_id). XP показывал 0 т.к. `cron_create_league_groups` не инициализировал `xp_earned` из существующего `league_xp_weekly`
- Применено: ✅ n8n API (PUT /workflows sxzbSc61YuHFbV4i) + ✅ Supabase SQL (psycopg2)
- Верификация: `get_league_standings(417002669)` → rank 1, xp=3765 ✅

### Session: 2026-04-09 — n8n Mixed-trigger nodes: IF + editMessageText/sendMessage split
- Что:
  1. **04_Menu** — Send Stats Response, Send Profile Response, Send Recharge Result: добавлены IF-ноды `Is Callback? (Stats/Profile/Recharge)` → true: HTTP Request `editMessageText` / false: Telegram `sendMessage` (оригинальная нода)
  2. **Build Stats Text, Build Profile Text, Build Recharge Result** — добавлены `reply_markup` (JSON string) + `callback_message_id` в output
  3. **Prepare for Location Edit** (04_Menu) — добавлен `callback_message_id`
  4. **Prepare Promo** (10_Payment) — добавлен `callback_message_id`
- Зачем: Правило из CLAUDE.md "Смешанные триггеры": если нода вызывается и от reply-кнопки и от inline callback → условный роутинг. Без этого при inline-навигации создавались дубли сообщений.
- Применено: ✅ n8n API (PUT /workflows sxzbSc61YuHFbV4i, xYKmBTWI4c0VoSqs)

### Session: 2026-04-09 — Payment UX/UI Phase 4: Stars + Stripe + TON/USDT
- Что:
  1. **01_Dispatcher** — Telegram Trigger: добавлен `successful_payment` в update_types (был только `pre_checkout_query`). Stars payments теперь полностью обрабатываются.
  2. **04_Menu — Build Profile Text** — добавлен блок статуса подписки (Plan: Free/Trial/Premium ✅) + последний ряд клавиатуры: [🌐 Language] + [👑 Go PRO] (для premium: декоративная [👑 Premium ✅] с `cmd_noop`)
  3. **04_Menu — Command Classifier** — `cmd_pay_crypto` → `route = 'payment'` (было `coming_soon`); добавлен `cmd_noop` → `coming_soon` (для декоративной кнопки premium)
  4. **04_Menu — Go to 10_Payment** — заполнены workflowInputs: `telegram_id`, `language_code`, `callback_message_id`, `command`, `translations`, `constants`, `subscription_status`, `nomscoins`
  5. **10_Payment — Payment Router** — добавлен 5-й выход `crypto` (условие `command === 'cmd_pay_crypto'`)
  6. **10_Payment — Build Crypto Instructions** (новая Code нода) — формирует инструкции по оплате USDT на сети TON: адрес кошелька, цены, memo = telegram_id
  7. **10_Payment — Send Crypto Instructions** (новая HTTP Request нода) — editMessageText с crypto инструкциями и кнопкой [Назад → Plans]
  8. **08.4_Shop — Build Shop Text** — убран hardcoded "coming soon" teaser; для free/trial пользователей добавляется PRO teaser + кнопка [👑 Go PRO] → `cmd_premium_plans` в keyboard; вся клавиатура теперь в `shop_reply_markup` (JSON string)
  9. **08.4_Shop — Edit Shop Message** — переключён с hardcoded jsonBody на `={{ JSON.stringify({...reply_markup: $json.shop_reply_markup}) }}`
  10. **Migration 048** — `app_constants`: `ton_wallet_address` (UQDG89...LfHm), `crypto_price_monthly` (3.00), `crypto_price_quarterly` (8.00), `crypto_price_yearly` (25.00 USD)
- Зачем: Органичная интеграция оплаты — без отдельного пункта "Payments" в меню. Точки входа: Profile (статус + [👑 PRO]) и Shop ([👑 PRO] для free юзеров). TON/USDT разблокирован (was coming_soon).
- Применено: ✅ n8n API (PUT /workflows 6XzdbQqpoUG0nsLe, sxzbSc61YuHFbV4i, xYKmBTWI4c0VoSqs, tQz8iUsEc7YRBLh0) + ✅ Supabase REST (migration 048)
- Верификация: 18/18 OK (автоматическая)

### Session: 2026-04-09 — Bug Fix: Премиум кнопка — двойной эмодзи + Plans не открывался
- Что:
  1. **04_Menu — Build Profile Text** — убран `(data.icon_premium || '👑') + ' '` перед текстом кнопки PRO. Перевод `payment.go_premium_button` уже содержит `{icon_premium}` → Template Engine подставляет 👑. Двойной префикс давал `👑 👑 Премиум`.
  2. **08.4_Shop — Build Shop Text** — то же исправление для кнопки [👑 PRO] в магазине.
  3. **10_Payment — Plans flow** — убраны 3 параллельных HTTP Request ноды (`Get Price Monthly/Quarterly/Yearly`). Они вызывали race condition: `Build Plans Text` запускался до завершения Quarterly/Yearly → ошибка `Node 'Get Price Quarterly' hasn't been executed`. `get_user_price` RPC всегда возвращал `final_stars: null` (данные в `subscription_prices` не заполнены), поэтому использовались только fallback значения.
  4. **Payment Router[0]** → теперь ведёт напрямую в `Build Plans Text` (минуя 3 параллельных ноды).
  5. **Build Plans Text** — читает цены из `d.stars_price_monthly/quarterly/yearly` (app_constants), не из HTTP ответов.
  6. **Migration 049** — `app_constants`: `stars_price_monthly` (49), `stars_price_quarterly` (129), `stars_price_yearly` (399), `stars_discount_quarterly` (12%), `stars_discount_yearly` (32%).
- Зачем: Кнопка Премиум в Профиле и Магазине показывала двойной эмодзи. При нажатии — Plans не открывались из-за n8n race condition в параллельных нодах.
- Применено: ✅ n8n API (PUT /workflows sxzbSc61YuHFbV4i, tQz8iUsEc7YRBLh0, xYKmBTWI4c0VoSqs) + ✅ Supabase REST (migration 049)
- Верификация: 10/10 OK

### Session: 2026-04-09 — Migration 050 + n8n: «Мой день» v2 — Герой-Бар UX
- Что:
  1. **Migration 050** — `get_day_summary` v2: добавлены `xp_today` (SUM из xp_events за сегодня) и `streak_current` (из users) в ответ RPC. Файл: `050_enhance_day_summary.sql`. Применено через n8n temp workflow (psycopg2 не работает с текущим DNS).
  2. **ui_translations** — добавлены 8 insight-ключей × 13 языков (`report.insight_*`) + `remaining_label` + `streak_label` в секцию `report`. Применено через Supabase REST API PATCH. Реальные lang_codes в БД: `ar, de, en, es, fa, fr, hi, id, it, pl, pt, ru, uk` (не ja/zh/ko/th/tr как в старой документации).
  3. **n8n 04_Menu — Build Stats Text v3.0** — полная замена кода: прогресс-бар `█░` (10 блоков), статусы БЖУ (`✅⚠️📉🔴`), rule-based insight по приоритетной цепочке (ключ → `ui_translations`), expandable blockquote для приёмов пищи (reverse order, latest first), стрик + XP в footer. Macro % берётся из `app_constants` (`macro_protein_pct/fat/carbs`), не хардкодится.
  4. **n8n 04_Menu — Edit Stats (Inline)** — добавлен `parse_mode: HTML` в bodyParameters (нужен для `<blockquote expandable>`).
- Зачем: «Мой день» был "мёртвой страницей" — только сухие цифры. Теперь: мгновенный визуальный ответ на «сколько осталось?», голос Sassy Sage, компактная история еды.
- Применено: ✅ Supabase REST API + ✅ n8n API (PUT /workflows sxzbSc61YuHFbV4i)
- Верификация: `get_day_summary(417002669)` → поля xp_today=245, streak_current=4 ✅; insight translations 13/13 ✅; n8n workflow PUT 200 ✅

