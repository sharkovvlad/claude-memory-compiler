# Daily Log: 2026-04-19

## Sessions

### Session 1 — Variant C (Headless Architecture) планирование (~14:00-16:00 UTC)

**Контекст:** После 8 итераций провалов Variant A/B (клонирование 04_Menu в Session 2 предыдущего дня показало что cache bug привязан к node UUID, не workflow ID), принято решение о полной архитектурной миграции на Config-driven UI + TMA-ready contract.

**Работа:**

1. **3 параллельных Explore агента** собрали полную картину:
   - UI inventory (85 callbacks, 30+ screens, reply-keyboard, screen hierarchy, translation sections)
   - State machine анализ (26+ workflow_states, v_user_context 43 поля, existing RPCs)
   - Existing config-driven patterns (Template Engine Phase 1-4, nav_stack 076-078, picker-unification planned, quest_templates как прототип)

2. **Критическая оценка 2 внешних ИИ ревью** плана:
   - ✅ Принято: убрать SQL `render_template` (остаётся в n8n JS), CHECK 64 bytes на callback_data, TMA bypass процесса process_user_input, split Phase 3 на 3A (dashboards) + 3B (mutations)
   - ❌ Отклонено: save_bot_message в транзакции (физически невозможно — message_id после sendMessage), previous_status update (ИИ #2 не знал про nav_stack)
   - ⚠️ Адаптировано: `back_state_code` в workflow_states → `back_screen_id_default` в ui_screens как fallback для empty nav_stack

3. **Финальный план** утверждён в `.claude/plans/buzzing-dreaming-brooks.md`:
   - **Scope:** Big Bang миграция всех 85 callbacks, но Profile v5 как pilot
   - **Timeline:** 2-3 недели (Phase 0-4)
   - **Migrations:** 081-094 (14 миграций, раздельные per logical group)
   - **Контракт:** `{screen_id, business_data, telegram_ui{text_key, template_vars, keyboard, ...}}`
   - **Dumb Renderer:** 135 нод → ~15 нод
   - **TMA-ready:** контракт работает с Phase 1, zero frontend lock-in
   - **Risk mitigation:** Plan B caching (materialized view) если benchmark > 1s

4. **Phase 0 выполнена:**
   - ✅ Удалён клон workflow `LgoqJtuosQxQ4PWo` (04_Menu_v2) через DELETE API → 404 verified
   - ✅ CLAUDE.md — добавлено правило 5 в "Правила изменений в БД": Subagent SQL Delegation Protocol (прогонять /nlm 3-5 вопросов → проверять ответы → передавать subagent'у с полным spec → проверять SQL построчно)
   - ✅ Создана KB stub `concepts/headless-architecture.md` — обзор архитектуры, tables, RPCs, phases
   - ✅ Создан draft `concepts/ui-inventory.md` (status: `DRAFT`) — full catalog 85 callbacks + 26 workflow_states + translation sections + 9 вопросов для user верификации

**Ключевые инсайты:**
- Cache bug на уровне node UUID, не workflow ID — любые попытки fix через PUT Switch обречены без регенерации UUID всех нод в workflow
- 30 canonical screen_names уже работают через nav_stack — их reuse в ui_screens
- `quest_templates` (migration 012) + `picker-unification-strategy` (planned 079) — готовые прототипы для ui_screens
- Template Engine в PostgreSQL — over-engineering: TMA не нужен pre-rendered text, JS в Dispatcher уже работает

**Что ждёт user:**
1. Верификация `concepts/ui-inventory.md` — дополнить пропущенные screens/callbacks/statuses
2. После верификации → NLM round 5 вопросов (latency, debounce edges, meal edit flow, cron renderers, localization)
3. После NLM → Phase 1 (SQL migrations 081, 083-085)

### Session 2 — UI Inventory Approved + NLM Round (~16:30-17:00 UTC)

**User verification (16:30):**
- ✅ Callbacks + workflow_states — полнота подтверждена в общем
- ✅ Pairing onboarding ↔ edit правильный
- Детальные решения по каждому экрану — делегированы мне; approval только на визуальные презентации (схема + текст + preview)
- Supabase Studio / Retool — выбраны как future CMS инструменты

**NLM Round (5 вопросов, notebook NOMS Supabase Data `fa75f4de-96c2-4dc0-bc9d-0bf07bd992f5`):**

| # | Вопрос | Ответ NLM (verified) |
|---|--------|----------------------|
| Q1 | Latency baseline | Current: ~500-1300ms (БД 300-800 + n8n 100-300 + Telegram 100-200). Headless = **ожидается улучшение** (fewer n8n nodes, Menu Router 28-output switch убран). Верифицировано: CLAUDE.md Стратегии снижения задержек. |
| Q2 | Debounce edge cases | Обходят: pre_checkout, successful_payment (отдельные триггеры), cron messages, /start, admin_payout_*. Нужен `p_skip_debounce BOOLEAN DEFAULT FALSE`. **Принято в план.** |
| Q3 | Meal edit RLHF | ✅ NLM полностью прав (после user correction): `edit_count` СУЩЕСТВУЕТ в food_logs (добавлено не через migrations/*.sql репо — возможно напрямую в Supabase). `raw_user_input`, `ai_parsed_result` также существуют. Архитектурно NLM прав: 03_AI_Engine делает DELETE+INSERT через ноды "Delete Old Records" + "Save Edited Food" — это **разрушает** RLHF trail (обнуляет edit_count). **Решение:** Phase 3B добавит RPC `update_meal_from_correction` (UPDATE не DELETE+INSERT, инкрементирует `edit_count = edit_count + 1`). Доп.миграция для колонки НЕ нужна. |
| Q4 | Cron renderers | 10 Python cron jobs сейчас самостоятельно формируют тексты через supabase_client.py. Миграция на `render_screen` улучшит consistency. **Решение:** DEFERRED — оставляем cron как есть в Phase 1-3. Phase 4+ опционально мигрируем (low priority). |
| Q5 | Localization | ✅ Подтверждено: callback_data language-independent, только тексты различаются в ui_translations. Колонка `variant_by_lang JSONB` НЕ нужна. Структура keyboards универсальна во всех 13 языках. |

**Верификация NLM против реального кода:**
- ✅ Q1 latency baseline: цифры из CLAUDE.md — factual
- ✅ Q2 debounce bypass: подтверждено в 01_Dispatcher Route Classifier
- ⚠️ Q3 edit_count halluciнация, но архитектурный insight про DELETE+INSERT **верифицирован** grep'ом в 03_AI_Engine.json (ноды id 423, 495 etc.)
- ✅ Q5 структура translations: подтверждено в migration 008 и sample data

**Обновления плана (по результатам NLM):**
1. `process_user_input` RPC signature: добавлен `p_skip_debounce BOOLEAN DEFAULT FALSE` (Q2)
2. Migration 093 (meals) spec расширен: новый `update_meal_from_correction` RPC + опциональная колонка `food_logs.edit_count` (Q3)
3. variant_by_lang колонка НЕ добавляется в ui_screens (Q5 — saved column)
4. Cron renderers migration отложена в Phase 4+ (Q4 — reduced scope)

**Phase 0 завершена. Готов к Phase 1 — подготовка spec Migration 081 для subagent.**

### Session 3 — Phase 1 Infrastructure APPLIED (~17:00-18:30 UTC)

**Подход:**
1. User заметил что предыдущие агенты применяли миграции автономно. Проверил `.env` — есть `DATABASE_URL` (Supabase pooler) + psycopg2 установлен. Обновил CLAUDE.md (правило 5 в "Правила изменений в БД"): autonomous apply через psycopg2 — default, НЕ просить user вручную.
2. User correction: `edit_count` всё-таки существует в food_logs (grep не нашёл потому что колонка добавлена не через migrations/*.sql). NLM был полностью прав — обновил daily log + plan.
3. Написал 3 детальных spec в `/tmp/migration_{083,084,085}_spec.md`
4. Запустил 3 parallel subagent'а (general-purpose) для генерации SQL — **не для apply**, только для кода
5. Verified 10 RPC signatures через psycopg2 до apply: `back_nav → jsonb (with parent key)`, `push_nav(bigint, text)`, `peek_nav(bigint) → text`, `debounce_user_action(bigint, int) → boolean`, etc.
6. Нашёл 1 issue в subagent 085 output: `phenotype_answers` не в v_user_context — fix: read directly from `users` table. Apply'ed fix в final 085.
7. Применил все 3 миграции через psycopg2 с autocommit isolation: **083 → 085 → 084** (порядок по dependencies). Все прошли с RAISE NOTICE подтверждением.
8. E2E smoke tests: 10 queries через psycopg2 — все helpers работают, render_screen error paths корректны, process_user_input forward paths (photo/start) → правильные sub-workflows.

**Результат Phase 1:**

| Migration | Содержание | Status |
|-----------|-----------|--------|
| 081 (applied earlier) | `ui_screens` (15 cols) + `ui_screen_buttons` (9 cols) + `workflow_states.screen_id`/`save_rpc` | ✅ applied |
| 083 | `render_screen(BIGINT, TEXT) → JSONB` (pure, SECURITY DEFINER) | ✅ applied |
| 084 | `process_user_input(5 params) → JSONB` + 3 helpers (`matches_callback_template`, `extract_callback_vars`, `validate_text_input`) | ✅ applied |
| 085 | 13 `get_*_business_data` wrappers (Profile, Stats, Progress, League, Friends, Shop, Quests, My Plan, Settings, Payout, Phenotype, Help, Personal Metrics) | ✅ applied |

**Smoke test results (user 417002669):**
- `get_profile_business_data` → полный JSONB (level 18, xp 5395, streak 14, 🥒 Pickle league)
- `render_screen(nonexistent)` → `{error:screen_not_found}` без crash
- `process_user_input('photo')` → `{forward_to:'03_AI_Engine'}`
- `process_user_input('start')` → `{forward_to:'02_Onboarding_v3'}`
- All helpers (regex match, var extraction, validation) — correct

**Инсайт про subagent delegation:**
- 3 parallel subagent'а написали ~1200 строк SQL за ~4 min
- Main agent (я) только spec'ил + review — context экономится
- 1 ошибка catched в review (`phenotype_answers` location) — протокол "subagent пишет, main agent проверяет" работает
- Rule 5 в CLAUDE.md (subagent SQL delegation + NLM pre-check) — proven в практике

**Следующая фаза (Phase 2 Pilot Profile v5):**
1. Migration 086: INSERT 9 screens (profile_main, my_plan, settings, personal_metrics, help, ask_weight, ask_age, ask_height, delete_account_confirm) + buttons
2. UPDATE workflow_states — добавить screen_id + save_rpc для edit_weight/age/height
3. Build n8n 04_Menu_v3 (Dumb Renderer, ~15 нод) параллельно с legacy 04_Menu
4. Dispatcher routing: Profile v5 callbacks → 04_Menu_v3, остальные → legacy
5. E2E test через Telegram (account 417002669)

**Workflows state (unchanged):**
- 04_Menu (`sxzbSc61YuHFbV4i`) — production, legacy
- 01_Dispatcher — указывает на legacy 04_Menu
- 6 routes (phenotype_quiz, skip_meal, save_speed, friends_info, payout, delete_account_*) — всё ещё сломаны, починятся в Phase 3B

### Session 4 — Phase 2 Pilot Seed applied (~18:30-20:00 UTC)

**User feedback incorporated:**
- Patched `get_personal_metrics_business_data` stub → proper impl (весь 4 поля + computed age). Rationale: TMA contract isolation (не выдавать streak/subscription/mana для simple metrics screen).
- settings/help UX: добавили Sassy Sage bottom-phrases
- ask_weight/age/height: button `Cancel` вместо `Back` (психология: прерывание ожидания vs навигация)
- ask_age: validation type='integer', min=13, max=120 (не date — `set_user_age` принимает text number)
- personal_metrics: `back_screen_id_default='my_plan'` (hierarchy: metrics живут под my_plan)

**Delegated to subagents (3 штуки параллельно):**
1. Extract 9 screens content из n8n workflows (из snapshot 2026-04-18) — structured extraction с text templates, keyboard, translation keys
2. Build visual mockups для approval (ASCII Telegram bubbles + keyboard borders)
3. Generate Migration 086 SQL — 4 blocks × 13 languages

**Main agent review caught:**
- Subagent hallucinated `get_profile_main_business_data` (правильно: `get_profile_business_data`)
- Subagent hallucinated `get_delete_account_business_data` (используем reuse `get_profile_business_data`)
- Missing text templates (`profile.main_text`, `profile.my_plan_text`...) — постановил в Migration 086a

**Migration 086 applied (psycopg2):**
- 9 `ui_screens` rows (profile_main, my_plan, settings, personal_metrics, help, ask_weight/age/height, delete_account_confirm)
- 30 `ui_screen_buttons` rows
- 6 translation keys × 13 languages (buttons.cancel, errors.invalid_weight/age/height, profile.sage_settings, profile.sage_help) + questions.age friendly refresh (en+ru)
- UPDATE 3 workflow_states: edit_weight→ask_weight+set_user_weight, аналогично age+height

**E2E render_screen('profile_main') test passed:**
- visible_condition работает (пременьеум показывает Manage subscription, не Go PRO)
- template_vars заполнены (xp=5395, level=18, age=41, name=Vladislav, mana=474)
- Keyboard построен правильно (3 rows)
- text_key='profile.main_text' → будет отрендерено после Migration 086a

**Migrations this session:**
- 086 applied ✅ (212 lines)
- 086a pending — text templates для 9 screens (минимум ru + en, опционально 11 languages позже через CMS)

**Next steps:**
1. Migration 086a — text templates с placeholders {name}, {level}, {xp}, {weight_kg}, ...
2. Build n8n 04_Menu_v3 (Dumb Renderer workflow ~15 nodes)
3. Dispatcher routing: Profile v5 callbacks → 04_Menu_v3 (остальные → legacy)
4. E2E test через Telegram

**Lessons learned:**
- psycopg2 autonomous apply окупился: 3 миграции (083/084/085) + 086 = 4 apply без user involvement
- Subagent delegation для aux tasks (mockups, extraction) работает так же как для SQL — ~400 строк полноценного output за 1-2 min
- Main agent review обязателен — 2 halluциnation fixed в mockups + 1 в SQL (phenotype_answers location)

### Session 5 — Phase 2 E2E + Critical Debug (~19:00-20:30 UTC)

**User applied migrations 087 (v1→v4_final) + 088 + тестировал бот.**

**3 критических bug'а найдены и исправлены:**

1. **Main Router connections corruption** (моя ошибка Session 4): insert 'menu_v3' rule в index 2 без shift → onboarding/location/pre_checkout все указывали на чужие nodes. REBUILT by outputKey mapping. Dispatcher versionId `cfc76ce8`.

2. **Extract Payload read wrong field name:** subagent JS читал `d.callback_data`, Dispatcher кладёт в `d.command/_cb_data/callback_query`. Fixed priority chain. 04_Menu_v3 versionId `985a4850`.

3. **PL/pgSQL RECORD IS NOT NULL gotcha** (корневой bug всех broken inline buttons):
   - `IF v_button IS NOT NULL` возвращает FALSE когда ЛЮБОЕ поле row = NULL
   - `ui_screen_buttons.visible_condition` nullable → матч есть но блок пропущен
   - Diagnosis: DO block с RAISE NOTICE показал loop found match, но IF не входит
   - **Fix (Migration 089):** `IF v_button.button_id IS NOT NULL` (PK всегда не NULL при match)
   - After fix: все 4 inline buttons route correctly

**Дополнительные fixes Session 5:**

4. **User stuck в status='edit_weight'**: после Cancel из ask_weight статус не сбросился. Reply-кнопки как text input matched PROFILE_V5_STATUSES → save_rpc validation fail → message_id 404.
   - Fix A: `UPDATE users SET status='registered'` unstick.
   - Fix B: Dispatcher Route Classifier patch — main-menu reply labels МИНУЮТ profile_v5_text_input routing. Dispatcher versionId `d4f62a3e`.

5. **4 missing button translations** → Migration 090 (edit_training/phenotype/language/confirm_delete × 13 langs).

**TODO deferred (для следующей сессии):**
- `cmd_back` from ask_* должен reset status → patch process_user_input (когда cmd_back из screen с input_type='text_input', UPDATE users SET status='registered')
- Reply-kb label detection через hardcoded fallbacks → сделать config-driven
- 5 inline buttons Settings/My Plan без meta.target_screen (Phase 3)
- Telegram markdown `**bold**` показывает литералы — решить parse_mode HTML или `<b>` tags

**KB updates:**
- `concepts/headless-architecture.md` — full rewrite with 5 gotchas, debug workflow, TODO

**IMPORTANT для следующего агента:** читай `headless-architecture.md` ПЕРЕД любым touch process_user_input. Gotcha #1 (RECORD IS NOT NULL) повторится если subagents регенерируют код.

**Артефакты:**
- `.claude/plans/buzzing-dreaming-brooks.md` — 520 строк, approved плана
- `knowledge/concepts/headless-architecture.md` — new
- `knowledge/concepts/ui-inventory.md` — draft for verification
- `CLAUDE.md` — +правило 5 в Правила изменений в БД

**Workflows state после Session 1:**
- 04_Menu (`sxzbSc61YuHFbV4i`) — active (production, legacy)
- 04_Menu_v2 clone — **удалён** ✅
- 01_Dispatcher — active, указывает на sxzbSc61YuHFbV4i (reverted после Variant B rollback в предыдущей сессии)
- Phenotype Quiz, Skip Meal, Save Speed, Friends Info, Payout, Delete Account — по-прежнему misroute'ятся в legacy 04_Menu. Будут починены в Phase 3B.

---

### Session 6 — Phase 2 Pilot Stabilization (хендофф от Session 5)

**Контекст:** После Session 5 (n8n Main Router + Extract Payload фиксы) остались 4 открытых бага, блокирующих финальную валидацию Profile v5 pilot. План утверждён в `.claude/plans/elegant-swimming-boot.md`. User дал автономный apply миграций, n8n PUT — только с approval.

**Итог:** все 3 БД фикса применены автономно, n8n PUT не потребовался (баг parse_mode решён через изменение templates, не рендерера).

**Migrations applied:**

1. **091_profile_pilot_business_data_fixes.sql** — Profile v5 business data
   - `get_my_plan_business_data`: добавлен `goal_type` (читается из `v_user_context.goal_type`). Раньше template `profile.my_plan_text` содержал `{goal_type}` во всех 13 языках, но RPC его не возвращал → literal `{goal_type}` в чате.
   - `get_personal_metrics_business_data`: реальная имплементация взамен stub из 085. Возвращает `weight_kg, height_cm, gender, birth_date, age, target_weight_kg` из `v_user_context`. Была причина "Личные метрики" crash (execution 9533 в 04_Menu_v3) — stub возвращал `{}`.
   - Verify: `get_my_plan_business_data(417002669)->>'goal_type' = 'lose'`, `get_personal_metrics_business_data(417002669)` возвращает 6 полей включая `age=41`.

2. **092_process_user_input_back_fixes.sql** — cmd_back двойной фикс + data cleanup
   - FIX A: при `cmd_back` с экрана `input_type='text_input'` — UPDATE `users SET status='registered'`. Раньше user оставался в `edit_weight` после Back → следующий text input уходил в `set_user_weight` silent mutation.
   - FIX B: валидация `v_next_screen` из `back_nav` против `ui_screens` — если экрана нет, fallback через `back_screen_id_default` → `profile_main`. Раньше legacy nav_stack с `'profile'` (до rename в 086) вызывал render error `screen_not_found`.
   - DATA CLEANUP: UPDATE users SET nav_stack = filter(existing ui_screens). 2 affected users (admin 417002669: `['profile']` → `['profile_main']`; 786301802: `['profile','help','friends_info']` → `['help']`).
   - Verify TEST A (back из ask_weight, valid parent): status=render, screen_id=profile_main, users.status='registered' ✓
   - Verify TEST B (back с legacy entry): status=render, screen_id=personal_metrics (fallback), users.status='registered' ✓

3. **093_profile_markdown_to_html.sql** — все bold Markdown → HTML
   - 04_Menu_v3 Dumb Renderer (нода Code) hardcode'ил `parse_mode: 'HTML'` в обеих success + error branches, но migration 087_v4_final заполняла шаблоны `**bold**` (Markdown). Результат: пользователь видел literal `**Profile**`.
   - Решение: конвертировать templates в HTML (не менять renderer) — проще, одна SQL миграция, без n8n PUT, HTML более forgiving для будущих шаблонов.
   - Regexp `\*\*([^*\n]+)\*\*` → `<b>\1</b>` для 5 ключей `profile.{main_text,my_plan_text,settings_text,personal_metrics_text,help_text}` × 13 языков.
   - Verify: 0 остаток `**` в Profile v5 ключах.

**E2E verification (без n8n):**

`render_screen(417002669, screen_id)` для 5 Profile v5 экранов:
- profile_main, my_plan, personal_metrics, settings, help — все возвращают валидные telegram_ui контракты с template_vars, keyboard.
- my_plan template_vars: goal_type='lose', goal_speed='normal', target_calories=2630 ✓
- personal_metrics: weight_kg=99.0, height_cm=193.0 ✓

**Latency benchmark (10 runs, p50/p95 ms):**
- profile_main: 80 / 98
- my_plan: 80 / 570 (единичный outlier; 9 остальных < 100ms)
- personal_metrics: 80 / 216
- settings: 95 / 183
- process_user_input(cmd_my_plan): 80 / 174

Все под 1s threshold — **Plan B caching пока не нужен**. E2E с n8n + Telegram добавит 300-500ms; my_plan на границе, но запас есть.

**Открытые вопросы для следующего агента:**
- 04_Menu_v3 (`ju0h4WStPZX54EfR`) всё ещё `active=false`. executeWorkflow работает с inactive — функционально ок, но нужно подтвердить что 01_Dispatcher в живом сценарии маршрутизирует Profile v5 callbacks (`cmd_get_profile`, `cmd_my_plan`, `cmd_personal_metrics`, `cmd_settings`, `cmd_help`) в v3, а не legacy. Live smoke через Telegram пользователем (открыть Профиль → Мой План → Личные метрики → Back) закроет вопрос.
- Dumb Renderer hardcoded `parse_mode: 'HTML'` устойчив к migrations 091-093. Если Phase 3A добавит screens с Markdown-style text без конверсии — literals вернутся. Принять решение: либо заморозить HTML-only в ui_translations contract, либо сделать `parse_mode` полем в `ui_screens`.
- my_plan p95 = 570ms outlier — можно добавить materialized view для `calculate_user_targets` если повторится в продовых замерах.

**Cornerstone документы не трогал:** headless-architecture.md Gotcha #1 соблюдён (используется `v_button.button_id IS NOT NULL` по всему patched коду).

**Артефакты:**
- `migrations/091_profile_pilot_business_data_fixes.sql` (~70 строк)
- `migrations/092_process_user_input_back_fixes.sql` (~230 строк, переписывает process_user_input из 089)
- `migrations/093_profile_markdown_to_html.sql` (~90 строк)
- `.claude/plans/elegant-swimming-boot.md` — approved план

## Memory Maintenance

### Memory Flush (13:26)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

### Memory Flush (22:16)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (22:25)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

