# 2026-06-07

## mig 478 — literal `\n` в luteal-переводах (en/ru) — FIXED, LIVE-applied, PR #349

**Симптом (owner repro, tid 786301802, lang=en):** хрон `luteal_morning` доставил пуш с литеральным `\n`:
`🌸 Good morning! Luteal phase active today.\nI've added +175 kcal...`

**Причина:** рецидив **mig 352 BUG B**. Mig 334 (RU/EN baseline) вписала строки вручную с Python-style `\n` → попали в JSONB как 2 символа (`\` + `n`). Mig 352 починила `cycle_intro.title` + `cycle_upsell.title`, но **пропустила два других ключа** той же ручной правки mig 334:
- `cron_notifications.reminder_luteal_morning` (пуш хрона `luteal_morning`)
- `wellbeing.cycle_widget_active` (строка виджета цикла в «Моём дне»)

Оба имели `%\\n%` ровно в **en + ru**. Остальные 11 langs (mig 335, программная заливка `json.dumps`) — корректны, не трогали.

**Важно (не баг):** текст пришёл по-английски потому что у тест-аккаунта 786301802 `language_code='en'`. Это норма. Единственный баг — литеральный `\n`.

**Фикс:** mig 478 — `replace(value, '\n', E'\n')` на 4 строках (2 ключа × en/ru), паттерн mig 352. Без изменений кода/RPC. **LIVE-applied + verified** (`literal_bs_n=False`, real newlines 1/2). Переводы читаются из БД в рантайме хрона → деплой не нужен, следующий пуш уже корректен.

**Durable:** Python `FALLBACK_TEMPLATES['reminder_luteal_morning']` (crons/reminders.py:86) использует настоящий `\n` (correct) — fallback не виноват, баг был в DB-значении. Урок: при ручной правке mig 334 cycle-строк проверять ВСЕ hand-edited ключи на литеральный `\n`, не только title'ы (mig 352 закрыла 2 из 4).

## Sage closed-budget guard hardening — PR #352 (services/sage.py, без миграции)

**Запрос owner'а:** разобрать переписку с внешним ИИ про «жиры зашкаливают / динамический пересчёт лимитов жир↔углеводы» + почему дешёвая mini советует есть при превышении калорий.

**Решения по существу (durable):**
- Динамический пересчёт макро-целей внутри дня — **ОТКЛОНЁН** (подтверждено owner'ом). Калорийный потолок уже балансирует жир↔углеводы; двигать сохранённые цели — плохой UX + over-engineering (аудит уже отклонял). «Гибкая диета» реализуется как **coaching-сообщение навигатора**, а не как сдвиг чисел.
- Внешний ИИ переврал пример 1 (75%, белок отстаёт → совет постного белка) — это НЕ баг, это правильный навигатор при открытом бюджете.
- Белок vs калории: на конкретный день при закрытом бюджете **недобор белка = меньшее зло** (оба кумулятивны; добор белка едой на профиците саботирует цель). Белок добирается ВЫБОРОМ еды внутри бюджета (рано в дне), не добавкой сверху. На длинном горизонте белок критичен (мышцы) — но защищается изо дня в день, не перееданием в один вечер.

**Корень бага (114%-день «как насчёт куриного филе»):** мы сами слали модели противоречивый сигнал — `_macro_remaining_line` печатал «P 27g remaining» при калориях +361 over. Модель логично предложила добрать белок. Гард «не звать есть при закрытом бюджете» жил только как закопанная негативная инструкция → слабые модели её роняют (LLM negation + curse-of-instructions: малые модели <42% при множестве конфликтующих правил; GPT-4o/Claude держат).

**4 детерминированных усиления (Python+промпты, без миграции):**
1. `_macro_remaining_line` при `kcal%≥100` → `budget CLOSED (over by N kcal)` вместо per-макро remaining.
2. `_budget_directive` — одна позитивная директива в КОНЕЦ промпта (модели весят конец), без enum-decode и без «do not». Оба пути (food_log + my_day).
3. Commit-then-generate JSON: модель декларирует `budget`/`next_meal_suggested` ДО `text`.
4. Детерминированный гейт (язык-агностичный, БЕЗ регекса): реально закрыт + модель `budget=open` ИЛИ `next_meal_suggested=true` → откат (food_log→pre-baked fallback, my_day→static SQL insight). Регекс по 13 langs сознательно НЕ делали (anti-pattern KB).

**Durable lesson:** когда критичный гард зависит от слабой LLM — (а) не шли противоречивый payload (per-макро remaining при закрытом бюджете); (б) вердикт давай ОДНОЙ позитивной директивой в конце, вычисленной в Python, не закопанной негацией; (в) commit-then-generate (поле-решение до текста) дёшево повышает соблюдение; (г) safety-net через структурное self-report поле, не через 13-язычный регекс.

**Тесты:** +10 unit (93 pass). 2 пред-существующих сетевых падения (sandbox) не связаны.

**TODO (для следующего/owner):** (1) merge #352 → deploy; (2) после деплоя инвалидировать my_day кэш (`UPDATE users SET my_day_insight_at=NULL WHERE telegram_id=417002669`) + 2 открытия «Мой день»; (3) снять телеметрию `ai_coach_logs.day_context` за 06.06 для 100% подтверждения корня; (4) локальный LLM-eval на закрытом-бюджете payload (N прогонов) для замера % срывов до/после. EA/RED-S — отдельная сессия-агент (chip).

## i18n полный аудит ui_translations + Tier 1 fill (mig 481, PR #353)

После mig 478 прогнал скан ВСЕЙ `ui_translations` (12 818 строк × 13 langs) на класс багов.

**Результаты скана:**
- Литеральный `\n` — **закрыт полностью** (0 осталось; mig 352 + 478 добили).
- Placeholder mismatch — 5 ключей, из них реальных багов 0: `league_demoted`/`dunning_d3` оба плейсхолдера подставляются кодом (`league_cycle.py:118+125`, `subscription_lifecycle.py:179+185`); `insight_leader` EN `#{2}` = опечатка (реальный, EN видел сырое).
- Missing keys → English-leak: 11 языков не имели ~18 ключей (были только en/ru). Группы: лиговые пуши (cron), UI цикла, sage.guarded (safety!), shop, мелочь.
- shop.buy_freeze/buy_mana: pl/uk потеряли название товара, pl/uk/fa/hi/id — иконку.

**Tier 1 (informational, no L1 gate) — LIVE-applied + PR #353 (mig 481):**
залил лиговые пуши (4 ключа) + cycle UI (4) + skip_xp_chip + messages + shop fix на 11 языков; EN `#{2}`→`#2`. Critic-субагент: 0 BLOCKER, исправил FR/PT gender-leak (`Reste vif`→`à l'affût`, `Fica esperto`→`Olho vivo`). Генератор `tools/i18n_fill_gen_mig481.py` (placeholder-автовалидация 115 строк).

**Tier 2 (clinical) — DRAFT only, держим под L1/L2:**
`sage.guarded.{maternal,underweight,underage}` × 11 langs = 99 строк (anti-shame fallbacks беременным/underweight/подросткам). Драфт `tools/sage_guarded_draft_2026-06-07.json` + anti-shame critic (0 BLOCKER, «годится для L1/L2»). Бриф — `handover/2026-06-07_sage_guarded_l1l2_brief.md`.

**🔴 Durable для owner/L1:** критик нашёл дефект EN-ИСТОЧНИКА `sage.guarded.underweight[0]` = «Pet enjoys every gram» — весовая лексика у BMI-гард-когорты, унаследована во все языки И сейчас в проде на en/ru. Нужно owner-решение по переписи (every gram → every bite).

**🔑 Durable процессные:**
- Скан-рецепт: рекурсивный walk JSONB → проверки (literal `\n`, placeholder-diff vs en, missing-keys union, English-identical). English-identical шумный (эндонимы/бренды) → L2-territory.
- При placeholder-mismatch СНАЧАЛА проверь код подстановки (`.replace`) — оба варианта могут быть валидны (league_demoted/dunning), не баг.
- mig-номер: брал 479→занят (design_b_commission), 480→занят (ambassador_stats), реально свободен 481. main ушёл далеко за сессию — всегда `git ls-tree origin/main migrations/` перед номером.

### CI-фикс mig 481: JSONB shallow-merge guard

PR #353 упал на `pr-jsonb-merge-guard` — гард грепает токен `\bcontent\s*\|\|`. Мой генератор писал `SET content = content || jsonb_build_object(...)` (top-level shallow-merge, тот самый паттерн P0 26.05 «282 ключа стёрты»).

**Важно:** мой apply БЫЛ безопасен (внутренний `COALESCE(content->ns,'{}') || новые_leaves` сохранял соседей — проверено: 0 потерь, все langs ~1004 ключа). Но полагаться на suppress не стал.

**Фикс:** переписал генератор на guard-safe alt #2 — per (lang, namespace): `jsonb_set(content, '{ns}', COALESCE(content->'ns','{}') || jsonb_build_object(...), true)`. Нет токена `content ||` → гард чист. Идемпотентно переприменил (DB уже была корректна). **Gotcha:** литерал `content ||` в КОММЕНТЕ SQL тоже флагается — переформулировал коммент.

**🔑 Durable:** при bulk-update `ui_translations` НИКОГДА `content = content || jsonb_build_object(ns, ...)` (top-level shallow-merge стирает siblings ns). Только `jsonb_set(content,'{ns}', COALESCE(old)||new, true)` per namespace. И не писать литерал `content ||` даже в комментах миграции. KB [[jsonb-shallow-merge-antipattern]].

### sage.guarded.* — Pet-removal + L1/L2 deploy (mig 482, PR #354)

**Pet-правило (owner):** «Pet»/«Пет» — фантомная сущность (NOMS = сам бот), пользователь не понимает кто это. Уже чистилось mig 278, всплыло в `sage.guarded.*` en/ru. Зафиксировал KB-концепт `phantom-pet-entity.md` + глоссарий §1 + index. Убрал Pet из драфта на 13 языках (заодно ушла «every gram» весовая лексика — критиковый WARN1).

**L1/L2 внешним ИИ — agent-vetted (НЕ применял вслепую):**
- Принято: ru underage gender, pl mistrz→mistrzowsko, hi `भरपेट`(ешь-до-отвала)→нейтрально, ar ×4 generic-masc→отглагольные.
- 🔴 Отклонено: (a) галлюцинация «кириллица в польском» — мой pl чист, патч ревьюера САМ внёс кириллицу `W sam раз` (проверено `[Ѐ-ӿ]`-сканом); (b) 5 «champion»-similes — не утечки (en/ru/hi champion оставлены, ревьюер непоследователен).
- **mig 482 LIVE+verified:** 13 langs × 3×3, **0 Pet во всей ui_translations**, 0 кросс-скрипт, en/ru Pet удалён. Собран пакет для ревью `review-packages/2026-06-07_sage_guarded_L1L2/`.

**🔴 Durable процессные:**
- **stale-base пойман sanity-diff'ом (§12.2):** ветка от старого main, `fetch` без `rebase` перед commit → diff показал −420 строк в чужих `services/sage.py`/тестах. `git rebase origin/main` ПЕРЕД commit обязателен, не только fetch. Поймал до push'а.
- **Не верь L2-ревьюеру вслепую:** машинно-проверяй cross-script (`[Ѐ-ӿ]` в латинских языках), placeholder'ы, Pet. Внешний ИИ галлюцинировал находку И внёс порчу в своём патче.
- **«champion»-simile ≠ gender-leak** (лексикализовано, en `champ` тоже). Не менять выборочно.

## (follow-up) Sage closed-budget — DEPLOYED + eval + фазы дня выровнены

- **PR #352 MERGED + DEPLOYED LIVE** (deploy run success 13:58). Закрытый-бюджет гард активен на проде.
- **Eval добавлен** `tools/sage_closed_budget_eval.py` — реальные вызовы gpt-4o-mini на баг-payload 06.06 (114%, жир 167%). Результат: **0/12 толканий еды, 0/12 протечек**, модель 12/12 сама декларирует `budget=closed`. Подтвердил фикс на живой модели (до фикса толкала «куриное филе»).
- **stale-base инцидент (§12, поймал CI):** PR #352 показал удаление `migrations/480` + −204 (другой агент смержил mig 480 после моего rebase). Лечение: `git fetch + rebase origin/main` → diff схлопнулся до моих файлов → `--force-with-lease`. Урок ещё раз: rebase ПЕРЕД push, не доверять «уже ребейзил час назад».
- **sage-tov экспорт проверен:** `tools/sage_transcript_export.py` → `~/Documents/NOMS/sage_transcripts/sage_transcript_YYYY-MM-DD.md`. Включает логи еды + реакции Номса + инсайты «Мой день» вперемешку по времени + «видел HH:MM/phase». Данные копятся в БД сами (ai_coach_logs), MD создаётся при запуске экспорта. Сгенерил 06-06 (pre-fix) и 06-07. Скилл `/sage-tov` = 2 фазы (экспорт-пакет + судья). Транскрипт 06.06 показал побочный тон-дрифт (повтор «куриное филе» в каждом инсайте = VARY игнорится, «брат» = забанено) — кандидат для прогона судьи, НЕ closed-budget.

## Фазы дня кэша «Мой день» выровнены под meal_period — PR #355 (handlers/menu_v3.py)

**Запрос owner:** утро начиналось с 6, а Sage по `meal_period` с 5 зовёт это завтраком — сделать консистентно без коллизий.

**Проблема:** `_day_phase` (свежесть кэша my_day) границы 6/12/17/22 противоречили `meal_period` (sage._local_time_context, по нему Sage пишет «завтрак/обед/ужин»): 05:xx=«night» но breakfast; 17:xx=«evening» но afternoon_snack; 22:xx=«night» но dinner → кэш мог остаться «свежим» при смене формулировки (ужинная фраза в 23:00).

**Фикс — единый источник правды:** фазы = union'ы meal_period-бакетов: morning=breakfast [5,11), day=lunch+afternoon [11,18), evening=dinner [18,23), night=late_night [23,5) (оборот через полночь). Night-wrap сохранён (репорт 417002669 01:33). Комментарий в коде просит синхронизировать обе функции при будущих правках. 32 тестов pass.

**Durable:** при правке `_local_time_context` (meal_period) ОБЯЗАТЕЛЬНО синхронить `_day_phase` (menu_v3.py) — иначе кэш-коллизии «фаза ≠ формулировка Sage». Локальная полночь = граница дня (логи после полуночи → новый день), оставляем.

## KB audit (/kb-close --audit)

- `audit.py`: 150 concepts, **0 orphans**, 12 merge-кандидатов (все низкий score ≤0.251, лексические false-positives — общий словарь n8n/architecture; реальных дублей нет, ничего не слито), 17 stale (все n8n-legacy by-design — миграция n8n→Python идёт; банеры не вешал массово, риск/churn на session-close).
- `lint.py --structural-only`: **0 errors** (мои KB-правки чистые), warnings/suggestions — пред-существующий фон.
- **MEMORY.md консолидирована: 35.5 KB/158 → 22.6 KB/138** (под лимитом системы 24.4 KB и <150 строк). Бэкап `MEMORY.backup-2026-06-07.md`. Метод: condense verbose «Current state» бул летов до 1-2 строк (детали в daily/KB), trim старого «Recently shipped», убрал done-item Calc-audit из Open. Все durable 🔑/🔴 + активные пункты (несмёрженные PR, Stage 7c, EA/RED-S) сохранены. consolidate-memory auto-skill НЕ запускал (owner-правило human-review) — ручная хирургия.

## Phantom «Pet» CI-guard

- Добавлен `.github/workflows/pr-phantom-pet-guard.yml` — статический guard, фейлит PR, если added-строка в `migrations/*.sql` вводит фантом «Pet»/«Пет» в ui_translations-контент. Зеркалит `pr-jsonb-merge-guard.yml` (changed-migrations grep → коммент + fail).
- Эвристика: per added-line (git diff -U0, portable awk без gawk-3arg-match для line#) → вырезать SQL-коммент (`--`) → токен `\bPet\b`/`Пет` в коде → контекст строкового литерала (`'`) ИЛИ строка трогает `ui_translations`. Комментарии (напр. mig 482 `-- remove «Pet»`) НЕ флагаются. Override `-- pet-ok: <reason>` для будущего экрана с реальным визуальным питомцем.
- Verified в temp-репо: violation `Pet approves` + `Пет рад` → fail (2 flagged, верные line#); comment-mention / `-- pet-ok:` / clean-текст → pass. YAML валиден.
- KB `phantom-pet-entity.md` §«Как ловить» дополнен секцией CI-guard (enforced 2026-06-07). Предотвращает 3-е повторение (mig 278 → sage.guarded.* mig 482 → guard).
- **PR [#358](https://github.com/sharkovvlad/noms-bot/pull/358)** (open, code-only, без mig). MEMORY обновлён (строка 2026-06-07).
- **Durable gotcha (авторам CI-guard'ов):** (1) full-file grep флагает легитимные `--`-комменты о самом правиле (mig 482 `-- remove «Pet»`) → вырезай комменты ДО матча; (2) для кириллицы `\bПет\b` (UTF-8 word-boundary) ненадёжен в grep → substring; для латиницы `\bPet\b` (не ловит «Peter»); (3) gawk-only 3-arg `match($0,re,arr)` падает на BSD awk (локальный mac) → portable `sub()`-срезка hunk-заголовка для номеров строк.

## ~поздно вечером — Barcode регионализация для LATAM (PR #341 расширен)

**Контекст:** owner спросил «что докрутить под наши LATAM-приоритеты». Real users сейчас: US(6)/UA(5)/ES(3) — Spain уже #3. Bottleneck = OpenFoodFacts coverage уровн неровный (FR ~3M / ES ~500K / MX-BR <100K / RU-IR-IN <50K). Сделал 3 quick-win улучшения без миграций БД, расширив существующий PR #341.

### Что в PR #341 (накопительно)

| Часть | Применено? |
|---|---|
| Часть 1: bug fixes (gate + sticker + retry + копи ×13) | mig 470 LIVE; код в PR, не merged |
| Часть 2: localized names + regional OFF + self-learning cache | код в PR, не merged, БЕЗ миграций |

### Часть 2 — три улучшения (commit `f518da6`)

**№1 Локализованные имена** (`services/barcode.py`)
- `_OFF_FIELDS` теперь запрашивает `product_name_<lc>` для всех 13 langs
- `_pick_localized_name(product, lang_code)`: lang → default → English
- LATAM/EU/RU юзеры видят продукты на родном языке вместо французского/английского по умолчанию.

**№2 Региональный фильтр OFF** (`services/barcode.py`)
- `fetch_openfoodfacts(barcode, *, client, lang_code, country_code)` пробрасывает `lc` + `cc` query params
- Для barcode endpoint основной эффект — localized fields prioritized server-side + при наличии региональной вариации (Coca-Cola MX vs EU) OFF выбирает правильную
- В `_try_barcode_lookup` подцеплено к `ctx.language_code` + `ctx.country_code`

**№3 Self-learning кэш — crowd correction** (`services/barcode.py` + `handlers/food_log.py:_handle_edit_meal_input`)
- После успешного `replace_meal_transaction`: если `origin_input_source=='barcode'` И GTIN валидный И 1 item И **порция не изменилась** (±10%) → fire-and-forget `upsert_barcode_cache` с пересчётом КБЖУ в per-100g, `source='user_corrected'`
- **🔑 Ключевая защита:** `is_macro_correction(original_portion, corrected_grams)` отсекает portion-changes — иначе юзер съел 100г вместо записанных 200г, и его per-portion макросы пересчитались бы в искажённые per-100g для всей crowd-базы
- Параллельно с уже существующим `_ufm.populate` (per-user memory) — это новый параллельный путь для shared crowd-base

### Защитные эвристики (durable)

- Только single-item meals — multi-item обычно name-only correction, не данные
- Только GTIN-checksum-valid origin (`origin_raw_text`) — defensive
- Только `is_macro_correction` (same portion ±10%) — иначе poison cache portion-recompute'ом
- `p_serving_size_g=None` в upsert — НИКОГДА не перетирать manufacturer pack weight юзерским вводом
- `p_brand=None` — user edit не сообщает бренд
- Fire-and-forget везде (`asyncio.create_task`) — не блокирует confirmation envelope
- Если хоть что-то пошло не так — silently skip, никаких user-facing errors

### Что НЕ сделано (по плану из обсуждения)

- №4 USDA FoodData Central (US fallback) — следующий PR
- №5 GPT-fallback по EAN для weak-coverage regions — следующий PR
- №6 Nutritionix (бренды) — отложено
- №7 OCR этикетки как ultimate fallback — отложено

### Тесты

7 новых unit-тестов в `tests/services/test_barcode.py`:
- `test_parse_off_picks_localized_name_for_user_lang` (es/pt)
- `test_parse_off_falls_back_to_default_when_lang_missing` (ar)
- `test_fetch_openfoodfacts_passes_lc_cc_to_off`
- `test_fetch_openfoodfacts_no_lang_no_cc_param`
- `test_is_macro_correction_same_portion_accepts` / `_different_portion_rejects` / `_unparseable_portion_rejects`
- `test_upsert_user_correction_scales_to_per_100g`
- `test_upsert_user_correction_skips_empty_name`

48 passed, 1 skipped (zxingcpp local-only).

### Деплой

Применено LIVE: НИЧЕГО (никаких миграций). Код в PR #341 — после merge активируется на ближайшем рестарте noms-webhooks. Cache на старые продукты остаётся прежним (localization подтянется при следующем cache-refresh; pending pre-existing cached products продолжат жить со старыми именами до TTL/инвалидации).

### Замечание по EXIF метаданным

Owner спросил про GPS из фото-метаданных. **Telegram удаляет EXIF** при загрузке (privacy). Источник правды для региональности — `users.country_code` + `users.language_code` из онбординга. Это покрыто №1+№2.

### KB

KB `barcode-logging-openfoodfacts.md` — добавляется §10 «Регионализация / crowd correction».

## ~ Ambassador screens polish + Design B follow-up (mig 483 LIVE, PR #356)

**Контекст:** owner-фидбэк по экрану «Статистика» (5 пунктов) + вопрос про курс звёзд.

**mig 483 (LIVE+applied, PR #356):**
1. Дедуп Панель↔Статистика: `get_friends_info_rpc` ambassador-ветка ужата до командного центра (только «Доступно к выводу + вывод от $10» + промокод; убраны 4 stat-строки). Статистика владеет цифрами.
2. Backfill: историческая майская оплата Евгении (1 PRO) → строка `ambassador_commission` $3.25 (идемпотентно).
3. Минимум вывода ($10 из `min_payout_amount`) на обоих экранах.
4. L1/L2 → «Друзья / Друзья друзей» × 13 (+ починены N1/N2 в es/fr/pt).
5. 13 языков проверены: tree_l1/dash_available/earn_min во всех 13 ✓.
- `get_ambassador_detail` +min-payout строка; `get_friends_info_rpc` full-body replace (трим).
- Stale-base рецидив (PR #356): ветка от старого main → diff показал чужие 481/482+sage-tools как «удалённые» → CI Diff-sanity заблокировал. Фикс: rebase origin/main → diff схлопнулся до 1 файла → force-with-lease. (Лесон §12 сработал как надо.)

**🔴 ВАЖНАЯ НАХОДКА (owner поймал): Stars-комиссия переплачивается + валюта не нормализована.**
- Как считалась комиссия Евгении: она заплатила **580 ⭐** (quarterly). Я взял **прайс-лист** $12.99 (DEFAULT quarterly USD) как базу → 25% = $3.25. Реального **курса звезды НЕ определял** — подставил номинал плана.
- Скрин owner = **курс покупки** звёзд юзером (~1.66–1.82 ₽/⭐) — это gross с маржой Telegram, к нам НЕ приходит.
- Реально NOMS получает ≈ **developer payout ~$0.013/⭐** (Telegram ~30% + Apple/Google 30% на мобиле). 580×$0.013 ≈ **$7.5**. Комиссия $3.25/$7.5 = **~43%**, а не 25% → **ПЕРЕПЛАТА**. Правильно 25%×$7.5 ≈ $1.9. Backfill $3.25 завышен ~вдвое.
- Источники курса: developer payout rate Telegram + реальные выписки Fragment/TON (ground truth). НЕ курс покупки, НЕ среднее по пакетам.
- **Испания/EUR:** юрлицо в Испании → учёт EUR. Сейчас комиссия в смешанных валютах (Stars=USD, Stripe=валюта оплаты), баланс складывается наивно. Нужна нормализация к ОДНОЙ валюте (EUR=юрлицо vs USD=payout USDT) — **решение owner**.
- KB про курс звёзд / EUR-пересчёт — ПУСТО (пробел, зафиксирован в promo-code-ambassador-flow §Stars-rate).

**🟡 OPEN для следующего агента (НЕ хотфикс, до реальных амбассадоров):** ввести `stars_payout_rate_usd` константу (~0.013) + нормализацию валюты комиссии (решение EUR vs USD) + пересчёт Stars-базы в `process_referral_payment_reward` (сейчас USD-цена плана DEFAULT) + коррекция backfill $3.25→~$1.9. Требует owner-решения по валюте.

---

## ~20:30 — RED-S / Energy Availability: discovery + educational дисклеймер (mig 484, PR #359)

**Контекст:** discovery-сессия с owner по теме EA/RED-S guard (отложен 18.05). Прочитаны KB `energy-availability-design-decision` + `calc-scientific-basis`.

**Данные аудитории (live, is_bot=false):** всего **14** живых юзеров; 6 женщин; goal `lose`=3, `maintain`=10; goal_speed — **нет 'fast'**; activity — **нет heavy/extreme**; **группа риска Ж/15-35/lose = 0**; `waist_circumference` заполнен **2/14**. Таблиц трекинга тренировок — **НЕТ ни одной** (P2.5a отсутствует; `waist_circumference` колонка есть — P2.1 частично).

**Решение owner (подтверждён defer-to-P2 на свежих данных):** настоящий EA-гард оставить отложенным (бот не видит тренировок → фейковый гард хуже отсутствия). Вместо него — честный **информационный дисклеймер** «💛 Энергия и цикл». Дом — **Women's Health** (НЕ Safety Center, см. ниже). Показ — только женщинам `goal_type='lose'`. Сторож разморозки — **только документация** (admin-алерт owner отклонил).

**Реализовано (mig 484, applied LIVE + PR #359 open, НЕ merged):**
- новый ключ `ui_translations` `profile.women_health_red_s_note` — **13 языков** (мастер RU+EN; остальные 11 — мой перевод, native L1 ещё нужен);
- placeholder `{red_s_note}` добавлен в `profile.women_health_text` (идемпотентно);
- `get_women_health_business_data` возвращает gated `red_s_note`: показ при `goal_type='lose' AND NOT is_pregnant AND NOT is_lactating`.
- эмодзи 💛 встроен в текст (как 🌸 в существующих ключах); 🌙 отвергнут owner.

**Проверено на live (psycopg2, беременная/кормящая — через BEGIN…ROLLBACK):** Ж+lose→показан; Ж+maintain→пусто; беременная+lose→пусто; кормящая+lose→пусто; 16лет+lose→показан (подростки — высший риск, текст на Women's Health не стыкуется с Safety Center).

**🔴 DURABLE GOTCHA:** беременность/кормление/underage в `calculate_user_targets` форсят `effective_goal_type='maintain'`, но **НЕ меняют сохранённый `users.goal_type`** (остаётся `lose`). Любой UI/гейтинг по `goal_type='lose'` ОБЯЗАН доп. исключать `is_pregnant`/`is_lactating`, иначе беременная увидит контент для худеющих.

**Архитектурный факт:** Safety Center (`get_safety_center_data`) — динамическое табло сработавших calc-гардов; mig 263 ЯВНО прячет severity `informational`/`silent_accuracy`. Справочные заметки туда не годятся → дом educational-контента = тематические экраны. `{placeholder}` из `business_data_rpc` работает в теле `text_key` экрана (подтверждено: `safety_center_text`→`{body_text}`, `women_health_text`→`{red_s_note}`).

**Путь живого теста:** Профиль → «Мой план» → «Мои данные» → «🌸 Женское здоровье». Абзац внизу.

**mig HEAD → 484.**

### Dunning trial-копирайт на 6 языков (mig 485, PR #360)

**Приоритет №1 «уязвимые/частые экраны» — проверка на фактах:**
- Safety-баннеры (`warning.*`, 49 ключей) — УЖЕ полностью переведены 13 langs, 0 дыр. Не трогал (проверил, а не предположил).
- Trial-dunning письма — текли английским у pt/pl/id/hi/ar/fa (12 ключей: trial_expired, dunning_d1/d3_trial, кнопки, plan_name_trial). Реальное влияние на удержание.

**Сделано (LIVE + PR #360):** локализация в тоне Sage (тыква-Золушка, космич.скорость, power-save), gender-neutral. Critic-субагент (строгий, в отличие от прошлого) нашёл:
- HI ×5 generic-masc будущее (जागोगे/गिरोगे/चाहते हो) → безличные. Реальная утечка пола.
- AR ×5 generic-masc → 1pl-inclusive (نفتحها)/именные.
- AR/FA RTL: добавлены LRM (U+200E) вокруг латинских Premium/NOMS.
- **Машинно проверил патч ПЕРЕД применением** (script-purity + LRM-codepoints + 0 English) — чист, в отличие от прошлого ревьюера. Артефакт `tools/dunning_trial_critic_patch_2026-06-07.json`.

**🟡 Открыто (опционально):** AR/FA gender-переформулировки грамматически корректны, но финальную «гладкость» 1pl стоит подтвердить носителем (тот же L2-канал). Деплоится версия уже лучше generic-masc.

**🔑 Durable:**
- **Проверяй на фактах перед «починкой»:** safety-баннеры оказались уже готовы — не тратил время. Скан → потом действие.
- **dunning split:** trial-ветка (trial_expired/dunning_*_trial) локализуется отдельно от paid-ветки; paid была готова, trial — нет.
- **HI generic-masc** прячется в будущем времени -गे и причастиях चाहते/जानते — ловить именно их; безличное/инфинитив снимает.
- Pet полностью закрыт (mig 482 + CI-guard), обновил phantom-pet-entity.md «Действие → закрыто».

### Sage ToV анализ + payout/shop Russian-in-EN баг (priorities 2,3)

**Приоритет №2 — характер Sage (ToV):** прогнал `/sage-tov` за 2026-06-07 (17 реакций, 4 юзера). **Главный паттерн: ~16/17 реакций = «добавь куриное филе/творог, не хватает белка»** — навигатор (PR#340) over-fire'ит, стал рефлексом на каждое сообщение, душит характер + штамп «куриное филе» (watch-item подтверждён). Отчёт `~/Documents/NOMS/sage_transcripts/sage_tov_review_2026-06-07.md`. Предложил 4 правки промпта. **Owner: «пока не трогать промпт»** → НЕ менял (промпт = бренд, только с go). Anti-shame/фазы дня — чисты. Баг: 1 реакция не сгенерилась (fallback).

**Приоритет №3 — «недоперевод» → вскрылся БАГ ДАННЫХ:**
- Фильтрованный скан: реально «непереведённого» ≈15 ключей (не 94 — остальное бренды/эндонимы/шаблоны вёрстки типа `stats.main_text`, правильно одинаковы).
- 🔴 **Системный баг:** 18 ключей `payout.*`(10)+`shop.*`(8) держат **РУССКИЙ текст в EN-слоте**, и так во ВСЕХ 12 не-ru языках (de/fr/es/it/pt/pl/uk/id/hi/ar/fa). Весь флоу вывода средств + магазина = русский для всего мира. 18×12=216 строк.
- **mig 486 (PR #361):** исправлен EN-слот → корректный английский (источник+fallback). Плейсхолдеры/`{tr:}` сохранены, валидированы.
- Остаток: 198 строк (11 langs) — переводить С исправленного английского. Spawn-task заведён.
- Доп. data-bug: `profile.set_trimester_text` RU = `<b></b>` (битый заголовок).

**🔑 Durable:**
- **Скан EN-слота на кириллицу** = быстрый детектор «русский в английском» бага (`[А-Яа-я]{3,}` в en content). Нашёл 18 ключей разом.
- **Фильтруй English-identical**: бренды (NOMS/Premium/XP), эндонимы (`answers.lang_*`), шаблоны вёрстки (`*main_text` с `{tr:}`/плейсхолдерами) — НЕ непереводы. Сырой скан 94 → реальных ~15.

---

## Сессия (вечер) — payout/shop i18n ×11 langs ЗАВЕРШЕНА (mig 487, PR #362)

Доделал остаток из дневной записи выше: 198 строк (18 ключей `payout.*`×10 + `shop.*`×8, 11 langs de/fr/es/it/pt/pl/uk/id/hi/ar/fa). Источник = **исправленный английский** (mig 486), НЕ русский.

- **mig 487 (PR #362, applied LIVE, 5/5 CI green):** per-key `jsonb_set(content,'{ns,key}',…,true)` (content вложенный: `content['payout']['confirm_text']`, не flat). 198 UPDATE. Applied транзакционно verify-before-commit (0 ошибок). PR open (deploy/merge — не из worktree).
- **Пайплайн качества:** автор = Claude (полный контроль байтов, без галлюцинаций chars), machine-verify (placeholder/`$`/tag/`\n` parity + script purity + FA Persian kaf/yeh + LRM/ZWNJ presence + 0 russian-leftover) → 3 параллельных critic-субагента (AR/FA gender+RTL, HI register, EU+ID false-friends). Фиксы: FA `موجودیت`/`مانات` коллизии, AR masc-imperatives→`يُرجى`+masdar/`يمكنك`, HI aap-slip, UK `невірний`→`неправильний`, ID dropped object.

**🔑 Durable-уроки:**
- **AR gender-neutral ≠ убрать все ـك.** Possessive-clitic `ـك` (رقمك) и `يمكنك` **без огласовок визуально идентичны** для муж/жен → УЖЕ нейтральны. Гендерятся только **императивы** (انتظر→انتظري добавляет ي). Чинить только их (يُرجى+masdar / يمكنك+verb), не трогать ـك. Иначе over-correction.
- **FA: возвратный clitic на قاموс-слова создаёт коллизии.** `موجودی`(balance)+`ت` → `موجودیت` = реальное слово «сущность/existence»; `مانا`+`ت` → `مانات` = валюта manat. Юзать `موجودی تو`/`مانای تو` (раздельно), не clitic. Перед FA clitic-формой — проверь, не омоним ли.
- **Вложенный `ui_translations.content`.** Ключ `payout.confirm_text` = `content->'payout'->'confirm_text'`, НЕ top-level `content->'payout.confirm_text'`. jsonb_set path = `'{payout,confirm_text}'`.
- **UK не флагать `[А-Яа-я]` russian-сканом** — украинский кириллический. Детектор Russian-в-UK = russian-only буквы `ыъэё` + сравнение с ru-значением.
