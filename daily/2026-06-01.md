# 2026-06-01

## Food-card deletion fix (One-Menu) + My Day wellbeing line (Opus 4.8)

### Part 1 — карточка распознанной еды удалялась при навигации (PR #264, Python-only)

**Симптом** (скрин тимлида, tid 417002669): юзер логирует еду → карточка КБЖУ → клик
«Мой день»/«Прогресс»/«Профиль» → карточка исчезает.

**Корень:** ветка Stage 7 GLOBAL AI Engine в `webhook_server._try_authoritative_path`
(`target=ai`, ~line 1631) звала `_send_and_persist(envelope, chat_id)` с дефолтным
`persist_as_menu=True` → карточка писала себя в `users.last_bot_message_id` (type
'menu') → следующий `delete_and_send_new` её удалял. Регрессия внесена в `ff3446f`
(Stage 7a AI Engine Python cutover): n8n-путь `/internal/food_log/render` имел
`persist_as_menu=False` (фикс 24.05), но Python global ветка скопировала вызов без
kwarg. С промоута AI Engine в GLOBAL (mig 373, 29.05) баг был у всех registered.

**Fix:** `persist_as_menu=False` (1 строка) + развёрнутый комментарий + регрессионный
тест `test_ai_gate_food_card_persisted_as_content_not_menu`. Карточка еды = persistent
content (дневник питания), не навигация. Ветку onboarding-еды (line 1681) НЕ трогал —
там last item = ре-рендер шага онбординга, persist_as_menu=True корректен. Тесты
43 passed. **Деплой через GitHub Actions после merge #264.**

KB: append в [[concepts/one-menu-ux]] «What NOT to track» → Python authoritative
`persist_as_menu` footgun + чек-лист при cutover контент-хендлера.

### Part 2 — строка самочувствия в «Мой день» (PR #265, mig 410 LIVE)

Под БЖУ: `🌙 Сон: 🥱 · 🌀 Стресс: 🤯`. Free-tier (качественный лог бесплатный;
макро-дельты остаются Premium в `active_modifiers_strip`). Значение = ЭМОДЗИ, не слово
(пропорциональный шрифт Telegram → right-column не выровнять; слова не влезают в
≤35 chars на части из 13 языков). Пусто если сегодня не залогано (trailing \n\n,
плейсхолдер схлопывается).

**Новый UX-набор эмодзи** на check-in кнопках + в строке (single source = app_constants):
Сон 🥱/🙂/😎 (было 😴/🙂/✨), Стресс 😌/😬/🤯 (было 😌/🌀/🔥). Причины свопа: 😴 (спящее
лицо) ≠ недосып; 🌀 дублировал иконку категории «🌀 Стресс»; 🔥 конфликтовал со 🔥
стрика на той же карточке.

mig 410: 6 app_constants + своп эмодзи 4 кнопок ×13 + `{wellbeing_line}` в
stats.main_text ×13 + блок 8.8 в `get_daily_stats_rpc` (base из pg_get_functiondef
живого прода). daily_metrics за сегодня (UNIQUE tid,date). **Applied LIVE + verified:**
6 constants, 13/13 templates, p95 178ms (Mac→pooler, no regression). Рендер
подтверждён на 417002669 (short/high → 🥱/🤯, great/none → 😎/😌, only-sleep). Python
не трогали — stats_main уже читает get_daily_stats_rpc.

KB: append в [[concepts/adaptive-modifiers-architecture]] «My Day wellbeing line (mig 410)».

### Durable rule зафиксирован

Owner: слово **«streak»/«стрик» НЕ транслитерировать в non-EN** — англицизм-пустышка,
EN-only; везде ещё смысловой эквивалент («серия дней» и т.п.). Добавлено в
[[concepts/copywriter-playbook]] §3 (mandatory checklist) + §5 (cross-lang insights).

### State

- mig HEAD: **410** (was 409).
- Open: PR #264 (Python, food-card fix), PR #265 (mig 410, applied LIVE, PR для истории).
- Обе ветки от origin/main, диффы чистые (sanity-check passed).

## Followup — компактизация «Мой день» (PR #266, mig 411 LIVE)

Owner UX после mig 410: (1) поднять строку самочувствия вплотную к БЖУ (−1 строка),
(2) прогресс-бар не влезал на узких экранах.

- **Wellbeing к БЖУ:** `wellbeing_line` теперь leading `\n` (было trailing `\n\n`);
  blank-разделитель перед NOMS — в шаблоне. Шаблон: `\n\n{wellbeing_line}NOMS` →
  `{wellbeing_line}\n\nNOMS`. Пустой случай схлопывается.
- **Прогресс-бар:** `▓▓▓░░░░░░░  27%  ·  ещё 1919` → `▓▓ 27% · 🎯 1919`. Бар 10→8
  (`stats_bar_blocks`, used только get_daily_stats_rpc), double→single space, слово
  `remaining_label` (2..7 симв ×13) → **🎯** = `icon_remaining` app_constants
  (no-hardcode, RPC отдаёт flat-полем `{icon_remaining}`, резолв из template_vars P1).
  Язык-инвариант — снимает перенос на PL «zostało»/ES «quedan».
- mig 411 base = pg_get_functiondef живого прода (post-410). Applied LIVE + verified
  (bar=8, 🎯, 13/13, 0 старого слова, p95 164ms). Python не трогали.
- **PR #264 + #265 MERGED** в main за время сессии (rebase подтянул). mig HEAD → **411**.

## KB audit (--audit режим)

- `audit.py`: 140 concepts, 0 orphans, 13 merge-candidate pairs (max score 0.25 — все vocabulary false-positives, `supabase-db-patterns` хаб ×4) → **0 merges**. 17 stale (все `warning`, нет `high`).
- Bump `updated:` 2026-06-01 на 2 концептах, реально отредактированных сегодня: `one-menu-ux`, `copywriter-playbook` (были false-stale по frontmatter).
- Остальные 15 stale — aging n8n-концепты (mid-migration, `warning`). НЕ баннерил массово: нужна per-file сверка с live-кодом = отдельная сессия. **Флаг владельцу:** n8n-концепты стареют, нужен dedicated re-validation pass.
- `lint.py --structural-only`: 157 errors (404 missing-backlinks и пр.) — **pre-existing baseline**, не от моих правок (я добавлял только валидные `[[concepts/]]` + bump дат).
- **MEMORY consolidation:** backup `MEMORY.backup-2026-06-01.md`. Сжал dated changelog-блоки (2× «Current state 05-31» + marathon 05-30, ~63 строки) в one-liners «Recently shipped» + вынес активные пункты в «Open / next agent» и durable факты в «Cutover state». Durable секции (Architecture/Critical Facts/Multi-agent/Watchlist/Prefs) сохранены. **195→154 строки, 37KB→17KB** (под лимитом 25KB).

## Sage — сквозная память дня + English-only промпты (PR #267, mig 412 LIVE)

Owner: убрать «клиповое мышление» — оба Sage-инсайта (food-log comment + my-day) должны
видеть весь день. Номс должен увязывать недосып→грелин, повторный кофе→ЦНС, стресс→кортизол.

- **Данные (mig 412):** `get_day_summary` + поле `wellbeing_today {sleep, stress}` из
  `daily_metrics` за сегодня (user-local date, free-tier). `meals` уже отдавался
  (time_local/items_summary/total_calories = today_meals_history). Base = pg_get_functiondef
  живого прода, additive. p95 73ms.
- **Payload (services/sage.py):** my-day — `_normalise_day_summary` пробрасывает
  `today_meals_history`+`wellbeing_today`, `_build_my_day_prompt` печатает. food-log —
  `generate_food_log_comment(rpc_caller=...)` тянет `get_day_summary`→`_build_day_context_lines`;
  `render_food_log_confirmation` передаёт `rpc_caller=supabase.rpc` (в timeout-bounded sage_task).
- **Промпты:** food-log блок `DAY CONTEXT AWARENESS` (повтор кофе/сладкое→ирония, связь с
  итогами, эндокринология insulin/ghrelin/dopamine); my-day блок `WELLBEING & CHRONOBIOLOGY`
  (недосып→грелин→быстрые углеводы+похвала; стресс→кортизол→сложные углеводы; не забегать по
  времени суток).
- **🔑 Архитектурное решение (durable):** системный промпт = ОДИН английский источник в коде
  (version-controlled), язык ответа = параметр `Response language: {users.language_code}` (уже
  был). Best-practice (OpenAI/Anthropic/Google). Убран per-lang DB-lookup `sage.system_prompt_*`
  — был **drift-trap**: food_log жил только в 2/13 langs, 11 молча уходили на код-дефолт.
  mig 412 удалил мёртвые DB-ключи ×13/×2. **Урок:** не переводить системные промпты ×13 —
  переводить только то, что видит юзер (output). KB [[concepts/sage-food-log-llm-integration]].
- Тесты: 6 новых + правка system-prompt-source теста. 615 passed, p95 73ms.
- **mig HEAD → 412.** PR #264/#265/#266 MERGED ранее в сессии; #267 открыт.

## Streak-forbid в LLM-промптах + merge/deploy (PR #268 MERGED + DEPLOYED)

- Точечная правка `services/sage.py`: в `STRICTLY FORBIDDEN` обоих системных промптов
  (`_DEFAULT_SYSTEM_PROMPT_EN` + `_MY_DAY_EN`) — пункт «в не-английском ответе НЕ
  транслитерировать 'streak' (RU стрик / UK стрік / AR/FA borrowings = бессмысленны),
  использовать локальный эквивалент; only en may use 'streak'». Один non-English-wide
  пункт покрывает все 13 (не только RU) — совпадает с durable owner-rule.
- PR #267 (sage day-context, mig 412) MERGED в 10:13. PR #268 (streak-forbid) MERGED
  через GitHub API (worktree workaround) → **deploy run 4c6bafd success** (smoke
  /health + journalctl clean). Прод здоров.
- **Spawn-task создан:** копирайтерский конвейер на 11 langs (ar/de/es/fa/fr/hi/id/it/pl/pt/uk)
  — аудит+фикс транслита «streak» в `ui_translations` (RU/EN уже compliant). По
  copywriter-playbook, L1 для AR/FA/HI/PL. Это про USER-facing переводы, не LLM-промпты.

## Streak-транслит конвейер в ui_translations (mig 413, PR #269)

Закрывает spawn-task из сессии #268. **Что:** live-аудит `ui_translations.content`
(tools/streak_translit_audit.py, греп с вырезанием плейсхолдеров) нашёл транслит/raw
«streak» в **5 языках** (de/fa/hi/id/uk), 16 ключей, 61 строка. Не 11, как в задании —
ar/es/fr/it/pl/pt/ru уже compliant, EN не тронут.
**Зачем:** durable owner-rule — англицизм «streak» допустим только в en.
**Замены** (опора на уже-compliant ключи того же языка): de→Serie, uk→серія, id→runtun,
fa→رکورد, hi→सिलसिला/silsila. Грамматический род поправлен (de m→f, uk m→f, hi f→m).
**Применено LIVE:** mig 413 через psycopg2 (одна транзакция), post-apply аудит = 0 hits;
спот-SELECT подтвердил array-элементы (streak_7[1] изменён, [0]/[2] сохранены) + EN intact.
**mig HEAD → 413.** PR #269 открыт (не merged). fa/hi флагнуты на L1 native review (non-blocking).
**KB:** copywriter-playbook §5 дополнен 3 execution-уроками (live-аудит сужает scope;
целевое слово из compliant-ключей; смена слова → правка рода) + naturalized loanword ≠ свежий англицизм.

---

## mig 415: payment.stars_balance_warning L1 ×6 + babel PL plural runtime — PR #271

**Scope (2 closed tech-debts из MEMORY watchlist):**
1. `payment.stars_balance_warning` — mig 414 поставил EN-fallback для AR/FA/HI/ID/PL/PT.
   Этот текст шлётся ОТДЕЛЬНЫМ `send_message` прямо перед Telegram payment modal
   (handlers/payment.py:466) — английский в Devanagari/Arabic-чате перед оплатой
   = пробитое доверие. mig 415 закрывает 6 langs native L1. UK уже был L1 в mig 414.
2. PL plural runtime: `payment.dunning_d3` PL шаблон содержит «{streak_days} dni»;
   «1 dni» грамматически неверно (правильно «1 dzień»). Добавлен `babel>=2.12` +
   helper `_pl_day_word(n)` на CLDR PluralRule в `crons/subscription_lifecycle.py`.
   Применяется к fragment'у «{streak_days} dni» только в `lang='pl'`; если будущий
   copywriter уберёт «dni» — fallback на стандартный `.replace('{streak_days}', n)`
   (fail-safe pattern).

**Метод:**
- Live-аудит DB подтвердил: AR/FA/HI/ID/PL/PT для stars_balance_warning = EN-literal
  (matches mig 414 ←lang list). UK — L1 (mig 414 ──3 ──). EN/RU/DE/ES/FR/IT — canonical.
- Live-grep подтвердил: `stars_balance_warning` используется только в
  `handlers/payment.py:466` через `_lookup_translation` + inline EN fallback (defensive).
- `{streak_days}` placeholder — только в `payment.dunning_d3` (across all 13 langs).
- Реальных PL юзеров в проде = 0 (lang distribution: ru=7, en=4, es=2). Fix — pure
  hygiene для будущих юзеров, не user-impact-fix.

**Применено LIVE:** mig 415 через psycopg2 (одна транзакция), POST-VERIFY DO-block
прошёл (EN-collision check + NULL-check). **mig HEAD → 415.** PR #271 открыт.

**Тесты:** `tests/crons/test_subscription_dunning_pl_plural.py` — 5/5 passed.
Покрытие: singular «dzień» (n=1), few «dni» (2..4/22..24), teens trap (11..14),
regular many, end-to-end на реальном PL шаблоне с проверкой что «Zostały 3 dni»
(bucket label) остался untouched.

**Что НЕ сделано (зафиксировано как open):**
- Fiverr native L1 review для dunning d7/d3/d1 + buttons + plan_name (AR/FA/HI/PL)
  — эти ключи в проде L1 из mig 409, но Opus-generated. Делать ещё одну Opus-итерацию
  поверх — иллюзия progress; правильный путь — Fiverr ~$50-100 (owner decision).
  Остаётся 🟡 в MEMORY watchlist.
- ID/PT/UK dunning sign-off (тот же handover).

---

## 2026-06-01 (3) — Trial Soft Downgrade (PR #273, mig 416/417 PENDING APPLY)

**Контекст:** owner spec — open trial (без карты) истекает → юзер мягко падает на free, бот шлёт Sage-tone push с CTA «Вернуть Premium» только в локальное окно 9..21 (не будить).

**Поставлено (код в PR, миграции ждут apply):**
- `mig 416`: 3 RPC.
  - `cron_check_subscription_expiry` — добавлен предикат `AND payment_method != 'trial' OR payment_method IS NULL` (база из live `pg_get_functiondef` 2026-06-01, lesson safe-create-or-replace).
  - `cron_expire_trials(p_local_hour_min INT DEFAULT 9, p_local_hour_max INT DEFAULT 21)` — tz-gate в SQL через `EXTRACT(HOUR FROM now() AT TIME ZONE users.timezone)`. Логирует `event_type='trial_expired'` (новый). `BEGIN/EXCEPTION` для невалидных tz strings → silent skip.
  - `expire_trial_subscription(p_telegram_id BIGINT)` — ad-hoc single-user expiry, без tz-фильтра, для admin/тестов/будущего cancel-during-trial.
- `mig 417`: `payment.trial_expired` JSONB-array (3 Sage-variants) + flat `payment.trial_expired_cta`. L1: ru/en/de/es/fr/it/uk. EN fallback: ar/fa/hi/id/pl/pt. «Streak»/«стрик» N/A (тексты не упоминают).
- Python: `crons/trial_expiry.py` — `TrialExpiryCron` hourly :15. Array-aware random.choice (KB jsonb-array-python-consumer-blind-spot.md). EN-fallback graceful. `crons/__init__.py` + `main.py` зарегистрированы.

**Ключевой архитектурный выбор: tz-gate в SQL, не Python.** Если бы фильтровали в Python после транзита `status='expired'`, out-of-window юзеры превращались бы в ghost'ов «expired но не отправлено». SQL-gate держит `status='active'` до момента, когда юзер реально в окне → следующий час подберёт.

**Расписание:** существующий `subscription_lifecycle` daily 12:00 UTC остаётся для paid/dunning. `TrialExpiry` отдельный hourly :15 (overlap с `freeze_pusher :15` OK — разные RPC).

**Skip на регрессии `users.subscription_status`:** live audit показал что `cron_check_subscription_expiry` УЖЕ сбрасывает на 'free' + resets `mana_max`/`mana_current` (mig 362 redesign закрыл это). Owner spec был основан на устаревшей версии. Фикс не нужен.

**Урок: race-condition на номера миграций (4-й случай).** При старте брал 415/416. К моменту коммита origin/main продвинулся: PR #271 закрепил 415 (stars_balance L1), PR #272 удалил `scripts/stars_balance.py`. Pre-push `git diff origin/main..HEAD --stat` (правило 12.2 global) поймал ОБА раза — переименовал 415→416, 416→417, rebase'ил дважды. Без правила 12.2 PR #273 затёр бы 132 строки `scripts/stars_balance.py`.

**Tests:** 5/5 unit pass (`tests/crons/test_trial_expiry.py`). Integration tests (`tests/integration/test_mig416_trial_soft_downgrade.py`) написаны но require apply + DATABASE_URL. p95 benchmark deferred — после apply на VPS.

**Open / для следующего агента:**
- 🟡 **Apply pending:** `python3 tools/apply_trial_soft_downgrade.py` после merge PR #273 (owner dismissed apply-during-session prompt).
- 🟡 **Integration tests + p95** на VPS после apply.
- 🟡 **L1 native review** для AR/FA/HI/ID/PL/PT trial_expired (EN fallback сейчас). Pickup при следующей копирайтер-сессии.
- 🟢 Никаких пересечений с PR #271 stars_balance (разные scope полностью).

## Admin Stars dashboard — Stars CLI (#272) + daily digest cron + /admin_stars (#275)

Owner спросил «куда приходят Stars и как посмотреть» + затем «сделай все три инструмента». Sделано в 2-х PR'ах + 1 spawn-task на CLDR plurals.

### Что в проде (PR #272 merged)

`scripts/stars_balance.py` — CLI на VPS: `getMyStarBalance` + `getStarTransactions` → human-readable таблица. USDT estimate (gross + net после Telegram ~30% fee). Запуск:
```bash
sudo -u taskbot bash -c "cd /home/taskbot/noms && set -a && source .env && set +a && \
    /home/taskbot/noms/venv/bin/python scripts/stars_balance.py"
```

**Live на 01.06:** баланс @nomsaibot = **580 ⭐** (~$2.44 net), 1 IN транзакция от tid 1670095403 (Евгения) — первая реальная Stars-покупка.

### PR #275 (open) — Daily digest + /admin_* commands

3 уровня доступа в одной session:

1. **`crons/stars_digest.py`** — BaseCron subclass, daily 06:30 UTC (между `subscription :00` и `reminders :20`). Filter транзакций за последние 24h. **Молчит когда 0 tx** — не спамит admin. При активности — HTML message: balance + IN/OUT counts + last 10 tx с partner info.
2. **`/admin_stars [N]`** — баланс + last N в чат (default 20, max 50). HTML render.
3. **`/admin_stars_export [N]`** — CSV файл через sendDocument (default 100, max 500). Пагинация для N>100 (Telegram API cap).

**Fast-path в webhook_server.py** — text starts with `/admin_stars` AND `chat_id == ADMIN_CHAT_ID` → handler bypass dispatch_with_render/menu_v3/ui_screens. Pattern аналогичен `admin_payout_*` callbacks (payout_handler.py).

**Файлы:** crons/stars_digest.py (+161) + handlers/admin_stars.py (+292) + crons/__init__.py (+2) + main.py (+16) + webhook_server.py (+17) = 5 files, +488/-0.

### Race-condition с PR #273 (Trial Soft Downgrade)

Я начал перед merge PR #273. После завершения работы — rebase нашёл что origin/main продвинулся (PR #273 merged), `crons/__init__.py` и `main.py` уже содержат `TrialExpiryCron`. **Git auto-merged без конфликтов** — наши изменения NON-OVERLAPPING (разные позиции в list import + разные scheduler jobs). После rebase + force-with-lease: 5 files, +488/-0 (без удалений), CI sanity audit зелёный.

**Lesson durable:** добавление в `crons/__init__.py` `__all__` через 2 разных PR не конфликтует если оба используют отдельные строки в imports + не модифицируют существующие строки. Это безопасный pattern для parallel agent work.

### 3 spawn-task'а отправлены (отдельные сессии)

- **L1 копирайтер AR/FA/HI/PL** + PL plural — chip выпущен (статус: запущен owner'ом)
- **P2.1 Trial Soft Downgrade** — chip → owner запустил → закончил PR #273 (merged ранее)
- **CLDR plural runtime для RU/UK/AR** — chip → owner запустил (отдельная сессия)

### MEMORY size

Не проверил. Кандидат на `/anthropic-skills:consolidate-memory` (last check 31.05 = 30 KB > 20 KB порог).

---

## CLDR Plural Runtime — расширение PL → RU/UK/AR + 4 Romance (mig 418, PR pending)

**Контекст.** mig 415 (PR #271, 2026-06-01) ввёл babel CLDR plural ТОЛЬКО для PL
в `crons/subscription_lifecycle.py:_pl_day_word`. Owner правильно заметил: «другие
языки тоже имеют похожую проблему». Это spawn-task этой сессии.

**Аудит** (psycopg2 grep по `{streak_days}|{days}|{count}|{n}` в 13 langs `ui_translations`):

| Шаблон                            | Placeholder | Plural-bug в langs |
|----------------------------------|-------------|--------------------|
| `payment.dunning_d3` **← scope** | `{streak_days}` | ru/uk/pl/ar/es/fr/it/pt (8) |
| `payment.days_remaining`         | `{days}`    | en/pl/ar/es/fr/it/pt (deferred) |
| `payment.renewal_reminder`       | `{days}`    | same (deferred) |
| `payment.days_remaining_template`| `{days_left}` | same (deferred) |
| `food_log.header_*_multi`        | `{count}`   | ru/uk/pl/ar/es/fr/it/pt (deferred) |
| `referral.cta_active_2`          | `{count}`   | same (deferred) |

`dunning_d3` шёл в pre-payment push — высокий приоритет (как и stars_balance_warning
в mig 415). Остальные — out of scope этой PR; задокументированы в
[[i18n-cldr-plural-runtime]] §«Где ещё ждёт миграции».

**Архитектура — Approach Б (новый placeholder, не два ключа).**

```
шаблон (DB)                     Python (cron)            выход
─────────────                  ─────────────              ──────
"серия из {streak_days_word}"  →  format_count(3, "ru",
                                     DAY_FORMS["ru"])
                                          ↓
                                       "3 дня"
```

Шаблон не знает грамматики; helper резолвит «N word» по CLDR-категории.

**Что сделано:**

1. **`services/i18n_plural.py`** (новый, +172 строки) — `plural_word(n, lang, forms)`,
   `format_count(n, lang, forms)`, табличка `DAY_FORMS` для 13 langs.
   - CLDR rules через `babel.plural.PluralRule` для ru/uk/pl/ar (≥3 категории).
   - Default 2-cat rule (n==1 → one, иначе other) для остальных.
   - Robust: missing category → fallback `other`; missing `other` → `""`; bad
     CLDR call → log + `other`.
2. **`crons/subscription_lifecycle.py`** — удалён hand-coded `_pl_day_word`
   (mig 415 deprecated), заменён на универсальный resolve `{streak_days_word}`.
   PL branch `if lang == "pl":` исчез — теперь работает для всех 8 langs.
3. **`migrations/418_dunning_d3_streak_days_word_placeholder.sql`** —
   `{streak_days} <word>` → `{streak_days_word}` в ru/uk/pl/ar/es/fr/it/pt.
   EN/DE (hyphenated `{streak_days}-day streak`), ID/HI/FA (no inflection) не
   тронуты. Idempotent (UPDATE с LIKE-guard), post-verify DO-block с EXCEPTION.
   ✅ applied LIVE 2026-06-01, re-apply no-op verified.
4. **Tests:** 188 unit (`tests/services/test_i18n_plural.py` — CLDR категории
   ru/uk/pl/ar/2-cat для en/de/es/fr/it/pt, DAY_FORMS для всех 13 langs,
   robustness) + 18 integration (`tests/crons/test_subscription_dunning_pl_plural.py`
   обновлён — старый тест импортировал удалённый `_pl_day_word`, заменён
   end-to-end на новый `{streak_days_word}` для всех 8 langs + sanity EN/DE).
   538 passed в `tests/crons/ + tests/services/`.
5. **KB:** [[i18n-cldr-plural-runtime]] + index.md entry.

**Урок — race-condition номера миграций (5-й случай).** При старте взял 416 (после
fetch'а main). Параллельно другая сессия (Trial Soft Downgrade, owner spawn-task)
заняла 416 + 417 через PR #273 merged в процессе моей работы. `git fetch
origin main && git rebase origin/main` (правило 12.1) обнаружил конфликт — rebase
auto-merged, но `migrations/416_*.sql` дублирован. Pre-rebase audit `git ls-tree
origin/main migrations/` показал 416 + 417 в main → renamed `416 → 418` + 4
internal references (header, cron comment, test docstring, error message). Это
**5-й случай** коллизии за месяц — pre-push hook + CI check ([[migration-collision-guard]])
работают для текстового guard'а, но для авто-rename'а нужна интеграция с
`gh pr list` или session-local lockfile. Бэклог.

**Open для follow-up (НЕ блокирует merge):**

- 🟡 Расширить на `days_remaining` / `days_remaining_template` / `renewal_reminder` —
  все используют `{days}` + literal "days/dni/дн.". Та же табличка `DAY_FORMS`,
  ничего нового в helper'е.
- 🟡 `food_log.header_*_multi` + `referral.cta_active_2` — новая табличка
  `ITEM_FORMS` (или `ENTRY_FORMS` — копирайтер выберет). Та же механика.
- 🟡 PR ассертит `subscription_status='premium'` остаётся в `cron_get_renewal_candidates`
  (mig 402), что не пересекается с trial_soft_downgrade mig 416 (статус `'free'`
  через `cron_check_subscription_expiry`). Sanity-checked в коде, не нужен fixup.

**Files:** 5 changed
  - migrations/418_dunning_d3_streak_days_word_placeholder.sql (+196, applied)
  - services/i18n_plural.py (+172)
  - crons/subscription_lifecycle.py (-22 hand-coded _pl_day_word, +15 helper resolve)
  - tests/services/test_i18n_plural.py (+217)
  - tests/crons/test_subscription_dunning_pl_plural.py (rewritten, +110 cover 8 langs)

---

## 2026-06-01 (4) — P2.1 Trial card-CTA в Dunning d3/d1 (PR #276, mig 418 LIVE)

**Контекст:** PR #273 (Trial Soft Downgrade) уже мерджнут owner'ом. Owner поднял P2.1: для trial-юзеров в dunning'е за 3 и 1 день — менять копию с paid-renewal на «привязать карту». Open-trial без карты → существующий paid-frame («продлим Premium», «потеряешь streak») — wrong framing для триальщика, который не платил. Actionable ask — карта.

**Поставлено + LIVE applied:**
- `mig 418`: 3 новых ключа × 13 langs.
  - `payment.dunning_d3_trial` — JSONB-array (3 Sage-варианта).
  - `payment.dunning_d1_trial` — JSONB-array (3 Sage-варианта).
  - `payment.dunning_btn_link_card` — flat string (💳 эмодзи).
  - L1: ru/en/de/es/fr/it/uk. EN fallback: ar/fa/hi/id/pl/pt.
- Python `crons/subscription_lifecycle.py`:
  - Helper `_resolve_text(raw)` на module-level — array-aware (JSONB-list → random.choice, str passthrough, '' для missing/empty). Заменяет inline `payment_t.get(...)` в `_send_renewal_reminders`.
  - Trial-branch при `sub_status=='trial' AND bucket IN (1,3)`: ключ → `dunning_d{bucket}_trial`, primary button → `dunning_btn_link_card`. Callback **остаётся `cmd_premium_plans`** — экран сам ведёт в Stripe checkout который захватит карту. Defensive: bucket=7 (RPC скипает, но если когда-нибудь отдаст) → fallback на paid d7 + activate button (НЕ link-card).
- Tests: `tests/crons/test_subscription_dunning_trial_card.py` — 8 unit-тестов (resolve_text, trial d3 + d1, premium регрессия, EN fallback, defensive d7). Все pass. Regression: PL plural (5/5) + trial_expiry (5/5) ещё зелёные.

**Apply сессия:** owner дал permission на psycopg2. В одной сессии применил **416 (PR #273 RPCs) + 417 (PR #273 trial_expired copy) + 418 (этот PR card-CTA)**. PR #273 был merged до apply миграций — auto-deploy ставил код, но РПС были not-found на проде до apply (cron upload bы алертил в admin chat, не катастрофа).

**Дизайн-выборы (документировал в PR #276):**
- Тот же callback `cmd_premium_plans`, не новый `cmd_link_card_now` — экран plans уже ведёт в Stripe checkout. Owner может позже сказать «нет, нужен one-tap скип plans → checkout» — отдельный хендлер.
- Без `{streak_days}` placeholder в trial-копии — у триального юзера streak обычно 0-7 дней, упоминать его = манипуляция. Anti-shame — каждый вариант предлагает «no harm done» / «спокойно перейдёшь на free» при отказе.

**Race-condition lesson (5-й случай за эту сессию):** между моим коммитом и push'ем PR #275 (StarsDigestCron) был мерджнут. Rule 12.2 (`git diff origin/main..HEAD --stat` перед push) показал что мой push затёр бы 470 строк stars_digest/admin_stars. Rebase → чисто. **Это уже привычка — каждый sanity-check ловит реальную ошибку.**

**Open / для следующего агента:**
- 🟢 P2.1 LIVE — копия применена; auto-deploy выкатит код когда PR #276 merge'ится. Cron daily 12 UTC → следующая фактическая отправка trial-d3/d1 push'ей — когда trial-юзер попадёт в окно (≥1 активный trial в проде).
- 🟡 Smoke verify: при следующем cron-tick'е `subscription_lifecycle` — посмотреть на `journalctl -u noms-cron`, что новые keys резолвятся, не падает в EN fallback из-за typo.
- 🟡 L1 native review для ar/fa/hi/id/pl/pt новых ключей (EN fallback сейчас) — pickup в next Fiverr batch.
