# Daily Log: 2026-04-08

## Sessions (migrated from CLAUDE.md Changelog)

### Session: 2026-04-08 — Migration 039: Cleanup views and columns
- Что: Пересоздан `v_user_context` без колонки `credits`; удалён `ai_response_payload` из `food_logs` (100% NULL)
- Зачем: Устаревшие данные засоряли схему; `credits` больше не должен попадать в Dispatcher
- Применено: ✅ Supabase SQL (psycopg2)

### Session: 2026-04-08 — Migration 040: image_url в log_meal_transaction
- Что: Добавлен 6-й параметр `p_image_url TEXT DEFAULT NULL` в RPC `log_meal_transaction`; сохраняется в `food_logs.image_url`
- Зачем: Фото распознавались, но URL файла никогда не сохранялся в БД
- Применено: ✅ Supabase SQL (psycopg2)

### Session: 2026-04-08 — n8n AI Engine: передача image_url
- Что: В workflow `03_AI_Engine` (ID: `24ZOwWEmdGOYS2EH`) — нода `Parse AI` строит URL из `file_path`; нода `RPC Log Meal` передаёт `p_image_url`
- Зачем: Связка с migration 040 — теперь URL реально пишется в БД
- Применено: ✅ n8n API (PUT /workflows)

### Session: 2026-04-08 — Migration 041: CHECK constraints
- Что: `chk_weight`, `chk_height` (≥100, существующий юзер с 100.2), `chk_target_cal`, `chk_mana_nonneg`, `chk_food_cal_nonneg`
- Зачем: Страховочная сетка против аномальных данных в обход RPC
- Применено: ✅ Supabase SQL (psycopg2)

### Session: 2026-04-08 — Migration 042: League NPC Bot System
- Что: +колонки `is_bot`/`bot_xp_per_day` в `users`; 25 NPC-ботов (id -1001..-1025); пересозданы `cron_create_league_groups`, `cron_process_league_week`, `get_league_standings`; фильтр `is_bot=false` во всех cron-RPCs
- Зачем: Лиги не работали с малым числом юзеров — Duolingo-подход с искусственной конкуренцией
- Применено: ✅ Supabase SQL (psycopg2); группа для текущей недели создана вручную (1 реальный + 19 ботов = 20 участников)

### Session: 2026-04-08 — n8n Stateful UI: sendMessage → editMessageText (04_Menu + 04.2)
- Что:
  1. **04_Menu** — Send Edit Options и Send Delete Confirmation заменены на HTTP Request `editMessageText`; удалены 4 промежуточные ноды (Remove Buttons + Passthrough × 2); добавлена Code нода Build Delete Result; Build Edit Options генерирует `reply_markup`
  2. **04.2_Edit_StatsDaily** — Send List, Send List1, Send Edit Prompt переведены на `editMessageText` + добавлен `message_id`; Send Delete Confirmation заменён на HTTP Request; Build List Message, Build Meal Detail, Build Edit Prompt дополнены `message_id`
  3. **callback_message_id** проброшен: Prepare for 04.2 (04_Menu) + Merge Data (04.2)
- Зачем: Stateful UI — при нажатии inline-кнопки сообщение ЗАМЕНЯЕТСЯ, а не создаётся новое. Чат остаётся чистым.
- Применено: ✅ n8n API (PUT /workflows sxzbSc61YuHFbV4i, YebaQhipJrKZcGRO)

### Session: 2026-04-08 — n8n: one_time_keyboard → false + [Профіль] → [Прогресс]
- Что:
  1. `one_time_keyboard: false` в 3 нодах: Send Main Menu (Back), Send Menu After Cancel, Send Add Food Prompt
  2. Inline-кнопка [Профіль/cmd_get_profile] → [Прогресс/cmd_progress] в Build Delete Result и Send Recharge Result
- Зачем: Reply-клавиатура пропадала после нажатия (one_time_keyboard=true + editMessageText не создаёт новых сообщений). Кнопка [Прогресс] приоритетнее [Профіль] для пользователей.
- Применено: ✅ n8n API (PUT /workflows sxzbSc61YuHFbV4i)

### Session: 2026-04-08 — CLAUDE.md: n8n Data Flow Patterns + Workflow Map
- Что: Добавлена секция "n8n Data Flow Patterns" (6 правил работы с потоками); дополнен Workflow Map (08.1_Quests, 08.2_League, 08.3_Friends, 08.4_Shop)
- Зачем: Агенты ломали потоки данных при правках — правила зафиксированы для передачи знаний
- Применено: ✅ Прямое редактирование CLAUDE.md

