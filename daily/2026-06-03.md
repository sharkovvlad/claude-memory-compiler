# 2026-06-03

## Reply-kb fix + онбординг-FSM разбор + 2 спавна (waist UX доводка)

**Контекст:** owner-тест — после ввода талии пропала reply-клавиатура; разбор онбординга для отдельного waist-шага.

### Reply-kb fix (PR #297, Python — deploy pending)
Корень: при text-input системная клавиатура телефона перекрывает reply-kb; восстановление делает save-toast «✅ Сохранено!» (`menu_v3._maybe_build_save_toast`), гейтнутый на `_PROFILE_V5_TEXT_INPUT_STATUSES`. `edit_waist` там не было (моя недоработка mig 432/438) → после ввода талии kb не возвращалась. Fix: +`edit_waist` в сет. Охват: только waist-edit юзеры (фича от 06-02, ~0 реальных) — mass-outage нет; /start восстанавливает.

### Reply-kb механика (полная карта — детально уйдёт в KB через спавн #2)
Reply-kb = постоянное chat-state Telegram. (Re)attach в `render_screen`: (1) `input_type='reply_kb'`; (2) `meta.reply_kb_entry=true` (stats_main/profile_main/progress_main/personal_metrics) + previous_status text_input; (3) `reply_kb_entry` + `pending_kb_reattach=TRUE` (save_via_callback sleep/stress mig 374); (4) `attach_main_kb_unconditional` (onboarding_success). + Python save-toast. **КРОН-ПУШИ reply-kb НЕ привязывают** — reminders/streak/trial/subscription все `inline_keyboard`. Gap: потерянную kb надёжно не вернуть кроме /start → hardening на food-log (спавн #2).

### Онбординг FSM (process_onboarding_input) — полная карта для owner + onboarding-агента
new→step_1 gender(edit_gender)→step_2 age(ask_age)→[♀15-50 maternal: status/trimester/lactation/cycle]→step_3 weight(ask_weight)→step_4 height(ask_height)→step_5 activity(edit_activity)→[training если не sedentary]→diet(onboarding_diet_question)→goal(edit_goal)→speed(edit_speed)→phenotype_quiz(edit_phenotype q1→q2/3/4→classify→phenotype_result)→onboarding:country→onboarding:timezone→**registered**. Квиз опционален (Пропустить). На phenotype_result сейчас кнопка cmd_edit_waist (без set_status → в онбординге не сохраняет — баг).

### Спавны (2 чипа)
1. **Онбординг-талия placement A** (owner approved): отдельный шаг `registration_step_waist` между Ростом и Активностью + экран `onboarding_waist` + [Пропустить]/[Назад] + FSM-ветка + убрать кнопку с phenotype_result + ×13 через субагента-переводчика (copywriter-playbook тон). Mockup в отчёте.
2. **Reply-kb hardening + KB-доку**: re-attach reply-kb на food-log (throttled раз/день, non-noisy) + задокументировать ВСЕ reply-kb attach/detach события в KB + зафиксировать gap «крон-пуши не привязывают».

PR #292/#296 (waist Pattern B + setter + ×13) merged ранее. PR #297 (reply-kb) — MERGED+deployed (edit_waist в restore-set, LIVE).

## KB audit (/kb-close --audit, 2026-06-03)
audit.py: 143 концепта; merge-candidates 13 (low-score ≤0.25, false-positives, не дубли); orphans 0; stale 17 (все warning, n8n-legacy, без действий). lint broken-link «errors» = format-noise (спавн на унификацию ссылок крутится отдельно). MEMORY 22.97KB/153→ убрана 1 done-строка (PR#293 merged) → ~22.8KB/152 (под 25KB hard; полную consolidate не гонял — делал в прошлый audit 31.9→22.1). Бэкап MEMORY.backup-2026-06-03.md.

## ✅ Онбординг-талия placement A РЕАЛИЗОВАН (mig 439, PR #299)

Спавн #1 (выше) закрыт. Талия — отдельный шаг `registration_step_waist` между ростом
(`ask_height`) и активностью (`edit_activity`).

**mig 439 (applied LIVE):**
- `ui_screens.onboarding_waist` (text_input, `meta.show_back_inline=true`, back→ask_height,
  validation 40–200/`errors.invalid_waist`) + `workflow_states.registration_step_waist`
  (FK target, next_step=registration_step_5). **Порядок в миграции важен: ui_screens ДО
  workflow_states** — `workflow_states.screen_id` имеет FK на `ui_screens` (поймал на dry-run).
- Кнопки: `[⏭ Пропустить]` (`cmd_skip_waist`, переиспользует существующий `buttons.skip` ×13 —
  DRY, нового ключа не плодил) + `[🔙 Назад]` (`cmd_back`, icon_back).
- `questions.waist_onboarding` ×13 (субагент-переводчик по copywriter-playbook; native, не
  машинный; ar/fa RTL). **Quoting-гоча:** `json.dumps` даёт ДВОЙНЫЕ кавычки — для SQL нужны
  одинарные (`'…'::jsonb`) + `.replace("'","''")` для апострофов (fr/it). JSON-строка с `\n`
  → jsonb парсит в реальные переводы строк.
- `process_onboarding_input` (база — live `pg_get_functiondef`, 4 surgical-правки): height→waist;
  новая ветка waist (text→setter→step_5 / skip→step_5 / cmd_back ловит mig 390 unified-блок);
  back-карта `step_5→waist→step_4`; `/start`-карта `+waist→onboarding_waist`.
- DELETE `cmd_edit_waist` с `phenotype_result` (ложная связь с квизом + не сохранял).

**Сеттер mig 438 переиспользован** (Pattern B — статус не двигает; в онбординге двигает FSM).
Recalc `calculate_user_targets` на шаге waist безопасен с частичными данными (activity ещё NULL) —
он же зовётся раньше на maternal-шаге.

**router.py (PR #299, deploy pending):** `registration_step_waist` в `NUMERIC_INPUT` (числовой
ввод→section 8) + `BUTTON_ONLY` (cmd_skip_waist/cmd_back→section 9→onboarding) + `ONBOARDING_STATUSES`
(защита от menu_v3/profile capture). Новые non-overlapping строки.

**E2E (dispatch_with_render, синтет.юзер male, ROLLBACK-txn) — все зелёные:** рост 180→waist-экран;
талия 85→saved+step_5+activity; skip→activity без талии; back→ask_height; невалид 300→re-render+
`validation_error/invalid_waist`. Клавиатура waist рендерится (`text_input_inline_back=True`→skip+back).
Reply-kb финала (`onboarding_success`) не затронут (онбординг-FSM не триггерит save-toast).

**Durable lesson (новое в этой статье + onboarding-v3-map):** для нового онбординг-шага = 3 провода:
(1) `workflow_states` row (FK!) **после** `ui_screens`, (2) router 3 сета (NUMERIC/BUTTON_ONLY/ONBOARDING),
(3) FSM-ветка + back-карта + /start-карта в `process_onboarding_input`. cmd_back лучше отдавать
unified mig 390 блоку (не дублировать в ветке).

---

## «Мой день» показывает фразу про удалённый приём — stale insight cache (mig 440, PR #302, applied LIVE)

**Баг (репро owner'а):** залогировал первый приём → удалил (кнопка) → открыл «Мой день» → фраза Номса всё ещё про удалённую еду, при том что цифры показывают 0 ккал. Нарушение консистентности «число ≠ слова Номса».

**Корень (по коду):** фраза «Мой день» — это **кэш** `users.my_day_insight_*`, чья свежесть (`menu_v3.py:_my_day_cache_fresh`) считается **по времени (≤4ч)+локали, НЕ по содержимому дня**. Путь удаления (`menu_v3.py:_handle_delete_meal_now:926`) чистил кэш **только в in-memory ctx** запроса (`replace(ctx, my_day_insight_at=None)`), а реальную инвалидацию перекладывал на fire-and-forget regen, который (а) гонится со следующим тапом и (б) молча `return False` при любом сбое LLM/JSON (`sage.py:403`). Итог: следующее открытие «Мой день» отдаёт «свежую по времени» устаревшую фразу; regen на этом открытии даже не запускается — кэш «свежий» по 4ч-правилу.

**Решение A (внедрено, mig 440, RPC-only):** инвалидировать кэш **атомарно** в `delete_meal_by_id`:
`WITH deleted AS (DELETE FROM food_logs WHERE meal_id=p_meal_id RETURNING telegram_id) SELECT count(*),max(telegram_id) INTO ...` → `IF v_telegram_id IS NOT NULL THEN UPDATE users SET my_day_insight_at=NULL ...`. NULL-им только timestamp (freshness-гейт падает на `at IS NULL`); stale-текст безвреден (не отдаётся), перезапишется regen'ом. Python-правок не требуется.

**Verify:** NLM (delete_meal_by_id = hard delete из `food_logs`, FK=`telegram_id bigint`, нет `meals`/`user_id`/`deleted_at`) → live `pg_get_functiondef` (база миграции) → applied LIVE psycopg2 → ROLLBACK-тест: insert 2-row meal tid 417002669 + `at=now()` → `delete_meal_by_id` → `deleted_count=2`, rows=0, **`at=NULL`** ✅. p95 (с мака, RTT-доминирован) 181ms; добавленная операция = 1 индексированный UPDATE по PK в том же вызове (не лишний round-trip), server-side ~sub-ms, delete = редкий не-hot путь.

**Durable lesson (→ KB [[concepts/my-day-llm-insight]]):** кэш со свежестью **по времени, а не по содержимому** = баг «stale-content-served-as-fresh». Любая мутация дня (add/edit/delete) обязана инвалидировать кэш; in-memory ctx-инвалидация НЕ персистится в БД; fire-and-forget regen ненадёжен (гонка + silent no-op). Место инвалидации — сама mutation-RPC (атомарно), не Python-хендлер.

**Решение B (бэклог, объяснено owner'у простым языком):** content-aware freshness — штамповать кэш «отпечатком дня» (meals_count + last_meal_at / day-version счётчик), `_my_day_cache_fresh` сверяет отпечаток с текущим состоянием → закрывает ВСЕ пути мутации разом, а не только delete.

---

## Reply-kb re-attach на food-log → WONTFIX (исследование, кода НЕ применялось)

**Контекст:** owner-задача — hardening: «любой юзер вернёт reply-kb просто залогировав еду». Исследовал, предложил подход, **владелец отменил (WONTFIX)** после продуктового ревью.

**Что выяснено (NLM + live + код, всё durable → вынесено в KB):**
- Комментарий Сейджа **встроен в карточку** (`{noms_comment_block}` в `food_log.confirmation_text_*`), карточка = 1 сообщение с inline `[Исправить][Удалить]` (`handlers/food_log.py:270`). Отдельного Sage-сообщения нет.
- Telegram: нельзя inline + reply-kb в одном msg; `deleteMessage` reply-kb-носителя убивает kb; стикер не несёт reply_markup; текст-индикатор «анализирую» удаляется `delete_thinking()`.
- `build_main_reply_keyboard()` → только КЛЮЧИ, метки резолвит Python (`_build_main_reply_keyboard_markup`); proxy-слой translations не имеет.
- Текст-индикатор throttled раз/UTC-день через `last_text_indicator_date` (set прокси ДО хендлера).

**Перебор носителей (все отвергнуты):**
1. Новый carrier-пузырь после карточки → owner: визуальный шум.
2. Текст-индикатор «анализирую» (persist + reply-kb) → owner: «зависший баг», грязный чат.
3. Вынос комментария Сейджа в отдельное сообщение → owner: ломает Golden Path (карточка на 2 сообщения, скролл за калориями).

**WONTFIX-аргументация владельца (Архитектор/UX):** Golden Path (лог еды) неприкосновенен; потеря reply-kb — edge-case ~1%; **риск уже закрыт ежедневными чек-инами сон/стресс** (mig 374 `reattach_main_kb_after_save` → `pending_kb_reattach` → `render_screen` возвращает kb каждый день при ответе на чек-ин). Дублировать в еде не нужно.

**Зафиксировано в KB:** новый концепт [[concepts/reply-keyboard-lifecycle]] — ПОЛНЫЙ список всех точек attach/detach/revive reply-kb (4 ветки render_screen + save-toast + lang-refresh + 30-day /start + чек-ины mig374) + GAP «крон-пуши НЕ привязывают» + WONTFIX-решение (чтобы не переоткрывали). + строка в `index.md`.

**Кода/миграций НЕ менял.** Plan-файл `~/.claude/plans/memoized-swimming-lightning.md` (черновик, безвреден).

---

## ~13:00–15:00 — Инцидент классификации типов контента + каскад фиксов (8 PR)

**Триггер:** обсуждение мультифото/качества распознавания → вскрылись регрессии в том, как классифицируется тип входящего контента. Корневой класс бага: тип контента решается в **ТРЁХ независимых местах**, которые обязаны быть синхронны:
- `dispatcher/router.py` (`_is_junk_content`, `_is_image_document`) — маршрут;
- `telegram_proxy.py:_content_needs_indicator` — показывать ли «думающий» индикатор;
- `handlers/location.py:_LOCATION_HANDLER_STATUSES` — какие статусы = location-flow.
За одну сессию они разъехались **трижды**.

**PR (все merged + LIVE):**
- **#294** silent-drop: document(image/*)→AI, audio→AI, прочее→help (`messages.spam_protect` ×13, переиспользован). НО внёс 2 бага ↓.
- **#298** hotfix: `_content_needs_indicator` не показывал индикатор для image-doc/audio (фото-файлом → нет «думания»). Зеркалит #294.
- **#300** КЛЮЧЕВОЙ: (а) **откат audio→AI** — audio теперь junk/help (Whisper лимит 25МБ, файлы до 2ГБ, не еда); (б) **геолокация → location-хендлер ТОЛЬКО в `_LOCATION_HANDLER_STATUSES`** (онбординг country/timezone + edit_country/timezone), прочие статусы → help. Чинит **зависание** (registered гео-пин форвардился в мёртвый n8n 02.1_Location → стикер висел вечно); (в) **size-cap image-doc 10МБ** (file_size из апдейта, без скачивания); (г) объединил `_is_image_document` (proxy импортирует из router) + lazy-import location-статусов = единый источник; (д) **контракт-тест** `tests/test_content_classification_contract.py` (инвариант «индикатор ⟺ распознавание»).
- **#301** `image_detail="high"` в `_vision_call` (дефолт auto→может low-res → теряются мелкие позиции «черри+редис→овощи»). Temperature отложен на eval-трек.
- **#303** очистка think-стикера в junk-ветке webhook (`AUTHORITATIVE_JUNK` теперь зовёт `delete_thinking`) — геолокация-зомби после help-сообщения. Онбординг-стикер НЕ затронут (он в `target=location`, не junk).
- **#304** mig **440** `edit_food.prompt_variants` ×13: подсказка «чёткое фото сверху + подпись / голос точнее» в [Исправить]-промпт. **APPLIED LIVE + verified** (13/13, базы сохранены — истинный APPEND через SQL-конкат, не replace; черновик субагента хардкодил базы и затёр бы ES «¡Vale...»). Idempotent guard `NOT LIKE '%💡%'`. L1 review нужен для 11 не-EN/RU langs (суффикс).
- **#305** CI workflow `pr-content-classification.yml` — гоняет контракт-тест на PR, трогающих router/proxy/location/food_log (advisory).

**Живые тесты (owner, скрин):** image-файл→распознан, mp3→help (без стикера), геолокация→help. Всё ок.

**Migration HEAD: 440** (applied). Деплои success, health ok, ошибок в логах нет.

**Durable урок (в MEMORY + KB):** правка классификации контента = обновить ВСЕ ТРИ классификатора + прогнать контракт-тест. Поспешное расширение в #294 (audio→AI + индикатор разъехался) дало 3 регрессии за день.

**KB:** новый концепт [[concepts/content-type-classification.md]] (паттерн + правило синхронизации + хроника инцидентов + контракт-тест как enforcement) + [[concepts/food-recognition-prompt-lab.md]] (текущие промпты, дефекты, дизайн оффлайн-эвала, рычаги image_detail/temperature). Обе в index.md.

**Отложено (отдельные сессии):** штрихкод (локальный декодер EAN + OpenFoodFacts), каскад FatSecret/OpenFoodFacts (точные макросы), temperature-tuning (после golden-set эвала), full pytest-CI gate (блокирован 4 пред-существующими падениями — payment-моки + router callbacks).

**Коллизия миграций (исправлено):** мой edit_food tip изначально был mig 440, но PR #302 (my-day stale-insight) влил свой mig 440 в окно моего stale-ref чека (не сделал `git fetch` прямо перед проверкой — §12.1). На main оказались два 440. PR #306 переименовал мой → **441** (поздний переименовывается; обе уже применены к БД, разные объекты, файл-rename ничего не реаплаит). Итог: 440 = #302 delete_meal, **441 = edit_food tip**.

## reply-kb ещё раз: порядок toast (PR #307)
После PR #297 (edit_waist в restore-set) тост «Сохранено» срабатывал, но reply-kb всё равно пропадала на Pattern-B edit_waist. **Корень — порядок:** save-toast (attach_reply_kb carrier) prepend'ился ПЕРЕД main-render. weight/age/height ок (навигируют на hub personal_metrics → hub render несёт reply-kb последним), а edit_waist остаётся на себе (не hub) → последнее сообщение без reply-kb → Telegram не показывает kb (reply-kb привязан к message_id последнего сообщения). Fix (PR #307): для status=edit_waist toast APPEND в конец (как _maybe_build_reply_kb_refresh). **Durable урок для reply-kb KB-концепта (агент #2): Pattern-B (stay) text-input экраны → reply-kb carrier append'ить в КОНЕЦ envelope, не prepend; prepend работает только если после save навигация на reply_kb_entry hub (который сам attach'ит kb последним сообщением).** Onboarding-waist (mig 439, router registration_step_waist) — спавн #1 отработал, merged.

---

## Waist числовой UX: возврат на хаб (Вариант A) + {example} подстановка (mig 442, PR #308)

**Owner-тест (скрин):** 2 бага числовых полей.

**BUG 1 — литерал `{example}`:** `errors.not_a_number` ×13 содержит плейсхолдер
`{example}`, но `menu_v3._validation_error_envelope` подставлял текст сырым
(`_lookup_translation`, без template-vars) → юзер видел literal. Ключ отдаётся
**только** Python-предвалидацией (`services/screen_validation.py:184`); SQL
`validate_text_input` всегда возвращает `error_key` правила (`invalid_*`, без
плейсхолдера) — поэтому баг виден лишь в меню-пути (все числовые edit-поля
вес/рост/возраст/талия идут через menu_v3 prevalidation). Рядом «спящие» ключи
`*_too_high/_too_low` с `{value}/{min}/{max}` — та же дыра, но не привязаны к
текущим validation_rules. **Фикс:** подстановка через существующий
`resolve_translation_text(ctx, key, template_vars)` + новый
`screen_validation.validation_template_vars(status)` (тянет example/min/max из
`validation_rules` — no-hardcode). mig 442 кладёт `example` в validation_rules
(вес 70 / рост 175 / возраст 30 / талия 82 / cycle 28).

**BUG 2 — талия «спрашивает дважды» (Pattern-B-stay ловушка для text-input):**
edit_waist (mig 438) после ввода ре-рендерил инструкцию-промпт (`next_step_code=
edit_waist` само-петля + сеттер no-advance) → выглядело как повторный запрос, и
следующий текст ловился как талия. **Owner approved Вариант A** (mockup в сессии,
AskUserQuestion): числовой ввод ≠ пикер → вести себя как вес/рост/возраст. mig 442:
`workflow_states.edit_waist.next_step_code=registered` + `ui_screens.edit_waist`
next_on_submit/parent/back → `personal_metrics`. После ввода → возврат на хаб
(meta.reply_kb_entry=true → reply-kb от хаба). **Сеттер НЕ трогал** (общий с
онбордингом; статус двигает dispatcher через next_step_code — `process_user_input`
стр.441 перезаписывает status даже при сеттере-no-advance). Recalc сохранён в
тосте `✅ Талия: 110 см · Белок: 142 г/день` (`messages.waist_saved` ×13, перевод-
субагент) — то, что owner ценил в Pattern B.

**Координация PR #297/#307 (оба merged):** #297 (edit_waist в
`_PROFILE_V5_TEXT_INPUT_STATUSES`) **сохранён** — тост для waist ещё строится.
#307 (append-спецслучай тоста для edit_waist) **откатан** — при Варианте A waist
возвращается на хаб, тост prepend'ится как у веса; append стал избыточен.
Унифицировал call-site: единый prepend для всех text-input полей. Закрывать
нечего (уже в main).

**Онбординг (registration_step_waist) НЕ затронут** — отдельный workflow_state +
`process_onboarding_input` явно двигает FSM на step_5 (advance, ловушки нет).
Проверено E2E.

**Верификация:** mig 442 **applied LIVE + committed** (E2E `dispatch_with_render`
сначала ROLLBACK-txn, затем commit). Меню «110»→registered+waist=110+белок 146→142
+рендер personal_metrics (не повтор); след.текст «sdlfksdf»→не ловится как талия;
онбординг «85»→registration_step_5 (questions.activity). Bug 1 против живого кеша:
`{example}`→82/70/175/30. Тесты: 596 passed +5 регресс (substitution / recalc-toast
/ validation_template_vars). p95: +1 RPC get_my_plan_business_data на редком
waist-save пути (не hot).

**DB-before-code window:** mig 442 live, Python (PR #308) deploy pending. Старый
deployed Python с #307-append + mig 442 = waist всё равно навигирует на
personal_metrics (SQL), тост «Сохранено» без recalc → безопасный transitional
state. После deploy PR #308 — recalc-тост + единый prepend.

**Durable lesson (→ KB headless-picker-pattern §Pattern B для text-input,
ПЕРЕСМОТР):** числовой text-input edit-экран ≠ пикер. Pattern-B-stay
(next_step_code=self + setter no-advance) для числа = ловушка «спрашивает дважды»
+ следующий текст ловится как поле. Правильно: **возврат на hub** (как
вес/рост/возраст, next_step_code=registered + screen.next_on_submit=hub), recalc
→ в тост. Pattern-B-stay остаётся owner-mandated дефолтом для ПИКЕРОВ (кнопки),
не для числа. Доп.: `{var}` в текстах validation-ошибок подставляется только если
рендерер идёт через template_engine (resolve_translation_text), не сырым lookup'ом.

### Follow-up (owner-вопрос про дробные): тост-консистентность + находка «запятая»
Проверка дробного ввода live (ROLLBACK) вскрыла дефект МОЕГО тоста: брал waist из
сырого `decision.text` → расходился с кнопкой/БД (NUMERIC(5,1) округляет 40.05→40.1
и печатает 1 знак 110→«110.0»). Fix (commit 4aa5117): тост берёт waist из того же
`get_personal_metrics_business_data`, что и кнопка [Талия] (+protein из my_plan) —
2 read-RPC на редком пути. Теперь 110→«110.0», 40.05→«40.1», 82.5→«82.5» везде
совпадают. Дробное и так принималось (NUMERIC(5,1)).

**Находка (pre-existing, НЕ фиксил — флаг владельцу):** запятая `82,5` → ошибка
`invalid_waist` («диапазон 40-200», вводит в заблуждение). Причина: Python-
предвалидация (`_coerce_number`) меняет `,`→`.` и пропускает, но SQL
`validate_text_input` (вызывается в process_user_input до сеттера) кастит сырой
`'82,5'::NUMERIC` → падает. Касается ВСЕХ numeric-полей (вес/рост/талия), не только
waist; age integer — не касается. Сеттеры (`set_user_*`) запятую как раз понимают,
но до них не доходит. Локали ru/de/fr/es — запятичный десятичный разделитель =
реальный кейс. Fix-кандидат: в `validate_text_input` numeric/integer-ветке кастить
`replace(p_text,',','.')` (scoped, не трогая date/string). Ждёт решения владельца.

### Запятая ИСПРАВЛЕНА (owner approved, mig 443, в том же PR #308)
Owner: «чинить в этом же PR». mig 443 (applied LIVE): `validate_text_input`
numeric/integer-ветка кастит `replace(p_text,',','.')::NUMERIC` (база — live
pg_get_functiondef; date/string НЕ трогаем — там запятая не десятичный). Verified
live (прямой fn + E2E dispatch_with_render): `82,5`→ok 82.5 render personal_metrics
status=registered; `110,0`→110.0; integer `25,5`→reject (не целое, корректно);
date `18,05`→invalid (не затронут); string→ok. +Python-тест на запятую. Принцип
«один логический блок = одна миграция» → отдельная 443, не в 442 (waist-return).

---

## Научный аудит `calculate_user_targets` v13 + Digital Twin v13 (вечер, владелец-driven)

**Задача владельца:** проверить, соответствует ли расчёт целей доказательной нутрициологии.
Ничего к проду НЕ применялось — только анализ + деливераблы на одобрение.

**Сделано:**
- Снята живая v13-формула (`pg_get_functiondef`, mig 429) + все `app_constants` + PAL-маппинг.
- **Digital Twin v13** — 21 профиль, провалидированы **1:1 против живого RPC** (kcal+Б+Ж+У, дельты 0):
  - Нативный Google Sheet (CSV-конверсия, values) — Drive folder `1aJOXi7Y…`, id `1GWSD4YiNkbkN_UQpOTlTNsEnNdKogHOS9STT9BG6zgw`.
  - Формульный `.xlsx` (live-движок, 4 листа) — для импорта в Drive.
- Промт для внешнего ИИ (English, self-contained) + мой deep-research аудит (94 цитаты, первоисточники).
- Финальный аудит с рекомендациями. Все деливераблы — `Нутрициолог (аудит расчётов v13)/` (untracked, в корне NOMS).

**Durable находки (KB → personalized-macro-split §audit-v13):**
1. **PAL cap 1.8 — мёртвый код.** Реальный потолок = `pal_heavy 1.725` (training_bonus heavy = 0). Нет Extreme-категории → `heavy+cardio+lose` недокормлен (EA≈25 < RED-S порога 30, Loucks 2003). Safety-adjacent.
2. **«Высокие углеводы» — следствие заниженного БЕЛКА, не «мало жира».** Грамм-floor жира 0.8 г/кг структурно ВЕРЕН (на дефиците лучше %-таргета: StrongerByScience/Whittaker 2021). Белок НЕ deficit-aware: кардио 1.4 г/кг массы / 2.0 г/кг LBM ниже Helms 2.3–3.1 г/кг FFM. → P1-правка: deficit-aware белок схлопывает углеводы-остаток.
3. **База PAL sedentary 1.2 занижает** (NASEM 2023: 1.2 = прикованный к постели; реально sedentary ≈ 1.4).
4. **Старая Digital Twin была v8.3** — дрейф на 5 версий от прода. Всегда сверять версию twin vs live перед использованием.
5. **Gotcha: бинарный `.xlsx` нельзя надёжно залить инлайном через Drive MCP** (37KB base64 не помещается/не воспроизводится байт-точно). Паттерн: `create_file` с `textContent` + `contentMimeType=text/csv` → нативный Google Sheet (конвертит, формулы исполняются НО запятые в формулах ломают CSV — нужны кавычки + риск EU-locale). Формульный движок → отдельный `.xlsx` для ручного импорта.

**Рекомендации (приоритет):** P1 deficit-aware белок (главное); P0/P1 Extreme PAL + EA-guard для выносливости; P1 sedentary PAL 1.2→~1.4; P2 ABW для obese-без-талии, diet breaks; P3 Adaptive TDEE. НЕ трогать: беременность/лактация, age-формулы, min-kcal floor, RFM/Katch, грамм-floor жира.

**Открыто:** внешний ИИ ещё не запускался владельцем (промт готов) → при возврате наложить на таблицу находок.
Deep-research workflow `wf_6ccd23f3-e36` оборвался на синтезе (103 агента, 3.46M токенов) — находки восстановлены из fetch/verify транскриптов.

## ✅ Онбординг: копирайт 10 экранов (RU+EN) + прогресс-бар по фазам (mig 444, PR #309)

Цель — вовлечение/удержание онбординга (owner + агент NOMS approved). Сделано RU+EN
(staged; ×13 — следующим этапом после sign-off на проде).

**Что:**
1. Переписаны 10 «голых» экранов под эталонный тон Sassy Sage (наука без пафоса,
   anti-shame, «зачем этот вопрос»): пол/возраст/вес/рост/активность/тренировки/
   цель/темп/страна/таймзона. Тексты — финальные от NOMS.
2. Прогресс-бар по 5 фазам (📋 Базовое → 🏃 Образ жизни → 🎯 Цель → 🔬 Точность →
   🏁 Финал) как template_var `{onb_progress}`.

**🔑 Durable архитектурный урок (shared screens):** экраны `edit_gender`/`ask_age`/
`ask_weight`/`ask_height`/`edit_activity`/`edit_training`/`edit_goal`/`edit_speed`
— **ОБЩИЕ** между онбордингом и профиль-редактированием (`edit_weight`→`ask_weight`,
`workflow_states.edit_*.screen_id` указывает на тот же экран). Проверять ПЕРЕД любой
правкой текста онбординг-экрана: `SELECT state_code, screen_id FROM workflow_states`
+ какие text_key переиспользуются. Онбординг-специфичный контент (прогресс-бар) НЕЛЬЗЯ
зашивать в текст — полезет в профиль. **Решение:** инъекция через `render_screen` как
template_var, gated на onboarding-статусы (`v_user.status` CASE → фаза; вне онбординга
`''` → плейсхолдер `{onb_progress}` исчезает). Тот же паттерн, что `text_input_inline_back`
(mig 396). Копирайт (строки «зачем») — наоборот, ОК зашивать: улучшает и профиль.

**Механика бара (SQL+data, без деплоя Python):** `render_screen` Step 5b добавляет
`onb_progress` в `v_template_vars` (SELECT прогресс-строки `onboarding.progress.{phase}`
для языка юзера; `''` если фазы нет или строки для языка нет). Тело экрана начинается с
`{onb_progress}`. Python (`_resolve_text`, Pass 2 `_VAR_PLACEHOLDER_RE`) подставляет;
`onb_progress` НЕ в `_USER_INPUT_FIELDS` → не html-экранируется (эмодзи/блоки целы).

**Решения по UX:** (1) бар монохром `⬛/⬜` — консистентность с существующим баром квиза
(🟩 нигде в БД не используется → новый визуальный язык не вводим). (2) Квиз: макро-метка
ТЕКСТОВАЯ «🔬 Микро-тест на фенотип» (без блоков) + сохранён родной `[⬛⬜⬜⬜] 1/4` —
без двойных рядов (решение NOMS). (3) Страна/таймзона «зачем» = распознавание местной
кухни (тортилья MX≠ES) + напоминания в твоё время; БЕЗ ценообразования (owner: толкало бы
указывать неверные данные).

**Staged rollout:** только ru+en. Остальные 11 языков — тела без `{onb_progress}` (бар не
показывается, старый текст), прогресс-строк для их языка нет. Перевод ×13 — следующий этап.

**Проверка:** E2E (render, rollback-txn) — онбординг все фазы→бар+копирайт; профиль-
редактирование→бара нет, копирайт улучшен; DE→бар нет, текст цел. p95 render_screen (VPS,
persistent): онбординг 43.9ms / registered 47ms — без регрессии (доп.SELECT только в
onboarding-статусах). mig 444 LIVE.

**KB-разведка по «сократить онбординг»:** прямого предложения урезать в KB нет; канон —
`user-data-collection-pattern.md` (только safety-critical в онбординге, остальное Profile/
cron/event-driven = progressive profiling) + skip-rule в `ux-crosscutting-principles.md`.
Drop-off метрик в KB НЕТ (≈5 активных, 119 NPC искажают). Owner выбрал минимальный путь
(тексты+бар), не реструктуризацию. Полный разбор шагов FSM сверен с live (talia-шаг,
maintain→пропуск speed, diet-skip) — в onboarding-v3-map.md.

**Open:** перевод 10 экранов + 5 прогресс-строк на 11 языков (ar/de/es/fa/fr/hi/id/it/pl/
pt/uk) — после owner/NOMS sign-off на проде RU+EN.

## My Day + карточка еды: вёрстка/контент (mig 445, PR #310, applied LIVE)

Owner-тест: 3 правки (data-only ui_translations, действуют сразу, owner approved через мокапы).

**(1) Дубль сна/стресса в My Day.** Две строки об одном: `active_modifiers_strip`
«🌙 Сон: Норма · 🌀 Стресс: Спокойно» (mig 343/348, словом, premium) дублировал
`wellbeing_line` «🌙 Сон: 🙂 · 🌀 Стресс: 😌» (mig 410, эмодзи чек-ин). Убран
плейсхолдер `{active_modifiers_strip}` из `stats.main_text` ×13 → осталась эмодзи-строка.
RPC-поле active_modifiers_strip остаётся (cleanup-кандидат, не рендерится).

**(2)+(3) Строки стрика/XP не влезали на мобильном** — корень: словесные метки сильно
разнятся по языкам (fr `Jours consécutifs` 17, `очков`/`Серия:`). Языко-устойчивое
решение: **иконка-как-метка** (🔥=стрик, 🌟=XP, 🔋=мана), убрать слова.
- My Day: `🔥 Дней в ударе: 32 дн. 🌟 +135` → `🔥 32 дн · 🌟 +135`.
- Карточка: `🌟 +15 очков · 🏆 Серия: 32 дн.` → `🌟 +15 · 🔥 32 дн` (+`· 🔋 12/14` with_mana).
- Унификация иконки стрика 🏆→🔥 (My Day был 🔥, карточка 🏆).

Реализация: `replace()` по подстрокам шаблонов (структура идентична ×13) +
`gamification.streak_kept` ×13 explicit (drop label-word, 🏆→🔥). cron/reset используют
КОЛОНКУ `last_streak_kept_date`, не перевод → правка streak_kept безопасна. Verified
LIVE + render-резолв ru/fr через template_engine.

**Durable: длина строки в multi-lang UI — не подгонять по русскому.** Словесные метки
разной длины по 13 языкам → переносы в романских/немецком. Иконка-как-метка (🔥🌟🔋)
убирает зависимость от языка разом. Применять для компактных строк геймификации.

### ⚠️ Git-инцидент (lesson §12, повтор): `cd` в основной клон сломал ветку
Дважды за сессию писал миграцию через Write в основной клон (`~/Documents/NOMS/migrations/`),
а не в worktree. Хуже: команда `cd ~/Documents/NOMS && git checkout <моя-ветка> ...` — checkout
ПРОВАЛИЛСЯ (ветка занята worktree), `2>/dev/null` скрыл ошибку, `;` (не `&&`) пропустил
дальше → коммит ушёл на ТЕКУЩУЮ ветку основного клона (`claude/n8n-04menu-cleanup-delete-blocker`,
от древнего 39f45e2) → `git diff origin/main..HEAD` показал **16449 удалений** (stale-base).
§12.2 sanity-check поймал ДО создания PR. Загрязнение (local-only коммит на чужой ветке)
убрано `git reset --hard 39f45e2`. Чистая ветка пересоздана в worktree от свежего main,
renumber 444→445 (коллизия с merged #309). **Durable: (а) ВСЕ файловые операции и git —
ТОЛЬКО в worktree, НИКОГДА `cd` в основной клон; (б) после `git checkout` всегда проверять
`git branch --show-current` (не верить `2>/dev/null`); (в) sanity-diff перед push — спас.**

## KB audit (/kb-close --audit, 2026-06-03 вечер)
audit.py: 145 концептов. **merge_candidates 13** — все low-score (≤0.251, топ sticker-architecture-adr↔n8n-self-hosting 0.251), лексические false-positives через общий словарь (хабы supabase-db-patterns), НЕ дубли → не сливал (согласуюсь с аудитами 06-02/06-03). **orphans 0.** **stale 17** — все severity=warning, флаг по n8n-тегу; часть (`n8n-data-flow-patterns`, `n8n-subworkflow-contract`, `save-bot-message-contract`, `food-log-python-cutover`, `one-menu-ux` 2d, `reply-keyboard-routing-pattern`) — ЖИВЫЕ canonical-рецепты, массовый re-banner сломал бы актуальное → не трогал. **lint --structural-only: 0 errors** (42 warn/422 sugg — pre-existing orphan/backlink baseline). База структурно здорова.
**MEMORY консолидирована: 34.3KB/162 → 26.3KB/153** (−23%). Слиты 3 пары дублей (системный-промпт ×2, migration-collision ×2, parallel-agent ×2 → объединены в §12 git-дисциплина); сегодняшние «Current state» shipped-буллеты (waist/comma/myday/git/content-type/my-day-stale/онбординг) сжаты в durable-уроки (детали в daily+KB); 3 старейших Recently-shipped (≤05-31) → 1 строка; promo tech-debt объединён. Все durable-уроки и open-айтемы сохранены. Бэкап `MEMORY.backup-2026-06-03-audit.md`. Не влез под 20KB hard — остаток durable-референс (Архитектура/Critical Facts/Watchlist), прун задел бы важное.

## ✅ Профиль-edit вес/рост/возраст: инлайн-[Назад] + сохранение reply-kb (mig 446, PR #311)

Owner-репорт: на профиль-редактировании «Рост» (edit_height; так же edit_weight/edit_age)
нет кнопки [Назад], reply-клавиатура снимается. Спросил, можно ли вернуть back БЕЗ потери reply-kb.

**Расследование (субагент + сам):** причина — флаг `text_input_inline_back` (`render_screen`,
mig 396) срабатывал только в онбординге (`registration_step_%`). В профиле `edit_*` →
remove_keyboard → `editMessageText` 400 «inline keyboard expected» → fallback **delete+send
нового пузыря** + reply-kb снята (её чинил mig 392 re-attach на возврате).

**🔑 Durable (механика доставки `telegram_send.py`):** для text-input edit-экрана
`remove_keyboard` несовместим с editMessageText → всегда delete+send (лишний пузырь + kb
снимается). **Решение — отдать `inline_keyboard` вместо `remove_keyboard`:** editMessageText
правит НА МЕСТЕ (чистота чата) + постоянная reply-kb НЕ снимается (Telegram не трогает
chat-level reply-kb при editMessageText; она chat-state, переживает сообщения пока не
remove/replace). Это и ответ на «как сохранить reply-kb»: просто перестать слать remove_keyboard.

**mig 446 (SQL-only, render_screen RPC, без деплоя):** gate `text_input_inline_back`
расширен на `edit_age/edit_weight/edit_height`. Кнопка [Назад] уже в `ui_screen_buttons`
экранов `ask_*` (meta NULL — достаточно). cmd_back в edit: router→menu_v3→`process_user_input`
(mig110 clear edit_%→registered + `back_screen_id_default=personal_metrics`). Онбординг НЕ
задет (cmd_back через `process_onboarding_input` mig390, гейт `registration_step_%` раньше
чтения meta). mig 392 re-attach стал избыточным/безвредным.

**Вне scope (флагнуто owner):**
- `edit_waist` — тот же паттерн числового edit, НО свой экран (mig 442, `show_back_inline=NULL`,
  НЕ в router `PROFILE_V5_STATUSES` — cmd_back-роутинг мутный). Нужна отдельная верификация.
- **Страна/таймзона (онбординг):** timezone-back УЖЕ есть (→country, `handlers/location.py:158-164`);
  country-back ОТСУТСТВУЕТ намеренно (`back_callback=None`, коммент «tunnel UX, NLM advice»,
  location.py:435). Пикеры строятся в Python (location.py `_build_picker_keyboard`), НЕ из
  `ui_screen_buttons`. Добавить country-back можно (2 провода: back_callback + ветка в handler),
  но это override продуктового решения + нужен таргет (phenotype_result?) + деплой Python.
  Решение за owner — не трогал.

**E2E (render, rollback):** edit_height/weight/age→flag True+[Назад]; онбординг
registration_step_4→True (не задет); edit_goal picker/registered/edit_waist→не задеты.
p95 render_screen edit_height 43.0ms (VPS) — без регрессии (изменение query-free, булево условие).

---

## Sage tone telemetry — reuse ai_coach_logs (mig 447, PR #312, applied LIVE, Python deploy pending)

**Цель owner'а:** наблюдать тон Номса по ВСЕМ юзерам с историей (food-log реакция + My Day). Food-log реакции нигде не сохранялись (`food_logs` без колонки реакции, в journalctl только эмоция+длина); My Day — только последняя в кэше `users.my_day_insight_*`.

**Ключевое решение (improve on NLM):** NLM велел создать новую таблицу `sage_responses_log` + RPC `log_sage_response`. Live-проверка (owner: «проверь у тебя инфо с полей»): **весь стек уже существует** — `ai_coach_logs` (generic AI-лог с `context_type` дискриминатором: `text`/`vision` для распознавания; колонки tokens_used/latency_ms/model/success/error/cached) + RPC `log_ai_request` + обёртка `services/ai_logging.py:log_request` (fire-and-forget, never-raises) + **уже под GDPR** (`crons/data_retention.py`). Reuse > дубль стека. Owner одобрил Path 2.

**Сделано (mig 447):** ALTER `ai_coach_logs` +4 nullable (`emotion`,`locale`,`meal_id`,`day_context jsonb`); extend `log_ai_request` +4 nullable param; `ai_logging.log_request` +4 kwarg'а; `sage.py` helper `_fire_sage_telemetry` + 2 fire-and-forget вызова (`context_type` = `sage_food_log`/`sage_my_day`, `day_context={"prompt":user_prompt}` — буквальный снимок для RLHF, `meal_id` для food-log).

**🔴 Live-prod гоча (валидация [[concepts/safe-create-or-replace-recipe]] §238):** смена числа параметров делает `CREATE OR REPLACE` **OVERLOAD'ом, а не заменой** → на проде на миг оказалось ДВА `log_ai_request` (12 и 16 param) → 12-арг вызовы распознавания → `AmbiguousFunction` (и PostgREST «could not choose candidate»). Fix по рецепту: `DROP FUNCTION IF EXISTS public.log_ai_request(<12 old sig>)` ПЕРЕД `CREATE`. Проверка `count(*) from pg_proc where proname='log_ai_request'` = 1. + `NOTIFY pgrst, 'reload schema'` (PostgREST мог закэшировать двойной overload). **Durable напоминание: при extend RPC новыми параметрами ВСЕГДА DROP старой сигнатуры в той же миграции + verify single overload + pgrst reload.**

**Verify:** NLM → live `pg_get_functiondef` → applied LIVE → ROLLBACK-txn (sage-строка пишет 4 поля; старый vision 12-арг не сломан: emotion=None, confidence/cascade заполнены; single overload) → 68/68 sage unit-тестов. GDPR уже покрыто.

**Запрос тона по всем юзерам (после deploy):** `SELECT created_at, telegram_id, context_type, emotion, ai_message, day_context FROM ai_coach_logs WHERE context_type LIKE 'sage_%' ORDER BY created_at DESC;`

---

## Barcode logging (упакованная еда по штрихкоду) — дизайн + находки (план, код pending)

**Задача owner'а:** новая фича — лог упакованной еды по штрихкоду → точные КБЖУ производителя (vs LLM «на глаз») + reply-кнопка [🔎 Штрихкод] рядом с [➕ Добавить еду]. Полностью Python, не n8n. KB: [[barcode-logging-openfoodfacts]] (полный MVP-spec).

**Архитектурное решение (ключевое):** barcode ≠ новый тип контента. Это ветка обработки уже существующего «фото», гейтированная `users.status='waiting_for_barcode'`. Фото остаётся `food-media` во всех 3 классификаторах → инвариант индикатор⟺распознавание держится → **3 классификатора по сути НЕ трогаются**. Barcode-путь отдаёт обычный `ParsedFoodResult` → весь downstream (log_meal_transaction input_source='barcode', рендер, Sage, мана) без изменений.

**Декодер:** `zxing-cpp`+`Pillow` (pip-wheels, БЕЗ apt) — НЕ pyzbar (требует libzbar0, а deploy=rsync). Локальный декод И ЕСТЬ роутер barcode-vs-еда (EAN checksum → ложные ≈0; нет EAN → vision). «Лёгкий ИИ» для этого не нужен (push back owner'у). QR≠товарный код (EAN-13/8/UPC), читаем но используем только если payload=GTIN.

**Owner-решения:** кнопка `[🔎 Штрихкод]`; OFF-not-found → откат на vision; порция → кнопка [✅ Да, всю упаковку (N г)] + дешёвая LLM парсит свободный текст/голос («да всё съел»→whole, «половину»→0.5, «грамм 30»→30); порядок — сначала КВ+daily, потом код.

**🔴 Находки «с полей» (live ≠ NLM/MEMORY — для всех агентов):**
1. **`barcode_cache` УЖЕ существует в проде (orphan)** — полная схема (barcode, product_name, calories/protein/fat/carbs, serving_size_g, country_code…), 0 строк, создана вручную, НЕ в миграциях, НЕ в коде. Узаконю `CREATE IF NOT EXISTS` + RPC, переиспользую как кэш OFF.
2. **Контракт-тест классификации — ФАНТОМ на main → CI сломан.** `pr-content-classification.yml` (PR #305) запускает `pytest tests/test_content_classification_contract.py`, но файла на main НЕТ — он на невмерженной `claude/content-classification-fix` (14d3a17). Любой PR на router/proxy/food_log/location → CI падает на сборе. Восстановлю в barcode-PR; быстрый фикс для всех — cherry-pick 14d3a17 в main. Обновил [[content-type-classification]] §5.
3. **NLM соврал: `workflow_states` «не существует».** Live: таблица есть + FK `users_status_fkey`. Новый статус `waiting_for_barcode` обязан быть в workflow_states (иначе 23503); screen_id nullable → экран не обязателен (паттерн editing_meal). Trust live, не NLM ([[memory-claim-vs-live-verification]]).

**Координация:** FatSecret — домен каскад-агента (хук ai_recognition.py:205, enabled=false). Barcode-MVP на OpenFoodFacts (независим), источник плагинный (services/barcode.py), общий barcode_cache. НЕ трогаю ai_recognition.py. Открытых PR на момент старта нет.

**Scope (для коллизий):** mig A (workflow_states + buttons.scan_barcode ×13 + build_main_reply_keyboard + messages.barcode_* ×13 + questions.barcode_grams ×13); mig B (barcode_cache CREATE IF NOT EXISTS + RPC). Python: services/barcode.py (новый), ветка в handle_ai_input, cmd_scan_barcode в menu_v3, reply-кнопка в router, requirements (+zxing-cpp/Pillow). Тесты: восстановить контракт-тест + barcode-кейс + юнит decode/OFF-mapping.

## ✅ Фиксы по owner-скринам: {onb_progress} страна (mig 448) + edit_waist [Назад] (mig 449)

**Task A — {onb_progress} literal на стране/таймзоне (mig 448 LIVE, PR #314).** Регрессия
mig 444: перезаписал `onboarding.ask_country/ask_timezone` (ru+en) строкой с `{onb_progress}`.
Но эти экраны рендерит `handlers/location.py` (минуя render_screen!) через
`_resolve_translation_with_variants` → резолвит только `{icon_X}`, не template_vars →
плейсхолдер literal в чате + потеряны Sassy JSONB-варианты ru+en. Фикс (data-only): эти
text_key онбординг-only (профиль → `questions.edit_country/edit_timezone`), бар 🏁 Финал
зашит ЛИТЕРАЛЬНО, тип JSONB-array[1]. **🔑 Durable: экраны страны/таймзоны рисует Python
(location.py), НЕ headless render_screen → onb_progress var и любые template_vars там НЕ
работают; для них баки/плейсхолдеры зашивать литерально.**

**Task B — edit_waist [Назад] (mig 449 LIVE, PR #315).** Талия не имела [Назад] (вес/рост/
возраст — имели, mig 446). edit_waist — числовой text-input, но экран без `meta.show_back_inline`
→ флаг False → remove_keyboard → кнопка (она УЖЕ в ui_screen_buttons) не рендерилась. Фикс
(SQL-only, как mig 446): show_back_inline=true + edit_waist в gate. Роутинг уже корректен
(edit_waist ∈ PROFILE_V5_STATUSES mig430).

**🔑 Durable (§12 повтор, важно):** `route()`/любой Python ЗАПУСКАТЬ ТОЛЬКО из worktree,
НЕ из основного клона `~/Documents/NOMS` — он на устаревшей ветке. Я потратил время на
ложное «edit_waist не в router» (трасса из основного клона дала text→ai/back→legacy menu);
из worktree (= origin/main) edit_waist корректно в PROFILE_V5_STATUSES. sys.path импортит
dispatcher из cwd → cwd основного клона = stale router.

**Task C — country [Назад] в онбординге (НЕ сделано, вопрос owner'у).** Возражение: у страны
нет чистого «предыдущего» экрана (перед ней phenotype_result — терминальный «continue»-экран
квиза; возврат туда / в 4-вопросный квиз неуклюж). Поэтому было tunnel-UX (NLM). Timezone-back
уже есть (→country). Если owner всё же хочет country-back — нужен location.py (back_callback +
ветка handler) + деплой + решение о таргете (phenotype_result?).

**Task C РЕШЕНО (owner):** Страну оставляем БЕЗ [Назад] — tunnel-UX подтверждён. Изменений нет.

### Barcode — РЕАЛИЗОВАНО (PR #316 + mini-PR #313), не смержено

**Сделано за сессию (после дизайна выше):**
- **mini-PR #313** — восстановил фантомный контракт-тест классификации из `14d3a17` (CI `pr-content-classification.yml` был сломан для ВСЕХ агентов: yml есть, тест-файла на main нет). ALL GREEN. Мержить первым.
- **PR #316** — фича barcode: `services/barcode.py` (decode zxing-cpp + OFF lookup + cache-адаптеры + scaling→ParsedFoodResult + portion-parser gpt-4o-mini с fast-path); mig 452 (barcode_cache CREATE IF NOT EXISTS + users.pending_barcode + 5 RPC); mig 453 (waiting_barcode_portion + [🔎 Штрихкод] reply-кнопка + 5 UI-ключей ×13); ветки в handle_ai_input + _handle_barcode_portion (additive); 1 router-правило (whole-pack callback). 30 тестов зелёные. Миграции validated через BEGIN…ROLLBACK (live не тронута). Green кроме contract-CI (зависит от #313).

**Архитектурный итог подтверждён в коде:** barcode = ветка фото по auto-detect (decode-first), НЕ новый тип контента → 3 классификатора не тронуты, инвариант держится. Кнопка не потребовала router-правки (reply-text → default text→ai → handle_ai_input ловит по _is_barcode_button_text).

**🔴 Двойной §12 stale-base за сессию:** main ушёл вперёд дважды (агенты мержили миграции 447→449). ОБА PR словили migration-collision + diff-sanity «deleted N migrations». Fix: rebase origin/main + renumber 448/449→452/453 (git mv + sed внутр. рефов) + force-with-lease. Урок: номер миграции брать + rebase НЕПОСРЕДСТВЕННО перед push (зафиксировано в KB barcode §8).

**Перед merge (для owner/тимлида):** (1) merge #313 первым, (2) rebase #316 → contract-CI зелёный, (3) применить mig 452+453 к LIVE вместе с Python-деплоем (кнопка видима сразу), (4) подтвердить что deploy делает `pip install -r requirements.txt` (zxing-cpp/Pillow), (5) `systemctl restart noms-webhooks`.

## ✅ Location pickers context-aware: бар только онбординг + [Назад] профиль (mig 454, PR #317)

Owner-скрин: на ПРОФИЛЬНОМ выборе страны (edit_country) висел «🏁 Финал [⬛⬛⬛⬛⬛]» +
не было [Назад].

**🔑 Durable (КОРЕНЬ, анти-паттерн):** список флагов страны/таймзоны — ОБЩИЙ Python-компонент
`handlers/location.py:_render_country_picker`, всегда берёт текст `onboarding.ask_country`
(и онбординг, и профиль `edit_country`). mig 448 зашил бар «🏁 Финал» ЛИТЕРАЛЬНО в этот текст
→ протёк в профиль. **Урок: онбординг-специфичный контент (прогресс-бар) НЕЛЬЗЯ зашивать в
текст общего компонента — ни в headless (mig444 shared-screens), ни в Python-pickers. Только
status-conditional в рендере.**

**Фикс:** (a) mig 454 (data, LIVE) — убрал бар из текста ask_country/ask_timezone (ru+en),
утечка устранена сразу. (b) location.py — бар добавляется УСЛОВНО (`_onb_progress_finish` читает
`onboarding.progress.finish`) только для onboarding:country/timezone; + [Назад] на профильном
country-list (back_callback для edit_country; `cmd_back→menu_v3→settings` проверено через dispatch).
Онбординг страна = tunnel (owner). Итог: онбординг бар+ (страна tunnel/тз back→страна); профиль
без бара + [Назад]→Настройки. test_location.py 41 зелёный.

**🔴 §12 СНОВА (третий раз за сессии!):** редактировал `location.py` через АБСОЛЮТНЫЙ путь
основного клона `~/Documents/NOMS/handlers/location.py` (Edit tool), а основной клон на ДРЕВНЕЙ
ветке `claude/n8n-04menu-cleanup-delete-blocker@39f45e2`. Правки ушли в основной клон, миграция —
в worktree → расщепление. Поймал на `git stash` (показал чужую ветку). Откатил основной клон
(`git checkout -- handlers/location.py`), переприменил правки в worktree, перепроверил мок-тест из
worktree (`loc.__file__` подтверждает путь). **Durable: Edit tool с путём основного клона = правка
не на твоей ветке. ВСЕГДА worktree-путь. python/route()/import — только cwd=worktree.**

**Discovered (out of scope):** 4 пре-существующих фейла в `tests/dispatcher/test_router.py`
(onboarding:country/edit_country free-text → location vs тест ждёт onboarding; cmd_cancel_subscription)
— стейл-тесты vs текущий роутер, НЕ от этой сессии (подтверждено на чистом origin/main). Спавн на чистку.

---

## Sage телеметрия верифицирована LIVE + транскрипт-тул (PR #312 merged+deployed, PR #318)

**Телеметрия (mig 447) задеплоена и проверена сквозняком:** owner залогировал «кофе с молоком» → в `ai_coach_logs` упали ДВЕ строки: `sage_food_log` (smirk, ru, meal_id захвачен, day_context={"prompt":...}) + `sage_my_day` (side-eye, асинхронно следом). tokens/latency пишутся. Деплой success, VPS содержит `_fire_sage_telemetry`. **Durable: телеметрия пишет только генерации ПОСЛЕ деплоя (прошлые реакции невосстановимы — нигде не хранились).**

**Транскрипт-тул `tools/sage_transcript_export.py` (PR #318, dev-генератор, не деплоится):** per-user «переписка с Номсом» в Markdown — food log ↔ реакция (пара по meal_id) + «Мой день», по времени. Для скармливания LLM (ToV-анализ) или чтения руками. Owner-требования: (1) **один календарный день** (локальная дата юзера, не скользящие 24ч); (2) **время-контроль фазы** — парсим `User Local Time`+`Meal Phase` из снимка промпта → `видел 20:00/dinner` в каждой реакции (видно, верно ли Номс ловит завтрак/обед/ужин + вскрывает кривой `users.timezone`). exports/ в .gitignore (user food data). **Гоча: `.env` в основном клоне/VPS, НЕ в worktree — `_db_url` ищет по кандидатам + env var.**

**Vision (owner-driven, обсуждено):** самоулучшающаяся система тона = глаза(телеметрия ✅)→чтение(транскрипт ✅)→судья(крон авто-оценки тона+админ-дайджест)→**память юзера** (ролловый дайджест на юзера, «свой MEMORY.md» — дёшево инъектить, не сырые 2 недели логов) → персональные инсайты → удержание. Анти-shame на уровне памяти (не слежка/диет-полиция). Следующие шаги: обёртка в Skill ИЛИ дизайн-док «памяти юзера».

**Post-PR #317 хвост:** (1) CI Migration Collision — 450 совпал с PR #316 → перенумеровал в
454 (main ушёл до 453; 450/451 заняты). (2) Контракт-тест content-classification «падал» —
ветка отстала от main (тест добавлен позже), rebase подтянул → 26 passed. (3) **§12 sanity-diff
снова спас:** `git add -A` затянул ЧУЖОЙ `CYCLE_TRACKING_DESIGN_QUESTIONS.md` (+289, leftover в
worktree) → diff origin/main..HEAD показал лишний файл → `git rm --cached` (оставил untracked).
**Durable: НИКОГДА `git add -A` в общем worktree — только явные пути; sanity-diff перед push обязателен.**
Все CI-чеки PR #317 зелёные.
