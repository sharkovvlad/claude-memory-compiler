# Daily — 2026-05-25

## Sprint stabilization + Phase 3d Luteal — full session

Огромная сессия по двум workstream'ам параллельно. 6 миграций + 2 hotfix + 3 subagent отчёта.

### Workstream 1: Sprint стабилизации sleep/stress UX (mig 330-333)

Closed 4 P0 проблемы из live тестов owner'а:

#### mig 330 — Premium-filter + soft mutex в `cron_get_reminder_candidates` (PR #172)
- **Premium-filter:** sleep_checkin / stress_checkin push отправляются только paid юзерам. Инверсная семантика `COALESCE(subscription_status,'free') <> 'free'` — покрывает любые будущие paid tiers (premium/trial/promo) без новой миграции. Не `user_has_renewable_sub` (он исключает trial).
- **Soft mutex sleep > meal_morning:** окно meal_morning расширено до [9, 10] local. В 9 local meal_morning suppressed если у Premium юзера sleep_checkin pending. В 10 local — догоняем. Один пуш в утренний слот, не два.
- 12 sentinel tests T1-T11 PASS (T2 split на 2a/2b).

#### mig 331 — stats_main wellbeing entry + hub + Premium teaser (PR #173)
- Новые screens: `wellbeing_today` (hub с [🌙 Сон] [😤 Стресс]) + `wellbeing_premium_teaser` (free CTA).
- stats_main row=0: col=0 [Исправить] + col=1 [🧬 Самочувствие] (Premium) + col=2 [🔒 Самочувствие] (free) — mutually exclusive visible_condition pattern из profile_main.
- Headless через `meta.target_screen`. Без Python handler'ов.
- EN+RU placeholder для 7 ключей (mig 332 потом Номсова правка).

#### mig 332 — EN+RU placeholders polish (Номс review, PR #174)
- Tone правки Номса: emoji `😤` → `🌀` (для wellbeing hub), `(Premium)` Jira-spec → «часть Premium», CTA «Открыть Premium» → «Хочу Premium».

#### mig 333 — 13 langs + critical sleep cron + audit fixes (PR #175)
Большая atomic migration через 2 subagent'а:
- **Subagent #1** (wellbeing_i18n_audit) — нашёл **CRITICAL bug**: `cron_notifications.reminder_sleep_checkin` = NULL во всех 13 langs (mig 310 не залила payload, mig 317 не сделала back-fill). **Закрыло баг про «утренний пуш сна в EN-заглушках»** из ТЗ owner'а.
- **Subagent #2** (mig333_missing_strings) — sassify `modifier.locked_teaser.stress_high` × 13 langs (был корпоративный «(Premium)»), 😤→🌀 swap × 39 strings (13 langs × 3 keys), RU/UK gender-leak fix `подкрутил/перекинул` → infinitive (тот же anti-pattern был в 2 ключах), AR title bidi swap.
- Итого **144 strings**: 77 wellbeing × 11 langs + 13 sleep cron + 39 emoji + 13 stress_high + 2 RU/UK gender.

### Workstream 2: Phase 3d Luteal Foundation (mig 334-335)

Закрыл финал 4-частного Phase 3 Adaptive Modifiers (3a foundation mig 301 → 3b sleep mig 310 → 3c stress mig 317 → **3d luteal mig 334-335**).

#### mig 334 — Phase 3d Luteal Foundation + EN/RU (PR #176)
706 строк SQL, 11 sections:
- `phase_from_cycle_day(day, length)` SQL helper — single source of truth (follicular/ovulation/luteal_early/luteal_late)
- `set_user_pregnancy_flag` + `set_user_lactation_flag` — добавлен auto-disable `cycle_tracking_enabled=FALSE` при maternal flag=TRUE (C1 decision тимлида)
- `save_user_cycle_data` — single-callback save RPC (упрощено vs первоначальный staging design, no `user_status_data` column)
- 2 новых screens: `cycle_tracking_intro` (privacy + ценность) + `cycle_tracking_setup` (3 кнопки date offset). `cycle_tracking_length` отброшен — `cycle_avg_length=28` fixed, кастом через future edit screen.
- Populate existing `personal_metrics_cycle_premium_upsell` (был dormant)
- Wire-up `cmd_cycle_premium` button: visible_condition `gender='female' AND ¬pregnant AND ¬lactating` + Premium/free split
- cron `luteal_morning` @ 08:00 local в `cron_get_reminder_candidates`. DRY — `compute_cycle_day_for_user` уже инкапсулирует maternal+opt-in gate.
- Widget integration в `wellbeing_today`: `business_data_rpc=get_day_summary` (reuse existing, не плодить новый RPC)
- EN+RU placeholder × 16 ключей
- 19/19 sentinel tests PASS

#### mig 335 — Luteal i18n × 11 new langs (PR #179)
Subagent #3 (mig335_luteal_i18n) + Номсова APPROVE WITH FALLBACKS:
- 143 строки переводов на 11 langs (DE/ES/FR/IT/PT/PL/UK/ID/HI/AR/FA)
- **Anti-РПП reframe «not weakness»** per-lang: DE «kein Kontrollverlust» / FR «pas un manque de discipline» / UK «не зриви» / AR «ضعف إرادة» / FA «ضعف اراده» / HI «kamzori nahin» / ID «bukan kelemahan». Critic 5/5 на всех 11 `cycle_intro.title`.
- AR/FA secular medical (`الدورة الشهرية`, NOT Quranic), MSA-Egyptian relaxed flavor
- HI Hinglish-Latin per glossary §HI (opt-in flow → Latin, Devanagari избегали)
- ID halal-safe, Ramadan-adjacent verified clean
- FA ZWNJ U+200C preserved в `می‌`/`نمی‌`/`داده‌هات` etc.
- Номсова правка: 5 langs (DE/ES/FR/PT/UK) получили **короче button_setup** для conversion («короткие глаголы конвертят лучше»)

### Workstream 3: 2 hotfix (P0 + CI)

#### PR #177 — defensive idempotency check блокировал food parsing для канарейки
**Root cause:** commit `ff3446f` (22.05, Stage 7a PR C: AI Engine cutover infra) добавил `is_idempotent_message` double-check в `handle_ai_input`. Этот RPC — атомарный **INSERT...ON CONFLICT**, не read-only check. `webhook_server` (line 1554) уже claim'ит row → defensive check в handler **гарантированно** получает TRUE → handler skip → empty envelope → юзер видит только thinking sticker.

Live confirmed: 0 `food_logs` за 6h для tid=417002669 (канарейка Python AI Engine). Журнал: `handle_ai_input: defensive idempotent skip` для каждой попытки.

Fix: удалить весь defensive check block. webhook_server — single source of idempotency claim. После merge — канарейка работает (owner confirmed).

**Не наша регрессия** (Stage 7a PR C от другого агента), но критично в проде → P0 hotfix.

Handover для Python AI Engine агента в KB (`handover/2026-05-25_python_ai_engine_idempotency_fix.md`).

#### PR #178 — smoke test retry on transient errors
Lesson run 26389723350: smoke check (2 мин окно после deploy) поймал `RPC get_indicator_context error` — transient Supabase pooler glitch (~30 сек). False positive алерт «🚨 NOMS deploy failed». Fix: если первый grep нашёл ERROR — sleep 30s, повторить с свежим окном. Если оба раза с ERROR — реальная регрессия. Если второй раз чисто — accepting deploy.

## Open issues для следующего агента

1. **Task #9 — Live UAT всех Phase 3 фич** (sleep/stress/luteal end-to-end в боте). Не успели в этой сессии из-за parallels.
2. **Task #8 — UX [Исправить] button на «Мой день»**. Mig 326 (другой агент) скрывает её при `meals_today=0`. Визуально криво (одиночная [🧬 Самочувствие] справа). Нужен decision: оставить как есть / убрать visible_condition / заменить на другую кнопку.
3. **KB cleanup proposal** — subagent работает в фоне, output будет в `/tmp/kb_cleanup_proposal.md`. Тимлид approve'ит в новой сессии.
4. **L2 native review** для AR/FA/HI luteal copy — recommended но не blocking. Post-deploy patch если flag.

## Lessons learned

### Lesson 1: `is_idempotent_message` — claim, не check (см. PR #177)
INSERT...ON CONFLICT DO NOTHING — это атомарная операция «попытаться положить + узнать удалось ли». **Только один** caller должен вызывать его per (tid, mid). Defensive double-check в downstream — гарантированно блокирует real flow. Anti-pattern.

→ Cleanup KB: создать concept `claim-vs-check-idempotency-anti-pattern.md` (TODO для следующего агента).

### Lesson 2: transient Supabase pooler glitches ~30 сек
Уже 3 случая за последние 2 недели (10:00 MSK, 10:50 MSK сегодня + lesson runs 26103598563/26103611524 от 19.05). Smoke test 2-min окно даёт false positive. Fix: retry с свежим окном.

### Lesson 3: Параллельные агенты съели номера 324-329 за день
Mig collisions guard работает (pre-push hook + GHA workflow), но темп параллельной работы высокий. **Caveat:** перед каждым commit обязательно `git fetch && rebase` + проверить `ls migrations/`.

### Lesson 4: Sleep_checkin push «в 10:20» = корректный (timezone confusion)
Owner написал что sleep пуш пришёл в 10:20 после того как он залогировал хлеб в 10:19. По факту — он в `Europe/Madrid` (UTC+2), пуш отправлен в **09:20 Madrid** (= 07:20 UTC). Telegram клиент owner'а в MSK показывает timestamp 10:20 MSK = 09:20 Madrid. Mutex работает корректно.

→ KB-урок: добавить в [[concepts/release-protocol]] или новый concept `timezone-debugging-guide.md`. UI timestamps Telegram-клиента ≠ user_tz в БД.

### Lesson 5: Subagent для i18n × N langs — proven workflow
Pattern (повторили 3 раза):
1. Spawn copywriter subagent с full brief + KB-документы для tone reference
2. Subagent делает Part 1 (translations × N langs) + Part 2 (cultural review notes) + Part 3 (self-check)
3. Main agent генерирует SQL программно (Python script) — safer для Unicode/RTL/ZWNJ чем hand-writing
4. Preview для Номса в markdown × N langs (компактные таблицы)
5. После Номсова APPROVE/CHANGE — apply + DO $$ verify + commit + PR

ETA per session: ~30-60 мин. Quality: 5/5 critic per Sassy Sage rubric.

### Lesson 6: Phase 3d Luteal — упрощение single-callback save
Изначальный design включал `staging` через `user_status_data` column (которой не существует). Cleanup на лету: single-callback save (`cmd_save_cycle_<today|7d_ago|14d_ago>`), length=28 fixed. Cleaner UX (3 кнопки → save → return), меньше state. Custom length через future `edit_cycle_length` screen.

## Что в проде после merge всех 5 PR

- ✅ Free-юзеры больше не получают sleep/stress пуши (mig 330)
- ✅ В 09:00 только один пуш (sleep), meal_morning catch-up в 10:00 если нужен (mig 330)
- ✅ `[🧬 Самочувствие]` на «Мой день» (Premium) / `[🔒 Самочувствие]` для free (mig 331)
- ✅ Утренний sleep push на родном языке (mig 333 закрыл NULL bug)
- ✅ Stress check-in screen с 🌀 эмодзи (mig 333)
- ✅ Premium teaser в Sassy Sage tone × 13 langs (mig 333)
- ✅ RU/UK bot gender-clean (mig 333)
- ✅ Phase 3d Luteal opt-in flow для female premium юзерш (mig 334)
- ✅ Cron `luteal_morning` @ 08:00 для opted-in (mig 334)
- ✅ Widget в hub title с cycle phase info (mig 334)
- ✅ Luteal × 13 langs (mig 335)
- ✅ Python AI Engine canary 417002669 работает (PR #177 hotfix)
- ✅ Smoke test no more false positive deploy алертов (PR #178)

## Migration HEAD: 335

Phase 3 Adaptive Modifiers полностью closed. Roadmap §P2.4 → DONE.

---

## KB cleanup session (вечер, отдельный workstream)

3-фазный cleanup `claude-memory-compiler/knowledge/` (119 файлов, 2.4 МБ накопились с 14 апреля параллельной работой). Цель — снизить токены/ошибки агентов при старте сессии.

### Phase 1 — audit (read-only)
2 итерации subagent (первая слабая, переделал с concrete failure modes). Финал: 119 файлов классифицированы по 11 доменам × 7 статусам (ACTIVE / HUB / LEGACY-n8n / LEGACY-superseded / DUPLICATE / OUTDATED-MARKED / STALE). Source: `/tmp/noms_kb_audit_v2.json`.

### Phase 1.5 — pairwise dupe scan
28 подозрительных пар проверены (1 TRUE_DUPLICATE + 7 OVERLAP_TEMPORAL + 9 OVERLAP_HUB_LEAF + 6 OVERLAP_PARTIAL + 5 NOT_DUPLICATE с evidence). Source: `/tmp/noms_kb_dupe_scan.json`.

### Phase 2 — restructure index + tag (KB-repo commit `e6a5ae4`)
- `index.md`: 75 КБ flat table → 19 КБ иерархия (11 доменов × live/archived секции, `🔥 HUB / 🏛 / 💤` badges, snapshot counters)
- 24 файла получили `> ⚠️ status: ...` баннер сверху: 2 dupe + 7 superseded + 8 legacy-n8n + 7 stale
- 25 файлов изменено, +283 / −118

### Phase 3 — real merges (KB-repo commit `fa6a484`)
- **Pair 1:** `sticker-architecture-adr.md` → `ui-stickers-headless.md`. ADR rationale (Context / Alternatives / Consequences / Stage 1.1 amendment) extracted, appended в winner как «## ADR rationale (исторический контекст)». Winner 164→329 LOC. Loser = 5-line stub (preserves wiki-links).
- **Pair 2:** `n8n-self-hosting.md` → `n8n-selfhost-migration.md`. Ongoing operations (env tuning, multi-agent SSH safety, n8n Cloud cancellation history) extracted в «## Ongoing operations». Winner 234→296 LOC. Loser = 5-line stub.
- Net: +252 / −402 (real dedup achieved)
- `index.md`: добавлена **«Start here for common tasks»** lookup table (12 типичных задач → 2-4 foundational hubs). Это первое что видит агент.

### Phase 4 — CLAUDE.md pointer (NOMS-repo PR #188, merged)
- Line 46 fix: `[[concepts/n8n-self-hosting]]` → `[[concepts/n8n-selfhost-migration]]` (stub → winner)
- Section «Документация и KB»: bullet про `index.md` расширен — описывает «Start here» / Snapshot / 11 доменов / status badges / `> ⚠️` баннеры. Цель — новый агент знает структуру до открытия файла.

### Что увидит следующий агент
1. CLAUDE.md → знает про новую структуру index.
2. `index.md` → «Start here for common tasks» → 2-4 hub'а на свою задачу.
3. Если случайно открыл legacy/stale/stub → баннер сверху явно redirect'ит на canonical.
4. `🔥 HUB` маркеры показывают что читать первым в незнакомом домене.

### Open для следующего KB-агента (опционально)
- Cross-link injection в 15 OVERLAP_HUB_LEAF + PARTIAL пар (низкий приоритет, emerges organically)
- Перенос superseded в `knowledge/_archive/` если index когда-то снова раздуется (сейчас баннеры + Archived секции достаточны)
- 8 STALE-файлов (fasting, leagues, squad-referral, и др.) — перепроверить через 60 дней, может вернуться в roadmap или окончательно архивировать.

### Lesson learned (не вошёл в KB — meta, не gotcha)
Read-only audit subagent с правилом «при сомнении → ACTIVE» использует это как escape hatch чтоб не думать (1-я итерация: 0 DUPLICATE, 0 LEGACY-n8n, 26 файлов в «other»). Лечится 2-й итерацией с **concrete failure modes** в prompt: «вот пары которые ОБЯЗАТЕЛЬНО проверь полностью, вот примеры что куда классифицировать, "other" ≤ 5 файлов, DUPLICATE = 0 запрещено».

---

## Evening session — UX redesign sweep (mig 347-355, +9 mig, 11 PR)

Owner UAT sweep после mig 333. Серия PR'ов с переработкой 3 главных экранов (stats_main / progress_main / profile_main) на мобильный compact + Sage tuning + clinical-safety прочее. Подробный handover: `handover/2026-05-25_ux_redesign_sweep_close.md`.

### Migrations (хронологический)

| # | PR | Что |
|---|---|---|
| 347 | #195 | checkmark ✅ на sleep_checkin/stress_checkin (новый `meta.current_value_source='daily_metrics'` extension к render_screen) |
| 348 | #196 | stats_main mobile redesign 7/8 правок (БЖУ построчно X/Y, ✅ ±10% коридор + neutral, sleep/stress strip premium-only вверху, blank line trim) |
| 349 | #197 | Sage v2 system_prompt_my_day 100-250 → 80-150 chars × 13 langs (per-lang substring rewrite, cache invalidation) |
| 350 | #198 | streak строку вернули label «Дней в ударе: N дн.» (mig 348 чрезмерно минимизировала) |
| 351 | #199 | ⚠️ для БЖУ при выходе из ±10% коридора (mig 348 ставила empty, owner попросил soft signal) |
| 353 | #201 | progress + profile mobile redesign (mana под XP/coins, БЖУ Letter→Icon→X/Y, premium-hide для logs_limit_line) |
| 354 | #202 | hide «🔋 Мана: Безлимит» на Progress для premium (тот же premium-hide pattern что mig 353) |
| 355 | #203 | Profile v2: inline title `👤 Vladislav · 📅 с MM.YYYY`, activity/training/phenotype строки, 🎯 Норма в конец, default→phenotype_standard label, 156 i18n rows + 44 phenotype explanation L2 refresh |

(+ mig 352 phase3d cycle fix параллельным агентом, не наш scope)

### Bug fixes без mig

- **PR #193** — cmd_stress_high silent drop (TypeError в `_handle_stress_high`: SimpleNamespace dict-spread с дубликат kwarg). Заменено на `dataclasses.replace`. + state pollution fix в test_phase3b_sleep_ux.py (5 тестов) и test_adaptive_modifiers_foundation.py (T11/T16 mig-329 контракт). + toast «✅ Учтено» для cmd_stress_high (mig 343 contract).
- **PR #194** — Sage fog-prompt regression после Stage 7 cutover. `handle_ai_input` + `_handle_edit_meal_input` молча `return ResponseEnvelope.empty()` при `is_food=False` — теперь резолвят `errors.ai_not_food` / `errors.ai_failed` через `resolve_translation_text` (variant pick + `{{icon_brain}}` substitution).

### Background subagent investigations

1. **fog-prompt root cause** (a68606acb5740a59c) — нашёл live evidence в `ai_coach_logs` за 25.05 (2 not-food события на админе, AI правильно классифицировал, юзер не получил ничего).
2. **phenotype quiz "не сохраняет"** (ab02a8c9b1d5c5110) — **false alarm**. Owner ответил (q1=c, q2=b, q3=c, q4=b) → классификация даёт `default` корректно по матрице. Stat: 129/130 юзеров на проде = `default`. `profile.phenotype_default` ключ отсутствует — для display reuse `phenotype_standard` (synonym per spec).

### Architectural finding: premium-hide line pattern

3 миграции подряд использовали один и тот же pattern для conditional-hide строки (premium юзер → строка отсутствует, без orphan blank line):
- mig 348 `active_modifiers_strip` — LEADING `\n` if non-empty
- mig 353 `logs_limit_line` (Profile) — TRAILING `\n` if non-empty
- mig 354 `mana_line` (Progress) — LEADING `\n` if non-empty

Документирован в новом KB концепте `concepts/premium-hide-line-pattern.md`.

### Latency observations (live ssh tail)

8 кликов админа подряд (00:46-00:47 MSK 26.05):
- routing корректен 8/8 — никаких подмен Прогресс↔Профиль (false alarm от owner)
- p95 ~330ms на click→render
- **double `GET /v_user_context`** на каждый callback (sync_user_profile + _try_authoritative_path независимо). 170ms из 330ms total на pure DB roundtrip. Hot-path optimization candidate (added в `~/.claude/projects/.../memory/project_menu_latency_watch.md`).

### Migration HEAD: 355

Active Python authoritative targets без изменений (Stage 7 cutover ещё активен).
