# 2026-06-02

## Sage ToV-аудит по live-логам owner → патч промптов (PR #283, НЕ merged)

**Что:** ToV-ревизия тона Sage по реальным логам owner `417002669` (01–02.06: food-log
реакции + «Мой день»). 7 точечных правок промптов в `services/sage.py`
(`_DEFAULT_SYSTEM_PROMPT_EN` + `_DEFAULT_SYSTEM_PROMPT_MY_DAY_EN`). Текст-только, без
миграций. PR #283 открыт, **на merge у тимлида** (агент из worktree не деплоит).

**Зачем (находки):**
- **Формула из 3 рефлексов на каждый лог** → предсказуемость убивает «живого собеседника»:
  (1) остроумный заход → (2) биохим-штрих (инсулин/липиды) → (3) хедж «завтра сбалансируем?».
- **Баг правды:** реакция на кашу заявила «зашкаливает по углеводам» при `C18%`. Payload
  food-log содержит строку `Today so far ... C%` (`_build_day_context_lines`, sage.py:764) —
  модель проигнорировала. У food-log НЕ было гардрейла «не противоречь итогам дня», хотя у
  My Day он есть (sage.py:1170). Это был потерянный близнец.
- **Алармизм в халате:** «липаза на грани» прошёл, т.к. запрет покрывал только liver/pancreas,
  а не «фермент на грани».
- **Хедж на чистой еде:** зелёный салат 241 ккал → «не забудем про баланс» (балансировать нечего).
- **Предписания следующей еды:** «не забудь про углеводы позже» (тот же жанр, что забаненные
  «пей воду / добавь клетчатку»).
- **Судящее «переборщил» + закрытие дня в обед** в My Day (guardrail #3 гнал «завтра» на 50%-дне).
- **Просторечие в over_target** («жиров просто завались»).

**Правки:**
1. Характер (оба промпта): `wear knowledge lightly` — биохимия не флекс.
2. MACRO OVERFLOW: обобщён запрет алармизма (любой «орган/фермент на грани/maxed out»);
   биохимия = OPTIONAL rare spice с ротацией, не рефлекс; balance-хинт только при реальном переливе.
3. food-log forbidden: + запрет предписаний следующей еды.
4. DAY CONTEXT пп.2-3: запрет противоречить `Today so far %%` (18% = LOW, не «высоко»);
   «заслужи оговорку» (чистая еда = чистая реакция); эндокринология demoted в опцию.
5. My Day guardrail #3: не закрывать открытый день в обед; убрано «переборщил/перебрал».
6. My Day over_target: планка слога — острая экономная строка, бан просторечия И вычурного «пурпура».

**Намеренно НЕ тронул:** биохимия не выпилена (30% характера Sage) — снята с позиции дефолта
в редкую специю с ротацией. Owner согласовал направление «острее, не пышнее» (отверг встречный
рецепт другого ИИ-агента «больше биохимии / пышные метафоры» — тот ломал лимит 250 символов и
создавал псевдоумные нечитаемые метафоры типа «демографический взрыв жиров»).

**⚠️ После merge+deploy ОБЯЗАТЕЛЬНО:** insight «Мой день» кэшируется (4ч/фазе) — промпт-правки
не видны, пока не сбросить: `UPDATE users SET my_day_insight_at=NULL WHERE telegram_id=417002669;`
+ 2 открытия экрана. Иначе Telethon покажет старый текст и решат, что не сработало.

**Verification план (следующий агент после deploy):** Telethon-прогон тех же приёмов
(салат / каша / курица / вафли) → сверить: (а) нет «зашкаливает» при низких %, (б) нет повтора
биохим-термина 2 лога подряд, (в) чистый завтрак без «но баланс».

**Файлы:** `services/sage.py` (+14/−10). KB-урок добавлен в
[[concepts/sage-food-log-llm-integration]] §«ToV anti-formula».

---

## Adaptive modifiers premium-gate bug + my_plan modifier-aware (mig 424, PR #284)

**Запрос owner'а (тест 417002669):** сон/стресс рисуют кнопки, но КБЖУ не пересчитывается. Плюс: «экран Подписка показывает Премиум до 8 авг, а агент сказал not-premium — как так?». Плюс: проверить научность сплита БЖУ (Б139/Ж79/У344).

**Расследование (live psycopg2 + KB):**
1. `apply_daily_modifier`: всегда пишет qualitative в `daily_metrics` (free), считает дельты, потом premium-gate. Гейт стоял на `user_has_renewable_sub(tid)` → требует `cancelled_at IS NULL`.
2. У 417002669 активная `quarterly` подписка `status='active'`, `expires_at=2026-08-08`, **`cancelled_at=2026-05-25`** (отменил автопродление), `payment_method='wallet'`. → `renewable=False` → гейт трактует как free → 12 строк `daily_modifiers` за 3 дня все `applied=FALSE/premium_required/deltas 0`. КБЖУ не двигалось нигде.
3. **Экран НЕ врал:** `get_subscription_business_data` считает подписку по `status='active'` (cancelled показывает «Доступ до {date}»). Это by design. Сбил с толку мой первый зонд `user_has_renewable_sub`.
4. Корень = **renewable ≠ entitlement.** KB `adaptive-modifiers-architecture:235` прямо называл `user_has_renewable_sub` «canonical Premium check» — ложь, исправлена.

**Fix (mig 424, генерация из live `pg_get_functiondef`):**
- **A:** new `user_has_active_premium(tid)` = `status='active' AND payment_method!='trial' AND expires_at>now()`. Свапнут в `apply_daily_modifier` + `get_daily_stats_rpc` (mig 348 strip gate). `user_has_renewable_sub` НЕ тронут (нужен для button visible_condition resume/cancel).
- **B:** `get_my_plan_business_data` накладывает `compute_daily_modifier_stack` дельты (формула как `get_day_summary`), пересчитывает % из скорректированных грамм. Это экран куда ведёт кнопка (`next_on_submit=my_plan`). Non-premium→0→no regression.

**Verified LIVE (ROLLBACK txn для функционала):** `active_premium=True` (было renewable=False); `apply sleep/short`→`applied=true` белок+15%/угли−15%; my_plan 139/79/344→**160/79/292** ккал 2643; day_summary совпал. p95 VPS: my_plan 45ms, daily_stats 47ms.
**Гайка:** сегодняшние ранние таппинги остались `applied=FALSE` — owner должен нажать заново чтобы увидеть.

**Наука БЖУ (нутрициолог-ревью):** Жир 79г = floor `0.8 г/кг` (обоснован). Белок 139г = `protein_g_per_kg_cardio=1.4` × 99кг → **занижен** для дефицита (норма 1.6–2.2 г/кг). Углеводы 344г (52%) = остаток → раздуты как следствие низкого белка. → **Fix C** (goal-aware protein floor для lose) спроектирован, НЕ реализован, design в `personalized-macro-split.md §PROPOSAL`. Owner: «спроектируй отдельно».

**Owner rule:** «КВ (KB) посмотреть обязательно, все агенты должны смотреть КВ» → `memory/feedback_kb_consult_first.md`.

**KB-правки:** `adaptive-modifiers-architecture.md` (убрана ложная canonical-догма + §mig424 lesson), `personalized-macro-split.md` (§PROPOSAL Fix C).

---

## Sage ToV — live-верификация PR #283 + «ты»-фикс (PR #287) + KB audit (вечер)

**PR #283 СМЕРЖЕН owner'ом + задеплоен** (подтверждено: VPS `sage.py` содержит «wear knowledge lightly» ×2; коммит f14d813 в origin/main). Кэш «Мой день» вручную сброшен (`my_day_insight_at=NULL`, был возраст 1ч9м — сам не очистился).

**Live-прогон owner 417002669 (19:37, день: ккал 68%, жир 151%, углеводы 39%, сон/стресс плохие) — все 4 тяжёлых бага закрыты:**
- ✅ **Carb-truth:** каша при углеводах ~40% — НЕТ «зашкаливает по углеводам» (вчера галлюцинировал). Гардрейл сработал.
- ✅ **Алармизм:** курица 151% жира → «праздник жиров», НЕТ «фермент на грани».
- ✅ **«Переборщил»:** Мой день (103% ккал, жир 229%) → «день вышел жировым» нейтрально, без суждения.
- ✅ **Биохим-рефлекс:** реальные генерации без форсированной биохимии; повтора термина нет.
- ℹ️ Котлета (3-й лог за минуту) выдала **fallback** «Логнул...» — LLM timeout на rapid-fire, страховка `pick_food_log_fallback`, не тон-регрессия.

**Новые мелкие наблюдения:**
1. **«Вы» вместо «ты»** — каша-ответ свалился на формальный регистр («Ваша каша», «держите»). → **Fix: PR #287** (НЕ merged) — строка в rule 2 (Language) обоих промптов: always informal 2nd-person singular (ты/tú/du/tu), never Вы. Языко-независимо. +2/−2.
2. **🟡 WATCH: новый хвост «стресс/недосып → потянет на сладкое»** — в 3/4 ответов. Сегодня корректно (сон/стресс реально плохие, wellbeing integration обязана), но если всплывёт на НОРМАЛЬНОМ дне → старый биохим-рефлекс переродился в wellbeing-рефлекс, надо demote как биохимию. Копим логи день-два, не фиксим вслепую.

**После merge PR #287 + deploy:** сбросить `my_day_insight_at=NULL` для 417002669 (food-log виден сразу, My Day кэшируется).

**KB audit (`/kb-close --audit`):** 141 концепт. Дубликаты — 13 пар, все low-score (≤0.25) vocab-overlap, реальных нет (n8n-selfhost пара уже взаимно слинкована). Orphans — **0**. Stale — 17, все `n8n`-tagged `warning` (миграционное старение); массовый баннер НЕ ставил — несколько без баннера являются активными canonical-рецептами (n8n-data-flow-patterns/subworkflow-contract/save-bot-message-contract/food-log-python-cutover/one-menu-ux), пометка сломала бы живое. База структурно здорова. MEMORY консолидирована (см. ниже).

---

## diet_type — просмотр+смена из меню профиля (PR #288, mig 425 applied LIVE)

**Запрос owner'а:** тип питания (omnivore/vegetarian/vegan) спрашивался только в
онбординге — юзер не видел и не мог сменить его из меню, хотя он влияет на белок
(DIAAS ×1.25 vegan / ×1.10 vegetarian, mig 291). Вывести как просматриваемое+
редактируемое поле, при смене — пересчёт КБЖУ.

**Находка (всё расследование на live psycopg2 — источник правды):** инфраструктура
была **полностью построена ещё в mig 291**, но кнопку-вход не подключили. Уже жили:
- экран `edit_diet` (parent=my_plan, `meta.current_value_col=diet_type`) + 3 кнопки
  выбора (`cmd_diet_omnivore/vegetarian/vegan`, `save_via_callback`, `save_rpc=set_user_diet_type`) + Back;
- RPC `set_user_diet_type(bigint,text)` — **уже вызывает** `calculate_user_targets(.., TRUE)`;
- переводы `questions.diet_question` + `answers.diet_*` ×13;
- заготовлены `profile.diet_label` («Стиль питания:») и неактивный `cron_notifications.diet_retrofit_prompt`
  («зайди в Профиль → Диета» — но кнопки не было; cron его не шлёт, вреда нет).

mig 291 comment прямо говорил «edit_diet screen + profile_main entry добавляются
отдельным commit'ом» — entry так и не доделали. Экран висел в воздухе (попасть нельзя).

**Размещение (согласовано с owner через AskUserQuestion):** оба кандидата owner'а
(«⚙️ Настроить план» / «рядом с Телосложением») сходятся в один экран — phenotype
(`cmd_edit_phenotype`) уже живёт на `my_plan_settings`. Кнопка diet поставлена в (1,1)
справа от «Телосложение» (1,0). Owner подтвердил.

**Сделано (достроены провода):**
- **mig 425:** `app_constants.icon_diet`=🍽 · `workflow_states.edit_diet` (ОБЯЗАТЕЛЬНО —
  иначе `set_status='edit_diet'` падает по FK `users.status→workflow_states.state_code` 23503) ·
  кнопка-вход `my_plan_settings(1,1)` cmd_edit_diet → meta{set_status,target_screen=edit_diet} ·
  `buttons.edit_diet` ×13 (лейбл = `profile.diet_label` минус двоеточие — уже отревьюен в 291,
  новой L1 не требует; deep-merge jsonb_set+COALESCE, sibling-safe).
- **router.py** (4 новые строки, не модифицируя старые → rebase clean): `edit_diet` →
  `BUTTON_ONLY_STATUSES` + `PROFILE_V5_STATUSES`; `cmd_edit_diet` → `PROFILE_V5_CALLBACKS`;
  `cmd_diet_` → `PROFILE_V5_PICKER_PREFIXES`.
  **Коллизия-гард проверен:** онбординг-diet `registration_step_diet ∈ ONBOARDING_STATUSES`,
  picker-секция 4b стоит на `status not in ONBOARDING_STATUSES` → онбординг-клики `cmd_diet_*`
  НЕ перехватываются в menu_v3, идут в onboarding-handler как раньше.

**Verified LIVE (owner 417002669):**
- routing 4/4: cmd_edit_diet@registered→menu_v3, cmd_diet_vegan@edit_diet→menu_v3,
  cmd_diet_vegan@registration_step_diet→onboarding (не перехвачен), cmd_back@edit_diet→menu_v3;
- пересчёт: omnivore 139г→vegan **174г** белка (×1.25; carbs 344→309 remainder; ккал 2643 без изменений), восстановлено в omnivore;
- render: кнопка в my_plan_settings (raw kb), `is_current=true` на vegan в edit_diet (галочка ✅ работает);
- p95 VPS (persistent conn): render my_plan_settings 41ms / edit_diet 41ms / set+recalc 48ms.

**Не трогалось:** экран edit_diet, его кнопки выбора, RPC, переводы вопроса/ответов
(уже живые). `my_plan_settings_text` не менялся (питание влияет на сплит белка, а не
на сумму калорий — фраза «влияют на расчёт калорий» осталась бы неточной).

**Lesson durable:** прежде чем строить «новую» фичу — grep экранов/RPC в live: edit_diet
+ set_user_diet_type + переводы уже существовали (mig 291), задача свелась к 1 кнопке +
4 строкам роутера. Паттерн «screen built, entry never wired» — проверяй `ui_screen_buttons`
на наличие кнопки с `target_screen=<screen>` прежде чем считать экран отсутствующим.

**Файлы:** `migrations/425_diet_type_profile_entry.sql`, `dispatcher/router.py` (+11).
KB: [[concepts/headless-picker-pattern]] §«edit_diet — screen-built-entry-missing».

---

## RFM/Katch для всех фенотипов + LBM-матрица белка + waist UI (mig 427/428, PR #289)

**Запрос owner'а:** замер талии → Katch-McArдle должен работать не только для obese; вынести ввод талии в меню ([📏 Мои Данные]). Owner+PM утвердили Вариант А по документу «Глубокий анализ формулы расчёта целей NOMS».

**Факты верифицированы в интернете (owner просил):** RFM `64−20×(рост/талия)+12×пол` (Woolcott-Bergman 2018, NHANES n=12581 vs DEXA) ✓; Katch `370+21.6×LBM` ✓; белок при дефиците 2.3–3.1 г/кг FFM (Helms 2013/2014) ✓ — наша матрица консервативна.

**Поднял голос (факт устарел):** owner думал, что не-obese считаются по Брока. Проверка live: **Брока ликвидирована ещё mig 295**; не-obese = Mifflin на полный вес (monw = ×0.85 flat). Вывод owner'а (расширить RFM) остался верным, премиса — нет.

**Критическая связка (поймал до реализации):** RFM делает target_weight=LBM, а белок считается `г/кг × target_weight` → переход на LBM **занизил бы белок** (owner с талией: 1.4×LBM=104г вместо 139). Поэтому RFM нельзя в отрыве от белка. PM подтвердил Fix C и выдал LBM-матрицу (sed1.6/cardio2.0/strength2.4 г/кг LBM). **2 находки сверх ТЗ из документа:** (Шаг 6) жир-floor должен быть на ФАКТИЧЕСКОМ весе, не target_weight — иначе при RFM просядет; (Шаг 2.2) матрица белка переключается только на RFM-ветке.

**mig 427** (calculate_user_targets v11→v12, база из live pg_get_functiondef):
- Step 3: RFM→LBM для любого фенотипа при waist (вынес из obese-only CASE).
- Step 7: белок `IF v_lbm_kg IS NOT NULL` → `protein_g_per_kg_lbm_*` матрица, иначе legacy.
- Step 6: `v_fat_floor := ROUND(fat_min × v_user.weight_kg)` (было target_weight).
- +5 app_constants protein_g_per_kg_lbm_* ('mixed'/'none' — интерполяция агента 2.2/1.6, флаг).

**mig 428** (UI): кнопка `cmd_edit_waist`/`buttons.edit_waist` в personal_metrics (2,0) icon_pencil meta.target_screen=edit_waist; women_health сдвинут (2,1); phenotype_result waist visible_condition `obese AND null`→`waist IS NULL` (все фенотипы); переводы ×13 (RU/EN/UK native, остальные good-faith → L1 review).

**Verified LIVE (ROLLBACK sim по 13 real users):** 12/13 — 0 изменений (нет талии); 1 monw — жир 48→56г (Шаг 6 коррекция, не регрессия, stored не меняется до пересчёта). Owner с талией 99см: 2643/139/79/344 → 2619/148/79/329, Katch BMR 1974, LBM 74.2, BF% 25%, белок ВЫРОС. p95 VPS 41.7ms.

**Migration collision:** 425 занят PR #288 (diet_type, спавн-агент) → переименовал свои 425/426→427/428.

**Durable lessons:**
- Брока уже нет (mig 295) — не верить устаревшим спекам/премисам, проверять live.
- RFM↔белок coupled: смена базиса веса (total→LBM) ОБЯЗАНА сопровождаться пересмотром г/кг коэффициентов, иначе тихое занижение белка.
- Жир-floor — на фактический вес (эндокринная буферизация от общей массы), НЕ на LBM/target_weight (DR Шаг 6).
- Digital Twin (v8.3) отстал от прода (v12) — перед след. правкой расчёта досинк (KB calc-user-targets-test-spreadsheet).

**KB:** personalized-macro-split.md §v12 + Fix C переведён OPEN→RESOLVED. MEMORY mig HEAD→428.

---

## Cutover cleanup: Stage 7c шаг 2 (deactivate 03/06) + 04_Menu_v3 Switch dead-ref cleanup

**Запрос owner'а:** проверить реальный статус миграции n8n→Python и почистить подтверждённо-idle остатки (Payment не трогать — другой агент).

**Верификация (live, не по памяти):**
- Python-authoritative подтверждено `webhook_server._try_authoritative_path`: menu_v3, onboarding, location, payment, profile, restore-flow + **`ai`/`add_food` (Add food + AI Engine) для всех статусов** (global cutover mig 373, 2026-05-29). `forward.py:TARGET_TO_PATH` содержит только `menu_v3` (легаси per-target форвард, не индикатор cutover; комментарий устарел, безвреден).
- **Live n8n: 7 workflows** (было 15). Active: 01_Dispatcher, 04_Menu_v3, 04_Menu, 04.2_Edit_StatsDaily. Inactive: 03_AI_Engine, 06_Indicator_Clear (после сегодня), 08.3_Friends.
- **Здоровье AI Python-пути** (journalctl, полная история с 9 мар): **60 `AUTHORITATIVE_AI` ок, 0 `ai_engine Python handler failed`**. n8n `03_AI_Engine` последний exec 2026-05-30 13:11 (3 успешных), с тех пор 0. 5 хитов `/internal/food_log/render`, 46 `forward_to_n8n` (это профильные `render_screen_empty`-фоллбэки + Edit Meal → 04_Menu, не AI).

**Сделано (с одобрения owner'а, plan→approve):**
1. **Deactivate 03_AI_Engine (`kjw4kkKMD0IqNALg`) + 06_Indicator_Clear (`jQn0nTxThFal4Kpe`)** — `UPDATE workflow_entity SET active=0` на live SQLite `/home/noms/n8n/data/database.sqlite`. Verified: SQLite SELECT + API GET = `active=false`.
   - **Gotcha n8n 2.17.7:** API `/activate`+`/deactivate` → `Forbidden`; PUT не меняет `active` (read-only). Только прямой SQLite UPDATE (прецедент 10_Payment). Харнесс сначала заблокировал прод-БД write → запросил явное разрешение owner'а → выдано.
   - **render-endpoint `/internal/food_log/render` НЕ удалял** — 03 ещё callable через executeWorkflow (fallback) и зовёт его. Удалять только при физическом DELETE 03 (Stage 7c шаг 5, ~после 05-06).
   - **Рестарт n8n НЕ делал** — `active` для executeWorkflow-sub-workflow'ов косметический, fallback (`01_Dispatcher → Go to 03_AI → Execute Workflow Trigger`) от флага не зависит. In-memory webhooks 03/06 живут до след. рестарта, Python их не зовёт.
2. **`04_Menu_v3 → Switch Forward To`** — Safe PUT (GET→whitelist→PUT→GET verify): rules `[03_AI_Engine, 02_Onboarding_v3, 02.1_Location, 10_Payment]` → `[03_AI_Engine, 10_Payment]`. Убраны 2 мёртвых case без connections; живой output[0]→`Go to 03_AI_Engine` сохранён; payment-case оставлен (вне scope). active=True, 20 нод целы.

**НЕ трогал (намеренно):** Stage 7c шаги 1/3/4/5 (окно 7д стабильности закрывается ~05-06, сегодня день 4 — global был 05-29; n8n AI срабатывал 05-30 внутри окна). `01_Dispatcher Route Classifier` устаревшие комментарии (риск на живом Code-node без пользы). Payment-case в Switch + любые payment-рефы.

**Python-кода не менял → PR/деплой не нужен.** Все изменения n8n-side (SQLite + API PUT).

**KB-правки:** `stage7-global-cutover.md` §7c прогресс-баннер; `architecture-registry.md` новая секция «Состояние на 2026-06-02» + обновлён top Status.

**Для следующего агента (~05-06):** свежий 0-exec чек 03/06 за чистые 7д → если 0, DELETE `/internal/food_log/render` + Route Classifier executeWorkflow refs cleanup + n8n DELETE 03/06. Рецепт — KB [[stage7-global-cutover]] §7c.

---

## BUGFIX diet save — set_user_diet_type не парсил callback (mig 429, PR #290)

**Репорт owner'а (скрин):** выбрал «Вегетарианство» на edit_diet — галочка не
сместилась, нет «сохранено», экран не сменился. Спросил про UX-паттерн фидбека.

**Расследование (live psycopg2, воспроизведение через `dispatch_with_render`):**
- PR #288 смёржен+задеплоен (роутер-фикс на VPS), окно деплоя НЕ причина.
- Owner в БД: `diet_type=omnivore` (не сохранилось), `status=registered`, nav top
  `my_plan_settings`. Пошаговый прогон: клик `cmd_edit_diet` ✓ (status→edit_diet,
  push), клик `cmd_diet_vegetarian` → navigate my_plan ✓, status cleared ✓, **но
  diet=omnivore (save не выполнился)**.
- **Корень:** `process_user_input` стр.331 передаёт сеттеру сырой `v_callback`
  (`cmd_diet_vegetarian`), не `save_value`. `set_user_diet_type` (mig 291)
  валидировал `IN (omnivore/vegetarian/vegan)` без парсинга префикса → INVALID →
  no UPDATE. clear_status/target_screen идут независимо → молчаливый no-op.
  `set_user_training_type` и все рабочие сеттеры парсят `cmd_*` — diet был одиночка.

**Fix (mig 429, RPC-first, applied LIVE):** strip `cmd_diet_` в set_user_diet_type,
чистое значение тоже принимается. Verified: omnivore→vegetarian белок 139→153г
(×1.10), navigate my_plan, restored. p95 VPS 43.5ms. Деплой кода не нужен (RPC).

**Durable lesson (в KB headless-picker-pattern):** picker-сеттер ОБЯЗАН парсить
сырой callback; тест-гейт — через `dispatch_with_render`, НЕ прямой вызов сеттера
(прямой с чистым значением зелёный, реальный клик красный — это и пропустил
verification mig 425).

**UX-вопрос owner'а про «сохранено»:** для inline-picker'ов NOMS паттерн = navigate
на родительский plan-screen (как training/goal/phenotype). Явный toast «✅ Сохранено!»
есть ТОЛЬКО для numeric text-input (edit_age/weight/height, `_PROFILE_V5_TEXT_INPUT_STATUSES`).
Обсудить с owner: оставить как у всех (консистентность) либо добавить toast/реакцию
к diet-пикеру (тогда и к остальным — иначе рассинхрон).

---

## Waist-кнопка fix: router wiring + current-value + RFM/Katch в app_constants (mig 430, PR #291)

**Запрос owner'а (скрин):** кнопка [Талия] в [📏 Мои Данные] есть, но НЕ работает (клик пустой). + требования: current-value display, ×13, инструкция по замеру, важные параметры в app_constants.

**Причина «не работает»:** `cmd_edit_waist` отсутствовал в `dispatcher/router.py` (`PROFILE_V5_CALLBACKS`), `edit_waist` — в `PROFILE_V5_STATUSES`. Из меню (status='active') клик уходил в legacy n8n 'menu' → silent fail. Из онбординга (phenotype_result, статус регистрации) работало — поэтому в mig 428 не заметил. Это **повтор diet/mig425 урока** «screen built, entry never wired», но на уровне роутера.

**Fix (PR #291, mig 430):**
- **router.py** (DEPLOY PENDING): `cmd_edit_waist`→PROFILE_V5_CALLBACKS, `edit_waist`→PROFILE_V5_STATUSES. НОВЫЕ строки (NON-OVERLAPPING с diet's cmd_edit_diet → rebase auto-merge без конфликта).
- **A.** `get_personal_metrics_business_data` += `waist_circumference` → value-label «Талия: {waist_circumference} см» (NULL→«—»).
- **B.** Кнопка: text_key→`buttons.edit_waist_value`, `meta.set_status='edit_waist'` (без set_status введённое число не роутится как талия).
- **C.** `buttons.edit_waist_value` ×13; `questions.waist_question` дополнена «утром натощак» ×13 (база ×13 с mig 295: горизонтально/выдох/выше пупка).
- **D.** RFM/Katch коэф. (rfm_intercept_male=64/female=76/coef=20/clamp5-60, katch 370/21.6) + waist_cm_min/max(40/200) → **app_constants** (no-hardcode/прозрачность). `calculate_user_targets` v12→v13 читает их через один SELECT (defaults=исходные).

**Verified LIVE (ROLLBACK):** вынос констант = ИДЕНТИЧНЫЙ вывод (13/13 real users 0 изменений; owner+талия99=2619/148/79/329 как mig427). p95 VPS calc 42.2ms / personal_metrics 42.6ms.

**Diet-агент параллельно (PR #290/mig 429):** их picker-сеттер не парсил `cmd_*` (save_via_callback передаёт сырой callback_data) → diet не сохранялся. Мой waist — **text-input, не save_via_callback** → сеттер получает число напрямую, этот баг не касается. Но durable: **picker-сеттеры (save_via_callback) ОБЯЗАНЫ парсить cmd_*; тест через dispatch_with_render, не прямой вызов сеттера.**

**Git §12:** новая ветка от устаревшей базы дала diff с удалением чужой mig 425 — поймал sanity-check, rebase на свежий main схлопнул. 429 занят PR #290 → renumber 430.

**Durable lessons:**
- Новая edit-кнопка в МЕНЮ = router-wiring (PROFILE_V5_CALLBACKS + STATUS) + set_status ОБЯЗАТЕЛЬНЫ. Из онбординга работающая кнопка ≠ работает из меню (разные статусы).
- Формульные константы (RFM/Katch) можно вынести в app_constants с защитным description; читать одним SELECT (latency не страдает, p95 42ms).

---

## UX pattern B для edit_diet — inline-фидбек смены (mig 437, PR #293)

**Owner выбрал pattern B** (после фикса save mig 429): остаёмся на экране выбора,
✅ переезжает, живая строка белка. «Если зайдёт — потянем на остальные picker'ы».

**Реализация (data-only, LIVE сразу, без Python):**
- edit_diet → свой text_key `questions.diet_question_edit` ×13 (shared
  `questions.diet_question` НЕ трогать — он у onboarding_diet_question без
  business_data → плейсхолдер не разрешится в онбординге).
- `edit_diet.business_data_rpc = get_my_plan_business_data` (reuse, modifier-aware) →
  render_screen кладёт `target_protein_g` в template_vars → template_engine подставляет
  `{target_protein_g}`.
- 3 кнопки: `target_screen=edit_diet` (re-render self; push_nav НЕ сработает —
  next==current, process_user_input стр.460 `IS DISTINCT`), `clear_status` убран
  (статус остаётся edit_diet → picker живёт). Back не трогали — nav-pop уводит на
  my_plan_settings (откуда пришёл).

**Verified live (dispatch_with_render + _resolve_text, owner):** тапы остаются на
экране, ✅ переезжает, финальный текст «📊 Белок: 160→200 г/день» (omnivore→vegan,
с модификаторами), nav без дублей, Back→my_plan_settings.

**Механика подстановки (durable, было неочевидно):** render_screen строит
`telegram_ui.template_vars = base_vars || business_data` (SQL стр.110), Python
`template_engine._resolve_text` подставляет `{key}` из template_vars. Чтобы экран
показывал данные — задать `ui_screens.business_data_rpc` (любой RPC(telegram_id)→jsonb)
+ `{placeholder}` в тексте. НЕ нужен Python-код.

**Watch:** число белка с адаптивными модификаторами (160/200, не голые 139/174) —
реальная дневная цель. Если owner захочет голую базу — 1 строка. Если pattern зайдёт
→ перенести на edit_training/edit_goal/edit_phenotype.

---

## Entitlement SSOT — user_has_active_access (mig 434, PR pending)

**Запрос тимлида:** свести ВСЕ разрозненные проверки B-тира прав (`IN ('premium','trial')`,
`NOT IN ('free','expired')`, `<> 'free'`) к одному предикату во всех RPC + починить 2
несостыковки trial. Мотив: техдолг — появится статус `'promo'`, забудем обновить половину.

**Расследование (live psycopg2, инвентарь 36 функций):** баг-паттерн mig 424 (renewability
как гейт) после 424 **не встречается нигде** — ни один RPC не зовёт `user_has_renewable_sub`
как гейт. Остальные гейты на колонке `subscription_status` (для cancelled-paid 417002669 =
'premium', багом не задета). Реальные статусы: только `free/premium/trial` (нет `promo`/`expired`
в колонке) → `<> 'free'` ≡ `IN ('premium','trial')` = тир B.

**Несогласие с реализацией тимлида (tid → text):** предложил предикат на СТАТУСЕ (text), не
tid. Почти все B-гейты уже загрузили `subscription_status` в локалку → tid-версия = повторный
lookup `users` на каждом логе еды/трате маны (горячие пути). text + `IMMUTABLE` → планировщик
инлайнит. **Доказано `EXPLAIN VERBOSE`:** `Output: (COALESCE(subscription_status,'free') =
ANY ('{premium,trial}'))` — вызова функции НЕТ, голое выражение → zero-cost. bigint-обёртка
делегирует в text (для будущих tid-only callers).

**mig 434 (генерация из live `pg_get_functiondef`, assert replacement-count per RPC):**
- `user_has_active_access(text)` IMMUTABLE = `COALESCE(status,'free') IN ('premium','trial')` +
  `(bigint)` STABLE-обёртка.
- Замена B-хардкодов: `cron_get_reminder_candidates` ×4, `check_and_deduct_mana` ×2,
  `log_meal_transaction`, `replace_meal_transaction` (был blacklist `NOT IN` → whitelist),
  `get_profile_business_data` ×2, `get_shop_business_data`.
- **3 fix'а того же класса (trial показывался как free):** `recharge_mana_with_coins`
  (`='premium'` → trial жёг коины на ману при безлимите), `get_shop_rpc.is_premium`,
  `get_progress_rpc` mana-display (×2, «N/M» вместо «Безлимит»). Третий (`get_progress`)
  нашёлся при полном скане — того же класса, добавлен.

**НЕ тронуто (boundary, документировано):** `user_has_active_premium` (A, готов),
`user_has_renewable_sub`/`resumable` (C, кнопки), `cron_regenerate_mana` (`!='premium'` тир A —
замена = grace behavior-change + EXISTS-в-крон, отдельное решение), writers
(`activate/renew/expire`), button `visible_condition` (UI render-слой).

**Verified LIVE (committed txn):** truth-table text+bigint предикатов (premium/trial=t, free/NULL=f);
каждый RPC содержит предикат expected×N; A/C предикаты целы. Функц-smoke trial 6378579500:
recharge→ALREADY_PREMIUM (FIX#1), shop_rpc.is_premium=true (FIX#2), progress mana=«Безлимит»
(FIX#3), shop_business hidden_for_premium=true. **p95 VPS:** worst 52.8ms (shop 42.5 / progress
51.9 / profile 46.5 / reminder-cron 43.6 / predicate 42.2) — порог 1000ms, ноль регрессии.

**KB:** [[subscription-management-headless]] §«Entitlement predicates — SSOT» (матрица A/B/C +
анти-паттерн renewable≠entitlement + «почему text не tid» + EXPLAIN-доказательство);
[[adaptive-modifiers-architecture]] cross-ref + premium-filter обновлён на mig 434.

**Файлы:** `migrations/434_entitlement_active_access_ssot.sql`, `tools/mig434_{gen,apply,bench}.py`,
`tools/entitlement_{inventory,probe,context}.py` (диагностика).

**Lesson durable:** entitlement = 2 предиката (A `user_has_active_premium` paid / B
`user_has_active_access` incl-trial) + C `renewable` ТОЛЬКО для кнопок. SSOT-предикат на B
бери на СТАТУСЕ (text)+IMMUTABLE → инлайн → zero-cost, не на tid (re-fetch на горячих путях).

---

## Content-type silent-drop fix (PR #294, worktree agent-a872322290c635549)

**Что:** Три под-фикса для молчаливого отбрасывания входящего контента бота.
Ветка `claude/content-type-silent-drop`, PR #294, NOT merged/deployed (агент из worktree).

**Изменённые файлы:**
- `dispatcher/router.py` — новый `_is_image_document()`, расширен `_is_junk_content()` (добавлены video_note/contact/poll/dice), новые поля `has_image_document`/`has_audio` в `RouteDecision`, роутинг image-doc и audio → `ai/food_media`
- `handlers/food_log.py` — извлечение `file_id` из `message.document` (image/*) и `message.audio` во всех трёх хендлерах (`handle_ai_input`, `_handle_edit_meal_input`, `handle_onboarding_food`)
- `webhook_server.py` — перехват `target=error/junk_content` в `_try_authoritative_path`, отправка `messages.spam_protect` через `send_telegram` (Python теперь owner этого ответа)
- `tests/dispatcher/test_router.py` — 37 новых table-driven cases + 5 flag-assertion тестов
- `tests/handlers/test_food_log_ai_input.py` — 4 новых integration-теста (image-doc→vision, audio→transcribe, photo/voice регрессия)

**Зачем:** Целые классы контента молча отбрасывались — юзер прислал, бот промолчал:
- (а) фото отправленное "как файл" (desktop, без сжатия) → `document` → junk → тишина
- (б) `message.audio` (mp3/m4a) → не детектировалось → тишина
- (в) sticker/video/animation/contact/poll/dice → `target=error` → n8n 01_Dispatcher → "Send Junk Error" — но n8n деcommissioning, Python не подхватывал

**Ключевой инсайт:** `messages.spam_protect` ×13 уже живёт в БД (n8n era, ключ использовался в "Send Junk Error" ноде). Миграция не нужна. Доступ: `translations['messages']['spam_protect']` из `ctx.translations`.

**Статус:** PR #294 открыт, NOT deployed. Tests: 200 passed (4 pre-existing failures в не-scope случаях).

**Lessons:**
- `video_note`/`contact`/`poll`/`dice` не были в оригинальном `_is_junk_content` tuple — молча проваливались в legacy n8n без ответа
- порядок в router.py критичен: image-doc/audio gate ПОСЛЕ junk gate (если `_is_image_document` выдаёт False → документ падает в junk; если True — escape выше через routing logic)
- `messages.spam_protect` = reuse pattern: перед созданием новых translation keys искать в БД по семантике

---

## Waist-кнопка: FK + сеттер-тип фиксы + нативные ×13 (mig 434/433, PR #292)

**Тест ownerّа (скрин):** mig 430 задеплоен (кнопка [Талия] отрисовалась с «Талия: — см» — router заработал), но клик → «⚠️ Что-то пошло не так».

**Два бага (оба в mig 430 недоделаны):**
1. **FK 23503:** `set_status='edit_waist'` падал — `edit_waist` отсутствовал в `workflow_states` (`users.status` → FK на `workflow_states.state_code`). Все прочие edit_* там есть; diet добавлял `edit_diet` в mig 425, я пропустил аналог. → `INSERT workflow_states.edit_waist (screen_id=edit_waist, save_rpc=set_user_waist_circumference, next=registered)`.
2. **Тип сеттера:** `set_user_waist_circumference(bigint, NUMERIC)` (mig 295), а text-input диспетчер `process_user_input` вызывает `EXECUTE ... USING p_telegram_id, v_text::TEXT` → функция `(bigint,text)` не резолвится → save_rpc_failed → generic error. Все прочие сеттеры (weight/height/age) — `(bigint, p_input_text TEXT)`. → DROP numeric + CREATE text-версию (паттерн set_user_weight: парсинг regex, range из app_constants waist_cm_min/max, **PERFORM calculate_user_targets(TRUE)** для recalc).

**Verified end-to-end (ROLLBACK):** Профиль→Мои Данные→[Талия]→«95»: клик→status=edit_waist (без ошибки), «95»→waist сохранён, статус→registered, пересчёт белок 139→152, ккал 2643→2667 (RFM BF%23.4→LBM75.8→Katch+LBM-матрица), кнопка «Талия: 95 см».

**mig 433 — нативные ×13 (PR #292):** субагент-переводчик сверился с KB copywriter-playbook (тон Sassy Sage, ≤18 симв/кнопка, gender-neutral, anti-shame); main agent проверил (плейсхолдер {waist_circumference} ×13, числа 40-200, fa-цифры→западные). Заменил good-faith mig 430 на L1 для buttons.edit_waist_value / questions.waist_question / errors.invalid_waist.

**Durable lessons:**
- **Новая edit-кнопка в меню = 3 провода:** (a) router (PROFILE_V5_CALLBACKS + STATUS), (b) `workflow_states.<status>` row (FK!), (c) сеттер с правильной сигнатурой `(bigint, text)` под text-input диспетчер. Пропуск любого → silent fail / generic error. Diet (mig 425/429/431) и waist (430/432) набили один и тот же набор граблей по очереди.
- **Сеттеры для text_input ОБЯЗАНЫ принимать TEXT** (не numeric/enum) — диспетчер всегда передаёт строку; парсинг/каст внутри функции.
- Migration collision: 431 заняли параллельно я (waist) и diet (UX pattern B PR#293) → renumber waist→432/433.

---

## KB audit (/kb-close --audit) + PR #292 merged

**PR #292 (waist кнопка fix mig 435/436) — MERGED** владельцем. Кнопка [Талия] полностью работает в проде.

**audit.py (143 концепта):**
- **merge_candidates:** 13 пар, scores ≤0.25 (в осн. 0.07, hub `supabase-db-patterns` ×4) → лексические false-positives, подтверждённых дублей НЕТ → не сливал.
- **orphans:** 0.
- **stale:** 17, ВСЕ severity=`warning` (нет `high`); почти все n8n-legacy уже мигрированы/с баннерами; часть свежие (one-menu-ux 1d — флаг по n8n-тегу, не реально stale). Без high-severity массовый re-banner = churn → не трогал.
- **lint --structural-only:** 163 «broken link» errors — **tool-noise**: ссылки `[[slug]]` существуют, но линтер ждёт `[[concepts/slug]]`-формат (format-mismatch, pre-existing baseline). Не вводил новых; concept-файлы в этом audit не правил. **Флаг владельцу:** линтер/ссылочный формат стоит привести к единому виду отдельной сессией (KB-health debt, не блокер).
- **MEMORY:** 31KB/153 строки — над порогом (20KB/150). Бэкап `MEMORY.backup-2026-06-02.md` сделан → консолидация.

**MEMORY консолидирована:** 31.9KB/153 → **22.1KB/151** (под порогом 25KB). Свёрнуто 10 многострочных «Current state» буллетов (вся сегодняшняя смёрженная работа: waist/RFM/diet/entitlement/Sage/premium-gate) в 5 плотных durable-буллетов; детали остались в daily + KB-концептах. Бэкап `MEMORY.backup-2026-06-02.md`. Durable-уроки сохранены (3-провода edit-кнопки; entitlement A/B/C; collision/stale-base §12; single-source system prompt).

---

## Session close — diet cluster (PR #288/#290/#293, mig 425/429/437)

**Итог сессии (3 PR по diet_type, все applied LIVE):**
1. **mig 425 / PR #288 (merged):** подключена кнопка-вход edit_diet (экран+RPC+переводы
   уже были с mig 291, не хватало провода). Lesson «screen built, entry never wired».
2. **mig 429 / PR #290 (merged):** фикс — `set_user_diet_type` не парсил `cmd_diet_*`
   callback → save молча no-op. Lesson «picker-сеттер ОБЯЗАН парсить cmd_*; тест через
   dispatch_with_render, не прямой вызов».
3. **mig 437 / PR #293 (open, applied LIVE):** UX Pattern B — остаёмся на picker,
   ✅ переезжает, live-строка белка. Renumber 431→437 (collision с #292 waist, CI зелёный).

**🟢 OWNER DIRECTIVE (важно для всех будущих агентов):** Pattern B признан лучшим и
**назначен дефолтом** для edit-picker'ов. Owner вживую: «пересчитывает очень быстро
белок и это наглядно». Цитата: «использовать как лучший и чтобы другие агенты в своей
работе принимали его». Зафиксировано:
- KB [[headless-picker-pattern]] §Pattern B — баннер OWNER DIRECTIVE, секция переименована
  из «альтернатива» в «⭐ РЕКОМЕНДОВАННЫЙ ДЕФОЛТ».
- index.md — новая строка «Новый/правимый edit-picker» → Pattern B.
- MEMORY User Preferences — durable-строка про Pattern B.
- **Action для след. агентов:** при касании старого edit-picker'а (training/goal/phenotype/
  activity/speed) — мигрировать на Pattern B, если нет причины обратного.

**Durable lessons этой сессии (все в KB [[headless-picker-pattern]]):**
- Новая edit-кнопка в меню = 3 провода: router (PROFILE_V5_CALLBACKS+STATUS) +
  `workflow_states` row (FK 23503!) + сеттер сигнатуры под диспетчер.
- save_via_callback dispatch передаёт сеттеру СЫРОЙ `callback_data` (стр.331) → парсить `cmd_*`.
- business_data substitution: `ui_screens.business_data_rpc` → render_screen мёржит в
  `template_vars` (SQL стр.110) → `template_engine._resolve_text` подставляет `{placeholder}`.
  Data-only, без Python-кода.
- Тест picker'а: только `dispatch_with_render` + `_resolve_text` (прямой вызов сеттера
  зелёный, реальный клик красный).
- Deploy: RPC/data-миграции действуют в проде сразу (применил psycopg2 → LIVE), деплой
  кода не нужен; роутер-правки — нужен deploy (DB-before-code window, флагать owner'у).
