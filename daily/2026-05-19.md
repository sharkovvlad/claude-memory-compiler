# Daily / Handover — 2026-05-19

## Что сделано сегодня

### 1. NLM sync — полная очистка и перезагрузка

Блокнот `NOMS Supabase Data` накопил мусор (214 источников). Удалены все, загружены заново 3 слоя:
- **DB (84):** все публичные таблицы Supabase + RPC-функции (69 файлов, 55+14 с ретраем)
- **n8n (8):** 7 активных воркфлоу + index (VPS теперь отдаёт 10 воркфлоу, 3 деактивированы)
- **Код (120+1 index):** 120 файлов Python/JS + CLAUDE.md
- Итого: 212/300 источников
- Манифест сброшен и пересоздан: `.nlm_code_manifest.json`

### 2. n8n backup — переход на self-hosted VPS (SSH tunnel)

`scripts/n8n_backup.py` полностью переписан:
- При `N8N_SSH_HOST` в `.env` → открывает SSH туннель Mac→VPS:5678, тянет API key с VPS, делает backup, закрывает туннель
- Без `N8N_SSH_HOST` → прямой HTTP (для отладки, указать `N8N_BASE_URL`)
- Cloud n8n отменён 2026-04-30; `N8N_API_KEY` в `.env` больше не нужен
- Добавлен флаг `--force` для перезаписи сегодняшнего бэкапа

### 3. code-sync — добавлен git pull перед синком

`noms-code-sync/SKILL.md` теперь запускает `git pull origin main --ff-only` перед сканированием.
Причина: агенты работают в worktrees, мержат PR на GitHub — локальный диск может отставать.

### 4. notebooks.json — обновлено описание

Описание блокнота `noms/supabase-data` теперь включает все 3 слоя с префиксами источников.

---

## Текущее состояние NLM-инфраструктуры

### Блокнот

| Параметр | Значение |
|---|---|
| Название | NOMS Supabase Data |
| ID | `fa75f4de-96c2-4dc0-bc9d-0bf07bd992f5` |
| Всего источников | 212/300 |
| Последний полный синк | 2026-05-18/19 |

### Слои и расписание

| Слой | Scheduled task | Cron | Ручная команда |
|---|---|---|---|
| DB (Supabase) | `noms-supabase-sync` | daily 02:05 | `/nlm-noms` |
| n8n workflows | `noms-n8n-backup` | every 2d 02:00 | — |
| Python/JS код | `noms-code-sync` | daily 03:00 | `/nlm-noms-code` |

### Ключевые скрипты

| Скрипт | Путь | Назначение |
|---|---|---|
| `n8n_backup.py` | `NOMS/scripts/` | SSH tunnel → VPS API → JSON архив |
| `n8n_to_markdown.py` | `NOMS/scripts/` | JSON → Markdown для NLM |
| `code_to_nlm.py` | `NOMS/scripts/` | Инкрементальный code sync |
| `supabase_source.py` | `NotebookLM+Claude/plugins/notebooklm-rag/scripts/` | Supabase export |
| `nlm.py` | `NotebookLM+Claude/plugins/notebooklm-rag/scripts/` | Batch upload + wait |

### Конфигурация `.env` (NOMS)

```
N8N_SSH_HOST=root@89.167.86.20   # self-hosted n8n VPS
# N8N_API_KEY — удалить после подтверждения (cloud отменён)
```

### Источники по префиксам

| Префикс | Слой | Удаляются при синке |
|---|---|---|
| (без префикса) `tablename_2026-...` | DB таблицы | По имени таблицы |
| `n8n_*` | n8n workflows | Все `n8n_*` перед загрузкой |
| `code_*` | Python/JS/CLAUDE.md | По манифесту (только изменённые) |

---

## Известные проблемы / TODO

### ⚠️ Красные источники в NLM (дубли от ретрая)

При сегодняшнем синке DB 14 файлов упали с 500, потом были загружены повторно.
Результат: ~14 источников в состоянии error + их успешные копии = дубли.
**Нужно:** запустить `/nlm-noms` ещё раз — он удалит старые по имени таблицы и загрузит свежие.
ИЛИ найти и вручную удалить источники с красной иконкой в NLM UI.

### ⚠️ NLM сессия истекает периодически

Google-куки в `~/.notebooklm/storage_state.json` протухают (несколько дней – неделя).
Признак: `notebooklm list` возвращает ошибку "Authentication expired".
Лечение: в терминале запустить `notebooklm login` (интерактивно, нужен браузер), нажать ENTER после логина.
Автоматизировать нельзя — требует ручного взаимодействия.

### ℹ️ n8n воркфлоу: 10 (было 15)

VPS сейчас отдаёт 10 воркфлоу (7 активных). 5 воркфлоу исчезли по сравнению с прошлым дампом от апреля.
Вероятно удалены или перенесены в архивную папку n8n. Уточнить у владельца.

### ℹ️ code_* источников много (120)

120 файлов Python/JS в кодовой базе — это реально много для одного проекта.
Много одноразовых скриптов (`_verify_*`, `_build_mig_*`, `_diag_*`, `_audit_*`) занимают слоты.
Рассмотреть: добавить паттерны исключения в `code_to_nlm.py` для `scripts/_*` (одноразовые).

---

## Handover для следующего агента

### Контекст задачи

Эта сессия занималась исключительно **NLM-инфраструктурой** для NOMS:
создание/починка автосинков всех 3 слоёв в блокнот `NOMS Supabase Data`.
Кодовая база бота (handlers, n8n, SQL) в этой сессии НЕ менялась.

### Что стабильно работает

- `/nlm-noms` — ручной DB синк (проверено, работает)
- `/nlm-noms-code` — ручной code синк (проверено, работает)
- `noms-n8n-backup` — scheduled n8n синк через SSH tunnel (проверено)
- `noms-code-sync` — scheduled code синк (проверено + добавлен git pull)
- `noms-supabase-sync` — scheduled DB синк (проверено)

### Что нужно сделать (приоритет: низкий)

1. **Почистить красные источники** в NLM: запустить `/nlm-noms` для DB (перепишет дубли)
2. **Проверить пропавшие 5 воркфлоу** в n8n: узнать у пользователя, куда делись
3. **Рассмотреть исключение одноразовых скриптов** из code-синка (`scripts/_*.py`)
4. **Удалить `N8N_API_KEY`** из `.env` — cloud отменён 2026-04-30

### Как использовать NLM

```bash
# Запросить NLM (всегда первым делом перед правками кода/SQL/n8n)
/nlm блокнот NOMS Supabase Data

# Ручной DB синк
/nlm-noms

# Ручной code синк  
/nlm-noms-code
```

**СТОП-ПРАВИЛО**: всегда 3-4 вопроса в NLM ДО написания плана/SQL/n8n-правки. Подробнее — в `CLAUDE.md` раздел "СТОП-ПРАВИЛО".
