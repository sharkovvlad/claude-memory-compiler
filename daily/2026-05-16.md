# Daily Log: 2026-05-16

## Sessions

## Memory Maintenance

### Memory Flush (03:17)

FLUSH_OK - Nothing worth saving from this session

### Session (03:17)

**Контекст:** Сравнительный анализ вариантов инфраструктуры n8n (self-host vs полная миграция в Python) и планирование следующих шагов.

**Ключевые обсуждения:**
- Детальный SWOT-анализ двух вариантов: B (self-host n8n на VPS) vs D (полная миграция в Python)
- Замеры реальной latency: cold-start n8n Cloud = 1.3-1.5 сек — главный враг UX
- VPS recon: Hetzner 4 ГБ RAM (3 ГБ свободно), 2 CPU, Docker не установлен, есть чужой `tasktracker-bot` — не трогать
- Объём миграции: ~240 нод в 14 воркфлоу, но 90% — тривиальные httpRequest/code/postgres ноды
- Главный риск Python-миграции — `03_AI_Engine` с 5 langchain.openAi нодами (Vision, Transcribe), но `openai` SDK в Python снимает блокер
- Консультация с «другим ИИ» (NotebookLM) — подтвердил RPC-first архитектуру и грабительские тарифы n8n Cloud

**Принятые решения:**
- **Шаг 0 (немедленно):** self-host n8n Community Edition в Docker на VPS — убирает cold-start и лимит 10K executions бесплатно
- **Шаги 1-7 (эволюционно):** постепенная миграция воркфлоу в Python, каждый поток в свой модуль (`handlers/menu.py`, `handlers/onboarding.py`, `services/ai_engine.py` и т.д.)
- **SQLite** (не Supabase) для внутренней БД n8n — проще, автономнее
- Подписку n8n Cloud отменять только через 3+ дня стабильной работы self-hosted
- Каждый n8n workflow → отдельный Python-модуль (не один монолитный файл)

**Выводы:**
- n8n Cloud cold-start (1.3-1.5 сек) — основной враг latency, не исходящий HTTP к Telegram
- Self-host решает проблему за 1 день без переписывания кода; Python-миграция — правильная цель, но на недели/месяцы
- На VPS живёт чужой проект `tasktracker-bot` — жёсткое ограничение при любых инфраструктурных работах
- Параллельная работа агентов требует координации — brief для следующего агента подготовлен

**Задачи:**
- [ ] **Шаг 0:** Новая сессия — агент разворачивает n8n self-host на VPS по плану `groovy-marinating-eclipse.md` (секции 0.1–0.10)
- [ ] Переключение трафика с n8n Cloud на self-hosted (ночное окно EU)
- [ ] 3 дня мониторинга стабильности → отмена подписки n8n Cloud
- [ ] После стабилизации — начать Phase 1-7 миграции воркфлоу в Python (roadmap в плане)

---

### Session — mig 230: gender-conditional carbs_min_g floor (v5)

**Контекст:** После v4 (mig 227) и валидации Google-Sheets twin'ом продукт-овнер дал зелёный свет на одну точечную safety-фичу — gender-split углеводного floor'а в `calculate_user_targets`.

**Что сделано:**

- **Migration 230** ([noms-bot PR #81](https://github.com/sharkovvlad/noms-bot/pull/81)). Номер 230, потому что 228 (`hide_training_skip_button`) и 229 (open PR #80 `help_support_button_icon`) уже заняты — типовая коллизия параллельных агентов, ловится `git ls-tree origin/main migrations/` + `gh pr list --state open --json files`.
- **app_constants:** INSERT `carbs_min_g_female=100`, `carbs_min_g_male=50`. Legacy `carbs_min_g=50` оставлен как 2-й уровень COALESCE fallback (safety net для NULL gender / неожиданных значений).
- **calculate_user_targets v5:** Только Step 7 carbs floor lookup изменён — трёхуровневый COALESCE с `LOWER(TRIM(COALESCE(v_user.gender, 'male')))`. Всё остальное v4 (PAL fusion, 'none' coefficients, conditional clamp, telemetry) — сохранено 1:1. Заодно обновил COMMENT на `v5 (mig 230)` (cosmetic gap MEMORY: `v4 (mig 217)`).
- **Stale-base rule:** стартовая база — `pg_get_functiondef('public.calculate_user_targets(BIGINT, BOOLEAN)')` из live прода, не из mig 227 файла. Surgical edit одного блока на 4 строки.
- **Snapshot:** `users_targets_backup_20260516` (35 строк). Drop ≥ 2026-05-23.
- **Backfill:** все 5 registered с полным профилем — без изменений (все male C=304-440 > 50; обе female C=149,296 > 100). Floor — защита для будущих юзериц с малым весом / агрессивным дефицитом, не для текущих.

**Sentinel verification (psycopg2 на проде, BEGIN/ROLLBACK без сайд-эффектов):**

- **A. F/65/155/40 strength sedentary lose+fast** (small elderly female on cut):
  - v4 (carbs_min=50):  P=80 F=32 **C=86**  target=952  eff_deficit=20.1%  floor_C=False
  - v5 (carbs_min=100): P=80 F=32 **C=100** target=1008 eff_deficit=15.4%  floor_C=True
  - Δ: C +14g, kcal +56, eff_deficit -4.7pp (менее агрессивный — safety страховка)
- **B. M/30/175/70 strength sedentary lose+normal** (mig 227 Sentinel #4): `1892/140/56/207` EXACT MATCH — male не задет ✓
- **C. F/30/165/55 strength moderate gain+normal**: TDEE=2032 → target=2238 → conditional clamp v4 правильно НЕ применён для gain ✓

**Latency:** p95 = **42.96 ms** (25 runs persistent psycopg2 с VPS). Лучше mig 227 baseline 45 ms — extra COALESCE level не добавил издержек, оба lookup hit same PK index `app_constants(key)`.

**Гочи / lessons:**

1. **Floor bite редко цепляется.** Первый кейс (F/45/170/55) ожидал floor bite — не сработал (C=122, формула v4 + 20% deficit + strength почти всегда даёт carbs ≥ 100 при normal-теле). Алгебраическая граница: floor триггерится только при ОЧЕНЬ малом весе + ОЧЕНЬ старом возрасте + sedentary PAL. Realistic floor-bite кейс: F/65/155/40 strength sedentary lose+fast. Это и есть смысл safety floor'а — он защищает edge cases, не центр распределения.
2. **Sentinel via transactional ROLLBACK.** Insert temp user с уникальным негативным `telegram_id` → call RPC → ROLLBACK. Никаких сайд-эффектов в проде, никаких артефактов в БД. Idempotent — можно гонять сколько угодно раз.
3. **Migration collision guard работает.** `gh pr list --state open --json files` сразу показал что 229 уже в работе у другого агента — взял 230 без коллизии. (Если бы не проверил → потерял бы дни на rebase войны.)

**Что флагнуть тимлиду (manual deliverable):**

- **Update digital twin** в Google Sheets v6.3 (folder `1aJOXi7Y0qpR0emLSSOBrMFGyRhBJICNe`, file `1t-5xsGaYP64Dk7NKfRnCGignlbtOKyG0a2LE6jjOGB0`). У этой сессии нет Google Drive/Sheets MCP — я не могу обновить spreadsheet автономно. Изменение в формуле `Min Carbs Limit (prod v4)`: заменить `=50` на `=IF(C[gender]="female"; CARBS_MIN_FEMALE; CARBS_MIN_MALE)` где новые reference-ячейки = 100 и 50 соответственно. Колонку переименовать в `Min Carbs Limit (prod v5)`. Это ~5 минут руками.

**Не сделано (отложено на следующую итерацию):**

- Age guard `<18` / `>75`, беременность/лактация поле (safety task).
- `phenotype='athlete'` no-op (продуктовое решение).
- Mifflin переоценка у obese → Katch-McArdle от LBM.
- Adaptive modifiers Phase 3 — spec в `.claude/specs/adaptive_modifiers_spec.md`.

**Артефакты:**
- PR: [noms-bot#81](https://github.com/sharkovvlad/noms-bot/pull/81) — **merged 2026-05-16** владельцем
- Branch: `claude/quizzical-curie-77a72a`
- Snapshot: `public.users_targets_backup_20260516`

**Post-merge update — digital twin verification.** После merge владелец дал доступ к Google Drive MCP (только read). Прочитал digital twin v6.3 (file `1t-5xsGaYP64Dk7NKfRnCGignlbtOKyG0a2LE6jjOGB0`) — оказалось, твин уже pre-staged под v5: формула `Min Carbs Limit (prod v5)` = `=IF(C38="female"; C18; C19)` с константами 100/50. Прогнал prod v5 для twin INPUT (F/30/165/70 sed+cardio lose+slow default) через psycopg2 — **1728/98/56/208, EXACT MATCH** с твин-цифрами. Lesson: владелец заранее заложил v5 в твин как "_proposal_v5" — после merge mig 230 эти "proposals" автоматически стали ground truth, никаких формул править не надо. Единственный cosmetic gap — labels двух констант (`carbs_min_g_female_proposal_v5` → `carbs_min_g_female`, и male аналогично). Drive MCP edit недоступен → renaming остаётся на владельце (1 минута). Pattern на будущее: для крупных изменений calculate_user_targets — закладывать в твин под `_proposal_vN` суффиксом ДО apply, после merge owner отрезает суффикс. Без переписывания формул.


---

### Session (UX-копирайтинг — план + Phase 0)

**Контекст:** UX/UI-задача от ведущего — пользователи не понимают «стрик»/«лог» в RU; не-EN локализации (особенно ES) — дословная калька, теряет характер Sassy Sage. Запрос: спроектировать многофазный план + начать pre-flight аудит.

**План создан:** `/Users/vladislav/.claude/plans/proud-tickling-willow.md` (approved owner-edited). 5 фаз:
- **Phase 0 (текущая сессия):** audit + snapshot
- **Phase 1:** RU терминология, 1 миграция (~231)
- **Phase 2:** Sassy Sage Multilingual Glossary в KB
- **Phase 3:** ES pilot (25 ключей, ~232)
- **Phase 4:** 11 языков в 3 волнах (DE/FR/IT/PT → PL/UK → ID/HI/AR/FA)
- **Phase 5:** post-deploy monitoring

**Жёсткие constraints на тексты (применяются во всех фазах):**
1. **Telegram SRE:** ≤35 chars/line, ≤12 lines/msg, ≤18 chars/inline-button. Романские +30% → идиоматическое сокращение.
2. **Гендерная политика:** passive voice / 1st-person bot / impersonal — НЕ 2nd-person past tense («залогировал(а)»). Никакого `users.gender`-dynamic в этой задаче.
3. **Anti-shame & РПП-этика:** auto-reject в critic-scoring если value <5 по оси «не шутит над юзером/телом/весом». Native Fiverr reviewer подписывает чек-лист.

**Закрытые решения (тимлид):**
- Глагол: «**записать**» (не «отметить»). Существительное — «запись».
- Native reviewers: Fiverr на все 12 не-EN языков (~$500-600).
- Терминология hybrid: «Дней в ударе» (статистика, live) + «Ударный режим» (профиль, live) + «Серия» (магазин/кроны/системные).

**Phase 0 выполнено:**
- ✅ `git rebase origin/main` — worktree подтянул mig 229 (icon_speech) + mig 230 (carbs gender floor).
- ✅ `gh pr list --state open` = `[]` → следующий свободный номер **231** для Phase 1.
- ✅ Audit SQL для RU-ключей со «стрик|лог|логир|залог» → **18 ключей** (5 variant arrays + 13 scalar):
  - **Variant (REPLACE, len-preserve):** `cron_notifications.comeback_2days`, `free_tier.trial_limit_3`, `gamification.streak_7`, `pay.paywall`, `payment.success`
  - **Scalar:** `cron_notifications.reminder_meal_lunch/reminder_streak_warning/streak_broken/streak_frozen`, `delete_account.warning_body`, `gamification.premium_limit_reached`, `league.info_text`, `profile_gamification.streak`, `progress.streak`, `referral.cta_newbie_2`, `report.insight_eating_little`, `shop.freeze_desc/freeze_title`
- ✅ Audit-файл с placeholders inventory: `tools/terminology_audit_ru_20260516.json` (вне git, временный).
- ✅ Snapshot: `public.backup_ui_translations_terminology_20260516` — 13 rows (один per lang_code, на случай если откатывать нужно не только RU).

**NLM-критика плана (verified):** триггеров на ui_translations нет, `v_user_context` — обычная VIEW (refresh не нужен), leaf-types только string/array-of-strings, glossary не дублируется (пробел в архитектуре). Главный риск — placeholders byte-by-byte (`{streak_days}` нельзя переводить как `{dias_de_racha}`) — это уже в брифинге субагента.

**Что НЕ сделано в текущей сессии (отложено на Phase 1):**
- Telethon `/live-test smoke` baseline — будет запущен в Phase 1 перед commit.
- Сама миграция 231 — отдельная сессия с делегированием субагенту-копирайтеру.

**Артефакты:**
- План: `/Users/vladislav/.claude/plans/proud-tickling-willow.md` (approved)
- Audit: `tools/terminology_audit_ru_20260516.json`
- DB snapshot: `public.backup_ui_translations_terminology_20260516` (13 rows, all langs)
- Branch: `claude/angry-williamson-bd67f0` (worktree)

**Pattern на будущее: text-terminology audit recipe.**
Для bulk-замены текстов в `ui_translations` нужен recursive walk JSONB с regex match на leaf-уровне (jsonb_each + WITH RECURSIVE). Audit-output должен включать (а) path, (б) type=array/string, (в) array_length для variant'ов, (г) placeholders set — это input для subagent'а и для verify-diff. Recipe-кандидат для KB → `concepts/ui-translations-bulk-update-recipe.md`.

---

### Session (UX-копирайтинг Phase 1 — done)

**Контекст:** Продолжение сессии Phase 0 — переход к исполнению Phase 1 (RU терминология) после approve плана.

**Что сделано:**
- Subagent-копирайтер (Sassy Sage RU) переписал 18 RU-значений по hybrid-глоссарию + жёстким constraints (placeholders byte-by-byte, Telegram SRE, гендерная нейтральность, anti-shame). Output: `tools/terminology_replacements_ru_20260516.json`.
- Pre-apply assertions (Python): placeholder set match snapshot vs new = 18/18 ✓, array lengths = 3 для всех 5 variant'ов, no old terms in new values.
- Сгенерирована `migrations/231_ru_terminology_streak_to_seria.sql` — per-key `jsonb_set` в одной транзакции + встроенные DO $$ assertions (variant length + recursive walk на остаточные «стрик/логир/залогир»).
- Миграция применена через psycopg2 (DATABASE_URL pooler). Post-apply audit: 0 ключей со старыми терминами; placeholder integrity vs snapshot 18/18 ✓.
- PR создан: [noms-bot#82](https://github.com/sharkovvlad/noms-bot/pull/82). Pre-push sanity (`git diff origin/main..HEAD --stat`) — 1 файл, +152 строки, всё в scope.
- CI: PR Sanity Check + Migration Collision Guard — оба прошли ✅.

**Hybrid-глоссарий применён:**
- «Серия» — `shop.freeze_title/desc`, `cron_notifications.streak_*`, `delete_account.warning_body`, `gamification.streak_7` variant 2 («НЕДЕЛЯ В УДАРЕ!»), `report.insight_eating_little`, `profile_gamification.streak`, `progress.streak`.
- «записать/запиши/запишет/запись» — глагол/существительное (`cron_notifications.reminder_meal_lunch`, `reminder_streak_warning`, `league.info_text`, `referral.cta_newbie_2`, `pay.paywall`, `payment.success`, `free_tier.trial_limit_3`, `gamification.premium_limit_reached`).

**Гендерная нейтральность (примечательный кейс):** `gamification.premium_limit_reached` — было «Ты залогировал ТАК много» (мужской по умолчанию = exclusion). Subagent предложил «Записей ТАК много» — снял и гик-термин, и гендер-маркер. Pattern для будущих фаз: passive voice / 1-е лицо бота / impersonal вместо 2nd-person past tense.

**Артефакты:**
- PR: [noms-bot#82](https://github.com/sharkovvlad/noms-bot/pull/82) (open, ожидает merge владельцем)
- Migration: `migrations/231_ru_terminology_streak_to_seria.sql`
- Snapshot: `public.backup_ui_translations_terminology_20260516`
- Audit: `tools/terminology_audit_ru_20260516.json` (вне git)
- Replacements plan: `tools/terminology_replacements_ru_20260516.json` (вне git)

**DB-state vs git HEAD после Phase 1:**
- DB live: mig 231 уже applied (через psycopg2 autonomous apply)
- Git origin/main: 230 (231 в open PR #82)
- Это ожидаемый desync — DB state идёт впереди git до merge. После merge `./deploy.sh` ничего не меняет в БД (миграции applied, только Python код rsync, в этом PR Python не трогается).

**Phase 2 (next session) — Sassy Sage Multilingual Glossary:**
- Создать `claude-memory-compiler/knowledge/concepts/sassy-sage-multilingual-glossary.md` + index entry
- 12 секций (по 1 на не-EN язык) × 8 подразделов (терминология, register, гендер, SRE, идиомы, anti-patterns, культурные табу, RTL gotchas)
- Двухступенчатый writer→critic цикл per язык
- Без миграции — это reference document для Phase 3-4

**Lesson для KB:** UI bulk-update recipe.
- Recursive JSONB walk через `WITH RECURSIVE walk(path, val)` для аудита (path как text[]).
- Per-key `jsonb_set` лучше bulk `jsonb_set_lax` для review.
- Встроенные DO $$ assertions (variant length + ILIKE-walk на staleness) — fail-fast в одной транзакции.
- Pre-apply Python assertions (placeholder regex set diff vs snapshot, array len, no old terms) — даёт catch на пре-flight стадии до DB hit.
- Snapshot всегда entire table (CREATE TABLE backup_X AS SELECT *) — позволяет partial rollback по lang_code.
- Кандидат для нового концепта `concepts/ui-translations-bulk-update-recipe.md` (после Phase 3 когда recipe устаканится).
