# 2026-06-05

## ~12:30 — Консолидация всех потоков «точность/расчёты» + урок про спавн

**Что:** свёл состояние всех параллельных потоков в один мастер-handover `handover/2026-06-05_workstream-map.md` (агенты убежали вперёд, не было единой карты). Проверил КВ + папку `Нутрициолог (аудит расчётов v13)/`.

**Ключевое — что УЖЕ В ПРОДЕ (агенты сделали 2026-06-03/04):**
- Штрихкод MVP (PR #316, mig 452/453): zxing-cpp → OpenFoodFacts, кнопка [🔎 Штрихкод].
- Локация-в-промпт (PR #323/#325): страна+время в запрос к LLM — «главный рычаг точности».
- Temperature 0.2 доходит до OpenAI (PR #322) — был мёртв.
- `image_detail:"high"` (#301), caption (#285).
- `user_food_memory` (PR #324, mig 457): память коррекций per-user (text-path ДО кэша+LLM).
- Классификация контента (mig 441, #294-306): контракт-тест + CI на main (проверено).

**Предложено, НЕ применено:** v14-калькулятор (deficit-aware белок P1, Extreme-PAL+EA-guard, sedentary PAL) — ждёт клинического апрува владельца; 6 downstream-брифов (Sage High Fat / голодание-IF / квесты-обучение / [Пропускаю] редизайн / [Мой день] БЖУ-диапазоны / анти-галлюцинации); FatSecret enrichment (gated на golden-set эвал); промпт v4 «перечисляй позиции»; `/sage-tov` (ждёт телеметрии).

**🔴 УРОК (process):** заспавнил владельцу два чипа — «Barcode» и «Cascade» — **не сверившись с актуальным состоянием**. Barcode MVP (#316), temperature (#322), локация (#323) УЖЕ были в проде/в работе → чипы РЕДУНДАНТНЫ. Нарушение CLAUDE.md §8 («проверь существующее до предложения»). **Правило: перед спавном задач — `gh pr list --state merged` за период + свежие daily + MEMORY «Recently shipped», а не состояние на старте своей сессии.** Рекомендация владельцу: дропнуть оба чипа; остаток (barcode Фаза 2, FatSecret enrichment) ведут существующие агенты.

**Содержательного кода/миграций НЕ менял** — только консолидация KB (handover + этот daily). Контракт-тест и CI-workflow проверены — на main, дефекта нет (Explore ошибочно флагнул «фантомный тест»).

---

## Phase 5 cutover: Edit Meal re-recognition → Python (PR #326)

**Контекст:** owner попросил завершить Stage 7c (удалить idle n8n 03/06). Verify-first перед удалением показал **03_AI_Engine ещё жив** (7 успешных exec 06-03…06-05, последний сегодня), несмотря на `active=0` (моя правка 06-02). Расследование (3 субагента: Python food_log / router / n8n legacy + live psycopg2 + journalctl):

**Находка:** n8n 03 исполняется не от ошибки (0 Python AI-failures) и не остаток — это **немигрированный Edit Meal re-recognition** (`reason='editing_meal'`: registered-юзер редактирует приём → новое фото/голос/текст). Python edit-pipeline (`food_log._handle_edit_meal_input`, Stage 7b PR D `d6f4c72`) был **смержен, но мёртв в проде** — webhook AI-гейт пропускал только `startswith(('food_media','text_food'))`, роутер ставит `reason='editing_meal'` → не матчит → silently fall through в n8n. `handle_ai_input` уже диспатчил на edit-хендлер по `status=='editing_meal'` — не хватало пропустить reason в гейт.

**Если бы удалили 03 по календарю (день 7) — сломали бы редактирование еды.** Урок: «время прошло» ≠ «путь мёртв»; гейт удаления = 0 реальных executions, не календарь.

**Fix (PR #326, merged-pending):**
- `webhook_server.py`: reason-предикат AI-гейта расширен на `'editing_meal'`; distinct лог `AUTHORITATIVE_AI_EDIT` для мониторинга. Fail-safe сохранён (исключение → n8n).
- `tests/test_webhook_ai_engine_gate.py`: editing_meal вызывает хендлер (не fallthrough) + exception fallthrough + persist_as_menu=False.
- Все RPC (`replace_meal_transaction`/`grant_correction_xp`/`clear_editing_state`/`get_meal_by_id`) подтверждены live в проде. **Миграций нет.**
- Тесты 37 passed (gate+edit+ai_input). 4 пре-существующих фейла test_router.py (onboarding:country) не связаны (MEMORY 06-03).
- §12: rebase на свежий main (ветка была 85 коммитов позади!) ПЕРЕД commit; sanity-diff = только 2 файла; force-with-lease. CI: migration-collision + diff-sanity pass.

**Нюансы (улучшения vs n8n):** Python поддерживает re-recognition по фото при edit (n8n деградировал в текст); edit не требует маны (n8n блокировал при mana=0). `edit_count`/`raw_user_input` closed-loop — follow-up.

**После deploy PR #326:** мониторить `AUTHORITATIVE_AI_EDIT`↑ + n8n 03 executions→0 → ТОГДА Stage 7c (DELETE 03/06 + render-endpoint + Route Classifier refs). 03/06 active=0 станет правдивым (idle) после deploy. KB [[stage7-global-cutover]] §поправка-06-05 + [[architecture-registry]] §Phase5.

### Phase 5 LIVE-подтверждён + anti-spam toast fix (PR #327)

**Deploy #326 verified в проде** (owner-тест tid=417002669, 2026-06-05 13:25): «яблоко» → `AUTHORITATIVE_AI reason=text_food` (+15); тап `cmd_edit_last` → edit-режим; «это груша» → `AUTHORITATIVE_AI_EDIT reason=editing_meal` (+5 correction, `replace_meal_transaction`+`grant_correction_xp`+`clear_editing_state`). **n8n `03_AI_Engine` в 13:25 НЕ вызывался** (только 05:59/07:46). Cutover работает.

**Баг найден owner'ом:** edit слал ДВА сообщения — карточка («🌟 +5 · 🔥 34») + отдельное «🌟 +5 XP · XP bonus» (mig 376 toast). Дубль того же +5 = спам.

**Fix (PR #327, CI зелёный, pending merge):** `_handle_edit_meal_input` — убран отдельный toast-OutboundItem. Correction XP по-прежнему грантится и виден в карточке через `payload["xp_gained"]` (футер). При кэпе — 0, карточка без бонуса (silent UX). Тесты `test_food_log_correction.py`/`test_food_log_edit_meal.py` переписаны: одна карточка, XP проверяется через render-payload; удалён toast-text fallback тест. 66 passed. §12: свежая ветка `claude/edit-meal-no-toast` от origin/main (вкл. #326), sanity-diff = 3 файла.

**Статус Stage 7c:** РАЗБЛОКИРУЕТСЯ после deploy+monitoring #326/#327: `AUTHORITATIVE_AI_EDIT`↑ + n8n `03_AI_Engine` exec → 0 за чистые ~3-5 дней. ТОГДА: DELETE `/internal/food_log/render` + Route Classifier executeWorkflow refs (`Go to 03_AI`) + n8n DELETE 03/06. До этого НЕ удалять.
