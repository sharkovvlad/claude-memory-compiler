# 2026-06-05

## ~12:30 — Консолидация всех потоков «точность/расчёты» + урок про спавн

**Что:** свёл состояние всех параллельных потоков в один мастер-handover `handover/2026-06-05_workstream-map.md` (агенты убежали вперёд, не было единой карты). Проверил КВ + папку `Нутрициолог (аудит расчётов v13)/`.

**Ключевое — что УЖЕ В ПРОДЕ (агенты сделали 2026-06-03/04):**
- Штрихкод MVP (PR #316, mig 452/453): zxing-cpp → OpenFoodFacts, кнопка [🔎 Штрихкод].
- Локация-в-промпт (PR #323/#325): страна+время в запрос к LLM — «главный рычаг точности».
- Temperature 0.2 доходит до OpenAI (PR #322) — был мёртв.
- `image_detail:"high"` (#301), caption (#285).
- `user_food_memory` (PR #324, mig 457): память коррекций per-user (text-path ДО кэша+LLM).
- Классификация контента (mig 441, #294-306): контракт-тест + CI на main (проверено).

**Предложено, НЕ применено:** v14-калькулятор (deficit-aware белок P1, Extreme-PAL+EA-guard, sedentary PAL) — ждёт клинического апрува владельца; 6 downstream-брифов (Sage High Fat / голодание-IF / квесты-обучение / [Пропускаю] редизайн / [Мой день] БЖУ-диапазоны / анти-галлюцинации); FatSecret enrichment (gated на golden-set эвал); промпт v4 «перечисляй позиции»; `/sage-tov` (ждёт телеметрии).

**🔴 УРОК (process):** заспавнил владельцу два чипа — «Barcode» и «Cascade» — **не сверившись с актуальным состоянием**. Barcode MVP (#316), temperature (#322), локация (#323) УЖЕ были в проде/в работе → чипы РЕДУНДАНТНЫ. Нарушение CLAUDE.md §8 («проверь существующее до предложения»). **Правило: перед спавном задач — `gh pr list --state merged` за период + свежие daily + MEMORY «Recently shipped», а не состояние на старте своей сессии.** Рекомендация владельцу: дропнуть оба чипа; остаток (barcode Фаза 2, FatSecret enrichment) ведут существующие агенты.

**Содержательного кода/миграций НЕ менял** — только консолидация KB (handover + этот daily). Контракт-тест и CI-workflow проверены — на main, дефекта нет (Explore ошибочно флагнул «фантомный тест»).

---

## Phase 5 cutover: Edit Meal re-recognition → Python (PR #326)

**Контекст:** owner попросил завершить Stage 7c (удалить idle n8n 03/06). Verify-first перед удалением показал **03_AI_Engine ещё жив** (7 успешных exec 06-03…06-05, последний сегодня), несмотря на `active=0` (моя правка 06-02). Расследование (3 субагента: Python food_log / router / n8n legacy + live psycopg2 + journalctl):

**Находка:** n8n 03 исполняется не от ошибки (0 Python AI-failures) и не остаток — это **немигрированный Edit Meal re-recognition** (`reason='editing_meal'`: registered-юзер редактирует приём → новое фото/голос/текст). Python edit-pipeline (`food_log._handle_edit_meal_input`, Stage 7b PR D `d6f4c72`) был **смержен, но мёртв в проде** — webhook AI-гейт пропускал только `startswith(('food_media','text_food'))`, роутер ставит `reason='editing_meal'` → не матчит → silently fall through в n8n. `handle_ai_input` уже диспатчил на edit-хендлер по `status=='editing_meal'` — не хватало пропустить reason в гейт.

**Если бы удалили 03 по календарю (день 7) — сломали бы редактирование еды.** Урок: «время прошло» ≠ «путь мёртв»; гейт удаления = 0 реальных executions, не календарь.

**Fix (PR #326, merged-pending):**
- `webhook_server.py`: reason-предикат AI-гейта расширен на `'editing_meal'`; distinct лог `AUTHORITATIVE_AI_EDIT` для мониторинга. Fail-safe сохранён (исключение → n8n).
- `tests/test_webhook_ai_engine_gate.py`: editing_meal вызывает хендлер (не fallthrough) + exception fallthrough + persist_as_menu=False.
- Все RPC (`replace_meal_transaction`/`grant_correction_xp`/`clear_editing_state`/`get_meal_by_id`) подтверждены live в проде. **Миграций нет.**
- Тесты 37 passed (gate+edit+ai_input). 4 пре-существующих фейла test_router.py (onboarding:country) не связаны (MEMORY 06-03).
- §12: rebase на свежий main (ветка была 85 коммитов позади!) ПЕРЕД commit; sanity-diff = только 2 файла; force-with-lease. CI: migration-collision + diff-sanity pass.

**Нюансы (улучшения vs n8n):** Python поддерживает re-recognition по фото при edit (n8n деградировал в текст); edit не требует маны (n8n блокировал при mana=0). `edit_count`/`raw_user_input` closed-loop — follow-up.

**После deploy PR #326:** мониторить `AUTHORITATIVE_AI_EDIT`↑ + n8n 03 executions→0 → ТОГДА Stage 7c (DELETE 03/06 + render-endpoint + Route Classifier refs). 03/06 active=0 станет правдивым (idle) после deploy. KB [[stage7-global-cutover]] §поправка-06-05 + [[architecture-registry]] §Phase5.

### Phase 5 LIVE-подтверждён + anti-spam toast fix (PR #327)

**Deploy #326 verified в проде** (owner-тест tid=417002669, 2026-06-05 13:25): «яблоко» → `AUTHORITATIVE_AI reason=text_food` (+15); тап `cmd_edit_last` → edit-режим; «это груша» → `AUTHORITATIVE_AI_EDIT reason=editing_meal` (+5 correction, `replace_meal_transaction`+`grant_correction_xp`+`clear_editing_state`). **n8n `03_AI_Engine` в 13:25 НЕ вызывался** (только 05:59/07:46). Cutover работает.

**Баг найден owner'ом:** edit слал ДВА сообщения — карточка («🌟 +5 · 🔥 34») + отдельное «🌟 +5 XP · XP bonus» (mig 376 toast). Дубль того же +5 = спам.

**Fix (PR #327, CI зелёный, pending merge):** `_handle_edit_meal_input` — убран отдельный toast-OutboundItem. Correction XP по-прежнему грантится и виден в карточке через `payload["xp_gained"]` (футер). При кэпе — 0, карточка без бонуса (silent UX). Тесты `test_food_log_correction.py`/`test_food_log_edit_meal.py` переписаны: одна карточка, XP проверяется через render-payload; удалён toast-text fallback тест. 66 passed. §12: свежая ветка `claude/edit-meal-no-toast` от origin/main (вкл. #326), sanity-diff = 3 файла.

**Статус Stage 7c:** РАЗБЛОКИРУЕТСЯ после deploy+monitoring #326/#327: `AUTHORITATIVE_AI_EDIT`↑ + n8n `03_AI_Engine` exec → 0 за чистые ~3-5 дней. ТОГДА: DELETE `/internal/food_log/render` + Route Classifier executeWorkflow refs (`Go to 03_AI`) + n8n DELETE 03/06. До этого НЕ удалять.

---

## 🟢 Фича «осознанная пауза / голодание» — Python с нуля, фаза 1 (mig 459/460 LIVE, PR #328, флаг OFF)

**Контекст:** брифы B+D из `Нутрициолог (аудит расчётов v13)/DOWNSTREAM_TASK_BRIEFS.md`. Owner: B+D — **не переделка легаси n8n, а стройка с нуля на Python**. Решения owner: геймификация КАК ЕСТЬ (+15 XP/−1 мана/стрик — не трогать), safety МЯГКО (разовый пропуск не блокировать, протоколы не продвигать уязвимым), scope с протоколами.

**Discovery (durable):** фича уже частично жила — `log_fasting_meal(p_telegram_id)` логирует 0-ккал `input_source='fasting'` приём → стрик держится + XP + мана через `log_meal_transaction`. `cmd_skip_meal` НЕ в Python-роутере → шёл в легаси n8n `04_Menu` (5 нод, KB `fasting-feature` 💤stale). Напоминания: `meals_today` считает fasting-лог → утреннее глушится, но обед(`<2`)/ужин(`<3`) прилетают постящемуся (дыра). Safety-сигналы users: `is_pregnant/is_lactating/birth_date/weight_kg+height_cm`. **НЕТ колонок: РПП-флаг (только паттерн), диабет (не собираем).**

**Сделано (фаза 1):**
- **mig 459** бэкенд: `food_logs.skip_reason`; `users.fasting_intent_asked/fasting_protocol(16:8|18:6|omad|flexible)+window`; RPC `get_fasting_eligibility` (SOFT safety — promote-or-not по pregnancy/lactation/<18/BMI<18.5), `record_skip_reason` (причина на сегодняшний fasting-лог + РПП-сигнал = повтор `no_appetite`≥3/14д; тренд веса недоступен — нет `weight_history`), `set_fasting_protocol`.
- **mig 460** флаг `handler_fasting_use_python=false` (OFF) + eligibility v2 (вернуть `intent_asked`) + RU/EN копия-**черновик**.
- **`handlers/fasting.py`** + роут `fasting`/`FASTING_CALLBACKS` (router §4k.7 до generic-menu) + `_fasting_flag_from_ctx` + блок диспатча в `webhook_server` (зеркало payment, forward_target/exception → legacy fallback).
- 15 unit-тестов зелёные, router-сьют 144 зелёный.

**🔑 Durable:** новый Python-target = 4 провода — (1) `TARGETS`+callback-set в router, (2) секция route() ДО generic 4l, (3) `_X_flag_from_ctx`+import+блок диспатча в webhook_server (зеркало payment), (4) флаг в app_constants (OFF). Graceful: forward_target='X' / exception → forward_to_n8n.

**🟡 Открыто:** (1) копирайт RU/EN на ревью Номса (ТЗ `.claude/specs/fasting_NOMS_copy_brief.md`) → потом 11 языков; (2) UI протоколов (16:8/18:6/OMAD) в профиле; (3) глушение meal-напоминаний в день поста (`cron_get_reminder_candidates`: `NOT EXISTS fasting today` в meal-ветках); (4) флаг «сегодня пост» в day_context Sage; (5) иконка 🤐→⏸/🧘 (owner). Спека: `.claude/specs/fasting_skip_spec.md`. Обновить stale KB `fasting-feature`.

### Фаза 1.1 — ревью Номса применено + противоречия отклонены (mig 461 LIVE, PR #330, флаг OFF)
Номс прислал фидбэк на копирайт+3 инж-фичи. **Сверил с КВ — 2 прямых противоречия рубрике sage-tov, отклонены:**
- 🔴 **«Breaking Fast» оценка рефида** («поджелудочной шок» + «в следующий раз начни с белка») = нарушает правила 4 (алярмизм об органах), 5 (предписание след. еды), 1 (шейминг еды). **Это запрещено даже в текущем системном промпте Sage** (`services/sage.py:~1159`: «NEVER...liver/pancreas/enzyme...struggling/on the edge», «Never tell the user to stop eating»). Отклонено.
- 🔴 **«Science Bytes» (аутофагия на 16ч)** = романтизирует длительный пост (против owner-решения safety «не продвигать») + недоказанные мед-утверждения (против доказательного подхода + Medical Disclaimer; phase-aware Sage заблокирован на safety-review — тот же класс). Отклонено.
- 🟡 **Семантика кнопки:** Номс слил «нет аппетита» с «сытостью» — **сохранил ОТДЕЛЬНО** (иначе теряется РПП-сигнал). Owner согласовал.

**Взято (owner-approved):** mig 461 — нейтральный confirm (без «навык»/празднования); **XP вынесен из фразы в футер-чипы** (из RPC `xp_gained`/`streak`, при софт-капе XP-чип скрыт); **иконка `icon_fasting` 🤐→⏸️** (`E'⏸️'`, hot-reload); РПП-чек-ин = объективное зеркало + кнопка «Центр заботы» (`cmd_safety_center`); мед-дисклеймер. 4-я причина `busy` (`cmd_skip_reason_busy`, RPC уже поддерживал). **Sage `_compute_day_status` → `fasting_logged`** для осознанного 0-ккал дня (эвристика `meals≥1 & kcal==0`, БЕЗ правки горячего `get_day_summary`; kcal_in/meals_count уже в `_build_my_day_prompt`) + хинт «не нудить поесть/не паниковать/не шеймить/не славить длительность». 19 тестов + router 144 + 623 сервис-сьют зелёные. **🔑 Durable: `log_meal_transaction`/`log_fasting_meal` возвращают `xp_gained`+`streak` в результате — футер строить из них, не из stale ctx.** Закрыты открытые пункты (4) Sage-флаг и (5) иконка. Остаётся: 11 langs, UI протоколов, глушение напоминаний.

## ~13:30 — v15: ответ на ВНЕШНИЙ ИИ-аудит v14 (24 профиля)

**Вход:** владелец принёс «Проверка расчётов КБЖУ диетологом» (другой ИИ, 2026-06-05) — независимая проверка **v14** (24 профиля). Деливераблы в `Нутрициолог (аудит расчётов v13)/`: `_engine_v15.py` (runnable, без БД), `Digital_Twin_v15_values.csv`, `AUDIT_v15_external_review_response.md`.

**Метод:** воспроизвёл v14 1:1 → 20/24 профилей байт-в-байт совпали с `Digital_Twin_v14_values.csv` (реплика верна), затем наложил фиксы. Меняются **только 4 edge-профиля** (06,10,14,23), 20 не тронуты → нулевой регресс.

**3 находки внешнего ИИ → мой вердикт:**
- **A (✅ согласен, баг подтверждён):** инверсия жиро-угл коридора у ожирения на дефиците. P06 жир низ88>цель85, угл схлопнут 205=205; P10 низ104>цель93, угл 172=172. Корень: `0.8 г/кг × фактич.вес` > `35% × урезанные ккал`. **Фикс глубже их предложения:** для взрослых с ожирением (BMI≥30) жир-якорь (floor/target/high) И белок считаются на **reference-весе (BMI 30)**; калории — на фактическом. Инверсия исчезает (P06→78/85/85 C212-228; P10→74/92/93 C253-296).
- **B (❌ НЕ согласен):** «deficit-надбавку +0.3 г/кг только активным». Отклонил: на дефиците белок про *удержание массы+сытость*, не гипертрофию (нужно и без тренировок — Pasiakos/Mettler/Helms); приверженность критична именно у sedentary; 1.5 г/кг не избыток. Кажущийся избыток P10 (195г) — это пропущенный obese-cap, НЕ надбавка → v15 даёт P10=138г, СОХРАНЯЯ надбавку. Надбавка осталась универсальной.
- **C (🟡 частично):** vegan-компаундинг ×1.25 поверх deficit+strength → P23 229г=3.38 г/кг LBM. Их механизм («× только на базу») не достигает их цели 180-190. **Решение (owner-выбор):** научный потолок `min(3.1 г/кг FFM, 2.5 г/кг ref)` + **мягкий vegan-cap 2.2 г/кг факт.веса** (ЖКТ-комфорт). P23 229→**187**, P14 230→**176** = ровно цель внешнего ИИ.

**Решения владельца (AskUserQuestion):** vegan-cap 2.2 г/кг = ДА; reference-вес и для белка, и для жира = ДА («делай как считаешь нужным»).

**Durable (для агента, который применяет к проду):** прод = **v13**. Применять v14 ядро (deficit-aware белок первым) + v15-дельты (ref-вес ожирения, потолки белка) **последовательными** миграциями, база — `pg_get_functiondef` с живого прода. Reference-вес трогает 0 реальных юзеров сейчас (нет onboarded BMI≥30 без талии на дефиците). golden-кейсы: 06/10/14/23.


### 🚀 Включено в проде (2026-06-05)
PR #328+#330 merged+deployed (Actions sha=3e05988, success). Миграции 459/460/461 уже были применены live. **Флаг `handler_fasting_use_python` → `true`** (hot-reload через v_user_context, без рестарта). Smoke: /health 200, noms-webhooks/cron active, 0 ошибок в логах, кнопка `cmd_skip_meal` присутствует на add_food, button label = `progress.skip_button` → «⏸️ Пропускаю». **cmd_skip_meal теперь Python.** RU/EN финал; 11 языков pending → не-RU/EN юзеры видят EN-fallback на survey-ключах. Откат = флаг в `false`.

---

## 🟢 Точность макросов: temperature + локация + память коррекций — ВСЁ MERGED+LIVE (агент cascade-macros-ea77)

Все PR смержены и задеплоены (deploy main success):
- **#322 temperature** — `ai_config.temperature=0.2` был мёртв (OpenAI юзал дефолт 1.0 → прыгающие макросы). Подключён в `_vision/_text/_recalculate_call`. `image_detail:high` уже был.
- **#325 локация-в-промпт** — страна+локальное время (zoneinfo) в vision/text user-сообщение. **Soft tie-breaker** (фото/текст первичны, страна только разрывает ничью): защита от «мексиканец ест испанскую тортилью».
- **#324 память коррекций (mig 457)** + **#329 refinement-гейт** — per-user `user_food_memory`: юзер 1 раз поправил → запомнили, читается ДО кэша+LLM (HIT=0 токенов).

**🔴 2 урока из РЕАЛЬНОГО теста на 417002669:**
1. **WRITE был merged-but-unwired:** правки шли в n8n (`reason=editing_meal` не проходил Python-гейт). Ожил после edit-meal Python cutover (Phase 5, др. агент). Урок: проверять РОУТИНГ, не только наличие хендлера.
2. **refinement-vs-replacement:** наивное «помнить всё» дало `яблоко→pera` → «яблоко» выдавало грушу. `is_refinement` (token-subset: слова ввода ⊆ исправленное название) → помним уточнения (борщ→борщ с мясом ✅), не замены (яблоко→pera ❌). Гейт = `is_rememberable AND is_refinement`. Стейл-строка яблоко удалена. Будущее (Big Tech): bias-LLM вместо hard-override + «(recordado)» лейбл.

**FatSecret:** creds в `.env` (worktree+VPS), verified с VPS (OAuth token + foods.search OK, IP whitelist активен). mig 458 — GDPR scrub `user_food_memory` в `cron_anonymize_deleted_users`.

**🟡 Следующему агенту (handover `2026-06-05_macro-accuracy-fatsecret.md`):** eval-стенд (region-tagged golden-set) + FatSecret enrichment (Вариант А, enrichment≠cascade-level), region-aware рамка (FatSecret основной, локализован под EU/US/RU/IR/LATAM-ES/Brazil), прозрачный вывод допущения («Гречка 150г (сухой 50г)»). 🔴 dormant cascade (config {cascade:[...]} vs код ждёт list → прод=один gpt-4o; owner: не чинить). KB [[cascade-macro-enrichment-fatsecret]].

## ~14:30 — 🟢 v15 ПРИМЕНЁН НА ПРОД (mig 462, PR #331)

**Что:** перенёс консенсус v15 в живой RPC `calculate_user_targets`. **Applied + committed** (psycopg2; RPC живёт в БД → deploy.sh не нужен). PR #331 фиксирует миграцию.

**Scope-решение:** мигрировал **ТОЛЬКО макро-распределение** (Step 7-8). Энергия (BMR/TDEE/PAL/cap 1.8/все guards/RFM/maternal/min-kcal) — **байт-в-байт v13**. Обоснование: «диапазоны» = как делить калории, не сам калораж; PAL-изменения (sedentary 1.2→1.3 + Extreme-тир + EA) — другая ось, риск для всех юзеров, внешний аудит калораж признал точным → отдельная будущая миграция. Уважает «deficit-белок первым, не смешивать».

**Внедрено:** (1) белок deficit-aware +0.3 г/кг на lose; (2) верхний потолок белка Helms 3.1 г/кг FFM / 2.5 ref + vegan-cap 2.2 г/кг факт.веса; (3) жир = ТАРГЕТ 1.0 г/кг + КОРИДОР (0.8/1.0/1.2, кап 35%); (4) углеводы = таргет + КОРИДОР; (5) reference-вес (BMI30) для взрослых с ожирением — якорь белка И жира → инверсия коридора устранена + obese-cap белка.

**Verify:** OLD vs NEW в одной транзакции (ROLLBACK), 24 эталона → **энергия неизменна 24/24** ✓, инверсий нет, таргет ∈ [низ;верх]. Владелец #417002669: жир 79→**99**, углеводы 327→**252** (жалоба закрыта). p95 **43.6ms** с VPS. Backfill 7 real users (is_bot=false) на v15 — калории сохранены, макросы перебалансированы, диапазоны заполнены.

**🔑 Durable:**
- **macros backward-compat:** `protein_g/fat_g/carbs_g` = single ТАРГЕТЫ (downstream menu_v3/food_log/sage/context читают сохранённые `users.target_*_g`, не JSON напрямую — не сломались). Диапазоны добавлены: `macros.{fat,carbs}_{min,max}_g` + новые nullable колонки `users.target_{fat,carbs}_{min,max}_g`.
- **Энергия и макро — независимые оси.** Можно менять распределение БЖУ не трогая TDEE (как тут).
- **app_constants v15:** `protein_deficit_bonus_g_per_kg=0.3`, `fat_target_g_per_kg=1.0`, `fat_high_g_per_kg=1.2`, `fat_cap_pct=35`, `protein_ceiling_g_per_kg_{lbm=3.1,ref=2.5,vegan=2.2}`, `obese_ref_bmi=30` (category `nutrition_calc`).
- **Узкие/точечные коридоры** (P06/P18) на тугом дефиците — НЕ баг: 35%-кап жёстко связывает жир, но без инверсии (низ≤цель≤верх соблюдается).

**Дальше:** (1) «Мой день» — отображение диапазонов (эмодзи по статусу), бриф E из DOWNSTREAM_TASK_BRIEFS.md; (2) PAL/Extreme-тир + EA-guard — энергетическая ось, отдельная миграция; (3) опц. backfill при росте юзербазы. Источник: `Нутрициолог (аудит расчётов v13)/AUDIT_v15_external_review_response.md`, KB calc-scientific-basis §Консенсус v15.

### Прозрачный вывод допущения (PR #332, mig 463)
`FoodItem.portion_note` — модель поясняет допущение про порцию/состояние когда двусмысленно (сухой/варёный вес, с/без масла). Рендер: `🍚 Гречка (150г · сухой 50г) — 180`. Промпт v4 (vision+text) = живой v3 + блок PORTION ASSUMPTION TRANSPARENCY (mig 463 применён). ⚠️ нужен restart noms-webhooks при деплое (per-process _prompt_cache). 48 тестов. **Принцип owner: вопросов юзеру не задаём — показываем допущение, если неверно → [Исправить].** Live-тест после деплоя: `гречка 150г` → ожидать пояснение сухой/варёный.

## ~16:50 — 🟢 v16 PAL14 (энергетическая ось) НА ПРОД + самопроверка vs таблица (mig 464, PR #333)

**Триггер:** владелец спросил «должны ли данные из Google-таблицы (Digital Twin) быть на проде, я же v14 — совпадать не должно?». Сверил.

**Самопроверка (важный артефакт):** прогнал живой прод (тогда = v15 макро + v13 энергия) против таблицы (v14 = PAL14 + v14 макро), 24 профиля:
- **Белок 21/24 совпал** (3 расхождения P10/14/23 = намеренные v15-потолки obese/vegan, совпадают с v15-колонкой таблицы).
- **Жир-цель 15/24**, углеводы почти везде ниже — КОРЕНЬ: таблица на **PAL14**, прод был на **PAL13**. Углеводы=остаток калорий → ниже TDEE → меньше углей. Белок/жир-цель = г/кг (от калоража не зависят) → совпали.

**Вывод владельцу:** таблица НЕ должна совпадать с продом по 2 осям: (1) PAL14 vs PAL13 (энергия, я её намеренно не мигрировал в mig 462), (2) v14 vs v15 макро-края. → решение владельца: **мигрировать PAL14**.

**Сделано (mig 464):** PAL14 в Step 5 — sedentary 1.2→1.3, light→1.45, moderate→1.6, heavy→1.8, extreme-тир 2.0; per-activity cap (heavy 2.0/extreme 2.4); бонус extreme 0.20. **База PAL из `activity_level`** (не stored `pal_coefficient`; все 7 real users имеют activity_level, NULL→sedentary). Макро v15 не тронуто.

**Verify:** прод-после-464 == `Digital_Twin_v15_values.csv` (PAL14+v15) **24/24 ТОЧНО** (белок+жир низ/цель/верх+угл низ/верх). Таблица теперь 1:1 на проде. p95 44.2ms. 7 real backfilled (+64…+148 ккал; владелец 2563→2691).

**🔑 Durable:**
- **Макро- и энерго-оси независимы** → мигрировал двумя миграциями (462 макро / 464 PAL), каждая верифицирована изолированно. Правильный способ проверить calc-изменение: 462 — «энергия неизменна 24/24 OLD-vs-NEW»; 464 — «== твин 24/24».
- **Если сверяешь прод с Digital Twin:** v14-колонка таблицы (PAL14+v14) даст ложные расхождения белка на P10/14/23 — это v15-потолки. Сверять с **v15-колонкой**.
- **EA-guard выносливости НЕ реализован** в твине (была лишь CSV-пометка «EA-floor» у P21) → в прод не входил. Если решат — отдельная задача.
- **Collision-урок (4-й раз):** пока готовил 463, другой агент смержил свой 463_food_recognition + мой 462 (PR #331 merged). Поймал на `git diff origin/main..HEAD --stat` (показал только мой файл → 462 уже в main) → переименовал 463→464. Rebase-перед-commit + sanity-diff сработали.

**Осталось:** «Мой день» — отображение диапазонов (бриф E); UI-опция onboarding для extreme-тира (константы готовы, но активити-level 'extreme' пока никто не выбирает).

### KB audit (/kb-close --audit)
- audit.py: 148 концептов, **0 orphans** (чисто), 13 merge-кандидатов (топ 0.25 = false-positive sticker-ADR↔n8n-hosting, остальные ≤0.12; единств. правдоподобная пара n8n-self-hosting↔selfhost-migration = n8n-legacy low-priority) → **концепты НЕ трогал** (scores низкие, «при сомнении не сливай»). Stale = известный n8n-legacy + свежие файлы.
- **MEMORY консолидирована: 44 KB/171 → 28.6 KB/159 строк (−35%).** Бэкап `MEMORY.backup-2026-06-05.md`. Сжато: 06-03 Current-state батч (13 bullet'ов→1 durable-блок+KB-ссылки), 3 fasting bullet'а→1, calc v15+v16→1, add_food/sage/barcode→компактно, calc-v13-audit Open→«применён в v15/v16», fix stale Architecture (edit-meal Phase 5 DONE→03 IDLE). Структура цела, durable-гочи сохранены как KB-pointer'ы. Остаток >25 KB — активные workstream'ы (не урезал, правило «не теряй свежее важное»).


## 🟢 Голодание копирайт v3 (фиксы по live-тесту owner) + локализация 13 языков (mig 465, PR #334 merged+deployed)
Owner live-тест RU/EN дал 4 проблемы → исправлены:
- **БАГ:** опрос показывал 3 кнопки, не 4 — причина/ответ `busy` были (фаза 1.1), но **кнопку в клавиатуру `_handle_skip` не добавили**. Урок: добавил REASON_CB+ack+router, но забыл сам button-row → проверять, что новая причина появилась в КЛАВИАТУРЕ, а не только в обработке.
- Жаргон «макросы»/«система пищеварения в режиме ожидания» убран → простой текст.
- Опрос без контекста — добавлена вводная «зачем + отвечать необязательно»; decline переименован «Не хочу отвечать» (было абстрактное «Моя автономия»); ответы называют последствие (пост→не напоминаю про еду в окно; decline→больше не спрашиваю).
- **Локализация 13 языков** (mig 465): 11 субагентов по glossary, gender-neutral, RTL (AR RLM ×8 / FA ZWNJ ×30), HI Hinglish-Latin, голодание религиозно-нейтрально (puasa/روزه/vrat — НЕ Ramadan/Hindu festival). Верификация: 15 ключей ×13, кнопки ≤22, плейсхолдеры целы.
- Деплой sha=ed355a5 success; /health 200; 4-я кнопка на VPS.
- §12: коллизия mig 464 (PR #333) → перенумеровал в **465**.

**🔑 Durable про fasting_logged (ответ owner):** это ВНУТРЕННИЙ статус тона Sage, не текст юзеру. Включается только когда ВЕСЬ день 0 ккал (эвристика `meals≥1 & kcal==0`). На обычном дне с едой (owner видел 2787 ккал) НЕ срабатывает → Sage реагирует нормально. Чтобы увидеть — нужен день только с паузой, без еды.

**🟡 Engagement-оценка (для owner):** фича сильна на УДЕРЖАНИИ (защита стрика в день поста снимает триггер оттока + anti-shame доверие), но ПАССИВНА/невидима и опрос = трение без явной пользы. Рычаг вовлечения = хук «настроить режим 16:8» (TODO Phase 2) → пропуск становится персональным режимом. Watch: XP/стрик за пропуск у уязвимых (телеметрия skip_reason).

**TODO/Phase 2 (owner-deferred, в `.claude/specs/fasting_skip_spec.md`):** (1) хук 16:8/18:6 после «Пощусь»; (2) фича 24ч-голодание (таймер+жёсткий disclaimer); (3) глушение meal-напоминаний в день поста; (4) вариативность ack.

### FatSecret client + eval-preview (PR #335)
`services/fatsecret_client.py` (OAuth2, foods.search, per-100g, fail-open) + 7 тестов + config. Live-smoke с VPS: англ. одиночные (chicken/steak/buckwheat/tortilla/apple) ✅ Generic score 1.0; **кириллица (гречка/борщ) → нет матча** (FatSecret англоцентричен). 🔑 Durable: enrichment lookup по АНГЛ. каноническому имени (не локализованному); RU/IR — англ-канон/регион/skip. Сингуляр/плюрал промах (apple→Apples) починен. KB [[cascade-macro-enrichment-fatsecret]] §12. Дальше: golden-set + харнес сравнения.

## ~17:20 — /kb-close: устранены противоречия calc-доков с продом
KB-гигиена после v15+v16. Поправлено (recent-правит, баннеры-указатели, не удаление):
- `calc-scientific-basis.md`: заголовок «Консенсус v14 (НЕ применено)» → «реализовано как v15+v16», баннер-указатель на LIVE-секцию; EA-floor помечен «не вошёл».
- `index.md`: строка calc-scientific-basis переписана на «v15+v16 LIVE mig 462+464, прод==твин 24/24»; personalized-macro-split помечен «частично superseded».
- `personalized-macro-split.md`: баннер «v3 fat_pct% устарел → жир=таргет+коридор v15/PAL14 v16, см. calc-scientific-basis».
- `calc-user-targets-test-spreadsheet.md`: баннер «v8 golden stale → актуальный Digital_Twin_v15_values.csv (24) + _engine_v15.py + verify463.py».
- `calc-user-targets-roadmap.md`: баннер «P1-блок отгружен v15+v16; остаток EA/Adaptive-TDEE».
lint --structural-only: 0 errors. Цель — другие агенты не наткнутся на «v14 не применено» при живом v15+v16 на проде.

### portion_note dry-weight fix v5 (PR #336, mig 466)
Owner-флаг (2×): «гречка 150г» → подсказка «сухой ~150г» (бред, 160 ккал = варёная). v4 говорила «поясни допущение» — модель просто повторяла вес. v5 явно: вес юзера=ВАРЁНЫЙ, сухой≈⅓ варёного (МЕНЬШЕ), не повторять; пример wrong-vs-right; ккал для съеденной порции. Применён к проду + **noms-webhooks рестартнут** (prompt-only, live сразу). Durable: prompt-инструкция-допущение должна давать ЛОГИКУ+пример, не только «поясни». **English-name для FatSecret: LLM отдаёт `name`(локализ.) + `name_en`(англ. канон для lookup) — покрывает все 13 языков; деталь будущего enrichment, eval-gated.**

### 🔴 Eval-стенд завершён → FatSecret name-enrichment NO-GO (PR #337)
`tools/macro_eval/` (golden-set 20 поз. + харнес, запуск с VPS). **Медиана откл. ккал: LLM 0% (одиночные) / 1% (брендовые); FatSecret НЕ превосходит** — ничья на одиночных (иногда хуже: курица 18%), промах 4/6 составных и ОБОИХ брендовых, fuzzy-шум (egg→Egg Foo Yung). **Вывод: name-enrichment не строим** — GPT-4o уже отлично знает per-100g; главная ошибка = порция/состояние (закрыто portion_note+память коррекций), не плотность; брендовое → штрихкод. Клиент FatSecret (#335) оставлен для barcode-2го-источника. Честный data-backed no-go (owner приглашал такой исход). KB [[cascade-macro-enrichment-fatsecret]] §13.

### FatSecret barcode-источник тоже NO-GO + накопительный стенд (/kb-close)
- 🔴 **FatSecret barcode-lookup недоступен на free-tier:** `food.find_id_for_barcode` требует scope `barcode` (Premier) → `code 14: Missing scope` (verified VPS). Оба применения FatSecret закрыты (name=нет выигрыша eval #337, barcode=платный). Для штрихкода — OpenFoodFacts (бесплатный, barcode-агент). Owner-инстинкт «проверить пользу до стройки» сэкономил стройку против заблокированного endpoint.
- **Накопительный стенд:** `run_eval.py` печатает прод-снимок (логи+correction-rate за окно) → еженедельно = тренд. Live: 378 логов/167 блюд/~1.2% (органически ≈0%).
- **Durable урок:** перед интеграцией внешнего API — проверять (а) доступность endpoint на нашем тарифе (scope!), (б) реальную пользу на нашей выборке (eval). Дёшево, спасает недели.

## ~18:30 — 🟢 БРИФ E LIVE: Ж/У диапазоны в «Мой день» (mig 467, PR #338)
Owner-approved через mockup-итерацию. «Мой день» теперь: жир/углеводы = коридор «79—105» (em-dash) + статус-эмодзи по направлению (в коридоре ✅ / ниже ⬆️ / выше ⬇️ / выше +15% ⚠️); белок = цель + научный потолок (Helms, уже в calc) → <90% ⬆️, >потолок ⬇️, >потолок+15% ⚠️ (закрывает «съел 300г белка — молчок»). Утренний gate сохранён, cutoff 15→12 (планировать обед). Эмодзи нейтральные.

**3 RPC (одна фича):** calculate_user_targets (save target_protein_max_g), get_day_summary (adjusted коридоры — та же lifestyle-дельта), get_daily_stats_rpc (коридор-токены under|in_range|over|over_much + range-строки) + колонка + app_constants + stats.main_text ×13 + Python docstring/defaults. Применено на прод + 7 backfilled.

**🔑 Durable (в KB [[stats-main-headless]] §mig467):**
- Коридор масштабируется lifestyle-дельтой в get_day_summary (не сырой) → консистентность с целью у premium luteal/сон/стресс.
- Потолок белка для алярма = научный потолок calc (Helms), сохранён в users.target_protein_max_g.
- render_screen полный merge → новые RPC-поля доходят до template без whitelist.
- Python-гейт реагирует только на 'under' → расширение словаря токенов не ломает; cutoff через app_constants (не код).
- ui_translations = headless JSONB content per lang → правка шаблона jsonb_set+replace по всем langs.

**Mockup-уроки (owner UX):** показывать сам диапазон нагляднее одного числа; утром недобор = пусто (без значка); порог ⚠️ = +15% к верху; белок «166 г» но с невидимым научным потолком для алярма сверху. Verify: 4 сценария токенов + реальный render_screen (target_f_range='79—105' em-dash). p95 46.8ms.

Осталось из calc-роадмапа: Adaptive TDEE (главный разрыв vs MacroFactor), EA-guard. Бриф E закрыт.
