# Daily Log: 2026-05-10

## Sessions

## Memory Maintenance

### Memory Flush (13:50)

FLUSH_OK - Nothing worth saving from this session

### Memory Flush (13:55)

FLUSH_OK - Nothing worth saving from this session

# Daily Log: 2026-05-10

## Sessions

### Session (10:00-17:30 MSK) — Phenotype Quiz emoji rollout + rollback

**Контекст:** Реализация UX эмодзи в phenotype quiz через `{icon_X}` placeholder pattern (mig 193). Live UAT обнаружил критический blocker — legacy n8n 04_Menu рендерит phenotype.q* keys своими JS code нодами и **НЕ резолвит** placeholder. Юзеры на legacy путь видят literal.

**Что сделано:**
- **mig 193**: 11 новых icon_pheno_* в app_constants + UPDATE 16 keys × 13 lang `{icon_X}` префиксы.
- **Live UAT 417002669**: bot показывает literal `{icon_pheno_q1}` в legacy путь (target=menu reason=menu_command).
- **mig 194 ROLLBACK**: regexp_replace удалил `{icon_X} ` префиксы из 16 keys × 13 lang. icon_pheno_* константы СОХРАНЕНЫ. Restart noms-webhooks (prewarm: 13 langs).
- PR #36 deploy.yml fail (false positive smoke check на WARNING `webhook_health: All connection attempts failed` — transient bootstrap retry). Не пофиксил — ждём решение от тимлида (sleep ↑ vs exclusion).
- PR #38 (rollback) merged → 46c26bd29.
- Spawn task создан: «Перенос phenotype quiz полностью в Python (deactivate legacy 04_Menu)».

**Принятые решения / lessons:**

- **Headless `{placeholder}` pattern требует ВСЕ consumers** уметь резолвить. В hybrid Python+legacy n8n архитектуре любые changes к ui_translations схеме нужно тестировать **на ОБОИХ платформах**.
- **Variant A (SQL substitution в render_screen) невозможен** в текущем дизайне — render_screen возвращает text_key, не resolved text. Text resolve consumer-side.
- **Legacy 04_Menu phenotype ноды (Build Quiz Screen, Edit Phenotype Screen, Build Quiz Result, Build Profile Sub-Screen) — мёртвый код**: callbacks уже идут через Python (`cmd_quiz_q*` headless вместо legacy `cmd_phenotype_q*`). Но active=true, могут случайно сработать — TODO деактивировать.
- **Тимлид утвердил полный отказ от n8n**: phenotype quiz → Python + deactivate 04_Menu phenotype ноды → replay mig 193 (после deactivation `{icon_X}` placeholder работает корректно когда **только** Python в пути).
- **Тимлид строго против half-measures**: manual UPDATE юзеров для обхода багов = anti-pattern (см. gotcha #24 phase4-onboarding-migration.md).

**Watchlist для агентов:**

- **mig 193 файл сохранён в git history** (`migrations/193_phenotype_quiz_inline_emoji.sql`). После деактивации legacy 04_Menu phenotype нодов — **replay** mig 193 (файл идемпотентен).
- **icon_pheno_* константы в app_constants остались** (rollback не трогал) — они станут активными снова при replay.
- **Bug 2 на скрине (q1_a + q1_b в одной строке)** — НЕ воспроизведён при render_screen test, скорее всего persistent старое сообщение в Telegram client.
- **Subagent ghost-click** (Skip 2 раза) — chip в UI, ждёт запуска.
- **deploy.sh smoke check false positive** — не пофиксил, ждём решение тимлида.

**Файлы:**
- `migrations/193_phenotype_quiz_inline_emoji.sql` (применена + откачена; в git history)
- `migrations/194_rollback_phenotype_emoji_placeholders.sql` (применена 17:20 UTC)
- PR #36 (mig 193) merged → 69105bb
- PR #38 (mig 194 rollback) merged → 46c26bd
- KB: `phase4-onboarding-migration.md` gotchas #23-25 уже обновлены ранее.

### Session (18:30-19:00 MSK) — Phenotype Quiz Phase 2 cutover завершён (PR #39)

**Контекст:** после mig 194 rollback (16:43-17:20 MSK) phenotype путь оставался без эмодзи. Тимлид утвердил полный Python cutover как условие для replay'а emoji. Сегодня — выполнение.

**Что сделано:**

1. **04_Menu surgical strip через n8n PUT API** (workflow `JQsipPWxijse3F0b`, self-hosted `127.0.0.1:5678`):
   - Удалены 6 phenotype-only нод: `Build Quiz Screen`, `Push Nav (Quiz)`, `Is Classify?`, `RPC Classify Phenotype`, `Build Quiz Result`, `Edit Phenotype Screen`.
   - Удалены branches phenotype из routers: `Command Classifier` jsCode block (`cmd_start_phenotype_quiz`/`cmd_phenotype_q*` → `phenotype_quiz` route), `Menu Router` outputKey `phenotype_quiz`, `Edit Type Router` outputKey `edit_phenotype`.
   - 7 connections orphaned → strip'нуты.
   - Itog: 130/136 нод (delta -6), 141/148 connections (delta -7).
   - **Pre-existing blocker:** PUT отклонился HTTP 400 из-за dangling executeWorkflow ref в ноде `Go to Language` (sub-workflow `JRaKFPb5sOFL3xlc` удалён давно, language editing давно мигрирован в Python). Решено через `disabled: true` на этой ноде. Минимальное расширение scope, lesson записан в KB.
   - Workflow остался `active=true`, fresh GET после PUT подтвердил все 4 strip-метрики (нет phenotype-only нод, no `cmd_phenotype_q` в Command Classifier, нет phenotype_quiz outputKey, нет edit_phenotype outputKey, Go to Language disabled).
   - Snapshot pre-strip: `n8n_workflows/_backups/04_Menu_pre_phenotype_strip_2026-05-10.json` (gitignored, 363 KB).

2. **mig 195 — replay mig 193 logic** через psycopg2 на pooler:
   - 11 icon_pheno_* констант (ON CONFLICT NO-OP, остались с mig 193).
   - 16 keys × 13 languages: префикс `{icon_X} ` к existing values. Идемпотентность через `value LIKE '{icon_%'` guard.
   - Verify: 13/13 q1_a и 13/13 q4_c с префиксом.

3. **Restart noms-webhooks + noms-cron** — prewarm cache подхватил новые переводы (`prewarm_caches: 13 langs in 784ms`).

4. **Verifications:**
   - **pytest tests/handlers/test_onboarding_v3.py: 37 passed** (3 ms).
   - **p95 latency** `render_screen('edit_phenotype', 417002669)` × 10 (warmup 2, persistent psycopg2 с VPS): **p50=44.2ms, p95=48.2ms, max=48.2ms**. Target ~46ms ✓.
   - **End-to-end resolve check:** `services.template_engine._resolve_text("phenotype.q1_prompt", ...)` для юзера 417002669 → input `{icon_pheno_q1} <b>В1/4 — Где облегающая футболка...</b>`, output `👕 <b>В1/4 — Где облегающая футболка давит сильнее всего?</b>`. Кнопки Q1: `👕 Плечи и грудь`, `⚖️ Равномерно везде`, `🍔 Живот и талия` — match эталона тимлида.

**Lessons → KB:**
- Новая секция в [[concepts/n8n-data-flow-patterns]] «Gotcha: PUT блокируется dangling executeWorkflow refs» — recipe audit + 3 resolution options (disable / update workflowId / remove node).
- [[concepts/phenotype-quiz]] обновлён секцией «Phase 2 status (2026-05-10): полная Python-only архитектура» — cutover chain + verify recipe + p95 баseline.

**Watchlist для следующих агентов:**
- `n8n_workflows/_strip_phenotype.py` + `_audit_refs.py` + `_verify_post.py` — оставлены в worktree как audit trail. Готовый шаблон для следующего legacy strip (Add food / AI Engine / Location / Payment).
- 9 «shared» phenotype-touch нод НЕ тронуты (Merge Data, Edit Type Router, Build Profile Sub-Screen, Build Edit Question, Build Ask Markup, Back Target Router, Build Edit Context from Back) — phenotype-упоминания в комментариях/dict-ах остались, но entry point больше нет → harmless dead code path внутри shared нод. Cleanup при следующем full pass.
- В `Build Profile Sub-Screen` остался `phenoMap` dict — Profile screen теперь Python-рендерится через my_plan target, нода unreachable но active. Можно вычистить при cleanup pass.

**Файлы:**
- `migrations/195_phenotype_quiz_inline_emoji_replay.sql` (применена 18:42 MSK).
- `n8n_workflows/_strip_phenotype.py` (transform + PUT body builder, идемпотентен на pre-strip JSON).
- `n8n_workflows/_audit_refs.py` (executeWorkflow refs vs existing workflows audit).
- `n8n_workflows/_verify_post.py` (post-PUT GET sanity check).
- `scripts/_bench_render_phenotype.py` (p95 benchmark template).
- `scripts/_verify_phenotype_resolved.py` (end-to-end Python resolve test).
- `n8n_workflows/_backups/04_Menu_pre_phenotype_strip_2026-05-10.json` (gitignored snapshot для rollback).
- `.gitignore` — добавлен `n8n_workflows/_backups/`.

