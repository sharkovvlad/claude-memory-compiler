# 2026-05-31

## mig 391 — «Назад» в онбординге: phenotype-квиз + экраны maternal/cycle (follow-up к mig 390)

**Что:** доделана отложенная часть mig 390 (back-навигация в онбординге).

**Контекст:** mig 390 (PR #246, смержен 30.05 вечером) вернул «Назад» на линейных шагах (пол→цель) + ветке maternal, но явно оставил вне scope: (1) back внутри phenotype-квиза во время онбординга, (2) кнопки «Назад» на самих экранах maternal/cycle.

**Сделано (RPC `process_onboarding_input`, 3 anchor-replace из live pg_get_functiondef):**
1. **Phenotype-квиз back раскрыт на онбординг.** Условие `cmd_back AND v_status = 'edit_phenotype'` → `IN ('edit_phenotype','registration_step_phenotype_quiz')`. q2→q1, q3→q2, q4→q3 работают общей nav_stack-логикой (она уже была корректна для обоих режимов — current screen = `nav_stack->>-1`). q1/NULL-ветка теперь режим-зависимая: retake → `my_plan_settings` (сохранено), онбординг → шаг до квиза (`goal_type='maintain'` → `edit_goal`, иначе → `edit_speed`).
2. **Cycle cmd_back** был re-render того же экрана → теперь `cycle → onboarding_maternal_status` (status `registration_step_maternal`; цикл достижим только из maternal "no").

**Кнопки (ui_screen_buttons):**
- q1-q4 (`edit_phenotype`,`phenotype_q2/3/4`) back: vis `u.status = 'edit_phenotype'` → `IN ('edit_phenotype','registration_step_phenotype_quiz')`. `phenotype_result` НЕ трогал (на result основное действие — Continue, вне scope).
- Добавлены back-кнопки (`buttons.back`/`cmd_back`) на `onboarding_maternal_status` (r3), `_trimester` (r2), `_lactation_type` (r2), `onboarding_cycle_question` (r2).

**Ключевой gotcha (nav_stack двухрежимный):** при входе в квиз в онбординге (из goal=maintain или speed-pick) `edit_phenotype` НЕ push'ится в nav_stack — push есть только при advance q1→q2 (`push_nav 'phenotype_q2'`) и в profile-retake entry (вне этого RPC). Значит на q1 в онбординге `nav_stack->>-1` ≠ 'edit_phenotype' → `v_current_q=NULL` → попадает в q1-ветку. Это и нужно: NULL трактуется как «не на конкретном вопросе» → выход к шагу до квиза. В retake же entry-handler push'ит 'edit_phenotype', поэтому q1 определяется корректно. Поэтому ветку q1 разделил по `v_status`, а не по nav.

**Применено:** LIVE через psycopg2 (anchor-replace, идемпотентность по маркеру `mig 391:`). mig HEAD → **391**.

**Верификация (psycopg2, синтетический user TID=-999999001, всё в ROLLBACK):**
- 12 RPC-сценариев переходов (onb q2→q1/q3→q2/q4→q3, q1→goal(maintain), q1→speed(lose/gain), retake q1→myplan, retake q3→q2, maternal→age, trimester→maternal, lactation→maternal, cycle→maternal) — все OK.
- 8 рендеров клавиатур: back-кнопки присутствуют где надо; на retake q1 `cmd_quiz_skip` отсутствует (не сломан).
- router.route(cmd_back) для всех 6 статусов → target `onboarding`.
- `pytest tests/handlers/test_onboarding_v3.py` — 61 passed (Python не менялся).

**PR:** (заполнить после создания). Деплой НЕ делал (только код-PR; RPC уже применён руками).
# 2026-05-31

## mig 390 регрессия: «Назад» на скорости → AI (PR #248)

Owner при тесте на 786301802 поймал баг: «Назад» на шаге скорости в онбординге вёл не на «Цель», а в мусор. Root cause: mig 390 показал кнопку «Назад» на `edit_speed`, но `registration_step_speed` отсутствовал в `BUTTON_ONLY_STATUSES` (router.py) → `cmd_back` маршрутизировался в `target='ai'` вместо onboarding. goal/activity/training там были (повезло), speed — нет. Классический [[fsm-state-whitelist-discipline]].

**Урок проверки:** мой sandbox-тест mig 390 вызывал `process_onboarding_input` НАПРЯМУЮ, в обход роутера → не поймал misrouting. Правильно тестировать back-навигацию через `route()` (полный путь), а не только RPC.

**Fix (PR #248, Python → нужен деплой):** + `registration_step_speed` в BUTTON_ONLY_STATUSES + регрессионный тест. mig 390/391 были DB-only (live), а это код — баг живёт в проде до деплоя.

**Открытый риск для будущего:** text-steps `registration_step_2/3/4` (возраст/вес/рост) ТОЖЕ route cmd_back → 'ai'. Сейчас не баг (text_input → remove_keyboard, кнопка не рендерится). НО если делать «инлайн-Назад на text-input онбординг-шагах» (owner хочет) — обязательно сперва добавить эти статусы в BUTTON_ONLY, иначе тот же misrouting.

## reply-kb пропадала после правки веса/возраста в профиле (PR #249, mig 392 LIVE)

Owner на 417002669: после правки веса/возраста в профиле пропадало нижнее меню (reply-kb). Owner верно заметил: нажать «Мой день» чтобы вернуть — нельзя, она на том же пропавшем меню.

**Диагноз (НЕ регрессия наших правок):** ask_weight/ask_age/ask_height — text_input → remove_keyboard снимает меню. Возврат на personal_metrics, у которого НЕ было meta.reply_kb_entry → re-attach (render_screen CASE branch 2, mig 160/183) не срабатывал. tech-debt #11 из [[one-time-attach-pattern]]. Пол (inline) меню не снимает → проблема только на text_input-правках.

**Fix:** mig 392 — personal_metrics.meta.reply_kb_entry=true. Существующий механизм возвращает reply-kb с тостом «✅ Сохранено!». Данные only, деплой не нужен. Проверено песочницей (tx+rollback): prev=edit_weight → reply_keyboard PRESENT; prev=edit_gender → NONE (лишнего re-attach нет).

## «Авилес» city recognition — сработал на ретесте 31.05 (data point, НЕ фикс)

Owner ретестил онбординг на 786301802: «Авилес» (Cyrillic→Avilés) распознался, онбординг завершился (скриншот: confetti + «¡Perfil listo!»). **city_resolver.py НЕ менялся** с mig 379 (порог _MIN_CONFIDENCE=0.7 прежний) → это не фикс, а вероятностный успех: gpt-4o-mini в этот раз дал confidence≥0.7. 30.05 падал (<0.7) из-за AI-вариативности. Edge case НЕ закрыт детерминированно — при рецидиве остаётся опция Geoapify forward-fallback / снижение порога / улучшение промпта. Все остальные тесты онбординга на 786301802 owner подтвердил рабочими (включая «Назад» на кнопочных шагах после деплоя #248).

## #248 (speed back routing) — merged + deployed (b0da952 deploy = ложная ошибка, SSH-обрыв на [6/6], прод здоров /health 200)

## Сессия закрыта — variant 1 (inline-«Назад» на text_input шагах) НЕ начат, передан в handover 2026-05-31_variant1_inline_back_text_steps.md (контекст исчерпан, рендерер общий — рискованно на исходе длинной сессии).

## mig 393/394 — онбординг UX: cycle «Назад» через шаг + галочки ✅ (PR #250, агент angry-liskov)

UAT на 786301802, два бага.

**Bug A (mig 393, RPC+router, LIVE):** на экране выбора срока менструации (`cycle_tracking_setup`) [Назад] вёл на пред-предыдущий (`onboarding_maternal_status`) вместо предыдущего (`onboarding_cycle_question`). Root cause: `onboarding_cycle_question` и `cycle_tracking_setup` делили ОДИН статус `registration_step_cycle` → единый cmd_back (mig 391) не различал экраны. Fix: выделен статус `registration_step_cycle_setup` для setup-экрана. `cmd_onb_cycle_yes` переводит в него; cmd_back стал условным (`IF v_status IN (...)` — один блок обслуживает оба статуса, без дублирования save/edit). Возвраты из `cycle_length_input`/`cycle_start_date_input` теперь в `_cycle_setup`. Новый статус: + `workflow_states` (FK!), + `BUTTON_ONLY_STATUSES`/`ONBOARDING_STATUSES` (router, иначе cmd_back→ai, ловушка #248). **Gotcha:** `users.status` имеет FK `users_status_fkey` → новый статус ОБЯЗАН быть в `workflow_states`, иначе onboarding_unexpected_exception (23503). Verify: synthetic full-flow + route() полный путь — all OK.

**Bug B (mig 394, data-only, LIVE):** при возврате «Назад» выбранный вариант не помечался ✅. Механизм: `ui_screens.meta.current_value_col` + `button.meta.save_value` → render_screen ставит is_current → template_engine рисует ✅. Уже работал на gender/activity/training/goal/speed/phenotype/lang. Добавлено: `onboarding_diet_question` (diet_type, owner-reported вегетарианец), `onboarding_maternal_trimester` (pregnancy_trimester), `onboarding_maternal_lactation_type` (lactation_type). `maternal_status` НЕ покрыт — ответ не в одной колонке (is_pregnant/is_lactating booleans), механизм current_value_col (одна колонка) неприменим.

**mig HEAD → 394.** RPC 393 + данные 394 применены к LIVE руками. Деплой нужен ТОЛЬКО для router.py (новый статус в whitelist). ⚠️ Узкое окно до деплоя: female 15-50 на цикле, cmd_back→ai.

## Variant 1 — DE-RISKED, НЕ имплементирован (контекст-бюджет + shared-renderer риск). Handover обновлён.

Подтверждено фактами (для исполнителя): (1) cmd_back на `registration_step_2/3/4` СЕЙЧАС роутится в `ai` (route() проверено) — нужен whitelist в BUTTON_ONLY; текст («30») роутится в onboarding корректно (numeric_answer фаза раньше). (2) `ask_age/ask_weight/ask_height` — ОБЩИЕ screen_id для онбординга (registration_step_2/3/4) И профиля (edit_age/weight/height, через workflow_states). Значит screen-level meta-флаг затронет ОБА → ОБЯЗАТЕЛЕН status-гейт `LIKE 'registration_step_%'` в render_screen, иначе сломается remove_keyboard в профиле (боль mig 392). (3) RPC mig 390 back-логика для step_2/3/4 уже в LIVE. Вывод: Variant 1 = render_screen RPC (status-gated флаг в telegram_ui) + template_engine (text_input+флаг→inline) + router (step_2/3/4 в BUTTON_ONLY, проверить что текст не ломается) + data (meta.show_back_inline на 3 экранах) + регресс-тесты профиля. Owner pre-approved; рекомендация — делать фокусной сессией со свежим контекстом из-за shared-renderer blast radius.

## ОБНОВЛЕНИЕ: Variant 1 ВСЁ-ТАКИ реализован (PR #251, mig 395/396) — owner сказал «делать сейчас»

Выше в этом daily было «Variant 1 не имплементирован» — owner после de-risk дал go. Сделано на отдельной ветке `claude/variant1-inline-back` (от origin/main, чтобы не мешать PR #250):
- **mig 395 (data):** `meta.show_back_inline=true` на ask_age/ask_weight/ask_height.
- **mig 396 (render_screen):** `telegram_ui.text_input_inline_back` = (input_type=text_input AND meta.show_back_inline AND status LIKE 'registration_step_%'). 3-е условие — гейт: ask_* общие с профилем (edit_*), status-гейт оставляет профилю remove_keyboard (mig 392 цел). Anchor-replace по live render_screen.
- **template_engine `_build_reply_markup`:** text_input + флаг → inline_keyboard.
- **router:** registration_step_2/3/4 в BUTTON_ONLY. **Проверено фактом:** числовой ввод ловится section 8 (numeric_answer) ДО button-section → сохранение веса/возраста не сломано; cmd_back → onboarding (фикс); не-числовой текст → ai (reason сменился numeric_step→button_step, target тот же — обновил 2 пред-существующих router-теста).
- Verify: render-флаг (онб True/проф False/прочие text_input False), markup, route(), RPC back-nav (age→gender/weight→age/height→weight). pytest router 114 / onboarding 61 / template 87 зелёные.
- mig 395/396 LIVE. **Деплой нужен для template_engine.py + router.py.** mig HEAD → 396.

**Note для мерджа:** PR #250 (mig 393/394) и PR #251 (mig 395/396) оба трогают router.py BUTTON_ONLY_STATUSES в РАЗНЫХ регионах — конфликта быть не должно, но rebase перед merge обязателен (дисциплина §12).

## mig 397 + PR #251 rebase + trial-mana investigation (агент angry-liskov, сессия 2)

**PR #251 (Variant 1) — конфликт разрешён, MERGEABLE.** #250 смержился в main → test_router.py конфликтовал (оба добавляли кейсы после якоря cmd_select_goal). Rebase на origin/main, оставил ОБА набора кейсов (mig 393 cycle_setup + mig 396 Variant 1). router.py авто-смержился (разные регионы). 116 router-тестов passed (4 пре-сущ. country/city). force-with-lease.

**Trial-мана (owner-вопрос + /nlm + live):** новый юзер получает **2 маны** (дефолт колонок users.mana_current/max=2), `mana_free_daily_limit=2`. Отдельного увеличенного trial для начинающих регистрацию НЕТ — все онбординг-юзеры имеют 2. `mana_gift_registration=5` начисляется только ПОСЛЕ завершения (grant_registration_mana / complete_onboarding). Owner: оставляем 2 (todo при желании расширить). Hard trial-gate отменён (см. handover food-during-onboarding — open trial предпочтителен).

**mig 397 (PR #252, data-only LIVE): wait-сообщение «Анализирую еду» × 13 langs.** Боль owner: текст скучный + без эмодзи. Root cause: telegram_proxy.py:446 `_PLACEHOLDER_RE.sub('', ...)` ВЫРЕЗАЕТ `{{...}}` (вариант[0] терял 🧠; остальные 4 работали — raw emoji). Решение owner+Номс: заменён весь массив messages.analyzing на 4 новых варианта (🔬🧬🧠🔮 scientific-biohacking Sage, raw emoji). RU/EN — Номс, 11 langs — copywriter-субагент (RTL ar/fa ZWNJ, идиоматика). Proxy-кэш фраз 1ч TTL → обновится ≤1ч без деплоя. **Gotcha задокументирован:** proxy STRIP'ает плейсхолдеры — в messages.analyzing нельзя `{{icon_*}}`, только raw emoji (или чинить proxy на резолв — отдельная задача).

**Pending (proposal-этап):** мягкий nudge «доделай настройку → стрики/статистика» после пары распознаваний в онбординге — owner ревьюит копи с агентом Номс, потом субагент делает миграцию. Hard-gate НЕ делаем.

**mig HEAD → 397.** Open PRs: #251 (Variant 1, mergeable), #252 (wait-copy).

## UAT 786301802 batch 2 — 5 фиксов (PR #253, mig 398-401 LIVE + telegram_proxy)

1. **mig 398 (reset_to_onboarding):** не чистил diet_type + maternal/cycle → stale ✅ при ре-онбординге (Vegetariano помечен заранее; lactation_type='exclusive' при is_lactating=FALSE). Добавлены 9 полей (diet_type, is_pregnant, is_lactating, pregnancy_trimester, pregnancy_due_date, lactation_type, lactation_started, cycle_tracking_enabled, cycle_period_choice). **delete_user_account НЕ трогали** — soft-delete хранит данные 30д для restore («vuelve todo»); чистка сломала бы restore. Финальный wipe — cron_anonymize. (owner думал что баг в soft-delete, но реальный путь ре-онбординга — reset_to_onboarding через cmd_restore_restart.)
2. **mig 399 (process_onboarding_input):** weight [Назад] female вёл на cycle_question (пред-пред) вместо cycle_tracking_setup. Fix: cycle_start_date IS NOT NULL → registration_step_cycle_setup, иначе cycle (skip).
3. **mig 400 (data):** единый 🔙 icon_back на всех back-кнопках онбординга (было ❌ icon_cancel на age/weight/height + NULL на cycle/maternal/diet). edit_* уже были icon_back.
4. **mig 401 (data):** ✅ на сроке цикла — current_value_col=cycle_period_choice (колонка хранит today/7d_ago/14d_ago = save_value кнопок). Сбрасывается в mig 398.
5. **telegram_proxy.py (deploy):** «Calculando calorías» food-индикатор стрелял на вводе числа возраст/вес/рост. **RACE:** индикатор fire-and-forget читал status через get_indicator_context уже ПОСЛЕ advance основным хендлером (step_4→step_5; step_5 не в NO_INDICATOR_STATUSES). Fix: глушим для всех `status.startswith("registration_step_")`. onboarding:country/timezone (двоеточие) не затронуты.

**NLM lesson:** колонка `cycle_period_choice` (today/7d_ago/14d_ago) существует — идеальна для checkmark cycle. icon_back=🔙 канонический. Подтверждено live.
**mig HEAD → 401.** Open PRs: #251 (Variant1), #252 (wait-copy), #253 (UAT batch2). Деплой нужен: router+template (#251), telegram_proxy (#253).

## Онбординг-еда → Python (PR #254, mig 404/405 LIVE) — архитектурный пробел Stage 7

**Вскрыт пробел:** Stage 7 (Python ai_engine global) закрыл только registered. Еда от незавершивших регистрацию (new/registration_step_*) шла в n8n 03_AI_Engine — webhook гейт `_try_authoritative_path` требует status registered/editing_meal (webhook_server.py:1583 «skip onboarding»). Owner подтвердил: давно планировалось на Python, предыдущие агенты не учли.

**Реализация (owner-решения: логировать + учитывать ману + nudge вариант 1):**
- **mig 404 `log_meal_onboarding`** — ИЗОЛИРОВАННАЯ RPC (НЕ трогает log_meal_transaction → нулевой риск для зарегов). food_logs insert + check_and_deduct_mana (2/день free; premium/trial gated внутри), БЕЗ XP/streak/league (соцкапитал только после регистрации — интент подтверждён: незареги xp=0/level=1).
- **mig 405** — messages.onboarding_food_nudge × 13 (вариант 1 метаболический, без «стрик»).
- **handle_onboarding_food** (food_log.py) — cascade parse → log_meal_onboarding → nudge + `_rerender_current_screen` (возврат на шаг). Свой thinking-стикер (proxy глушит индикатор для registration_step_*). NO_MANA → не логируем, но nudge.
- **webhook гейт** — target=ai + food + onboarding-статус → Python, idempotency, взаимоисключающий с registered. Registered НЕ тронут.

**Почему не логировалось раньше:** не RPC (stateless), а рендер карточки (нужны цели «X/Y ккал», у незарега NULL). Карточку не строим → лог безопасен. food_logs: FK telegram_id only, без констрейнтов на registered/targets (live-проверено).

**Lesson (NLM vs live):** NLM утверждал «незарегам геймификация заблокирована» — но log_meal_transaction НЕ имеет status-гейта (грантит XP всем). Эмпирика: 0 логов от онбординг-юзеров (никогда не логировали). Доверяй live.

**Verify:** mig 404 synthetic (2 лога, мана 2→0, NO_MANA, xp/streak/status целы); 3 новых теста + 83 регрессии зелёные. **Деплой нужен** (food_log.py + webhook_server.py).
**mig HEAD → 405** (404/405 мои; параллельный агент берёт ≥406 по договорённости с owner). #251/#252/#253 merged. Open: #254.

## Индикатор: онбординг-еда (new) + геолокация (PR #257, telegram_proxy)

UAT 786301802 + логи VPS вскрыли 2 бага в `maybe_send_indicator`:
1. **status='new' не глушился** — suppression была только `registration_step_*`. На первом шаге (new) еда → proxy слал food-текст (лог `indicator sent kind=text msg_id=7388`), а `handle_onboarding_food` слал свой стикер → `clear` удалял стикер, food-текст орфанился. Fix: глушим `new` + `registration_step_*`.
2. **Геолокация → food-текст** (лог 14:05:17 `kind=text` на location share). «Анализирую еду» неверно для геопозиции (reverse-geocode 895мс). Fix: для location всегда **стикер** (`use_text=False`) + location НЕ глушится onboarding-статусом.
Тесты: 5 новых (`tests/test_proxy_indicator_onboarding.py`). Деплой нужен.

**Доп. находки UAT (не баги наши):**
- Кнопка [País] в Профиле «ничего не делает» — **логи (14:09) показывают cmd_edit_country → menu_v3 200 + cmd_list_countries → location рендерит список**. Технически работает. Нужен свежий repro. UX-вопрос owner (registered может менять страну → price arbitrage; вариант — сообщение «обратитесь в саппорт») — продуктовое решение, отложено.
- Онбординг-еда не показывает распознанное (только nudge) — by design (целей нет). Повторное welcome на status='new' = `_rerender_current_screen` рендерит текущий шаг (welcome) — корректно.
- **save_indicator_state logged ERROR (HTTP 204)** — 204 = success, но supabase_rpc-обёртка логирует как ERROR. Пре-существующий шум, не баг. Кандидат на отдельный фикс.

**Lesson:** suppression индикатора по статусу должна совпадать с гейтом обработчика (handle_onboarding_food ловит `new`+`registration_step_*`, а suppression покрывала только `registration_step_*` → рассинхрон → двойной индикатор). Контент-тип (location vs food) тоже важен: один и тот же `messages.analyzing` текст не подходит для не-food контента.

## Компактная карточка КБЖУ для онбординг-еды (PR #258, mig 406)

owner UAT 31.05: распознавание «вхолостую» — незарег видел только nudge, КБЖУ были в БД но невидимы. Owner выбрал «компактно КБЖУ + nudge».
- **mig 406 `food_log.onboarding_recognized`** — карточка `confirmation_text_no_mana` минус строки 🎯 цели и 🌟 геймификации. **Языко-нейтральная** (под-ключи {tr:macros.*}/{tr:food_log.*}/{icon_*} уже × 13 → перевод не нужен). Одна строка для всех 13 langs.
- `handle_onboarding_food`: success → карточка + nudge одним сообщением (`_resolve_text` + `_format_items_block`); no-mana → только nudge (не сохранено); not-food → ничего.
- Smoke-render ES: «🍳 Tortilla — 250 kcal / 💪P:18g·🥑G:19g·🌾HC:2g» + nudge ✅.
- **Lesson:** для не-полной карточки переиспользуй под-ключи существующего шаблона ({tr:...}/{icon_*}) → языко-нейтральный шаблон без перевода × 13. mig 406 LIVE, деплой нужен (food_log.py).

**Открытые PR (этой сессии, ждут деплоя):** #257 (indicator: new+геолокация), #258 (карточка КБЖУ). mig HEAD 406.
