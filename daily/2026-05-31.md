# 2026-05-31

## mig 391 — «Назад» в онбординге: phenotype-квиз + экраны maternal/cycle (follow-up к mig 390)

**Что:** доделана отложенная часть mig 390 (back-навигация в онбординге).

**Контекст:** mig 390 (PR #246, смержен 30.05 вечером) вернул «Назад» на линейных шагах (пол→цель) + ветке maternal, но явно оставил вне scope: (1) back внутри phenotype-квиза во время онбординга, (2) кнопки «Назад» на самих экранах maternal/cycle.

**Сделано (RPC `process_onboarding_input`, 3 anchor-replace из live pg_get_functiondef):**
1. **Phenotype-квиз back раскрыт на онбординг.** Условие `cmd_back AND v_status = 'edit_phenotype'` → `IN ('edit_phenotype','registration_step_phenotype_quiz')`. q2→q1, q3→q2, q4→q3 работают общей nav_stack-логикой (она уже была корректна для обоих режимов — current screen = `nav_stack->>-1`). q1/NULL-ветка теперь режим-зависимая: retake → `my_plan_settings` (сохранено), онбординг → шаг до квиза (`goal_type='maintain'` → `edit_goal`, иначе → `edit_speed`).
2. **Cycle cmd_back** был re-render того же экрана → теперь `cycle → onboarding_maternal_status` (status `registration_step_maternal`; цикл достижим только из maternal "no").

**Кнопки (ui_screen_buttons):**
- q1-q4 (`edit_phenotype`,`phenotype_q2/3/4`) back: vis `u.status = 'edit_phenotype'` → `IN ('edit_phenotype','registration_step_phenotype_quiz')`. `phenotype_result` НЕ трогал (на result основное действие — Continue, вне scope).
- Добавлены back-кнопки (`buttons.back`/`cmd_back`) на `onboarding_maternal_status` (r3), `_trimester` (r2), `_lactation_type` (r2), `onboarding_cycle_question` (r2).

**Ключевой gotcha (nav_stack двухрежимный):** при входе в квиз в онбординге (из goal=maintain или speed-pick) `edit_phenotype` НЕ push'ится в nav_stack — push есть только при advance q1→q2 (`push_nav 'phenotype_q2'`) и в profile-retake entry (вне этого RPC). Значит на q1 в онбординге `nav_stack->>-1` ≠ 'edit_phenotype' → `v_current_q=NULL` → попадает в q1-ветку. Это и нужно: NULL трактуется как «не на конкретном вопросе» → выход к шагу до квиза. В retake же entry-handler push'ит 'edit_phenotype', поэтому q1 определяется корректно. Поэтому ветку q1 разделил по `v_status`, а не по nav.

**Применено:** LIVE через psycopg2 (anchor-replace, идемпотентность по маркеру `mig 391:`). mig HEAD → **391**.

**Верификация (psycopg2, синтетический user TID=-999999001, всё в ROLLBACK):**
- 12 RPC-сценариев переходов (onb q2→q1/q3→q2/q4→q3, q1→goal(maintain), q1→speed(lose/gain), retake q1→myplan, retake q3→q2, maternal→age, trimester→maternal, lactation→maternal, cycle→maternal) — все OK.
- 8 рендеров клавиатур: back-кнопки присутствуют где надо; на retake q1 `cmd_quiz_skip` отсутствует (не сломан).
- router.route(cmd_back) для всех 6 статусов → target `onboarding`.
- `pytest tests/handlers/test_onboarding_v3.py` — 61 passed (Python не менялся).

**PR:** (заполнить после создания). Деплой НЕ делал (только код-PR; RPC уже применён руками).
# 2026-05-31

## mig 390 регрессия: «Назад» на скорости → AI (PR #248)

Owner при тесте на 786301802 поймал баг: «Назад» на шаге скорости в онбординге вёл не на «Цель», а в мусор. Root cause: mig 390 показал кнопку «Назад» на `edit_speed`, но `registration_step_speed` отсутствовал в `BUTTON_ONLY_STATUSES` (router.py) → `cmd_back` маршрутизировался в `target='ai'` вместо onboarding. goal/activity/training там были (повезло), speed — нет. Классический [[fsm-state-whitelist-discipline]].

**Урок проверки:** мой sandbox-тест mig 390 вызывал `process_onboarding_input` НАПРЯМУЮ, в обход роутера → не поймал misrouting. Правильно тестировать back-навигацию через `route()` (полный путь), а не только RPC.

**Fix (PR #248, Python → нужен деплой):** + `registration_step_speed` в BUTTON_ONLY_STATUSES + регрессионный тест. mig 390/391 были DB-only (live), а это код — баг живёт в проде до деплоя.

**Открытый риск для будущего:** text-steps `registration_step_2/3/4` (возраст/вес/рост) ТОЖЕ route cmd_back → 'ai'. Сейчас не баг (text_input → remove_keyboard, кнопка не рендерится). НО если делать «инлайн-Назад на text-input онбординг-шагах» (owner хочет) — обязательно сперва добавить эти статусы в BUTTON_ONLY, иначе тот же misrouting.

## reply-kb пропадала после правки веса/возраста в профиле (PR #249, mig 392 LIVE)

Owner на 417002669: после правки веса/возраста в профиле пропадало нижнее меню (reply-kb). Owner верно заметил: нажать «Мой день» чтобы вернуть — нельзя, она на том же пропавшем меню.

**Диагноз (НЕ регрессия наших правок):** ask_weight/ask_age/ask_height — text_input → remove_keyboard снимает меню. Возврат на personal_metrics, у которого НЕ было meta.reply_kb_entry → re-attach (render_screen CASE branch 2, mig 160/183) не срабатывал. tech-debt #11 из [[one-time-attach-pattern]]. Пол (inline) меню не снимает → проблема только на text_input-правках.

**Fix:** mig 392 — personal_metrics.meta.reply_kb_entry=true. Существующий механизм возвращает reply-kb с тостом «✅ Сохранено!». Данные only, деплой не нужен. Проверено песочницей (tx+rollback): prev=edit_weight → reply_keyboard PRESENT; prev=edit_gender → NONE (лишнего re-attach нет).

## «Авилес» city recognition — сработал на ретесте 31.05 (data point, НЕ фикс)

Owner ретестил онбординг на 786301802: «Авилес» (Cyrillic→Avilés) распознался, онбординг завершился (скриншот: confetti + «¡Perfil listo!»). **city_resolver.py НЕ менялся** с mig 379 (порог _MIN_CONFIDENCE=0.7 прежний) → это не фикс, а вероятностный успех: gpt-4o-mini в этот раз дал confidence≥0.7. 30.05 падал (<0.7) из-за AI-вариативности. Edge case НЕ закрыт детерминированно — при рецидиве остаётся опция Geoapify forward-fallback / снижение порога / улучшение промпта. Все остальные тесты онбординга на 786301802 owner подтвердил рабочими (включая «Назад» на кнопочных шагах после деплоя #248).

## #248 (speed back routing) — merged + deployed (b0da952 deploy = ложная ошибка, SSH-обрыв на [6/6], прод здоров /health 200)

## Сессия закрыта — variant 1 (inline-«Назад» на text_input шагах) НЕ начат, передан в handover 2026-05-31_variant1_inline_back_text_steps.md (контекст исчерпан, рендерер общий — рискованно на исходе длинной сессии).

## mig 393/394 — онбординг UX: cycle «Назад» через шаг + галочки ✅ (PR #250, агент angry-liskov)

UAT на 786301802, два бага.

**Bug A (mig 393, RPC+router, LIVE):** на экране выбора срока менструации (`cycle_tracking_setup`) [Назад] вёл на пред-предыдущий (`onboarding_maternal_status`) вместо предыдущего (`onboarding_cycle_question`). Root cause: `onboarding_cycle_question` и `cycle_tracking_setup` делили ОДИН статус `registration_step_cycle` → единый cmd_back (mig 391) не различал экраны. Fix: выделен статус `registration_step_cycle_setup` для setup-экрана. `cmd_onb_cycle_yes` переводит в него; cmd_back стал условным (`IF v_status IN (...)` — один блок обслуживает оба статуса, без дублирования save/edit). Возвраты из `cycle_length_input`/`cycle_start_date_input` теперь в `_cycle_setup`. Новый статус: + `workflow_states` (FK!), + `BUTTON_ONLY_STATUSES`/`ONBOARDING_STATUSES` (router, иначе cmd_back→ai, ловушка #248). **Gotcha:** `users.status` имеет FK `users_status_fkey` → новый статус ОБЯЗАН быть в `workflow_states`, иначе onboarding_unexpected_exception (23503). Verify: synthetic full-flow + route() полный путь — all OK.

**Bug B (mig 394, data-only, LIVE):** при возврате «Назад» выбранный вариант не помечался ✅. Механизм: `ui_screens.meta.current_value_col` + `button.meta.save_value` → render_screen ставит is_current → template_engine рисует ✅. Уже работал на gender/activity/training/goal/speed/phenotype/lang. Добавлено: `onboarding_diet_question` (diet_type, owner-reported вегетарианец), `onboarding_maternal_trimester` (pregnancy_trimester), `onboarding_maternal_lactation_type` (lactation_type). `maternal_status` НЕ покрыт — ответ не в одной колонке (is_pregnant/is_lactating booleans), механизм current_value_col (одна колонка) неприменим.

**mig HEAD → 394.** RPC 393 + данные 394 применены к LIVE руками. Деплой нужен ТОЛЬКО для router.py (новый статус в whitelist). ⚠️ Узкое окно до деплоя: female 15-50 на цикле, cmd_back→ai.

## Variant 1 — DE-RISKED, НЕ имплементирован (контекст-бюджет + shared-renderer риск). Handover обновлён.

Подтверждено фактами (для исполнителя): (1) cmd_back на `registration_step_2/3/4` СЕЙЧАС роутится в `ai` (route() проверено) — нужен whitelist в BUTTON_ONLY; текст («30») роутится в onboarding корректно (numeric_answer фаза раньше). (2) `ask_age/ask_weight/ask_height` — ОБЩИЕ screen_id для онбординга (registration_step_2/3/4) И профиля (edit_age/weight/height, через workflow_states). Значит screen-level meta-флаг затронет ОБА → ОБЯЗАТЕЛЕН status-гейт `LIKE 'registration_step_%'` в render_screen, иначе сломается remove_keyboard в профиле (боль mig 392). (3) RPC mig 390 back-логика для step_2/3/4 уже в LIVE. Вывод: Variant 1 = render_screen RPC (status-gated флаг в telegram_ui) + template_engine (text_input+флаг→inline) + router (step_2/3/4 в BUTTON_ONLY, проверить что текст не ломается) + data (meta.show_back_inline на 3 экранах) + регресс-тесты профиля. Owner pre-approved; рекомендация — делать фокусной сессией со свежим контекстом из-за shared-renderer blast radius.
