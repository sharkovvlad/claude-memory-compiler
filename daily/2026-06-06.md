# Daily 2026-06-06

## ~11:00 — fix(ambassador): кнопки «Поделиться кодом» и «Статистика» не работали (mig 471, PR #342)

**Проблема:** owner репортнул что на экране Банда/Прогресс кнопки [Поделиться кодом] и [Статистика] ни к чему не ведут.

**Root cause:** оба `ui_screen_buttons` на `friends_info` имели `meta={}`. `process_user_input` находил кнопку в БД (FOUND), но `target_screen=NULL` → `v_next_screen = v_current_screen` → ре-рендер того же экрана `friends_info` → Telegram видел «not modified» или silent no-op → пользователь ничего не замечал.

Это известный паттерн «Gotcha 4» в `[[concepts/headless-architecture]]` §meta.target_screen — только на этот раз кнопки были добавлены при миграции headless но meta не заполнили.

**Доп. контекст:**
- Кнопки видны только ambassadors/paid-referrers/trainers (visible_condition), поэтому массово не репортили — только owner с `ambassador_tier='active'`.
- `share_invite_link` работал (url_template:'share_invite' в meta) — именно поэтому не-ambassador пользователи не замечали проблем.

**Что применено LIVE (mig 471 applied):**

1. `share_ambassador_code` button: добавлен `meta = {"url_template": "share_ambassador_code"}` → теперь URL-кнопка открывает Telegram share-диалог с текстом «Используй мой код NOMS417002669 для 10% скидки на NOMS Premium!»

2. `cmd_ambassador_stats` button: добавлен `meta = {"target_screen": "ambassador_stats"}` → теперь навигирует на новый экран

3. Новый экран `ambassador_stats`:
   - `business_data_rpc = 'get_friends_info_rpc'` (те же данные)
   - `text_key = 'ambassador.stats_detail_text'` (новый ключ, без CTA-цитаты Nomsa → content отличается от friends_info → Telegram editMessageText принимает)
   - `back_screen_id_default = 'friends_info'`
   - кнопка cmd_back

4. `template_engine.py`: добавлен resolver `_resolve_share_ambassador_code_url` → строит t.me/share/url с NOMS{telegram_id}

5. Новый translation key `ambassador.stats_detail_text` × 13 языков: `📊 <b>{title_resolved}</b>\n\n{body_formatted}\n\n{link_block}`

**Файлы:** `services/template_engine.py`, `migrations/471_ambassador_buttons_fix.sql`

**Статус:** mig 471 APPLIED LIVE, PR #342 открыт (не смержен, деплой через GitHub Actions после merge).

**Проверено:** 
- dispatch_with_render для cmd_ambassador_stats → status=render, screen=ambassador_stats ✅
- _build_inline_keyboard → share_ambassador_code рендерится как url-кнопка без callback_data ✅
- 90 tests passed ✅

## ~02:00 — Ambassador share redesign + promo code full wiring (mig 472, PR #343)

**Контекст:** owner репортнул (а) кнопка «Поделиться кодом» → должна быть «ссылкой»; (б) механика промокода не доделана (код шарится, но применить негде); (в) UX хуже чем Big Tech.

**Root cause promo code:**
- `set_user_status` RPC отсутствовал → `entering_promo` статус не устанавливался → введённый юзером текст уходил в AI engine
- `apply_discount_code` только валидировал код, не сохранял pending
- `get_all_plan_prices` и `get_user_price` не читали pending скидку → Stripe выставлял полную цену
- `activate_subscription` имел `p_discount_code_id uuid` параметр но webhook handler не передавал его → комиссия амбассадора не начислялась
- Кнопка в UI закомментирована в `payment.py` с комментарием «до готовности бэкенда»

**Что применено LIVE (mig 472 applied):**

1. `set_user_status(p_telegram_id, p_status)` RPC создан — простой UPDATE users

2. `users.pending_promo_code TEXT` — новое поле, хранит активный промокод между шагами

3. `apply_discount_code` обновлён (CREATE OR REPLACE из live pg_get_functiondef):
   - На success: `UPDATE users SET pending_promo_code = UPPER(p_code)`

4. `get_all_plan_prices` обновлён: при `pending_promo_code IS NOT NULL` читает скидку из `discount_codes` и применяет к `sp.amount` → возвращает `promo_discount_percent` + скорректированный `final_price`

5. `get_user_price` обновлён: то же самое → Stripe checkout создаётся на правильную сумму

6. Translations (jsonb_set):
   - `ambassador.share_button` × 13 → «Поделиться ссылкой» / «Share Link»
   - `ambassador.share_code_text` × 13 → clean share message
   - `payment.promo_button` × 13 → «🎟 Ввести промокод»

7. Helper RPCs: `get_user_pending_promo(tid)` → {discount_code_id, promo_code, discount_percent}; `clear_user_pending_promo(tid)` → NULL

**Python changes (PR #343, не смержен, деплой нужен):**

8. `services/template_engine.py`: `_resolve_share_ambassador_code_url` обновлён — теперь URL = `https://t.me/{bot}?start=ref_{tid}` (реферальная ссылка для трекинга), text = share_code_text с промокодом. Результат в Telegram share dialog: "Попробуй NOMS... промокод NOMS417002669...\nhttps://t.me/nomsaibot?start=ref_417002669"

9. `handlers/payment.py`:
   - Promo button раскомментирован
   - `_handle_apply_promo`: при success — вызывает `_handle_premium_plans` (планы с обновлёнными ценами), при fail — stays in entering_promo state
   - `_format_plan_button_label`: при `promo_discount_percent > 0` → "Месяц — $3.59 (🎟 -10%)"

10. `webhook_server.py`:
    - `handle_checkout_completed`: перед `activate_subscription` вызывает `get_user_pending_promo` → если есть — передаёт `p_discount_code_id` → амбассадор получает 25% комиссии
    - После успешной активации → `clear_user_pending_promo`

**Полный флоу (Big Tech pattern):**
```
Амбассадор: Поделиться ссылкой → Telegram share:
  "Попробуй NOMS! Промокод NOMS417002669 даст 10% скидку"
  https://t.me/nomsaibot?start=ref_417002669

Новый юзер: жмёт ссылку → бот → оплата →
  [🎟 Ввести промокод] → вводит NOMS417002669 →
  планы: $3.59 / $6.74 / $35.99 (🎟 -10%) →
  Stripe checkout с правильной ценой →
  амбассадор → +25% комиссии
```

**Статус:** mig 472 + helper RPCs APPLIED LIVE. PR #343 открыт, не смержен. Нужен deploy (GitHub Actions) после merge.

**Durable gotchas:**
- `get_all_plan_prices` — STABLE, но в нём теперь `UPDATE users` (при expired promo — auto-clear). Формально нарушает STABLE семантику. Если будет проблема — убрать STABLE с функции.
- `apply_discount_code` не проверяет `allowed_plans` → ambassador код применим ко всем тарифам (это намеренно).
- `pending_promo_code` не expires автоматически — только очищается при оплате или при повторном вводе нового кода. Если юзер введёт код и не заплатит — код останется и скидка будет показываться постоянно.
