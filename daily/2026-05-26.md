# Daily Log: 2026-05-26

## Sessions

### Phase 3d cycle UX + P0 incident (agent nervous-cerf-566d7a, session 8)

**Migrations applied LIVE:** 352 (cycle premium routing fix) → 356 (cycle UX v2) → 357 (edit cycle length) → 359 (onboarding cycle question — BROKE PROD) → 360 (P0 recovery) → router.py whitelist additions.

**Timeline:**
- **Morning:** Phase 3d UAT continuation. Owner reported T2 fail (`[🌸 День 1/28]` button silent) — router whitelist gotcha (mig 356 callbacks not in `PROFILE_V5_CALLBACKS`). Fixed in PR #206 (router whitelist for cmd_cycle_view + cmd_disable_cycle_tracking).
- **Subagent 357** (edit cycle length screen with 5 presets 21/25/28/30/35) — 9/9 PASS via SAVEPOINT, applied LIVE.
- **Subagent 359** (onboarding cycle question integration) — applied LIVE with `ut.content || ns.payload` SHALLOW jsonb merge. **WIPED `content->'buttons'` (~200 keys) + `content->'onboarding'` (~100 keys) × 13 langs.** 182 ui_screen_buttons broke (rendered raw text_key).
- **04:00 MSK incident.** Different agent (bug investigator) caught via owner UAT screenshot showing raw `buttons.X` labels.
- **Recovery 09:00-11:00 MSK:**
  - Multi-pattern parser over migrations 1..358 + worktree mig 357. 5 SQL patterns: nested jsonb_set, to_jsonb('value'::text), CASE-style multi-lang, jsonb_build_object inside concat, inline JSON payload literal. Recovered 32-36 keys/lang.
  - Hand-crafted ~42 missing canonical UI labels × 13 langs (Sage-tone). Total 1005 leaf entries written via safe jsonb_set.
  - Mig 360 SQL file generated from final live state (13 UPDATE per lang via jsonb_set with full namespace object).
  - Mig 359 file fixed: `||` shallow merge replaced with `jsonb_each LATERAL + jsonb_set + coalesce(content->ns,'{}') || inner` pattern (inner `||` at LEAF level is safe).
  - Router.py: `registration_step_cycle` added to `BUTTON_ONLY_STATUSES` + `ONBOARDING_STATUSES`.
  - Owner reported double-emoji on screenshot — 19 keys had `icon_const_key` set AND emoji prefix in value. Stripped × 13 langs = 247 entries.
  - Mig 360 file regenerated from post-strip live state.

**PR #207 open:** mig 360 recovery + mig 359 forward+fix + router whitelist. CI green, awaiting owner merge.

**3 NEW KB concepts:**
- `jsonb-shallow-merge-antipattern.md` — 3 safe alternatives, CI guard TODO, incident record
- `subagent-live-apply-review-rule.md` — orchestrator MUST review SQL before LIVE-apply, TaskStop ≠ rollback
- `double-emoji-button-anti-pattern.md` — icon_const_key + emoji-in-value → double render

KB index.md updated: 4 new lookup-table entries + 2 inline concept references.

**Handover for next agent:** `handover/2026-05-26_p0_recovery_and_phase3d_close.md`

## Memory Maintenance

### Memory Flush (02:02)

FLUSH_ERROR: Exception: Command failed with exit code 1 (exit code: 1)
Error output: Check stderr output for details

