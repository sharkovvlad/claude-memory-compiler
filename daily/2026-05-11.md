# Daily Log: 2026-05-11

## Sessions

### Session (12:00-13:00 MSK) — mig 197: phenotype quiz Sub-FSM cmd_back (PR follow-up to #42)

**Контекст:** после merge'а PR #42 пользователь сообщил 3 связанных UX-бага в Profile retake flow:
- (A) Q-buttons не работают при revisit через cmd_edit_phenotype после Back
- (B) Кнопка Назад исчезает при revisit
- (C) Back на Q3 шлёт в my_plan, не в Q2

**Расследование:** прочитал FSM cmd_back branch в `process_onboarding_input` (mig 188 L273-277): `UPDATE status='registered' + render('my_plan')` без `pop_nav`. Nav_stack после exit'а: `[..., my_plan, edit_phenotype]` (edit_phenotype остался top). Revisit через cmd_edit_phenotype с status='registered' → `process_user_input` ищет button на current_screen=nav_stack[-1]=edit_phenotype, но cmd_edit_phenotype button на my_plan → FOUND=FALSE → meta-dispatch не сработал → status=registered → последующий cmd_quiz_q* через section 4l catch-all → legacy void. Plus visible_condition='u.status=edit_phenotype' для cmd_back → пропадает на revisit.

**Архитектурный совет (тимлид + НЛМ):** не плодить micro-статусы phenotype_q1/q2/...; единый макро-статус FSM `edit_phenotype` + step вывод из JSONB — Stateless Sub-FSM паттерн. Единая кнопка cmd_back, context-aware semantic в SQL FSM (Headless SDUI: клиент остаётся Dumb Renderer). Reuse `pop_nav` helper.

**Mig 197** (utvержден):

```sql
IF v_callback = 'cmd_back' AND v_status = 'edit_phenotype' THEN
    SELECT phenotype_answers INTO v_ans_pheno FROM users WHERE telegram_id=p_telegram_id;
    v_answered := CASE
        WHEN v_ans_pheno ? 'q4' THEN 4
        WHEN v_ans_pheno ? 'q3' THEN 3
        WHEN v_ans_pheno ? 'q2' THEN 2
        WHEN v_ans_pheno ? 'q1' THEN 1
        ELSE 0
    END;
    IF v_answered = 0 THEN
        -- exit + pop_nav
        UPDATE users SET status='registered' WHERE telegram_id=p_telegram_id;
        PERFORM pop_nav(p_telegram_id);
        RETURN render_screen(p_telegram_id, 'my_plan');
    END IF;
    -- rollback last answer + render previous Q
    UPDATE users SET phenotype_answers = v_ans_pheno - ('q'||v_answered)::text WHERE telegram_id=p_telegram_id;
    IF v_answered = 1 THEN
        RETURN render_screen(p_telegram_id, 'edit_phenotype');
    ELSE
        RETURN render_screen(p_telegram_id, 'phenotype_q'||v_answered);
    END IF;
END IF;
```

**Verifications:**
- Live savepoint flow (`scripts/_verify_mig_197_flow.py`, 4 сценария):
  1. Q1 → a → Q2 → Back → Q1, ans={} ✓
  2. Q1→a → Q2→b → Q3 → Back → Q2, ans={q1:'a'} ✓
  3. Q1→a → Q2→b → Q3→c → Q4 → Back → Q3, ans={q1,q2} ✓
  4. Q1 → Back → exit my_plan, status=registered, nav_stack pop'нул edit_phenotype ✓
- `pytest tests/` — 399 passed, 1 skipped (без регрессий).
- Stale-base recipe соблюдён: source через `pg_get_functiondef`, не из git.

**Lesson → KB:** [phase4-onboarding-migration.md gotcha #30](../knowledge/concepts/phase4-onboarding-migration.md) «Stateless Sub-FSM для multi-step quiz / wizard» — Recipe для будущих wizards: один макро-статус, шаг из JSONB, cmd_back context-aware, pop_nav на exit, Closed-Loop Learning.

**TODO (отдельный PR):** symmetric pop_nav в 2 других exit paths — `cmd_quiz_continue` (L386-389) и catch-all (L393-398). Те же UPDATE status='registered' без pop_nav. Не критично (rare paths), но для консистентности.

**Файлы:**
- `migrations/197_phenotype_quiz_back_previous_question.sql` — применена.
- `scripts/_build_mig_197.py` — builder (stale-base из live source).
- `scripts/_verify_mig_197_flow.py` — 4-scenario savepoint repro для регрессии.
- KB: `phase4-onboarding-migration.md` gotcha #30.

### Session (14:30-15:00 MSK) — mig 199: ✅ checkmarks + symmetric pop_nav (PR follow-up)

**Запрос юзера:** на quiz-кнопках показывать ✅ если данный вариант ранее выбран (как в Settings/My Plan). Если параметр не выбран — галочка НЕ ставится. Паттерн зафиксировать в KB.

**Расследование existing checkmark механизма:**
- Settings/My Plan (edit_goal, edit_activity, …): `ui_screens.meta.current_value_col` = имя плоской колонки users; `ui_screen_buttons.meta.save_value` = значение кнопки. `render_screen` L161-177 сравнивает → `is_current=TRUE`. `template_engine._build_button_text` префиксует '✅ '.
- Phenotype quiz: state в `users.phenotype_answers` JSONB, не плоской колонке. Каждый Q-экран нуждается в своём JSON path.

**Решение (mig 199):** generated columns в users — read-only, авто-sync:
```sql
ALTER TABLE users
  ADD COLUMN phenotype_q1 TEXT GENERATED ALWAYS AS (phenotype_answers->>'q1') STORED,
  ... q2, q3, q4 (аналогично)
```

Затем 4 UPDATE на ui_screens (current_value_col = phenotype_qN) + 12 UPDATE на ui_screen_buttons (save_value = a/b/c). **Ноль изменений в render_screen / template_engine** — существующий паттерн работает «как есть».

**Часть 2 (попутно):** симметричный pop_nav для 2 exit paths (cmd_quiz_continue Profile retake + catch-all) — закрывает TODO из mig 197. Каждый exit-from-wizard теперь pop'ает nav_stack.

**Verifications** (`scripts/_verify_mig_199_checkmarks.py`, 5 сценариев — все ✓):
- Fresh quiz → no ✅ (phenotype_q1=NULL → render_screen IF v_user_val IS NOT NULL fails)
- Mid-quiz {q1:a} → ✅ на q1_a (Q1), нет ✅ на Q2
- Classified → ✅ на каждом Q соответствующего ответа (Closed-Loop Learning view)
- cmd_quiz_continue → pop_nav OK
- catch-all (cmd_get_profile в edit_phenotype) → pop_nav OK

`pytest tests/` — 399 passed, 1 skipped (без регрессий).

**Lesson → KB:** [phase4-onboarding-migration.md gotcha #31](../knowledge/concepts/phase4-onboarding-migration.md) «✅ checkmark на кнопках текущего выбора — JSONB через generated columns» — Recipe для будущих JSONB-based wizards (4 шага: generated col + current_value_col + save_value + не трогать render_screen).

**Антипаттерны зафиксированы:**
- Хардкодить ✅ в text_key переводов — статичный, не reactive.
- Расширять render_screen новым current_value_jsonpath — дублирует механизм.
- Micro-статусы FSM на каждый шаг — overhead для UI feature.

**Файлы:**
- `migrations/199_phenotype_quiz_checkmarks_and_symmetric_popnav.sql` — применена.
- `scripts/_build_mig_199.py` — builder.
- `scripts/_verify_mig_199_checkmarks.py` — 5-scenario savepoint repro.
- KB: `phase4-onboarding-migration.md` gotcha #31.

---

## Sticker Architecture Foundation (Channel A live)

**Контекст:** 3 разрозненных способа отправки стикеров (thinking-индикатор из proxy, freeze-push из cron, заложенная-но-нерабочая декларация `ui_screens.meta.show_sticker` для welcome/success). Планируется ~30–40 стикеров. Без фундамента — каскад тактических правок.

**Решение:** ADR 0001 — 4 канала с единой таблицей `bot_stickers` как source-of-truth и общим in-memory кэшем. Stage 1 этой сессии — Channel A (UI content stickers, headless).

**Сделано (mig 198 + Python Stage 1):**

- `migrations/198_stickers_unified_foundation.sql` — применена. 4 блока:
  - `bot_stickers` + `meta jsonb` + `updated_at` + `trg_set_updated_at` trigger + partial index `(category, sort_order) WHERE is_active`.
  - `render_screen` Step 8b + extra `sticker_category` поле в `telegram_ui` (точечно над baseline `_baseline_render_screen_2026-05-11.sql`).
  - Seed: `onboarding_welcome_1` активирован, `onboarding_success_1` создан (file_id от владельца).
  - `ui_screens.onboarding_success.meta` ← `show_sticker:true, sticker_category:'onboarding_success'` (через jsonb `||` сохранил `no_back`, `canonical`).
- `services/stickers_cache.py` (284 LOC) — in-memory dict, TTL 60s + background refresher, fail-open keep-stale, exp backoff на retry, sync `lookup()`, DI через `query_fn`.
- `services/template_engine.py` — +23 строки Channel A блок (после ACQ FIRST, перед основным item). Lazy import `services.stickers_cache` ради anti-cycle.
- `webhook_server.py` — startup wires `prewarm()` + `start_background_refresher()`; shutdown stops refresher; новый `POST /internal/stickers/reload` с защитой `X-Stickers-Reload-Token` (env). 503 если token не настроен.

**Тесты:**
- `tests/services/test_stickers_cache.py` — 17 unit-тестов через DI (purely in-memory, без сети).
- `tests/services/test_template_engine.py` — +7 Channel A тестов (порядок envelope, cache miss graceful skip, non-string category ignored).
- `pytest tests/services/` — 173/173 passed.

**Bench (10 прогонов, persistent psycopg2 с мака → Hetzner pooler EU):**
- `render_screen('onboarding_welcome')` p95 = 93ms (со стикером)
- `render_screen('onboarding_success')` p95 = 75ms (со стикером)
- `render_screen('main_menu')` p95 = 233ms (контроль без стикера, один outlier — jitter)
- Overhead Channel A в render_screen — в пределах шума. С VPS будет p95 < 50ms.

**End-to-end smoke (без сети к Telegram):** `SQL render_screen` → `telegram_ui.sticker_category` → `template_engine.render_envelope` → `OutboundItem(send_sticker)`:
- onboarding_welcome: envelope `[send_sticker(CAACAgIAAxkBAAERMJ9q…), send_new]` ✅
- onboarding_success: envelope `[send_sticker(CAACAgIAAxkBAAERMJlq…), send_new]` ✅
- main_menu: envelope без send_sticker ✅
- Порядок гарантирован — `send_sticker` всегда перед основным item.

**Race-condition guard:** `services/telegram_send.py` отправляет `send_sticker` строго через `await` (не `asyncio.create_task`). ADR 0001 фиксирует решение: Telegram-балансировщики не гарантируют порядок параллельных запросов, для UI-контент-стикеров (Trophy-mode) это означает риск «текст приходит раньше стикера».

**Coexistence:** Stage 1 НЕ трогает `telegram_proxy.py:_stickers_cache` / `_freeze_sticker_cache`. Старая инфра Channel D работает параллельно с новым cache. Stage 2 (отдельный PR) консолидирует.

**KB / ADR:**
- ADR `docs/adr/0001-sticker-architecture.md` (новый, repo). 4 канала, 6 rejected alternatives (LISTEN/NOTIFY, language_code, send_count, fire-and-forget для A и т.д.).
- KB `concepts/ui-stickers-headless.md` обновлён до 4-канальной модели + рецепт «как добавить новый стикер на новый экран» (2 SQL вставки, 0 строк Python).

**Файлы (Stage 1 PR):**
- `migrations/198_stickers_unified_foundation.sql`
- `migrations/_baseline_render_screen_2026-05-11.sql`
- `services/stickers_cache.py`
- `services/template_engine.py`
- `webhook_server.py`
- `tests/services/test_stickers_cache.py`
- `tests/services/test_template_engine.py`
- `docs/adr/0001-sticker-architecture.md`

**Антипаттерны зафиксированы в ADR 0001:**
- Хардкодить file_id в коде/`.env`/`app_constants` — single source = `bot_stickers`.
- Класть «когда показать UI-стикер» в `handlers/onboarding_v3.py` — нарушение headless.
- `asyncio.create_task` для Channel A — race на балансировщиках Telegram.
- `LISTEN/NOTIFY` — несовместимо с PgBouncer transaction mode (Supabase pooler `:6543`).
- `language_code` / `send_count` колонки — YAGNI / hot-row risk.

### Session (~16:30 MSK) — mig 200: nav_stack-based Sub-FSM + buttons.done

**Контекст (после Live UAT PR #44):** юзер сообщил 2 связанных бага в phenotype quiz Profile retake:
1. **✅ checkmark не показывается** при revisit квиза через 🧬 Тип тела.
2. **Back на Q3 (classified)** не работает корректно — остался на том же экране без ✅.

Plus UX-предложение: переименовать «Поехали» на phenotype_result → нейтральное (юзер acknowledges, не starts).

**Расследование:**

**Bug 1 — НЕ баг кода**: live diagnostic показал SQL render_screen возвращает `is_current=True` для q1_a, Python `_build_button_text` корректно префиксует `'✅ 👕 Плечи и грудь'`. `systemctl status noms-webhooks` показал рестарт `2026-05-11 16:25:51 MSK` (deploy PR #44). Скрин юзера от **16:19** MSK = ДО deploy. После deploy кликов от юзера в логах нет → user не перетестировал. Bug 1 — артефакт pre-deploy state.

**Bug 2 — реальный SQL bug**: mig 197 cmd_back step inference через answered count ломается для classified revisit:
```
phenotype_answers={q1:'a',q2:'b',q3:'c',q4:'c'} (classified)
current screen = phenotype_q3
cmd_back → mig 197 logic: v_answered=4 → rollback q4 → render Q4 ✗
```

Корень: answered count = **history**, не **current position**. Правильный источник current step — `nav_stack[-1]`.

**Mig 200** (4 части атомарно):

A. cmd_back rewrite: step из nav_stack top (edit_phenotype/phenotype_qN/...). Q1→exit. Q2/3/4→previous+rollback q(curr-1)+pop_nav.

B. Forward push_nav: `cmd_quiz_q[1-3]` push'ает next Q screen в nav_stack. nav_stack растёт `[..., edit_phenotype, phenotype_q2, phenotype_q3, ...]` симметрично с pop в Back.

C. Strip-all-on-exit: 3 exit paths (cmd_back Q1, cmd_quiz_continue Profile retake, catch-all) LOOP pop_nav пока top — phenotype-* screen. Иначе после forward'ов в стеке несколько quiz screens — single pop недостаточно.

D. `buttons.done` text_key в 13 языках (Готово/Done/Listo/Fertig/...). UPDATE phenotype_result.cmd_quiz_continue button → buttons.done. buttons.start («Поехали») остался для onboarding_welcome где семантически верен.

**Verifications** (`scripts/_verify_mig_200_flow.py`, 5 сценариев — все ✓):
1. Initial Q1→Q2→Q3→Q4: nav_stack растёт корректно.
2. Back chain Q4→Q3→Q2→Q1→exit: rollback + pop правильно.
3. Classified revisit: ✅ на q1_a (рендеринг работает после deploy).
4. **Classified Q3 Back → Q2** (THE BUG): теперь правильно (mig 197 → Q4 ✗ → mig 200 → Q2 ✓).
5. cmd_quiz_continue exit: ВСЕ phenotype-* screens strip'нуты из nav_stack.

`pytest tests/` — 399 passed, 1 skipped.

**Lesson → KB:** [phase4-onboarding-migration.md gotcha #32](../knowledge/concepts/phase4-onboarding-migration.md) «Sub-FSM step inference: nav_stack ≻ answered count». Recipe для будущих wizards с revisit'ом — source of truth для current step = nav_stack[-1], не accumulator. push_nav/pop_nav симметрия. Strip-all на exit. Headless SDUI invariant.

**Файлы:**
- `migrations/200_phenotype_quiz_navstack_tracking_and_buttons_done.sql` — применена.
- `scripts/_build_mig_200.py` — builder (stale-base через pg_get_functiondef).
- `scripts/_verify_mig_200_flow.py` — 5-scenario flow savepoint repro.
- `scripts/_diag_quiz_revisit_bugs.py` + `scripts/_diag_bug1_python_render.py` — diagnostics.
- KB: `phase4-onboarding-migration.md` gotcha #32.

---

## Sticker Stage 1.1 — One-Time Semantics + Stage 2 Delegated + KB Reorg

**Триггер:** Live observation на тестовом 267703 — welcome-стикер выпадал **повторно** при `cmd_select_lang` (юзер в `status='new'` меняет язык, экран `onboarding_welcome` re-рендерится, но FSM не двигается — мой Stage 1 это не покрывал). Тимлид и я ранее предположили что FSM сам не покажет повторно — **неверно**.

### Stage 1.1 — mig 201 (applied; originally took slot 200, renamed after collision)

**Number-collision lesson:** взял mig 200, применил psycopg2 на проде + закоммитил + push'нул PR #48. Тимлид заметил что параллельный агент (phenotype-quiz, PR #47, та же дата) тоже занял 200. Переименовал файл `git mv migrations/200_* migrations/201_*`, обновил все ссылки в KB/ADR/коммите, amend + force-push-with-lease. SQL уже выполнен на проде — переименование чисто косметическое (БД не знает о номерах, только последовательность apply). **Правило закреплено:** перед commit миграции **обязательно** `git fetch origin main && ls migrations/ && gh pr list --state open | grep -i mig` чтобы видеть слоты, занятые в open PRs.

- `users.stickers_shown jsonb` — per-user per-category tracking { 'onboarding_welcome': '2026-05-11T14:01:18Z', ... }.
- `ui_screens.meta.show_sticker_once: true` — opt-in флаг для welcome + success.
- `render_screen` Step 8b расширен: read+mark атомарно. Если уже в `stickers_shown` → emit NULL. Иначе UPDATE inside same transaction.
- `bot_stickers.meta.channel` (A/C/D) — самодокументирующий маркер для каждой строки (видно сразу к какому каналу принадлежит без grep по коду).
- Backfill: 31 `registered` юзер получили оба ключа (welcome+success); 2 `registration_step_*` — только welcome (success ещё не положен).
- Verify (live):
  - Тестовый 786301802 — `render_screen('onboarding_welcome') → sticker_category=NULL` ✅
  - Свежий `status='new'` юзер — первый вызов → `'onboarding_welcome'`, второй → `NULL`, stickers_shown заполнен ✅
- ADR обновлён (Stage 1.1 amendment) с rejected alternatives: «mark on Python side» / «previous_status detection» / «отдельная колонка на стикер».

### Stage 2 — делегировано субагенту (PR #46)

Параллельно запущен субагент с детальным ТЗ:
- `services/stickers_cache.list_active_for_category(cat) -> [(sticker_key, file_id)]` — новый API для Channel D (rotation by index).
- `_by_category` shape расширен до `(sort_order, sticker_key, file_id)`.
- `telegram_proxy._get_stickers()` / `_get_freeze_sticker_id()` → переписаны на общий cache. Локальные `_stickers_cache` / `_freeze_sticker_cache` удалены.
- `crons/freeze_notification_pusher._load_freeze_sticker()` → `stickers_cache.lookup('freeze')`. Guard `file_id.startswith('TODO_')` сохранён.
- Cron startup (`main.py`) добавил `await stickers_cache.prewarm() + start_background_refresher()`.
- Tests: 179 passed (было 173 + 6 новых для `list_active_for_category`). FN1-FN7 freeze integration tests untouched.

### KB Reorganization (lesson)

**Промах в Stage 1:** ADR положил в `docs/adr/0001-sticker-architecture.md` в основном NOMS-репо. Другие агенты ищут в KB и не найдут. Тимлид указал.

**Исправление:**
- Перенёс `docs/adr/0001-sticker-architecture.md` → `claude-memory-compiler/knowledge/concepts/sticker-architecture-adr.md`.
- Удалил `docs/` из NOMS-репо.
- Создал `claude-memory-compiler/RULES.md` — формализованные правила «всё описательное в KB, не в основном репо».
- Дописал в проектный `CLAUDE.md` запрет на `docs/` для архитектурных артефактов + ссылку на RULES.md.
- Обновил MEMORY.md — новое правило в шапке и пойнтер на RULES.md.
- Добавил `sticker-architecture-adr.md` в `index.md` KB.

### Антипаттерн зафиксирован

- **«FSM сам обеспечит one-time»** — неверно для in-status callback'ов (`cmd_select_lang`, `cmd_change_*` итд). Любой Trophy-стикер должен иметь явный one-time флаг.
- **ADR / архитектурные доки в `docs/` основного репо** — невидимы для других агентов. Всё описательное → KB.

### Файлы (PR #47 — этот один-time fix)

- `migrations/201_sticker_one_time_semantics.sql` — applied (renamed from 200 after collision detection)
- `migrations/_baseline_render_screen_2026-05-11_post_mig198.sql` — witness
- `docs/adr/0001-sticker-architecture.md` — DELETED (перенесено в KB)

### Файлы KB-репо

- `claude-memory-compiler/knowledge/concepts/sticker-architecture-adr.md` — moved + Stage 1.1 amendment
- `claude-memory-compiler/knowledge/concepts/ui-stickers-headless.md` — обновлён (recipe с show_sticker_once, история до mig 201)
- `claude-memory-compiler/knowledge/index.md` — +строка sticker-architecture-adr, обновлена строка ui-stickers-headless
- `claude-memory-compiler/RULES.md` — новый, формализует правило «всё в KB»

---

### Session (17:00-19:00 MSK) — Sticker pipeline: frozen Noms + KB-фиксация

**Контекст:** продолжение работы над Telegram-стикерами Номса. Третий стикер за серию (после `noms_hello_wave` 10.05 clean mode и `noms_celebrate_registration` 10.05 scene mode).

**Что сделано:**

1. **Стикер `noms_frozen.webm`** (`Дизайн/stickers/`, 169 KB, 24 fps, CRF 35, clean mode). Из `Заморозка/mp_.mp4`, окно 3.5–6.5 сек — дрожь + подмигивание + пиксельное пламя в руках + ледяные кристаллы. Огонь и лёд внутри контура Номса → rembg сохраняет автоматически, decoration recovery не нужен. Пар изо рта (~4с в source) rembg отбросил как отдельный объект (приемлемый компромисс).

2. **Промпт-шаблоны для Nano Banana / Veo** добавлены в `Дизайн/STICKER_PIPELINE.md` (новая секция 8, +180 строк):
   - 8.1 Workflow (prompt → image refs → seed image → video → make_sticker.py)
   - 8.2 Выбор цвета фона: **рекомендация перейти на magenta** (`#FF1493`) вместо зелёного. У NOMS на теле зелёный binary-код + иногда зелёные декорации (pixel-сердце, конфетти) → при зелёном фоне chromakey съедает эти элементы и приходится rembg-композитить с костылями. Magenta не пересекается с palette Номса.
   - 8.3 Image generation template (с slot'ами под сценарии)
   - 8.4 Video generation template (image-to-video, описывает только action — внешний вид из seed)
   - 8.5 Pro-tips: character_bible, negative prompts, watermark, seamless loop, разница Nano Banana (image) vs Veo (video)

3. **KB-фиксация** для агента-преемника:
   - `claude-memory-compiler/knowledge/concepts/telegram-sticker-pipeline.md` — новый concept-файл, конспект для агента-преемника. Содержит: правила результата, два режима скрипта, краткий workflow генерации исходников, **промт-шаблон для делегирования через Agent tool**, pro-tips, ссылку на главный гайд.
   - `claude-memory-compiler/knowledge/index.md` — добавлена строка в начало таблицы.

**Принятые решения:**

- Magenta chroma `#FF1493` принят пользователем как стандарт для будущих генераций. Скрипт уже поддерживает через `--bg-color HEX`, дефолт остаётся `#127B28` для совместимости с legacy зелёными mp4-заготовками.
- Промт-шаблоны (image + video) каноничны, копировать и заполнять slot'ы `[SCENARIO-SPECIFIC TRAITS]` / `[ACTION]`. Negative prompts обязательны.
- Character bible (`Дизайн/character_bible/`) — пока не создана, добавится при первой генерации в magenta.

**Артефакты:**

- `/Users/vladislav/Documents/NOMS/Дизайн/stickers/noms_frozen.webm` (169 KB)
- `/Users/vladislav/Documents/NOMS/Дизайн/STICKER_PIPELINE.md` (был 14 KB, теперь 31 KB — +секция 8)
- `/Users/vladislav/Documents/NOMS/claude-memory-compiler/knowledge/concepts/telegram-sticker-pipeline.md` (новый, ~7 KB)
- `/Users/vladislav/Documents/NOMS/claude-memory-compiler/knowledge/index.md` (+1 строка в шапке)

### Session (19:30-20:00 MSK) — Consolidation: STICKER_PIPELINE.md → KB single source

**Контекст:** пользователь попросил перенести `Дизайн/STICKER_PIPELINE.md` (31 KB полный гайд) в `claude-memory-compiler/` чтобы был один источник истины для агентов. Я предложил гибрид:

- **Полный гайд** — `claude-memory-compiler/knowledge/concepts/telegram-sticker-pipeline.md` (~450 строк, заменил предыдущий компактный конспект). Содержит всё: правила, два режима, флаги, верификация, промт-шаблоны (image + video), промт-шаблон для делегирования агенту, известные кейсы, тонкости, история.
- **Stub-указатель** — `Дизайн/STICKER_PIPELINE.md` теперь 17 строк, ссылается на KB. Это для того, чтобы человек/агент, открывший папку `Дизайн/`, нашёл путь к полному документу. Файл в KB-репо отдельный (gitignored в main NOMS), без указателя в коде это легко пропустить.

**Обновления:**

- `claude-memory-compiler/knowledge/concepts/telegram-sticker-pipeline.md` — расширен с 143 до ~450 строк (полный гайд)
- `claude-memory-compiler/knowledge/index.md` — обновлена summary (раньше говорила «полный гайд в `Дизайн/STICKER_PIPELINE.md`», теперь — «эта статья = источник правды; `Дизайн/STICKER_PIPELINE.md` — stub»)
- `Дизайн/STICKER_PIPELINE.md` — переписан в stub-указатель (17 строк)
- `Дизайн/stickers/scripts/make_sticker.py` — docstring и `--mode` help обновлены: ссылаются на KB вместо `Дизайн/STICKER_PIPELINE.md`

**Принцип:** для документов, у которых есть и operational (как запустить), и conceptual (правила, lessons learned) части — единый файл лежит в KB. У места кода/материалов остаётся stub-указатель. Этот паттерн масштабируется на другие documentation-heavy задачи.

### Session (~23:30 MSK) — mig 202: Back preserves answers + onboarding result + test-user-reset KB

**Контекст после Live UAT mig 200:**
1. **Bug** (user 417002669, classified): Back из Q4 → render Q3 без ✅ на сохранённом ответе. mig 200 rollback'ал q(prev_q) при Back ради «свежего ответа», но юзер ожидает TypeForm-style preserve.
2. **UX** (user 786301802, новый онбординг): после Q4 сразу location picker, нет понимания «что я узнал о себе». mig 189 skip-result для скорости, но Closed-Loop Learning требует closing screen.
3. **Tool**: reset script для повторного E2E тестирования — критический пробел в инфраструктуре, нужна доступная утилита.

**Mig 202 (2 patch к process_onboarding_input):**

A. **cmd_back: убрать rollback** в Profile retake branch. Просто `pop_nav + render previous Q`, без `phenotype_answers - 'q'||prev_q`. save_phenotype_answer idempotent: клик same = no-op navigate, клик different = overwrite navigate. UX: TypeForm-style preserve.

B. **Q4 в онбординге: render phenotype_result** вместо forward. status остаётся 'registration_step_phenotype_quiz' + push_nav('phenotype_result'). Existing cmd_quiz_continue handler в этом статусе forward'ит на location → chain не прерывается. Юзер видит result screen с «Готово» (mig 200 buttons.done), потом Continue → location.

**Existing infrastructure переиспользована:**
- screen `phenotype_result`, text_key `phenotype.result_template`, 4 explanation keys × 13 lang (mig 187).
- `classify_phenotype` RPC уже строит `result_html`.
- `push_nav` truncate-on-existing (mig 147) корректно работает с возможными повторами после Back.

**Verifications** (`scripts/_verify_mig_202.py`, 3 сценария — все ✓):
1. Profile retake Q4 → Back → Q3 + ✅ на q3_c. phenotype_answers preserved.
2. Back chain Q4→Q3→Q2→Q1 — ✅ visible на каждом step с сохранённого ответа.
3. Onboarding Q4 → render phenotype_result (status preserved) → click Continue → forward to location (chain не прерван).

`pytest tests/` — 398 passed, 1 skipped, 1 flake (integration test contaminated savepoint state от prior runs, passes в isolation).

**KB:**
- **Новая статья** `knowledge/concepts/test-user-reset-recipe.md` — verified SQL UPDATE для обнуления тестового юзера. Lessons про NLM stale schema, NOT NULL constraints (notifications_mode, stickers_shown), pitfalls. Index entry добавлен.
- `phase4-onboarding-migration.md` gotcha #33 — recipe «Back preserves answers + result screen перед exit» для будущих wizards.

**Файлы:**
- `migrations/202_phenotype_back_preserve_and_onboarding_result_screen.sql` — применена.
- `scripts/_build_mig_202.py`, `scripts/_verify_mig_202.py`, `scripts/_audit_users_schema.py`.
- KB: `phase4-onboarding-migration.md` gotcha #33, `test-user-reset-recipe.md` (new).

**Open:** ждём UAT после reset'а юзера 786301802 для проверки full onboarding flow с phenotype_result.
